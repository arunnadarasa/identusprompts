{
  "theme": {
    "slug": "photography",
    "name": "Photography",
    "emoji": "📷",
    "audience": "photographers, photo editors, photojournalists",
    "market_anchor": "the photo software market (~$2.4B) and >15M pro photographers"
  },
  "ideas": [
    {
      "id": "photography-golden-hour-whisper-0",
      "theme": "photography",
      "title": "Golden Hour Whisper",
      "pitch": "Golden Hour Whisper spins up a fresh public Sprite for each session so location scouting gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "location scouting",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits location scouting in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Golden Hour Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGolden Hour Whisper spins up a fresh public Sprite for each session so location scouting gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (location scouting).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits location scouting in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `location-scouting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for location scouting.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$400M — landscape photography apps",
      "som": "$15M — premium scouting tool subscriptions"
    },
    {
      "id": "photography-portrait-posture-prompter-1",
      "theme": "photography",
      "title": "Portrait Posture Prompter",
      "pitch": "Portrait Posture Prompter composes subject posing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "subject posing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits subject posing in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Portrait Posture Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPortrait Posture Prompter composes subject posing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (subject posing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits subject posing in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `subject-posing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for subject posing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for subject posing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$800M — portrait studio software",
      "som": "$25M — posing guide app revenues"
    },
    {
      "id": "photography-field-note-narrator-2",
      "theme": "photography",
      "title": "Field Note Narrator",
      "pitch": "Field Note Narrator boots a long-running service inside a Sprite for photojournalism context — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "photojournalism context",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches photojournalism context in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Field Note Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nField Note Narrator boots a long-running service inside a Sprite for photojournalism context — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (photojournalism context).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches photojournalism context in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `photojournalism-context-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for photojournalism context.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits photojournalism context — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for photojournalism context.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$300M — journalism field tools",
      "som": "$10M — photojournalist productivity apps"
    },
    {
      "id": "photography-kit-bag-caller-3",
      "theme": "photography",
      "title": "Kit Bag Caller",
      "pitch": "Kit Bag Caller runs a one-shot shell command inside a Sprite for equipment packing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "equipment packing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits equipment packing in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Kit Bag Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nKit Bag Caller runs a one-shot shell command inside a Sprite for equipment packing and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (equipment packing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits equipment packing in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `equipment-packing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a equipment packing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for equipment packing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$150M — camera inventory management",
      "som": "$5M — gear tracking subscriptions"
    },
    {
      "id": "photography-darkroom-agitation-timer-4",
      "theme": "photography",
      "title": "Darkroom Agitation Timer",
      "pitch": "Darkroom Agitation Timer spins up a fresh public Sprite for each session so analog film processing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "analog film processing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits analog film processing in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Darkroom Agitation Timer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDarkroom Agitation Timer spins up a fresh public Sprite for each session so analog film processing gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (analog film processing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits analog film processing in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `analog-film-processing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for analog film processing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$100M — analog photography supplies",
      "som": "$8M — darkroom companion apps"
    },
    {
      "id": "photography-culling-flow-coach-5",
      "theme": "photography",
      "title": "Culling Flow Coach",
      "pitch": "Culling Flow Coach composes post-processing mindset in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "post-processing mindset",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits post-processing mindset in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Culling Flow Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCulling Flow Coach composes post-processing mindset in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (post-processing mindset).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits post-processing mindset in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `post-processing-mindset-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for post-processing mindset (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for post-processing mindset.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$900M — photo editing software",
      "som": "$30M — workflow optimization plugins"
    },
    {
      "id": "photography-luminary-ratio-guide-6",
      "theme": "photography",
      "title": "Luminary Ratio Guide",
      "pitch": "Luminary Ratio Guide boots a long-running service inside a Sprite for studio lighting setup — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "studio lighting setup",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches studio lighting setup in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Luminary Ratio Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLuminary Ratio Guide boots a long-running service inside a Sprite for studio lighting setup — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (studio lighting setup).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches studio lighting setup in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `studio-lighting-setup-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for studio lighting setup.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits studio lighting setup — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio lighting setup.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$500M — studio management tools",
      "som": "$12M — lighting tutorial platforms"
    },
    {
      "id": "photography-blind-patience-whisper-7",
      "theme": "photography",
      "title": "Blind Patience Whisper",
      "pitch": "Blind Patience Whisper runs a one-shot shell command inside a Sprite for wildlife photography and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "wildlife photography",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits wildlife photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Blind Patience Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlind Patience Whisper runs a one-shot shell command inside a Sprite for wildlife photography and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (wildlife photography).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits wildlife photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `wildlife-photography-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a wildlife photography-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for wildlife photography.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$200M — nature photography guides",
      "som": "$7M — wildlife tracking apps"
    },
    {
      "id": "photography-star-trail-calculator-8",
      "theme": "photography",
      "title": "Star Trail Calculator",
      "pitch": "Star Trail Calculator spins up a fresh public Sprite for each session so astrophotography exposure gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "astrophotography exposure",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits astrophotography exposure in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Star Trail Calculator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStar Trail Calculator spins up a fresh public Sprite for each session so astrophotography exposure gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (astrophotography exposure).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits astrophotography exposure in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `astrophotography-exposur-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for astrophotography exposure.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$120M — astronomy photography tools",
      "som": "$4M — night sky planning apps"
    },
    {
      "id": "photography-garnish-styling-prompter-9",
      "theme": "photography",
      "title": "Garnish Styling Prompter",
      "pitch": "Garnish Styling Prompter composes food photography styling in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "food photography styling",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits food photography styling in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Garnish Styling Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGarnish Styling Prompter composes food photography styling in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (food photography styling).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits food photography styling in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `food-photography-styling-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for food photography styling (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for food photography styling.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$350M — commercial photography software",
      "som": "$11M — food styling resources"
    },
    {
      "id": "photography-room-flow-narrator-10",
      "theme": "photography",
      "title": "Room Flow Narrator",
      "pitch": "Room Flow Narrator boots a long-running service inside a Sprite for architectural photography — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "architectural photography",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches architectural photography in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Room Flow Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRoom Flow Narrator boots a long-running service inside a Sprite for architectural photography — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (architectural photography).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches architectural photography in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `architectural-photograph-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for architectural photography.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits architectural photography — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for architectural photography.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$600M — real estate media tools",
      "som": "$20M — property photography apps"
    },
    {
      "id": "photography-focus-stack-caller-11",
      "theme": "photography",
      "title": "Focus Stack Caller",
      "pitch": "Focus Stack Caller runs a one-shot shell command inside a Sprite for macro photography and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "macro photography",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits macro photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Focus Stack Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFocus Stack Caller runs a one-shot shell command inside a Sprite for macro photography and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (macro photography).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits macro photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `macro-photography-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a macro photography-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for macro photography.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$180M — specialized photography tools",
      "som": "$6M — macro focusing aids"
    },
    {
      "id": "photography-vow-moment-coach-12",
      "theme": "photography",
      "title": "Vow Moment Coach",
      "pitch": "Vow Moment Coach spins up a fresh public Sprite for each session so wedding photography gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "wedding photography",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits wedding photography in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Vow Moment Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVow Moment Coach spins up a fresh public Sprite for each session so wedding photography gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (wedding photography).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits wedding photography in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `wedding-photography-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for wedding photography.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$700M — wedding photography software",
      "som": "$25M — event timeline apps"
    },
    {
      "id": "photography-sidewalk-ethics-anchor-13",
      "theme": "photography",
      "title": "Sidewalk Ethics Anchor",
      "pitch": "Sidewalk Ethics Anchor composes street photography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "street photography",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits street photography in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Sidewalk Ethics Anchor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSidewalk Ethics Anchor composes street photography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (street photography).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits street photography in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `street-photography-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for street photography (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for street photography.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$250M — street photography resources",
      "som": "$9M — travel photography guides"
    },
    {
      "id": "photography-reef-buoyancy-guide-14",
      "theme": "photography",
      "title": "Reef Buoyancy Guide",
      "pitch": "Reef Buoyancy Guide boots a long-running service inside a Sprite for underwater photography — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "underwater photography",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches underwater photography in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Reef Buoyancy Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nReef Buoyancy Guide boots a long-running service inside a Sprite for underwater photography — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (underwater photography).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches underwater photography in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `underwater-photography-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for underwater photography.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits underwater photography — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for underwater photography.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$100M — dive photography gear",
      "som": "$3M — scuba logging apps"
    },
    {
      "id": "photography-gimbal-move-choreographer-15",
      "theme": "photography",
      "title": "Gimbal Move Choreographer",
      "pitch": "Gimbal Move Choreographer runs a one-shot shell command inside a Sprite for aerial photography and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "aerial photography",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits aerial photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Gimbal Move Choreographer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGimbal Move Choreographer runs a one-shot shell command inside a Sprite for aerial photography and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (aerial photography).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits aerial photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `aerial-photography-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a aerial photography-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for aerial photography.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$450M — drone flight software",
      "som": "$15M — aerial cinematography tools"
    },
    {
      "id": "photography-portfolio-pitch-rehearser-16",
      "theme": "photography",
      "title": "Portfolio Pitch Rehearser",
      "pitch": "Portfolio Pitch Rehearser spins up a fresh public Sprite for each session so client presentations gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "client presentations",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits client presentations in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Portfolio Pitch Rehearser\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPortfolio Pitch Rehearser spins up a fresh public Sprite for each session so client presentations gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (client presentations).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits client presentations in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `client-presentations-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for client presentations.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$800M — photography business tools",
      "som": "$30M — CRM and pitching software"
    },
    {
      "id": "photography-release-form-summarizer-17",
      "theme": "photography",
      "title": "Release Form Summarizer",
      "pitch": "Release Form Summarizer composes photography legalities in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "photography legalities",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits photography legalities in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Release Form Summarizer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRelease Form Summarizer composes photography legalities in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (photography legalities).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits photography legalities in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `photography-legalities-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for photography legalities (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for photography legalities.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$150M — legal tech for creatives",
      "som": "$5M — contract management apps"
    },
    {
      "id": "photography-hue-mood-whisperer-18",
      "theme": "photography",
      "title": "Hue Mood Whisperer",
      "pitch": "Hue Mood Whisperer boots a long-running service inside a Sprite for color grading theory — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "color grading theory",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches color grading theory in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Hue Mood Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHue Mood Whisperer boots a long-running service inside a Sprite for color grading theory — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (color grading theory).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches color grading theory in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `color-grading-theory-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for color grading theory.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits color grading theory — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for color grading theory.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$900M — photo editing software",
      "som": "$40M — LUT and grading packs"
    },
    {
      "id": "photography-panning-rhythm-metronome-19",
      "theme": "photography",
      "title": "Panning Rhythm Metronome",
      "pitch": "Panning Rhythm Metronome runs a one-shot shell command inside a Sprite for sports photography and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "sports photography",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits sports photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Panning Rhythm Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPanning Rhythm Metronome runs a one-shot shell command inside a Sprite for sports photography and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (sports photography).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits sports photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `sports-photography-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a sports photography-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sports photography.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$300M — sports media tools",
      "som": "$10M — action photography aids"
    },
    {
      "id": "photography-editorial-vibe-curator-20",
      "theme": "photography",
      "title": "Editorial Vibe Curator",
      "pitch": "Editorial Vibe Curator spins up a fresh public Sprite for each session so fashion photography gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "fashion photography",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits fashion photography in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Editorial Vibe Curator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEditorial Vibe Curator spins up a fresh public Sprite for each session so fashion photography gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (fashion photography).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits fashion photography in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `fashion-photography-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for fashion photography.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$500M — fashion production software",
      "som": "$18M — creative direction tools"
    },
    {
      "id": "photography-reflection-angle-caller-21",
      "theme": "photography",
      "title": "Reflection Angle Caller",
      "pitch": "Reflection Angle Caller composes product photography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "product photography",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits product photography in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Reflection Angle Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nReflection Angle Caller composes product photography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (product photography).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits product photography in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `product-photography-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for product photography (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for product photography.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$400M — e-commerce photography tools",
      "som": "$14M — product styling software"
    },
    {
      "id": "photography-swaddle-safety-whisper-22",
      "theme": "photography",
      "title": "Swaddle Safety Whisper",
      "pitch": "Swaddle Safety Whisper boots a long-running service inside a Sprite for newborn photography — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "newborn photography",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches newborn photography in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Swaddle Safety Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSwaddle Safety Whisper boots a long-running service inside a Sprite for newborn photography — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (newborn photography).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches newborn photography in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `newborn-photography-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for newborn photography.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits newborn photography — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for newborn photography.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$350M — family photography tools",
      "som": "$12M — newborn posing guides"
    },
    {
      "id": "photography-treat-timing-prompter-23",
      "theme": "photography",
      "title": "Treat Timing Prompter",
      "pitch": "Treat Timing Prompter runs a one-shot shell command inside a Sprite for pet photography and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "pet photography",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits pet photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Treat Timing Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTreat Timing Prompter runs a one-shot shell command inside a Sprite for pet photography and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (pet photography).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits pet photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `pet-photography-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a pet photography-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for pet photography.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$250M — pet photography resources",
      "som": "$8M — animal training aids"
    },
    {
      "id": "photography-pricing-negotiation-coach-24",
      "theme": "photography",
      "title": "Pricing Negotiation Coach",
      "pitch": "Pricing Negotiation Coach spins up a fresh public Sprite for each session so photography business gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "photography business",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits photography business in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Pricing Negotiation Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPricing Negotiation Coach spins up a fresh public Sprite for each session so photography business gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (photography business).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits photography business in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `photography-business-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for photography business.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$800M — photography business tools",
      "som": "$25M — pricing calculator apps"
    },
    {
      "id": "photography-shadow-shape-whisperer-0",
      "theme": "photography",
      "title": "Shadow Shape Whisperer",
      "pitch": "Shadow Shape Whisperer composes studio lighting setup in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "studio lighting setup",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits studio lighting setup in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Shadow Shape Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nShadow Shape Whisperer composes studio lighting setup in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (studio lighting setup).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits studio lighting setup in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `studio-lighting-setup-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for studio lighting setup (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio lighting setup.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo editing and studio management software",
      "sam": "$800M — professional portrait and studio photography tools",
      "som": "$45M — independent studio photographers adopting AI assistants"
    },
    {
      "id": "photography-posture-prompt-talker-1",
      "theme": "photography",
      "title": "Posture Prompt Talker",
      "pitch": "Posture Prompt Talker boots a long-running service inside a Sprite for portrait posing — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "portrait posing",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches portrait posing in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Posture Prompt Talker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPosture Prompt Talker boots a long-running service inside a Sprite for portrait posing — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (portrait posing).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches portrait posing in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `portrait-posing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for portrait posing.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits portrait posing — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for portrait posing.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and creative services market",
      "sam": "$600M — portrait and headshot photography software",
      "som": "$30M — freelance portrait photographers seeking posing aids"
    },
    {
      "id": "photography-empathy-angle-rehearser-2",
      "theme": "photography",
      "title": "Empathy Angle Rehearser",
      "pitch": "Empathy Angle Rehearser runs a one-shot shell command inside a Sprite for photojournalism interviewing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "photojournalism interviewing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits photojournalism interviewing in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Empathy Angle Rehearser\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEmpathy Angle Rehearser runs a one-shot shell command inside a Sprite for photojournalism interviewing and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (photojournalism interviewing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits photojournalism interviewing in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `photojournalism-intervie-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a photojournalism interviewing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for photojournalism interviewing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.5B — global journalism and editorial media software",
      "sam": "$400M — photojournalism and documentary field tools",
      "som": "$25M — freelance photojournalists and NGO documentarians"
    },
    {
      "id": "photography-golden-hour-navigator-3",
      "theme": "photography",
      "title": "Golden Hour Navigator",
      "pitch": "Golden Hour Navigator spins up a fresh public Sprite for each session so location scouting gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "location scouting",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits location scouting in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Golden Hour Navigator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGolden Hour Navigator spins up a fresh public Sprite for each session so location scouting gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (location scouting).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits location scouting in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `location-scouting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for location scouting.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and planning applications",
      "sam": "$350M — landscape and outdoor photography planning tools",
      "som": "$20M — professional landscape and travel photographers"
    },
    {
      "id": "photography-cull-fatigue-companion-4",
      "theme": "photography",
      "title": "Cull Fatigue Companion",
      "pitch": "Cull Fatigue Companion composes post-production workflow in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "post-production workflow",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits post-production workflow in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Cull Fatigue Companion\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCull Fatigue Companion composes post-production workflow in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (post-production workflow).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits post-production workflow in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `post-production-workflow-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for post-production workflow (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for post-production workflow.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo editing and culling software",
      "sam": "$900M — high-volume wedding and event culling tools",
      "som": "$50M — high-volume event photographers managing burnout"
    },
    {
      "id": "photography-rate-card-sparrer-5",
      "theme": "photography",
      "title": "Rate Card Sparrer",
      "pitch": "Rate Card Sparrer boots a long-running service inside a Sprite for client negotiation — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "client negotiation",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches client negotiation in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Rate Card Sparrer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRate Card Sparrer boots a long-running service inside a Sprite for client negotiation — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (client negotiation).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches client negotiation in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `client-negotiation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for client negotiation.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits client negotiation — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for client negotiation.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5.0B — global freelance business and CRM software",
      "sam": "$800M — photography studio management and sales tools",
      "som": "$40M — independent studio owners refining sales pitches"
    },
    {
      "id": "photography-soundscape-shutter-guide-6",
      "theme": "photography",
      "title": "Soundscape Shutter Guide",
      "pitch": "Soundscape Shutter Guide runs a one-shot shell command inside a Sprite for blind accessibility and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "blind accessibility",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits blind accessibility in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Soundscape Shutter Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSoundscape Shutter Guide runs a one-shot shell command inside a Sprite for blind accessibility and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (blind accessibility).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits blind accessibility in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `blind-accessibility-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a blind accessibility-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for blind accessibility.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — global accessibility and assistive technology software",
      "sam": "$200M — creative arts accessibility tools",
      "som": "$15M — visually impaired creators and inclusive art programs"
    },
    {
      "id": "photography-sensor-dust-doctor-7",
      "theme": "photography",
      "title": "Sensor Dust Doctor",
      "pitch": "Sensor Dust Doctor spins up a fresh public Sprite for each session so gear maintenance gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "gear maintenance",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits gear maintenance in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Sensor Dust Doctor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSensor Dust Doctor spins up a fresh public Sprite for each session so gear maintenance gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (gear maintenance).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits gear maintenance in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `gear-maintenance-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for gear maintenance.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and gear maintenance services",
      "sam": "$150M — camera repair and diagnostic software",
      "som": "$10M — field photographers and indie repair technicians"
    },
    {
      "id": "photography-moodboard-muse-talker-8",
      "theme": "photography",
      "title": "Moodboard Muse Talker",
      "pitch": "Moodboard Muse Talker composes conceptual pre-production in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "conceptual pre-production",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits conceptual pre-production in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Moodboard Muse Talker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMoodboard Muse Talker composes conceptual pre-production in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (conceptual pre-production).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits conceptual pre-production in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `conceptual-pre-productio-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for conceptual pre-production (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for conceptual pre-production.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and pre-production tools",
      "sam": "$500M — commercial and conceptual photography planning",
      "som": "$35M — commercial photographers and creative directors"
    },
    {
      "id": "photography-silver-halide-timer-9",
      "theme": "photography",
      "title": "Silver Halide Timer",
      "pitch": "Silver Halide Timer boots a long-running service inside a Sprite for analog darkroom printing — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "analog darkroom printing",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches analog darkroom printing in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Silver Halide Timer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSilver Halide Timer boots a long-running service inside a Sprite for analog darkroom printing — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (analog darkroom printing).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches analog darkroom printing in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `analog-darkroom-printing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for analog darkroom printing.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits analog darkroom printing — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for analog darkroom printing.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and analog supplies market",
      "sam": "$150M — fine art analog darkroom equipment and tools",
      "som": "$12M — fine art printers and analog photography educators"
    },
    {
      "id": "photography-hide-silence-keeper-10",
      "theme": "photography",
      "title": "Hide Silence Keeper",
      "pitch": "Hide Silence Keeper runs a one-shot shell command inside a Sprite for wildlife photography and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "wildlife photography",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits wildlife photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Hide Silence Keeper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHide Silence Keeper runs a one-shot shell command inside a Sprite for wildlife photography and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (wildlife photography).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits wildlife photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `wildlife-photography-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a wildlife photography-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for wildlife photography.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and outdoor gear market",
      "sam": "$300M — wildlife and nature photography planning tools",
      "som": "$20M — professional wildlife photographers and ecotourism guides"
    },
    {
      "id": "photography-plating-palette-coach-11",
      "theme": "photography",
      "title": "Plating Palette Coach",
      "pitch": "Plating Palette Coach spins up a fresh public Sprite for each session so food styling gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "food styling",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits food styling in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Plating Palette Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlating Palette Coach spins up a fresh public Sprite for each session so food styling gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (food styling).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits food styling in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `food-styling-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for food styling.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and culinary media market",
      "sam": "$250M — food photography and styling software",
      "som": "$18M — professional food photographers and prop stylists"
    },
    {
      "id": "photography-star-trail-tracker-12",
      "theme": "photography",
      "title": "Star Trail Tracker",
      "pitch": "Star Trail Tracker composes astrophotography planning in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "astrophotography planning",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits astrophotography planning in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Star Trail Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStar Trail Tracker composes astrophotography planning in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (astrophotography planning).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits astrophotography planning in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `astrophotography-plannin-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for astrophotography planning (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for astrophotography planning.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and astronomy planning apps",
      "sam": "$180M — astrophotography and night sky tools",
      "som": "$15M — dedicated astrophotographers and dark sky guides"
    },
    {
      "id": "photography-vow-whisper-catcher-13",
      "theme": "photography",
      "title": "Vow Whisper Catcher",
      "pitch": "Vow Whisper Catcher boots a long-running service inside a Sprite for wedding photojournalism — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "wedding photojournalism",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches wedding photojournalism in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Vow Whisper Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVow Whisper Catcher boots a long-running service inside a Sprite for wedding photojournalism — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (wedding photojournalism).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches wedding photojournalism in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `wedding-photojournalism-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for wedding photojournalism.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits wedding photojournalism — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for wedding photojournalism.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4.0B — global wedding services and photography software",
      "sam": "$1.2B — wedding photojournalism and editing tools",
      "som": "$60M — high-end wedding photographers and studio owners"
    },
    {
      "id": "photography-perspective-line-corrector-14",
      "theme": "photography",
      "title": "Perspective Line Corrector",
      "pitch": "Perspective Line Corrector runs a one-shot shell command inside a Sprite for architectural photography and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "architectural photography",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits architectural photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Perspective Line Corrector\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPerspective Line Corrector runs a one-shot shell command inside a Sprite for architectural photography and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (architectural photography).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits architectural photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `architectural-photograph-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a architectural photography-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for architectural photography.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and architectural visualization",
      "sam": "$400M — real estate and architectural photography tools",
      "som": "$30M — commercial architectural and interior photographers"
    },
    {
      "id": "photography-focus-stack-breather-15",
      "theme": "photography",
      "title": "Focus Stack Breather",
      "pitch": "Focus Stack Breather spins up a fresh public Sprite for each session so macro focus stacking gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "macro focus stacking",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits macro focus stacking in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Focus Stack Breather\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFocus Stack Breather spins up a fresh public Sprite for each session so macro focus stacking gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (macro focus stacking).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits macro focus stacking in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `macro-focus-stacking-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for macro focus stacking.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and scientific imaging",
      "sam": "$120M — macro and product photography tools",
      "som": "$10M — macro nature and high-end product photographers"
    },
    {
      "id": "photography-candid-rhythm-walker-16",
      "theme": "photography",
      "title": "Candid Rhythm Walker",
      "pitch": "Candid Rhythm Walker composes street photography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "street photography",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits street photography in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Candid Rhythm Walker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCandid Rhythm Walker composes street photography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (street photography).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits street photography in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `street-photography-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for street photography (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for street photography.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and mobile photography apps",
      "sam": "$200M — street and documentary photography tools",
      "som": "$15M — fine art street photographers and urban explorers"
    },
    {
      "id": "photography-buoyancy-color-coach-17",
      "theme": "photography",
      "title": "Buoyancy Color Coach",
      "pitch": "Buoyancy Color Coach boots a long-running service inside a Sprite for underwater lighting — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "underwater lighting",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches underwater lighting in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Buoyancy Color Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBuoyancy Color Coach boots a long-running service inside a Sprite for underwater lighting — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (underwater lighting).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches underwater lighting in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `underwater-lighting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for underwater lighting.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits underwater lighting — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for underwater lighting.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and dive equipment market",
      "sam": "$150M — underwater photography and lighting tools",
      "som": "$12M — professional underwater and marine life photographers"
    },
    {
      "id": "photography-garment-drape-director-18",
      "theme": "photography",
      "title": "Garment Drape Director",
      "pitch": "Garment Drape Director runs a one-shot shell command inside a Sprite for fashion editorial and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "fashion editorial",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits fashion editorial in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Garment Drape Director\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGarment Drape Director runs a one-shot shell command inside a Sprite for fashion editorial and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (fashion editorial).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits fashion editorial in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `fashion-editorial-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a fashion editorial-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for fashion editorial.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.0B — global fashion tech and photography software",
      "sam": "$600M — editorial and fashion photography tools",
      "som": "$40M — fashion photographers and editorial producers"
    },
    {
      "id": "photography-peak-action-anticipator-19",
      "theme": "photography",
      "title": "Peak Action Anticipator",
      "pitch": "Peak Action Anticipator spins up a fresh public Sprite for each session so sports photography gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "sports photography",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits sports photography in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Peak Action Anticipator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPeak Action Anticipator spins up a fresh public Sprite for each session so sports photography gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (sports photography).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits sports photography in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `sports-photography-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sports photography.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and sports media market",
      "sam": "$450M — sports and action photography tools",
      "som": "$35M — professional sports and extreme action photographers"
    },
    {
      "id": "photography-imposter-syndrome-soother-20",
      "theme": "photography",
      "title": "Imposter Syndrome Soother",
      "pitch": "Imposter Syndrome Soother composes creative mindset in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "creative mindset",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits creative mindset in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Imposter Syndrome Soother\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nImposter Syndrome Soother composes creative mindset in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (creative mindset).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits creative mindset in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `creative-mindset-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for creative mindset (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for creative mindset.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global mental health and creative coaching apps",
      "sam": "$300M — creative professional wellness and coaching",
      "som": "$20M — freelance photographers seeking mindset coaching"
    },
    {
      "id": "photography-rotor-wind-whisperer-21",
      "theme": "photography",
      "title": "Rotor Wind Whisperer",
      "pitch": "Rotor Wind Whisperer boots a long-running service inside a Sprite for drone aerial photography — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "drone aerial photography",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches drone aerial photography in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Rotor Wind Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRotor Wind Whisperer boots a long-running service inside a Sprite for drone aerial photography — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (drone aerial photography).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches drone aerial photography in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `drone-aerial-photography-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for drone aerial photography.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits drone aerial photography — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for drone aerial photography.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5.0B — global commercial drone and flight software",
      "sam": "$1.2B — aerial photography and drone mapping tools",
      "som": "$60M — licensed drone photographers and cinematographers"
    },
    {
      "id": "photography-hue-harmony-debater-22",
      "theme": "photography",
      "title": "Hue Harmony Debater",
      "pitch": "Hue Harmony Debater runs a one-shot shell command inside a Sprite for color grading and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "color grading",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits color grading in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Hue Harmony Debater\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHue Harmony Debater runs a one-shot shell command inside a Sprite for color grading and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (color grading).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits color grading in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `color-grading-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a color grading-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for color grading.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo and video editing software",
      "sam": "$800M — advanced color grading and post-production tools",
      "som": "$45M — high-end retouchers and colorists"
    },
    {
      "id": "photography-curator-eye-sparrer-23",
      "theme": "photography",
      "title": "Curator Eye Sparrer",
      "pitch": "Curator Eye Sparrer spins up a fresh public Sprite for each session so portfolio curation gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "portfolio curation",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits portfolio curation in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Curator Eye Sparrer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCurator Eye Sparrer spins up a fresh public Sprite for each session so portfolio curation gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (portfolio curation).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits portfolio curation in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `portfolio-curation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for portfolio curation.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software and art market services",
      "sam": "$200M — portfolio building and gallery submission tools",
      "som": "$15M — fine art photographers preparing for exhibitions"
    },
    {
      "id": "photography-shoot-day-fueler-24",
      "theme": "photography",
      "title": "Shoot Day Fueler",
      "pitch": "Shoot Day Fueler composes on-set nutrition in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "on-set nutrition",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits on-set nutrition in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Shoot Day Fueler\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nShoot Day Fueler composes on-set nutrition in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (on-set nutrition).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits on-set nutrition in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `on-set-nutrition-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for on-set nutrition (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for on-set nutrition.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global health tracking and productivity software",
      "sam": "$250M — creative professional wellness and scheduling tools",
      "som": "$20M — high-volume event and wedding photographers"
    },
    {
      "id": "photography-vista-scout-whisper-0",
      "theme": "photography",
      "title": "Vista Scout Whisper",
      "pitch": "Vista Scout Whisper boots a long-running service inside a Sprite for location scouting — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "location scouting",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches location scouting in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Vista Scout Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVista Scout Whisper boots a long-running service inside a Sprite for location scouting — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (location scouting).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches location scouting in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `location-scouting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for location scouting.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits location scouting — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for location scouting.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$120M — location scouting and pre-production apps",
      "som": "$5M — AI scout assistants"
    },
    {
      "id": "photography-lumen-ratio-caller-1",
      "theme": "photography",
      "title": "Lumen Ratio Caller",
      "pitch": "Lumen Ratio Caller runs a one-shot shell command inside a Sprite for studio lighting and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "studio lighting",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits studio lighting in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Lumen Ratio Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLumen Ratio Caller runs a one-shot shell command inside a Sprite for studio lighting and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (studio lighting).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits studio lighting in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `studio-lighting-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a studio lighting-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for studio lighting.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$300M — studio lighting and grip gear software",
      "som": "$15M — AI lighting assistants"
    },
    {
      "id": "photography-field-note-catcher-2",
      "theme": "photography",
      "title": "Field Note Catcher",
      "pitch": "Field Note Catcher spins up a fresh public Sprite for each session so photojournalism gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "photojournalism",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits photojournalism in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Field Note Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nField Note Catcher spins up a fresh public Sprite for each session so photojournalism gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (photojournalism).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits photojournalism in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `photojournalism-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for photojournalism.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$500M — photojournalism and editorial tools",
      "som": "$25M — AI field logging"
    },
    {
      "id": "photography-pose-cue-prompter-3",
      "theme": "photography",
      "title": "Pose Cue Prompter",
      "pitch": "Pose Cue Prompter composes portrait direction in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "portrait direction",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits portrait direction in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Pose Cue Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPose Cue Prompter composes portrait direction in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (portrait direction).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits portrait direction in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `portrait-direction-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for portrait direction (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for portrait direction.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$400M — portrait studio management software",
      "som": "$18M — AI posing coaches"
    },
    {
      "id": "photography-cull-reaction-tag-4",
      "theme": "photography",
      "title": "Cull Reaction Tag",
      "pitch": "Cull Reaction Tag boots a long-running service inside a Sprite for metadata culling — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "metadata culling",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches metadata culling in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Cull Reaction Tag\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCull Reaction Tag boots a long-running service inside a Sprite for metadata culling — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (metadata culling).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches metadata culling in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `metadata-culling-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for metadata culling.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits metadata culling — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for metadata culling.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$850M — professional workflow and DAM tools",
      "som": "$40M — AI metadata taggers"
    },
    {
      "id": "photography-blind-tracker-murmur-5",
      "theme": "photography",
      "title": "Blind Tracker Murmur",
      "pitch": "Blind Tracker Murmur runs a one-shot shell command inside a Sprite for wildlife photography and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "wildlife photography",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits wildlife photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Blind Tracker Murmur\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlind Tracker Murmur runs a one-shot shell command inside a Sprite for wildlife photography and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (wildlife photography).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits wildlife photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `wildlife-photography-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a wildlife photography-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for wildlife photography.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$90M — nature and wildlife logging apps",
      "som": "$4M — AI blind trackers"
    },
    {
      "id": "photography-night-sky-dictation-6",
      "theme": "photography",
      "title": "Night Sky Dictation",
      "pitch": "Night Sky Dictation spins up a fresh public Sprite for each session so astrophotography gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "astrophotography",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits astrophotography in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Night Sky Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nNight Sky Dictation spins up a fresh public Sprite for each session so astrophotography gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (astrophotography).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits astrophotography in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `astrophotography-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for astrophotography.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$60M — astrophotography planning software",
      "som": "$3M — AI night sky assistants"
    },
    {
      "id": "photography-aisle-shot-whisper-7",
      "theme": "photography",
      "title": "Aisle Shot Whisper",
      "pitch": "Aisle Shot Whisper composes wedding photography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "wedding photography",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits wedding photography in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Aisle Shot Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAisle Shot Whisper composes wedding photography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (wedding photography).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits wedding photography in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `wedding-photography-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for wedding photography (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for wedding photography.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$350M — wedding photography CRM and tools",
      "som": "$20M — AI wedding coordinators"
    },
    {
      "id": "photography-plating-crumb-coach-8",
      "theme": "photography",
      "title": "Plating Crumb Coach",
      "pitch": "Plating Crumb Coach boots a long-running service inside a Sprite for food styling — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "food styling",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches food styling in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Plating Crumb Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlating Crumb Coach boots a long-running service inside a Sprite for food styling — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (food styling).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches food styling in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `food-styling-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for food styling.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits food styling — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for food styling.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$110M — commercial food photography tools",
      "som": "$6M — AI food styling aides"
    },
    {
      "id": "photography-focus-stack-caller-9",
      "theme": "photography",
      "title": "Focus Stack Caller",
      "pitch": "Focus Stack Caller runs a one-shot shell command inside a Sprite for macro photography and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "macro photography",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits macro photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Focus Stack Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFocus Stack Caller runs a one-shot shell command inside a Sprite for macro photography and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (macro photography).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits macro photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `macro-photography-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a macro photography-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for macro photography.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$75M — macro and scientific imaging software",
      "som": "$4M — AI focus stackers"
    },
    {
      "id": "photography-facade-line-critic-10",
      "theme": "photography",
      "title": "Facade Line Critic",
      "pitch": "Facade Line Critic spins up a fresh public Sprite for each session so architectural surveying gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "architectural surveying",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits architectural surveying in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Facade Line Critic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFacade Line Critic spins up a fresh public Sprite for each session so architectural surveying gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (architectural surveying).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits architectural surveying in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `architectural-surveying-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for architectural surveying.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$200M — architectural and real estate media tools",
      "som": "$12M — AI perspective critics"
    },
    {
      "id": "photography-candid-ethics-journal-11",
      "theme": "photography",
      "title": "Candid Ethics Journal",
      "pitch": "Candid Ethics Journal composes street photography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "street photography",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits street photography in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Candid Ethics Journal\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCandid Ethics Journal composes street photography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (street photography).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits street photography in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `street-photography-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for street photography (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for street photography.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$80M — street photography community and legal apps",
      "som": "$5M — AI ethics journalers"
    },
    {
      "id": "photography-rotor-wind-dictation-12",
      "theme": "photography",
      "title": "Rotor Wind Dictation",
      "pitch": "Rotor Wind Dictation boots a long-running service inside a Sprite for drone flight planning — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "drone flight planning",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches drone flight planning in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Rotor Wind Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRotor Wind Dictation boots a long-running service inside a Sprite for drone flight planning — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (drone flight planning).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches drone flight planning in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `drone-flight-planning-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for drone flight planning.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits drone flight planning — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for drone flight planning.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$450M — drone mapping and flight software",
      "som": "$22M — AI drone co-pilots"
    },
    {
      "id": "photography-reef-depth-murmur-13",
      "theme": "photography",
      "title": "Reef Depth Murmur",
      "pitch": "Reef Depth Murmur runs a one-shot shell command inside a Sprite for underwater logging and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "underwater logging",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits underwater logging in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Reef Depth Murmur\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nReef Depth Murmur runs a one-shot shell command inside a Sprite for underwater logging and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (underwater logging).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits underwater logging in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `underwater-logging-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a underwater logging-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for underwater logging.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$40M — dive logging and underwater housing software",
      "som": "$2M — AI reef loggers"
    },
    {
      "id": "photography-grip-truck-rollcall-14",
      "theme": "photography",
      "title": "Grip Truck Rollcall",
      "pitch": "Grip Truck Rollcall spins up a fresh public Sprite for each session so gear inventory gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "gear inventory",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits gear inventory in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Grip Truck Rollcall\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGrip Truck Rollcall spins up a fresh public Sprite for each session so gear inventory gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (gear inventory).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits gear inventory in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `gear-inventory-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for gear inventory.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$150M — equipment rental and inventory management",
      "som": "$8M — AI gear packers"
    },
    {
      "id": "photography-darkroom-agitation-count-15",
      "theme": "photography",
      "title": "Darkroom Agitation Count",
      "pitch": "Darkroom Agitation Count composes film processing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "film processing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits film processing in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Darkroom Agitation Count\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDarkroom Agitation Count composes film processing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (film processing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits film processing in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `film-processing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for film processing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for film processing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$60M — analog film and darkroom tools",
      "som": "$3M — AI darkroom timers"
    },
    {
      "id": "photography-sideline-panning-tracker-16",
      "theme": "photography",
      "title": "Sideline Panning Tracker",
      "pitch": "Sideline Panning Tracker boots a long-running service inside a Sprite for sports photography — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "sports photography",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches sports photography in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Sideline Panning Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSideline Panning Tracker boots a long-running service inside a Sprite for sports photography — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (sports photography).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches sports photography in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `sports-photography-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for sports photography.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits sports photography — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sports photography.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$280M — sports and action photography software",
      "som": "$14M — AI panning trackers"
    },
    {
      "id": "photography-gallery-proof-narrator-17",
      "theme": "photography",
      "title": "Gallery Proof Narrator",
      "pitch": "Gallery Proof Narrator runs a one-shot shell command inside a Sprite for client delivery and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "client delivery",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits client delivery in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Gallery Proof Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGallery Proof Narrator runs a one-shot shell command inside a Sprite for client delivery and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (client delivery).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits client delivery in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `client-delivery-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a client delivery-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for client delivery.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$400M — client gallery and proofing platforms",
      "som": "$20M — AI proof narrators"
    },
    {
      "id": "photography-glare-sweep-dictation-18",
      "theme": "photography",
      "title": "Glare Sweep Dictation",
      "pitch": "Glare Sweep Dictation spins up a fresh public Sprite for each session so product staging gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "product staging",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits product staging in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Glare Sweep Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGlare Sweep Dictation spins up a fresh public Sprite for each session so product staging gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (product staging).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits product staging in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `product-staging-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for product staging.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$180M — e-commerce and product photography tools",
      "som": "$10M — AI glare sweepers"
    },
    {
      "id": "photography-consent-verbal-drafter-19",
      "theme": "photography",
      "title": "Consent Verbal Drafter",
      "pitch": "Consent Verbal Drafter composes model releasing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "model releasing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits model releasing in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Consent Verbal Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nConsent Verbal Drafter composes model releasing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (model releasing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits model releasing in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `model-releasing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for model releasing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for model releasing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$90M — legal and release management apps",
      "som": "$5M — AI consent drafters"
    },
    {
      "id": "photography-grade-tone-whisper-20",
      "theme": "photography",
      "title": "Grade Tone Whisper",
      "pitch": "Grade Tone Whisper boots a long-running service inside a Sprite for color grading — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "color grading",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches color grading in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Grade Tone Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGrade Tone Whisper boots a long-running service inside a Sprite for color grading — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (color grading).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches color grading in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `color-grading-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for color grading.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits color grading — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for color grading.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$600M — color grading and post-production software",
      "som": "$30M — AI grade whisperers"
    },
    {
      "id": "photography-vip-spotter-murmur-21",
      "theme": "photography",
      "title": "VIP Spotter Murmur",
      "pitch": "VIP Spotter Murmur runs a one-shot shell command inside a Sprite for event photography and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "event photography",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits event photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"VIP Spotter Murmur\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVIP Spotter Murmur runs a one-shot shell command inside a Sprite for event photography and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (event photography).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits event photography in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `event-photography-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a event photography-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for event photography.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$220M — event and corporate photography tools",
      "som": "$11M — AI VIP spotters"
    },
    {
      "id": "photography-pet-wrangler-caller-22",
      "theme": "photography",
      "title": "Pet Wrangler Caller",
      "pitch": "Pet Wrangler Caller spins up a fresh public Sprite for each session so animal portraiture gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "animal portraiture",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits animal portraiture in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Pet Wrangler Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPet Wrangler Caller spins up a fresh public Sprite for each session so animal portraiture gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (animal portraiture).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits animal portraiture in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `animal-portraiture-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for animal portraiture.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$130M — pet and family portrait studio software",
      "som": "$7M — AI pet wranglers"
    },
    {
      "id": "photography-room-flow-narrator-23",
      "theme": "photography",
      "title": "Room Flow Narrator",
      "pitch": "Room Flow Narrator composes real estate media in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "real estate media",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits real estate media in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Room Flow Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRoom Flow Narrator composes real estate media in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (real estate media).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits real estate media in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `real-estate-media-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for real estate media (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for real estate media.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$310M — real estate media and virtual tour software",
      "som": "$16M — AI room flow narrators"
    },
    {
      "id": "photography-sensor-dust-dictation-24",
      "theme": "photography",
      "title": "Sensor Dust Dictation",
      "pitch": "Sensor Dust Dictation boots a long-running service inside a Sprite for gear maintenance — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "gear maintenance",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches gear maintenance in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Sensor Dust Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSensor Dust Dictation boots a long-running service inside a Sprite for gear maintenance — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (gear maintenance).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches gear maintenance in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `gear-maintenance-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for gear maintenance.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits gear maintenance — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for gear maintenance.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$70M — camera repair and maintenance logging tools",
      "som": "$4M — AI sensor dust mappers"
    },
    {
      "id": "photography-essay-tension-scorer-0",
      "theme": "photography",
      "title": "Essay Tension Scorer",
      "pitch": "Essay Tension Scorer runs a one-shot shell command inside a Sprite for photojournalism pacing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "photojournalism pacing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits photojournalism pacing in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Essay Tension Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEssay Tension Scorer runs a one-shot shell command inside a Sprite for photojournalism pacing and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (photojournalism pacing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits photojournalism pacing in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `photojournalism-pacing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a photojournalism pacing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for photojournalism pacing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$500M — editorial and photojournalism software",
      "som": "$15M — AI pacing tools for photo essays"
    },
    {
      "id": "photography-subject-calm-generator-1",
      "theme": "photography",
      "title": "Subject Calm Generator",
      "pitch": "Subject Calm Generator spins up a fresh public Sprite for each session so portrait studio atmosphere gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "portrait studio atmosphere",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits portrait studio atmosphere in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Subject Calm Generator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSubject Calm Generator spins up a fresh public Sprite for each session so portrait studio atmosphere gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (portrait studio atmosphere).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits portrait studio atmosphere in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `portrait-studio-atmosphe-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for portrait studio atmosphere.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$300M — portrait studio management tools",
      "som": "$10M — AI subject relaxation audio aids"
    },
    {
      "id": "photography-retouching-flow-beats-2",
      "theme": "photography",
      "title": "Retouching Flow Beats",
      "pitch": "Retouching Flow Beats composes post-production focus in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "post-production focus",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits post-production focus in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Retouching Flow Beats\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRetouching Flow Beats composes post-production focus in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (post-production focus).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits post-production focus in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `post-production-focus-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for post-production focus (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for post-production focus.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$800M — professional editing and workflow tools",
      "som": "$25M — AI focus aids for photo editors"
    },
    {
      "id": "photography-blind-wait-companion-3",
      "theme": "photography",
      "title": "Blind Wait Companion",
      "pitch": "Blind Wait Companion boots a long-running service inside a Sprite for wildlife photography patience — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "wildlife photography patience",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches wildlife photography patience in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Blind Wait Companion\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlind Wait Companion boots a long-running service inside a Sprite for wildlife photography patience — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (wildlife photography patience).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches wildlife photography patience in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `wildlife-photography-pat-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for wildlife photography patience.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits wildlife photography patience — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for wildlife photography patience.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$150M — wildlife and nature photography gear",
      "som": "$5M — AI patience aids for nature shooters"
    },
    {
      "id": "photography-slideshow-swell-composer-4",
      "theme": "photography",
      "title": "Slideshow Swell Composer",
      "pitch": "Slideshow Swell Composer runs a one-shot shell command inside a Sprite for wedding video editing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "wedding video editing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits wedding video editing in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Slideshow Swell Composer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSlideshow Swell Composer runs a one-shot shell command inside a Sprite for wedding video editing and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (wedding video editing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits wedding video editing in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `wedding-video-editing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a wedding video editing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for wedding video editing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$400M — wedding photography and videography software",
      "som": "$20M — AI audio scoring for wedding slideshows"
    },
    {
      "id": "photography-urban-rhythm-walker-5",
      "theme": "photography",
      "title": "Urban Rhythm Walker",
      "pitch": "Urban Rhythm Walker spins up a fresh public Sprite for each session so street photography pacing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "street photography pacing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits street photography pacing in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Urban Rhythm Walker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nUrban Rhythm Walker spins up a fresh public Sprite for each session so street photography pacing gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (street photography pacing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits street photography pacing in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `street-photography-pacin-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for street photography pacing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$100M — street photography apps and guides",
      "som": "$8M — AI pacing tools for street photographers"
    },
    {
      "id": "photography-reveal-foley-crafter-6",
      "theme": "photography",
      "title": "Reveal Foley Crafter",
      "pitch": "Reveal Foley Crafter composes commercial product video in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "commercial product video",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits commercial product video in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Reveal Foley Crafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nReveal Foley Crafter composes commercial product video in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (commercial product video).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits commercial product video in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `commercial-product-video-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for commercial product video (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for commercial product video.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$250M — commercial product photography tools",
      "som": "$12M — AI Foley generators for product reels"
    },
    {
      "id": "photography-deep-space-timer-7",
      "theme": "photography",
      "title": "Deep Space Timer",
      "pitch": "Deep Space Timer boots a long-running service inside a Sprite for night sky exposure — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "night sky exposure",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches night sky exposure in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Deep Space Timer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDeep Space Timer boots a long-running service inside a Sprite for night sky exposure — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (night sky exposure).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches night sky exposure in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `night-sky-exposure-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for night sky exposure.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits night sky exposure — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for night sky exposure.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$50M — astrophotography planning software",
      "som": "$3M — AI audio timers for night shooters"
    },
    {
      "id": "photography-sensory-sizzle-scorer-8",
      "theme": "photography",
      "title": "Sensory Sizzle Scorer",
      "pitch": "Sensory Sizzle Scorer runs a one-shot shell command inside a Sprite for food photography reels and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "food photography reels",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits food photography reels in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Sensory Sizzle Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSensory Sizzle Scorer runs a one-shot shell command inside a Sprite for food photography reels and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (food photography reels).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits food photography reels in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `food-photography-reels-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a food photography reels-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for food photography reels.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$120M — food photography and styling tools",
      "som": "$6M — AI sensory audio for food creators"
    },
    {
      "id": "photography-spatial-resonance-drafter-9",
      "theme": "photography",
      "title": "Spatial Resonance Drafter",
      "pitch": "Spatial Resonance Drafter spins up a fresh public Sprite for each session so architectural visualization gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "architectural visualization",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits architectural visualization in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Spatial Resonance Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSpatial Resonance Drafter spins up a fresh public Sprite for each session so architectural visualization gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (architectural visualization).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits architectural visualization in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `architectural-visualizat-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for architectural visualization.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$200M — architectural photography and rendering tools",
      "som": "$10M — AI spatial audio for architecture tours"
    },
    {
      "id": "photography-runway-pulse-generator-10",
      "theme": "photography",
      "title": "Runway Pulse Generator",
      "pitch": "Runway Pulse Generator composes fashion editorial shoots in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "fashion editorial shoots",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits fashion editorial shoots in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Runway Pulse Generator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRunway Pulse Generator composes fashion editorial shoots in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (fashion editorial shoots).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits fashion editorial shoots in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `fashion-editorial-shoots-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for fashion editorial shoots (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for fashion editorial shoots.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$180M — fashion photography production software",
      "som": "$9M — AI mood-setters for fashion shoots"
    },
    {
      "id": "photography-micro-world-ambience-11",
      "theme": "photography",
      "title": "Micro World Ambience",
      "pitch": "Micro World Ambience boots a long-running service inside a Sprite for macro photography immersion — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "macro photography immersion",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches macro photography immersion in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Micro World Ambience\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMicro World Ambience boots a long-running service inside a Sprite for macro photography immersion — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (macro photography immersion).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches macro photography immersion in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `macro-photography-immers-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for macro photography immersion.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits macro photography immersion — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for macro photography immersion.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$75M — macro and close-up photography tools",
      "som": "$4M — AI soundscapes for macro exhibitions"
    },
    {
      "id": "photography-flight-path-sweeper-12",
      "theme": "photography",
      "title": "Flight Path Sweeper",
      "pitch": "Flight Path Sweeper runs a one-shot shell command inside a Sprite for drone photography planning and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "drone photography planning",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits drone photography planning in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Flight Path Sweeper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFlight Path Sweeper runs a one-shot shell command inside a Sprite for drone photography planning and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (drone photography planning).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits drone photography planning in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `drone-photography-planni-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a drone photography planning-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for drone photography planning.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$350M — drone and aerial photography software",
      "som": "$15M — AI flight-scoring tools for drone pilots"
    },
    {
      "id": "photography-action-hype-builder-13",
      "theme": "photography",
      "title": "Action Hype Builder",
      "pitch": "Action Hype Builder spins up a fresh public Sprite for each session so sports photography editing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "sports photography editing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits sports photography editing in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Action Hype Builder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAction Hype Builder spins up a fresh public Sprite for each session so sports photography editing gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (sports photography editing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits sports photography editing in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `sports-photography-editi-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sports photography editing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$220M — sports photography and editing tools",
      "som": "$11M — AI hype tracks for sports editors"
    },
    {
      "id": "photography-darkroom-flow-beats-14",
      "theme": "photography",
      "title": "Darkroom Flow Beats",
      "pitch": "Darkroom Flow Beats composes analog film processing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "analog film processing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits analog film processing in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Darkroom Flow Beats\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDarkroom Flow Beats composes analog film processing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (analog film processing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits analog film processing in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `analog-film-processing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for analog film processing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for analog film processing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$40M — analog and film photography apps",
      "som": "$2M — AI audio aids for darkroom printers"
    },
    {
      "id": "photography-dispatch-audio-masker-15",
      "theme": "photography",
      "title": "Dispatch Audio Masker",
      "pitch": "Dispatch Audio Masker boots a long-running service inside a Sprite for field reporting privacy — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "field reporting privacy",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches field reporting privacy in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Dispatch Audio Masker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDispatch Audio Masker boots a long-running service inside a Sprite for field reporting privacy — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (field reporting privacy).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches field reporting privacy in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `field-reporting-privacy-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for field reporting privacy.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits field reporting privacy — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for field reporting privacy.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$500M — editorial and photojournalism software",
      "som": "$12M — AI privacy audio for field reporters"
    },
    {
      "id": "photography-golden-hour-drifter-16",
      "theme": "photography",
      "title": "Golden Hour Drifter",
      "pitch": "Golden Hour Drifter runs a one-shot shell command inside a Sprite for landscape photography mindset and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "landscape photography mindset",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits landscape photography mindset in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Golden Hour Drifter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGolden Hour Drifter runs a one-shot shell command inside a Sprite for landscape photography mindset and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (landscape photography mindset).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits landscape photography mindset in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `landscape-photography-mi-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a landscape photography mindset-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for landscape photography mindset.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$160M — landscape photography planning tools",
      "som": "$7M — AI mindfulness audio for landscape shooters"
    },
    {
      "id": "photography-pit-rhythm-anticipator-17",
      "theme": "photography",
      "title": "Pit Rhythm Anticipator",
      "pitch": "Pit Rhythm Anticipator spins up a fresh public Sprite for each session so concert photography timing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "concert photography timing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits concert photography timing in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Pit Rhythm Anticipator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPit Rhythm Anticipator spins up a fresh public Sprite for each session so concert photography timing gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (concert photography timing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits concert photography timing in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `concert-photography-timi-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for concert photography timing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$90M — event and concert photography tools",
      "som": "$5M — AI rhythm aids for music photographers"
    },
    {
      "id": "photography-property-tour-scorer-18",
      "theme": "photography",
      "title": "Property Tour Scorer",
      "pitch": "Property Tour Scorer composes real estate marketing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "real estate marketing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits real estate marketing in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Property Tour Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProperty Tour Scorer composes real estate marketing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (real estate marketing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits real estate marketing in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `real-estate-marketing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for real estate marketing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for real estate marketing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$280M — real estate photography software",
      "som": "$14M — AI audio tours for property photographers"
    },
    {
      "id": "photography-canine-focus-clicker-19",
      "theme": "photography",
      "title": "Canine Focus Clicker",
      "pitch": "Canine Focus Clicker boots a long-running service inside a Sprite for pet photography engagement — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "pet photography engagement",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches pet photography engagement in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Canine Focus Clicker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCanine Focus Clicker boots a long-running service inside a Sprite for pet photography engagement — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (pet photography engagement).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches pet photography engagement in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `pet-photography-engageme-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for pet photography engagement.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits pet photography engagement — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for pet photography engagement.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$60M — pet photography studio tools",
      "som": "$3M — AI engagement audio for pet photographers"
    },
    {
      "id": "photography-gallery-space-resonator-20",
      "theme": "photography",
      "title": "Gallery Space Resonator",
      "pitch": "Gallery Space Resonator runs a one-shot shell command inside a Sprite for exhibition curation and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "exhibition curation",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits exhibition curation in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Gallery Space Resonator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGallery Space Resonator runs a one-shot shell command inside a Sprite for exhibition curation and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (exhibition curation).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits exhibition curation in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `exhibition-curation-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a exhibition curation-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for exhibition curation.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$110M — fine art and gallery software",
      "som": "$6M — AI curation audio for gallery spaces"
    },
    {
      "id": "photography-reef-breath-pacer-21",
      "theme": "photography",
      "title": "Reef Breath Pacer",
      "pitch": "Reef Breath Pacer spins up a fresh public Sprite for each session so underwater photography diving gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "underwater photography diving",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits underwater photography diving in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Reef Breath Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nReef Breath Pacer spins up a fresh public Sprite for each session so underwater photography diving gets its own shareable sandbox URL in seconds.\nDiscipline: Photography (underwater photography diving).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits underwater photography diving in Photography because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `underwater-photography-d-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for underwater photography diving.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$45M — underwater photography gear and apps",
      "som": "$2M — AI breathing aids for dive photographers"
    },
    {
      "id": "photography-engine-roar-syncer-22",
      "theme": "photography",
      "title": "Engine Roar Syncer",
      "pitch": "Engine Roar Syncer composes automotive photography reels in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "automotive photography reels",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits automotive photography reels in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Engine Roar Syncer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEngine Roar Syncer composes automotive photography reels in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Photography (automotive photography reels).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits automotive photography reels in Photography because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `automotive-photography-r-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for automotive photography reels (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for automotive photography reels.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$130M — automotive photography and video tools",
      "som": "$8M — AI engine audio for car content creators"
    },
    {
      "id": "photography-nursery-shush-weaver-23",
      "theme": "photography",
      "title": "Nursery Shush Weaver",
      "pitch": "Nursery Shush Weaver boots a long-running service inside a Sprite for newborn photography soothing — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "newborn photography soothing",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches newborn photography soothing in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Nursery Shush Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nNursery Shush Weaver boots a long-running service inside a Sprite for newborn photography soothing — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Photography (newborn photography soothing).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches newborn photography soothing in Photography because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `newborn-photography-soot-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for newborn photography soothing.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits newborn photography soothing — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for newborn photography soothing.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$85M — newborn and maternity photography tools",
      "som": "$4M — AI soothing audio for newborn photographers"
    },
    {
      "id": "photography-transit-soundscape-journal-24",
      "theme": "photography",
      "title": "Transit Soundscape Journal",
      "pitch": "Transit Soundscape Journal runs a one-shot shell command inside a Sprite for travel photography memory and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "travel photography memory",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits travel photography memory in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Transit Soundscape Journal\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTransit Soundscape Journal runs a one-shot shell command inside a Sprite for travel photography memory and streams the stdout back so the user watches the work happen live.\nDiscipline: Photography (travel photography memory).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits travel photography memory in Photography because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `travel-photography-memor-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a travel photography memory-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for travel photography memory.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.4B — global photo software market",
      "sam": "$190M — travel photography and journaling apps",
      "som": "$9M — AI memory audio for travel photographers"
    }
  ]
}