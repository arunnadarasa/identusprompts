{
  "theme": {
    "slug": "video",
    "name": "Videography & Film",
    "emoji": "🎥",
    "audience": "videographers, editors, content creators",
    "market_anchor": "the video editing software market (~$1.1B) and >50M creators"
  },
  "ideas": [
    {
      "id": "video-lens-whisperer-0",
      "theme": "video",
      "title": "Lens Whisperer",
      "pitch": "Lovable AI generates dynamic shot lists so directors can focus on set; ElevenLabs speaks it.",
      "subDiscipline": "shot listing",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Hands-free audio cues keep the director's eyes on the actors and viewfinder.",
      "megaPrompt": "Build \"Lens Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates dynamic shot lists so directors can focus on set; ElevenLabs speaks it.\nDiscipline: Videography & Film (shot listing).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Hands-free audio cues keep the director's eyes on the actors and viewfinder.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the shot listing use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for shot listing. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for shot listing.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and production software",
      "sam": "$400M — professional on-set production and directing tools",
      "som": "$60M — indie directors and commercial production agencies"
    },
    {
      "id": "video-chroma-meditation-1",
      "theme": "video",
      "title": "Chroma Meditation",
      "pitch": "Lovable AI writes visual meditations so colorists can rest their eyes; ElevenLabs narrates it.",
      "subDiscipline": "colorist mindset",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Audio storytelling provides a necessary screen-free mental break during grueling grading sessions.",
      "megaPrompt": "Build \"Chroma Meditation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes visual meditations so colorists can rest their eyes; ElevenLabs narrates it.\nDiscipline: Videography & Film (colorist mindset).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Audio storytelling provides a necessary screen-free mental break during grueling grading sessions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the colorist mindset use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for colorist mindset. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for colorist mindset.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global post-production and color grading market",
      "sam": "$250M — professional color grading software and plugins",
      "som": "$35M — freelance colorists and boutique post-houses"
    },
    {
      "id": "video-table-read-phantom-2",
      "theme": "video",
      "title": "Table Read Phantom",
      "pitch": "Lovable AI rewrites dialogue so writers can test pacing; ElevenLabs voices it.",
      "subDiscipline": "script rehearsal",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS allows a single writer to hear multi-character interactions without hiring actors.",
      "megaPrompt": "Build \"Table Read Phantom\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites dialogue so writers can test pacing; ElevenLabs voices it.\nDiscipline: Videography & Film (script rehearsal).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS allows a single writer to hear multi-character interactions without hiring actors.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the script rehearsal use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for script rehearsal. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for script rehearsal.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global screenwriting and pre-production software",
      "sam": "$300M — professional script development and rehearsal tools",
      "som": "$45M — independent screenwriters and showrunner assistants"
    },
    {
      "id": "video-footstep-maestro-3",
      "theme": "video",
      "title": "Footstep Maestro",
      "pitch": "Lovable AI analyzes the scene so foley artists know what to record; ElevenLabs reads it.",
      "subDiscipline": "foley artistry",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Spoken cues synced to video playback keep the artist's hands free for props.",
      "megaPrompt": "Build \"Footstep Maestro\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes the scene so foley artists know what to record; ElevenLabs reads it.\nDiscipline: Videography & Film (foley artistry).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Spoken cues synced to video playback keep the artist's hands free for props.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the foley artistry use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for foley artistry. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for foley artistry.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global audio post-production and sound design market",
      "sam": "$200M — professional foley and sound effects creation tools",
      "som": "$30M — freelance foley artists and sound design studios"
    },
    {
      "id": "video-cut-pacer-4",
      "theme": "video",
      "title": "Cut Pacer",
      "pitch": "Lovable AI plans emotional beats so editors maintain flow; ElevenLabs speaks it.",
      "subDiscipline": "editing rhythm",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Auditory pacing helps editors internalize the rhythm of a montage without looking away.",
      "megaPrompt": "Build \"Cut Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans emotional beats so editors maintain flow; ElevenLabs speaks it.\nDiscipline: Videography & Film (editing rhythm).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Auditory pacing helps editors internalize the rhythm of a montage without looking away.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the editing rhythm use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for editing rhythm. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for editing rhythm.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$500M — professional NLE and timeline editing tools",
      "som": "$80M — YouTube creators and freelance video editors"
    },
    {
      "id": "video-lumen-caller-5",
      "theme": "video",
      "title": "Lumen Caller",
      "pitch": "Lovable AI calculates lighting ratios so gaffers can adjust flags; ElevenLabs voices it.",
      "subDiscipline": "lighting design",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Gaffers need both hands on stands and barn doors while adjusting complex rigs.",
      "megaPrompt": "Build \"Lumen Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates lighting ratios so gaffers can adjust flags; ElevenLabs voices it.\nDiscipline: Videography & Film (lighting design).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Gaffers need both hands on stands and barn doors while adjusting complex rigs.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the lighting design use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for lighting design. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for lighting design.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global cinematography and lighting equipment market",
      "sam": "$350M — professional on-set lighting design and planning tools",
      "som": "$50M — directors of photography and commercial gaffers"
    },
    {
      "id": "video-scripty-echo-6",
      "theme": "video",
      "title": "Scripty Echo",
      "pitch": "Lovable AI reviews take details so the script supervisor stays sharp; ElevenLabs speaks it.",
      "subDiscipline": "continuity tracking",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Discreet audio prompts prevent continuity errors without distracting the crew on set.",
      "megaPrompt": "Build \"Scripty Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI reviews take details so the script supervisor stays sharp; ElevenLabs speaks it.\nDiscipline: Videography & Film (continuity tracking).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Discreet audio prompts prevent continuity errors without distracting the crew on set.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the continuity tracking use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for continuity tracking. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for continuity tracking.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production management software",
      "sam": "$150M — on-set continuity and script supervision tools",
      "som": "$25M — freelance script supervisors and indie producers"
    },
    {
      "id": "video-node-navigator-7",
      "theme": "video",
      "title": "Node Navigator",
      "pitch": "Lovable AI troubleshoots the node tree so compositors avoid dead ends; ElevenLabs reads it.",
      "subDiscipline": "VFX compositing",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Voice-guided shortcuts keep the VFX artist's hands on the keyboard and mouse.",
      "megaPrompt": "Build \"Node Navigator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI troubleshoots the node tree so compositors avoid dead ends; ElevenLabs reads it.\nDiscipline: Videography & Film (VFX compositing).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Voice-guided shortcuts keep the VFX artist's hands on the keyboard and mouse.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the VFX compositing use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for VFX compositing. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for VFX compositing.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global VFX and motion graphics software market",
      "sam": "$450M — professional node-based compositing applications",
      "som": "$70M — freelance VFX artists and boutique FX studios"
    },
    {
      "id": "video-emotion-prompter-8",
      "theme": "video",
      "title": "Emotion Prompter",
      "pitch": "Lovable AI drafts psychological motivations so directors can guide performances; ElevenLabs narrates it.",
      "subDiscipline": "actor coaching",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Private audio coaching helps directors deliver nuanced notes without breaking the actor's focus.",
      "megaPrompt": "Build \"Emotion Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts psychological motivations so directors can guide performances; ElevenLabs narrates it.\nDiscipline: Videography & Film (actor coaching).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Private audio coaching helps directors deliver nuanced notes without breaking the actor's focus.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the actor coaching use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for actor coaching. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for actor coaching.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production and directing tools",
      "sam": "$200M — professional actor coaching and rehearsal software",
      "som": "$30M — indie directors and acting coaches"
    },
    {
      "id": "video-archive-scout-9",
      "theme": "video",
      "title": "Archive Scout",
      "pitch": "Lovable AI describes ideal archival footage so editors search faster; ElevenLabs speaks it.",
      "subDiscipline": "stock footage curation",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Spoken keywords allow editors to type search queries while watching the preview monitor.",
      "megaPrompt": "Build \"Archive Scout\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI describes ideal archival footage so editors search faster; ElevenLabs speaks it.\nDiscipline: Videography & Film (stock footage curation).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Spoken keywords allow editors to type search queries while watching the preview monitor.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the stock footage curation use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for stock footage curation. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for stock footage curation.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global stock media and asset management market",
      "sam": "$300M — professional video asset curation and licensing tools",
      "som": "$45M — documentary editors and newsroom producers"
    },
    {
      "id": "video-rotor-choreographer-10",
      "theme": "video",
      "title": "Rotor Choreographer",
      "pitch": "Lovable AI plots flight paths so drone pilots capture cinematic sweeps; ElevenLabs voices it.",
      "subDiscipline": "drone cinematography",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Pilots must keep their eyes on the drone and FPV goggles, not a screen.",
      "megaPrompt": "Build \"Rotor Choreographer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plots flight paths so drone pilots capture cinematic sweeps; ElevenLabs voices it.\nDiscipline: Videography & Film (drone cinematography).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Pilots must keep their eyes on the drone and FPV goggles, not a screen.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the drone cinematography use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for drone cinematography. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for drone cinematography.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global aerial cinematography and drone software",
      "sam": "$250M — professional drone flight planning and camera tools",
      "som": "$40M — freelance drone operators and aerial production houses"
    },
    {
      "id": "video-logline-rehearser-11",
      "theme": "video",
      "title": "Logline Rehearser",
      "pitch": "Lovable AI refines the movie pitch so filmmakers sound confident; ElevenLabs speaks it.",
      "subDiscipline": "pitch preparation",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Simulating a stressful Q&A via voice builds muscle memory for high-stakes meetings.",
      "megaPrompt": "Build \"Logline Rehearser\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI refines the movie pitch so filmmakers sound confident; ElevenLabs speaks it.\nDiscipline: Videography & Film (pitch preparation).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Simulating a stressful Q&A via voice builds muscle memory for high-stakes meetings.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the pitch preparation use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for pitch preparation. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for pitch preparation.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film financing and pitch deck software",
      "sam": "$100M — professional pitch preparation and presentation tools",
      "som": "$15M — independent producers and film festival applicants"
    },
    {
      "id": "video-frequencies-ghost-12",
      "theme": "video",
      "title": "Frequencies Ghost",
      "pitch": "Lovable AI identifies muddy frequencies so mixers can EQ perfectly; ElevenLabs reads it.",
      "subDiscipline": "audio mixing",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Mixers need to listen to the audio bed, making visual screen-reading disruptive.",
      "megaPrompt": "Build \"Frequencies Ghost\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies muddy frequencies so mixers can EQ perfectly; ElevenLabs reads it.\nDiscipline: Videography & Film (audio mixing).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Mixers need to listen to the audio bed, making visual screen-reading disruptive.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the audio mixing use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for audio mixing. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for audio mixing.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global audio post-production and mixing market",
      "sam": "$350M — professional DAW and mixing plugin software",
      "som": "$55M — freelance re-recording mixers and podcast editors"
    },
    {
      "id": "video-sun-tracker-13",
      "theme": "video",
      "title": "Sun Tracker",
      "pitch": "Lovable AI calculates golden hour shadows so scouts pick the best angles; ElevenLabs narrates it.",
      "subDiscipline": "location scouting",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Scouts need to look at the physical environment, not a mobile screen.",
      "megaPrompt": "Build \"Sun Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates golden hour shadows so scouts pick the best angles; ElevenLabs narrates it.\nDiscipline: Videography & Film (location scouting).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Scouts need to look at the physical environment, not a mobile screen.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the location scouting use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for location scouting. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for location scouting.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global pre-production and location management software",
      "sam": "$150M — professional location scouting and sun-tracking apps",
      "som": "$25M — location managers and indie cinematographers"
    },
    {
      "id": "video-sensor-sweeper-14",
      "theme": "video",
      "title": "Sensor Sweeper",
      "pitch": "Lovable AI outlines cleaning steps so techs avoid damaging sensors; ElevenLabs speaks it.",
      "subDiscipline": "gear maintenance",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Techs need both hands and steady focus on the fragile camera sensor.",
      "megaPrompt": "Build \"Sensor Sweeper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI outlines cleaning steps so techs avoid damaging sensors; ElevenLabs speaks it.\nDiscipline: Videography & Film (gear maintenance).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Techs need both hands and steady focus on the fragile camera sensor.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the gear maintenance use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for gear maintenance. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for gear maintenance.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global camera rental and equipment maintenance market",
      "sam": "$200M — professional camera tech and repair management tools",
      "som": "$30M — camera rental houses and on-set DITs"
    },
    {
      "id": "video-clickbait-critic-15",
      "theme": "video",
      "title": "Clickbait Critic",
      "pitch": "Lovable AI critiques thumbnail composition so creators boost click-through rates; ElevenLabs voices it.",
      "subDiscipline": "thumbnail strategy",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Audio feedback lets creators adjust Photoshop layers while absorbing the critique.",
      "megaPrompt": "Build \"Clickbait Critic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI critiques thumbnail composition so creators boost click-through rates; ElevenLabs voices it.\nDiscipline: Videography & Film (thumbnail strategy).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Audio feedback lets creators adjust Photoshop layers while absorbing the critique.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the thumbnail strategy use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for thumbnail strategy. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for thumbnail strategy.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global creator economy and video marketing tools",
      "sam": "$400M — professional YouTube optimization and thumbnail software",
      "som": "$60M — full-time YouTubers and digital marketing agencies"
    },
    {
      "id": "video-fall-caller-16",
      "theme": "video",
      "title": "Fall Caller",
      "pitch": "Lovable AI sequences stunt beats so coordinators ensure safety; ElevenLabs speaks it.",
      "subDiscipline": "stunt safety",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Loud, clear voice commands are essential for timing dangerous physical movements safely.",
      "megaPrompt": "Build \"Fall Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sequences stunt beats so coordinators ensure safety; ElevenLabs speaks it.\nDiscipline: Videography & Film (stunt safety).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Loud, clear voice commands are essential for timing dangerous physical movements safely.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the stunt safety use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for stunt safety. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for stunt safety.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production and stunt coordination market",
      "sam": "$100M — professional stunt planning and safety management tools",
      "som": "$15M — stunt coordinators and action sequence directors"
    },
    {
      "id": "video-alt-text-weaver-17",
      "theme": "video",
      "title": "Alt-Text Weaver",
      "pitch": "Lovable AI describes visual action so blind audiences can enjoy the film; ElevenLabs narrates it.",
      "subDiscipline": "video accessibility",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "High-quality TTS creates an immediate, emotive audio description track without hiring voice actors.",
      "megaPrompt": "Build \"Alt-Text Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI describes visual action so blind audiences can enjoy the film; ElevenLabs narrates it.\nDiscipline: Videography & Film (video accessibility).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: High-quality TTS creates an immediate, emotive audio description track without hiring voice actors.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the video accessibility use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for video accessibility. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for video accessibility.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video localization and accessibility software",
      "sam": "$250M — professional audio description and captioning tools",
      "som": "$40M — streaming platforms and indie film distributors"
    },
    {
      "id": "video-set-menu-minder-18",
      "theme": "video",
      "title": "Set Menu Minder",
      "pitch": "Lovable AI designs allergy-safe meals so the crafty team feeds the crew; ElevenLabs reads it.",
      "subDiscipline": "craft services planning",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Radio broadcasts ensure the entire crew hears dietary warnings over the noise.",
      "megaPrompt": "Build \"Set Menu Minder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs allergy-safe meals so the crafty team feeds the crew; ElevenLabs reads it.\nDiscipline: Videography & Film (craft services planning).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Radio broadcasts ensure the entire crew hears dietary warnings over the noise.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the craft services planning use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for craft services planning. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for craft services planning.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production logistics and catering market",
      "sam": "$150M — professional set management and craft services tools",
      "som": "$20M — unit production managers and craft service captains"
    },
    {
      "id": "video-focus-whisper-19",
      "theme": "video",
      "title": "Focus Whisper",
      "pitch": "Lovable AI calculates depth of field so the AC nails the mark; ElevenLabs speaks it.",
      "subDiscipline": "focus pulling",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "The first AC must keep their eyes locked on the lens marks and actors.",
      "megaPrompt": "Build \"Focus Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates depth of field so the AC nails the mark; ElevenLabs speaks it.\nDiscipline: Videography & Film (focus pulling).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: The first AC must keep their eyes locked on the lens marks and actors.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the focus pulling use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for focus pulling. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for focus pulling.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global cinematography and camera department tools",
      "sam": "$200M — professional focus pulling and lens measurement apps",
      "som": "$35M — first assistant cameras and indie DP crews"
    },
    {
      "id": "video-kinetic-type-coach-20",
      "theme": "video",
      "title": "Kinetic Type Coach",
      "pitch": "Lovable AI suggests typography animations so designers avoid cliches; ElevenLabs voices it.",
      "subDiscipline": "motion graphics",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Hearing the animation rhythm helps motion designers match the visual bounce perfectly.",
      "megaPrompt": "Build \"Kinetic Type Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI suggests typography animations so designers avoid cliches; ElevenLabs voices it.\nDiscipline: Videography & Film (motion graphics).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Hearing the animation rhythm helps motion designers match the visual bounce perfectly.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the motion graphics use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for motion graphics. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for motion graphics.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global motion graphics and title design software",
      "sam": "$350M — professional 2D animation and typography tools",
      "som": "$50M — freelance motion designers and broadcast networks"
    },
    {
      "id": "video-radio-protocol-tutor-21",
      "theme": "video",
      "title": "Radio Protocol Tutor",
      "pitch": "Lovable AI generates walkie-talkie scenarios so PAs learn set lingo; ElevenLabs speaks it.",
      "subDiscipline": "production assistant training",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Realistic voice simulations train PAs to stay calm under intense radio pressure.",
      "megaPrompt": "Build \"Radio Protocol Tutor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates walkie-talkie scenarios so PAs learn set lingo; ElevenLabs speaks it.\nDiscipline: Videography & Film (production assistant training).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Realistic voice simulations train PAs to stay calm under intense radio pressure.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the production assistant training use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for production assistant training. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for production assistant training.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production training and management software",
      "sam": "$100M — professional crew onboarding and set protocol tools",
      "som": "$15M — production coordinators and film school students"
    },
    {
      "id": "video-xml-translator-22",
      "theme": "video",
      "title": "XML Translator",
      "pitch": "Lovable AI writes handoff notes so editors brief the colorist perfectly; ElevenLabs narrates it.",
      "subDiscipline": "workflow handoffs",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Audio summaries allow the receiving artist to listen while importing massive project files.",
      "megaPrompt": "Build \"XML Translator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes handoff notes so editors brief the colorist perfectly; ElevenLabs narrates it.\nDiscipline: Videography & Film (workflow handoffs).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Audio summaries allow the receiving artist to listen while importing massive project files.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the workflow handoffs use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for workflow handoffs. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for workflow handoffs.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global post-production pipeline and collaboration software",
      "sam": "$300M — professional NLE handoff and XML translation tools",
      "som": "$45M — post-production supervisors and assistant editors"
    },
    {
      "id": "video-retention-detective-23",
      "theme": "video",
      "title": "Retention Detective",
      "pitch": "Lovable AI analyzes retention graphs so creators fix boring segments; ElevenLabs reads it.",
      "subDiscipline": "audience analytics",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Spoken timestamps let the editor instantly scrub to the problem area on the timeline.",
      "megaPrompt": "Build \"Retention Detective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes retention graphs so creators fix boring segments; ElevenLabs reads it.\nDiscipline: Videography & Film (audience analytics).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Spoken timestamps let the editor instantly scrub to the problem area on the timeline.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the audience analytics use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for audience analytics. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for audience analytics.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global creator analytics and video optimization market",
      "sam": "$400M — professional audience retention and A/B testing tools",
      "som": "$60M — high-tier YouTubers and digital content strategists"
    },
    {
      "id": "video-wrap-speech-writer-24",
      "theme": "video",
      "title": "Wrap Speech Writer",
      "pitch": "Lovable AI drafts a heartfelt wrap speech so the producer thanks everyone; ElevenLabs voices it.",
      "subDiscipline": "production wrap",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Hearing the speech aloud helps the producer memorize the emotional beats before the party.",
      "megaPrompt": "Build \"Wrap Speech Writer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts a heartfelt wrap speech so the producer thanks everyone; ElevenLabs voices it.\nDiscipline: Videography & Film (production wrap).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Hearing the speech aloud helps the producer memorize the emotional beats before the party.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the production wrap use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for production wrap. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for production wrap.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production management and wrap software",
      "sam": "$100M — professional production wrap and crew morale tools",
      "som": "$15M — line producers and indie film directors"
    },
    {
      "id": "video-scene-emotion-whisperer-0",
      "theme": "video",
      "title": "Scene Emotion Whisperer",
      "pitch": "Lovable AI generates emotional cues so directors guide actors; ElevenLabs whispers it.",
      "subDiscipline": "actor direction",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Real-time conversation allows directors to adjust cues mid-scene without looking at a screen.",
      "megaPrompt": "Build \"Scene Emotion Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates emotional cues so directors guide actors; ElevenLabs whispers it.\nDiscipline: Videography & Film (actor direction).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Real-time conversation allows directors to adjust cues mid-scene without looking at a screen.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the actor direction use case (e.g.\n\"You are a senior actor direction mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for actor direction.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and production software",
      "sam": "$400M — indie film and commercial production tools",
      "som": "$50M — on-set digital assistant apps"
    },
    {
      "id": "video-palette-vision-coach-1",
      "theme": "video",
      "title": "Palette Vision Coach",
      "pitch": "Lovable AI suggests cinematic grades so editors match moods; ElevenLabs narrates it.",
      "subDiscipline": "color grading",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Conversational interaction lets colorists keep their eyes on the reference monitor while tweaking nodes.",
      "megaPrompt": "Build \"Palette Vision Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI suggests cinematic grades so editors match moods; ElevenLabs narrates it.\nDiscipline: Videography & Film (color grading).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Conversational interaction lets colorists keep their eyes on the reference monitor while tweaking nodes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the color grading use case (e.g.\n\"You are a senior color grading mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for color grading.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$300M — professional post-production tools",
      "som": "$20M — AI color grading assistants"
    },
    {
      "id": "video-footstep-foley-director-2",
      "theme": "video",
      "title": "Footstep Foley Director",
      "pitch": "Lovable AI designs foley cues so artists layer audio; ElevenLabs mimics it.",
      "subDiscipline": "foley artistry",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Real-time voice interaction lets foley artists instantly request and hear variations of specific sound textures.",
      "megaPrompt": "Build \"Footstep Foley Director\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs foley cues so artists layer audio; ElevenLabs mimics it.\nDiscipline: Videography & Film (foley artistry).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Real-time voice interaction lets foley artists instantly request and hear variations of specific sound textures.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the foley artistry use case (e.g.\n\"You are a senior foley artistry mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for foley artistry.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and production software",
      "sam": "$250M — audio post-production software",
      "som": "$15M — foley and sound design aids"
    },
    {
      "id": "video-craft-service-chef-3",
      "theme": "video",
      "title": "Craft Service Chef",
      "pitch": "Lovable AI plans high-energy meals so crews avoid crashes; ElevenLabs reads it.",
      "subDiscipline": "set catering",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Hands-free voice dialogue is essential for busy caterers chopping and cooking on a chaotic film set.",
      "megaPrompt": "Build \"Craft Service Chef\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans high-energy meals so crews avoid crashes; ElevenLabs reads it.\nDiscipline: Videography & Film (set catering).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Hands-free voice dialogue is essential for busy caterers chopping and cooking on a chaotic film set.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the set catering use case (e.g.\n\"You are a senior set catering mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for set catering.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production services market",
      "sam": "$200M — on-set logistics and catering",
      "som": "$10M — crew wellness and catering apps"
    },
    {
      "id": "video-dialogue-rhythm-surgeon-4",
      "theme": "video",
      "title": "Dialogue Rhythm Surgeon",
      "pitch": "Lovable AI rewrites clunky dialogue so screenwriters fix pacing; ElevenLabs acts it.",
      "subDiscipline": "script polishing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational agent can instantly switch voices and tones to simulate a live table read.",
      "megaPrompt": "Build \"Dialogue Rhythm Surgeon\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites clunky dialogue so screenwriters fix pacing; ElevenLabs acts it.\nDiscipline: Videography & Film (script polishing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: A conversational agent can instantly switch voices and tones to simulate a live table read.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the script polishing use case (e.g.\n\"You are a senior script polishing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for script polishing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and pre-production software",
      "sam": "$350M — screenwriting and story development tools",
      "som": "$25M — AI script doctoring assistants"
    },
    {
      "id": "video-dolly-track-choreographer-5",
      "theme": "video",
      "title": "Dolly Track Choreographer",
      "pitch": "Lovable AI calculates dolly speeds so operators match pacing; ElevenLabs counts it.",
      "subDiscipline": "camera operation",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Operators need audible, real-time rhythmic counting while physically pushing heavy camera rigs.",
      "megaPrompt": "Build \"Dolly Track Choreographer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates dolly speeds so operators match pacing; ElevenLabs counts it.\nDiscipline: Videography & Film (camera operation).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Operators need audible, real-time rhythmic counting while physically pushing heavy camera rigs.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the camera operation use case (e.g.\n\"You are a senior camera operation mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for camera operation.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software and tools",
      "sam": "$400M — cinematography and camera operation aids",
      "som": "$30M — on-set camera movement assistants"
    },
    {
      "id": "video-lumens-ratio-mentor-6",
      "theme": "video",
      "title": "Lumens Ratio Mentor",
      "pitch": "Lovable AI calculates lighting ratios so gaffers shape shadows; ElevenLabs dictates it.",
      "subDiscipline": "cinematography lighting",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Gaffers can verbally confirm and adjust light levels while climbing ladders or adjusting heavy flags.",
      "megaPrompt": "Build \"Lumens Ratio Mentor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates lighting ratios so gaffers shape shadows; ElevenLabs dictates it.\nDiscipline: Videography & Film (cinematography lighting).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Gaffers can verbally confirm and adjust light levels while climbing ladders or adjusting heavy flags.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the cinematography lighting use case (e.g.\n\"You are a senior cinematography lighting mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for cinematography lighting.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software market",
      "sam": "$300M — lighting design and pre-visualization tools",
      "som": "$20M — AI gaffer and lighting assistants"
    },
    {
      "id": "video-cut-point-metronome-7",
      "theme": "video",
      "title": "Cut Point Metronome",
      "pitch": "Lovable AI analyzes scene tension so editors find cuts; ElevenLabs taps it.",
      "subDiscipline": "video editing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Real-time audio feedback helps editors feel the pacing viscerally without staring at timeline waveforms.",
      "megaPrompt": "Build \"Cut Point Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes scene tension so editors find cuts; ElevenLabs taps it.\nDiscipline: Videography & Film (video editing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Real-time audio feedback helps editors feel the pacing viscerally without staring at timeline waveforms.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the video editing use case (e.g.\n\"You are a senior video editing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for video editing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$500M — professional NLE and editing plugins",
      "som": "$40M — AI pacing and editing assistants"
    },
    {
      "id": "video-showrunner-pitch-sparrer-8",
      "theme": "video",
      "title": "Showrunner Pitch Sparrer",
      "pitch": "Lovable AI generates executive questions so creators practice pitches; ElevenLabs roleplays it.",
      "subDiscipline": "project pitching",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Conversational roleplay provides a realistic, high-pressure verbal sparring session for nervous creators.",
      "megaPrompt": "Build \"Showrunner Pitch Sparrer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates executive questions so creators practice pitches; ElevenLabs roleplays it.\nDiscipline: Videography & Film (project pitching).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Conversational roleplay provides a realistic, high-pressure verbal sparring session for nervous creators.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the project pitching use case (e.g.\n\"You are a senior project pitching mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for project pitching.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global creator economy and production software",
      "sam": "$200M — pitch deck and presentation tools",
      "som": "$15M — AI pitch coaching for filmmakers"
    },
    {
      "id": "video-prop-placement-watcher-9",
      "theme": "video",
      "title": "Prop Placement Watcher",
      "pitch": "Lovable AI tracks scene details so supervisors catch errors; ElevenLabs whispers it.",
      "subDiscipline": "script supervision",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Supervisors need discreet, real-time audio alerts while watching the monitor and taking notes.",
      "megaPrompt": "Build \"Prop Placement Watcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI tracks scene details so supervisors catch errors; ElevenLabs whispers it.\nDiscipline: Videography & Film (script supervision).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Supervisors need discreet, real-time audio alerts while watching the monitor and taking notes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the script supervision use case (e.g.\n\"You are a senior script supervision mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for script supervision.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software market",
      "sam": "$150M — script supervision and continuity tools",
      "som": "$10M — AI continuity tracking assistants"
    },
    {
      "id": "video-roto-mask-guide-10",
      "theme": "video",
      "title": "Roto Mask Guide",
      "pitch": "Lovable AI plans spline paths so artists isolate subjects; ElevenLabs narrates it.",
      "subDiscipline": "visual effects",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "VFX artists can listen to technical guidance while keeping both hands on the keyboard and mouse.",
      "megaPrompt": "Build \"Roto Mask Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans spline paths so artists isolate subjects; ElevenLabs narrates it.\nDiscipline: Videography & Film (visual effects).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: VFX artists can listen to technical guidance while keeping both hands on the keyboard and mouse.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the visual effects use case (e.g.\n\"You are a senior visual effects mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for visual effects.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and VFX software",
      "sam": "$400M — compositing and rotoscoping tools",
      "som": "$30M — AI VFX workflow assistants"
    },
    {
      "id": "video-render-break-therapist-11",
      "theme": "video",
      "title": "Render Break Therapist",
      "pitch": "Lovable AI designs mindfulness exercises so editors decompress; ElevenLabs guides it.",
      "subDiscipline": "creator wellness",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A soothing, interactive voice agent helps stressed editors transition from intense focus to deep relaxation.",
      "megaPrompt": "Build \"Render Break Therapist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs mindfulness exercises so editors decompress; ElevenLabs guides it.\nDiscipline: Videography & Film (creator wellness).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: A soothing, interactive voice agent helps stressed editors transition from intense focus to deep relaxation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the creator wellness use case (e.g.\n\"You are a senior creator wellness mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for creator wellness.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$100M — creator wellness and productivity tools",
      "som": "$5M — AI mental health aids for editors"
    },
    {
      "id": "video-sun-angle-scout-12",
      "theme": "video",
      "title": "Sun Angle Scout",
      "pitch": "Lovable AI calculates golden hour shadows so scouts pick locations; ElevenLabs describes it.",
      "subDiscipline": "location management",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Scouts can converse with the tool while driving or walking through potential locations hands-free.",
      "megaPrompt": "Build \"Sun Angle Scout\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates golden hour shadows so scouts pick locations; ElevenLabs describes it.\nDiscipline: Videography & Film (location management).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Scouts can converse with the tool while driving or walking through potential locations hands-free.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the location management use case (e.g.\n\"You are a senior location management mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for location management.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software market",
      "sam": "$150M — location scouting and pre-production apps",
      "som": "$10M — AI location management assistants"
    },
    {
      "id": "video-lip-sync-prompter-13",
      "theme": "video",
      "title": "Lip Sync Prompter",
      "pitch": "Lovable AI rewrites mismatched lines so actors match flaps; ElevenLabs feeds it.",
      "subDiscipline": "dialogue replacement",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Actors can converse with the agent to warm up and practice emotional delivery before stepping to the mic.",
      "megaPrompt": "Build \"Lip Sync Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites mismatched lines so actors match flaps; ElevenLabs feeds it.\nDiscipline: Videography & Film (dialogue replacement).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Actors can converse with the agent to warm up and practice emotional delivery before stepping to the mic.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the dialogue replacement use case (e.g.\n\"You are a senior dialogue replacement mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dialogue replacement.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and audio software",
      "sam": "$250M — ADR and dubbing studio tools",
      "som": "$20M — AI dialogue replacement assistants"
    },
    {
      "id": "video-lens-fungus-inspector-14",
      "theme": "video",
      "title": "Lens Fungus Inspector",
      "pitch": "Lovable AI diagnoses optical flaws so assistants clean gear; ElevenLabs voices it.",
      "subDiscipline": "equipment upkeep",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Techs need hands-free verbal instructions while delicately handling expensive, fragile glass elements.",
      "megaPrompt": "Build \"Lens Fungus Inspector\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI diagnoses optical flaws so assistants clean gear; ElevenLabs voices it.\nDiscipline: Videography & Film (equipment upkeep).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Techs need hands-free verbal instructions while delicately handling expensive, fragile glass elements.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the equipment upkeep use case (e.g.\n\"You are a senior equipment upkeep mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for equipment upkeep.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software and services",
      "sam": "$100M — gear rental and maintenance software",
      "som": "$5M — AI camera equipment upkeep aids"
    },
    {
      "id": "video-hook-retention-hacker-15",
      "theme": "video",
      "title": "Hook Retention Hacker",
      "pitch": "Lovable AI identifies viral hooks so creators boost retention; ElevenLabs narrates it.",
      "subDiscipline": "short-form editing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Creators can rapidly brainstorm and audibly test different hook variations while scrolling through raw footage.",
      "megaPrompt": "Build \"Hook Retention Hacker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies viral hooks so creators boost retention; ElevenLabs narrates it.\nDiscipline: Videography & Film (short-form editing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Creators can rapidly brainstorm and audibly test different hook variations while scrolling through raw footage.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the short-form editing use case (e.g.\n\"You are a senior short-form editing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for short-form editing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$600M — social media and short-form video tools",
      "som": "$50M — AI retention and hook assistants"
    },
    {
      "id": "video-shot-list-architect-16",
      "theme": "video",
      "title": "Shot List Architect",
      "pitch": "Lovable AI sequences camera angles so directors plan shoots; ElevenLabs reads it.",
      "subDiscipline": "pre-visualization",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Directors can audibly review and tweak the visual flow of a scene while commuting or resting.",
      "megaPrompt": "Build \"Shot List Architect\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sequences camera angles so directors plan shoots; ElevenLabs reads it.\nDiscipline: Videography & Film (pre-visualization).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Directors can audibly review and tweak the visual flow of a scene while commuting or resting.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the pre-visualization use case (e.g.\n\"You are a senior pre-visualization mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for pre-visualization.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software market",
      "sam": "$300M — storyboarding and pre-visualization tools",
      "som": "$25M — AI shot listing assistants"
    },
    {
      "id": "video-eq-frequency-hunter-17",
      "theme": "video",
      "title": "EQ Frequency Hunter",
      "pitch": "Lovable AI identifies muddy frequencies so mixers clean audio; ElevenLabs announces it.",
      "subDiscipline": "sound mixing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Mixers can keep their eyes on the frequency analyzer while verbally confirming EQ adjustments.",
      "megaPrompt": "Build \"EQ Frequency Hunter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies muddy frequencies so mixers clean audio; ElevenLabs announces it.\nDiscipline: Videography & Film (sound mixing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Mixers can keep their eyes on the frequency analyzer while verbally confirming EQ adjustments.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the sound mixing use case (e.g.\n\"You are a senior sound mixing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sound mixing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and audio software",
      "sam": "$350M — professional audio mixing plugins",
      "som": "$25M — AI audio EQ assistants"
    },
    {
      "id": "video-cutaway-context-weaver-18",
      "theme": "video",
      "title": "Cutaway Context Weaver",
      "pitch": "Lovable AI suggests visual metaphors so editors cover jumps; ElevenLabs describes it.",
      "subDiscipline": "documentary editing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Documentary editors can discuss thematic visual ideas conversationally while scrubbing through hours of interview footage.",
      "megaPrompt": "Build \"Cutaway Context Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI suggests visual metaphors so editors cover jumps; ElevenLabs describes it.\nDiscipline: Videography & Film (documentary editing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Documentary editors can discuss thematic visual ideas conversationally while scrubbing through hours of interview footage.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the documentary editing use case (e.g.\n\"You are a senior documentary editing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for documentary editing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$200M — documentary and long-form editing tools",
      "som": "$15M — AI b-roll and metaphor assistants"
    },
    {
      "id": "video-stunt-rigging-caller-19",
      "theme": "video",
      "title": "Stunt Rigging Caller",
      "pitch": "Lovable AI calculates load limits so coordinators plan stunts; ElevenLabs reads it.",
      "subDiscipline": "on-set safety",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Coordinators need clear, interactive verbal confirmations to ensure all safety protocols are met before a dangerous take.",
      "megaPrompt": "Build \"Stunt Rigging Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI calculates load limits so coordinators plan stunts; ElevenLabs reads it.\nDiscipline: Videography & Film (on-set safety).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Coordinators need clear, interactive verbal confirmations to ensure all safety protocols are met before a dangerous take.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the on-set safety use case (e.g.\n\"You are a senior on-set safety mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for on-set safety.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software market",
      "sam": "$100M — stunt coordination and safety software",
      "som": "$5M — AI on-set safety assistants"
    },
    {
      "id": "video-idiom-adaptation-coach-20",
      "theme": "video",
      "title": "Idiom Adaptation Coach",
      "pitch": "Lovable AI translates cultural jokes so adapters keep humor; ElevenLabs speaks it.",
      "subDiscipline": "language localization",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Adapters can converse with the agent to refine comedic timing and cultural nuances in real time.",
      "megaPrompt": "Build \"Idiom Adaptation Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates cultural jokes so adapters keep humor; ElevenLabs speaks it.\nDiscipline: Videography & Film (language localization).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Adapters can converse with the agent to refine comedic timing and cultural nuances in real time.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the language localization use case (e.g.\n\"You are a senior language localization mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for language localization.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and localization software",
      "sam": "$400M — video dubbing and translation tools",
      "som": "$30M — AI localization and adaptation aids"
    },
    {
      "id": "video-click-contrast-critic-21",
      "theme": "video",
      "title": "Click Contrast Critic",
      "pitch": "Lovable AI analyzes visual hierarchy so designers boost clicks; ElevenLabs critiques it.",
      "subDiscipline": "packaging design",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Designers can get immediate, conversational feedback on visual balance without breaking their creative flow in Photoshop.",
      "megaPrompt": "Build \"Click Contrast Critic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes visual hierarchy so designers boost clicks; ElevenLabs critiques it.\nDiscipline: Videography & Film (packaging design).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Designers can get immediate, conversational feedback on visual balance without breaking their creative flow in Photoshop.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the packaging design use case (e.g.\n\"You are a senior packaging design mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for packaging design.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global creator economy and video software",
      "sam": "$200M — thumbnail and metadata optimization tools",
      "som": "$15M — AI thumbnail critique assistants"
    },
    {
      "id": "video-footage-metadata-miner-22",
      "theme": "video",
      "title": "Footage Metadata Miner",
      "pitch": "Lovable AI connects historical events so researchers find clips; ElevenLabs summarizes it.",
      "subDiscipline": "archival production",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Researchers can ask follow-up questions conversationally while reviewing dense historical documents and video logs.",
      "megaPrompt": "Build \"Footage Metadata Miner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI connects historical events so researchers find clips; ElevenLabs summarizes it.\nDiscipline: Videography & Film (archival production).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Researchers can ask follow-up questions conversationally while reviewing dense historical documents and video logs.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the archival production use case (e.g.\n\"You are a senior archival production mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for archival production.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and asset management software",
      "sam": "$150M — archival and MAM tools",
      "som": "$10M — AI archival research assistants"
    },
    {
      "id": "video-tempo-map-conductor-23",
      "theme": "video",
      "title": "Tempo Map Conductor",
      "pitch": "Lovable AI maps emotional beats so composers align music; ElevenLabs counts it.",
      "subDiscipline": "film scoring",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Composers can interactively adjust hit points and stream-of-consciousness ideas while playing their MIDI keyboards.",
      "megaPrompt": "Build \"Tempo Map Conductor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps emotional beats so composers align music; ElevenLabs counts it.\nDiscipline: Videography & Film (film scoring).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Composers can interactively adjust hit points and stream-of-consciousness ideas while playing their MIDI keyboards.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the film scoring use case (e.g.\n\"You are a senior film scoring mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for film scoring.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and audio software",
      "sam": "$300M — film scoring and DAW tools",
      "som": "$20M — AI tempo mapping assistants"
    },
    {
      "id": "video-focus-group-simulator-24",
      "theme": "video",
      "title": "Focus Group Simulator",
      "pitch": "Lovable AI predicts viewer reactions so directors gauge impact; ElevenLabs voices it.",
      "subDiscipline": "test screening",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Directors can have a realistic, conversational debrief with simulated audience personas to understand emotional takeaways.",
      "megaPrompt": "Build \"Focus Group Simulator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI predicts viewer reactions so directors gauge impact; ElevenLabs voices it.\nDiscipline: Videography & Film (test screening).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Directors can have a realistic, conversational debrief with simulated audience personas to understand emotional takeaways.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the test screening use case (e.g.\n\"You are a senior test screening mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for test screening.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production and analytics software",
      "sam": "$100M — test screening and audience feedback tools",
      "som": "$5M — AI focus group simulators"
    },
    {
      "id": "video-scout-whisper-0",
      "theme": "video",
      "title": "Scout Whisper",
      "pitch": "Lovable AI structures scouting notes into shot lists so you keep exploring; ElevenLabs captures your voice in real-time.",
      "subDiscipline": "location scouting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows hands-free logging while physically navigating uneven or busy locations.",
      "megaPrompt": "Build \"Scout Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures scouting notes into shot lists so you keep exploring; ElevenLabs captures your voice in real-time.\nDiscipline: Videography & Film (location scouting).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows hands-free logging while physically navigating uneven or busy locations.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for location scouting:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken location scouting notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for location scouting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global film production services",
      "sam": "$500M — location scouting and pre-production software",
      "som": "$15M — indie filmmaker location management tools"
    },
    {
      "id": "video-dailies-dictator-1",
      "theme": "video",
      "title": "Dailies Dictator",
      "pitch": "Lovable AI tags metadata from your verbal reactions so editors find clips faster; ElevenLabs captures your live review notes.",
      "subDiscipline": "footage logging",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT turns spontaneous verbal reactions during dailies into searchable metadata without breaking viewing flow.",
      "megaPrompt": "Build \"Dailies Dictator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI tags metadata from your verbal reactions so editors find clips faster; ElevenLabs captures your live review notes.\nDiscipline: Videography & Film (footage logging).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT turns spontaneous verbal reactions during dailies into searchable metadata without breaking viewing flow.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for footage logging:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken footage logging notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for footage logging.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — video editing software market",
      "sam": "$300M — post-production asset management",
      "som": "$10M — DIT and dailies logging utilities"
    },
    {
      "id": "video-blocking-caller-2",
      "theme": "video",
      "title": "Blocking Caller",
      "pitch": "Lovable AI converts your verbal movement cues into actor diagrams so you can visualize; ElevenLabs captures your rehearsal commands.",
      "subDiscipline": "scene blocking",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the director's spontaneous spatial instructions while they are physically moving with actors.",
      "megaPrompt": "Build \"Blocking Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI converts your verbal movement cues into actor diagrams so you can visualize; ElevenLabs captures your rehearsal commands.\nDiscipline: Videography & Film (scene blocking).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the director's spontaneous spatial instructions while they are physically moving with actors.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for scene blocking:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken scene blocking notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for scene blocking.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — production planning software",
      "sam": "$200M — digital storyboarding and blocking tools",
      "som": "$5M — indie director rehearsal aids"
    },
    {
      "id": "video-b-roll-brainstormer-3",
      "theme": "video",
      "title": "B-Roll Brainstormer",
      "pitch": "Lovable AI suggests visual cutaways from interview answers so you never miss coverage; ElevenLabs captures the subject's live words.",
      "subDiscipline": "documentary interviewing",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Live STT of an interview allows the AI to instantly suggest B-roll based on spoken keywords.",
      "megaPrompt": "Build \"B-Roll Brainstormer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI suggests visual cutaways from interview answers so you never miss coverage; ElevenLabs captures the subject's live words.\nDiscipline: Videography & Film (documentary interviewing).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Live STT of an interview allows the AI to instantly suggest B-roll based on spoken keywords.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for documentary interviewing:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken documentary interviewing notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for documentary interviewing.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — documentary production market",
      "sam": "$400M — interview and field production gear and software",
      "som": "$12M — solo documentary creator tools"
    },
    {
      "id": "video-grade-whisperer-4",
      "theme": "video",
      "title": "Grade Whisperer",
      "pitch": "Lovable AI translates your emotional color feedback into LUT adjustments so you stay in the dark; ElevenLabs captures your murmurs.",
      "subDiscipline": "color grading",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT enables colorists to dictate grading tweaks without taking their hands off the grading panel.",
      "megaPrompt": "Build \"Grade Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates your emotional color feedback into LUT adjustments so you stay in the dark; ElevenLabs captures your murmurs.\nDiscipline: Videography & Film (color grading).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT enables colorists to dictate grading tweaks without taking their hands off the grading panel.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for color grading:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken color grading notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for color grading.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — video editing software market",
      "sam": "$250M — color grading and finishing software",
      "som": "$8M — freelance colorist workflow plugins"
    },
    {
      "id": "video-foley-dictation-5",
      "theme": "video",
      "title": "Foley Dictation",
      "pitch": "Lovable AI generates foley cue sheets from your vocalized sound effects so mixers prep faster; ElevenLabs captures your beatboxing.",
      "subDiscipline": "sound design",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the sound designer's vocalized rhythms and mimicked effects to auto-generate a structured foley session.",
      "megaPrompt": "Build \"Foley Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates foley cue sheets from your vocalized sound effects so mixers prep faster; ElevenLabs captures your beatboxing.\nDiscipline: Videography & Film (sound design).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the sound designer's vocalized rhythms and mimicked effects to auto-generate a structured foley session.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for sound design:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken sound design notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sound design.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — audio post-production market",
      "sam": "$300M — foley and sound effects libraries",
      "som": "$10M — indie sound designer workflow tools"
    },
    {
      "id": "video-pitch-pacer-6",
      "theme": "video",
      "title": "Pitch Pacer",
      "pitch": "Lovable AI critiques your verbal pacing and tone so you nail the studio meeting; ElevenLabs captures your rehearsal monologue.",
      "subDiscipline": "project pitching",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT provides the transcript needed for the AI to analyze speech patterns and persuasive language.",
      "megaPrompt": "Build \"Pitch Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI critiques your verbal pacing and tone so you nail the studio meeting; ElevenLabs captures your rehearsal monologue.\nDiscipline: Videography & Film (project pitching).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT provides the transcript needed for the AI to analyze speech patterns and persuasive language.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for project pitching:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken project pitching notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for project pitching.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — creator coaching and education",
      "sam": "$150M — filmmaker pitch and business tools",
      "som": "$5M — film student career prep software"
    },
    {
      "id": "video-vlog-flow-catcher-7",
      "theme": "video",
      "title": "Vlog Flow Catcher",
      "pitch": "Lovable AI dynamically adjusts your outline based on your tangents so you stay authentic; ElevenLabs captures your live rambling.",
      "subDiscipline": "solo content creation",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT tracks the creator's actual spoken words to dynamically update the remaining bullet points in their teleprompter.",
      "megaPrompt": "Build \"Vlog Flow Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI dynamically adjusts your outline based on your tangents so you stay authentic; ElevenLabs captures your live rambling.\nDiscipline: Videography & Film (solo content creation).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT tracks the creator's actual spoken words to dynamically update the remaining bullet points in their teleprompter.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for solo content creation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken solo content creation notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for solo content creation.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — creator economy software",
      "sam": "$800M — vlogging and YouTube production tools",
      "som": "$20M — solo creator teleprompter apps"
    },
    {
      "id": "video-table-read-tracker-8",
      "theme": "video",
      "title": "Table Read Tracker",
      "pitch": "Lovable AI highlights script pacing issues from the read-through so writers can rewrite; ElevenLabs captures the actors' live dialogue.",
      "subDiscipline": "script rehearsal",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT logs the actual spoken timing and overlapping dialogue during a table read for structural analysis.",
      "megaPrompt": "Build \"Table Read Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI highlights script pacing issues from the read-through so writers can rewrite; ElevenLabs captures the actors' live dialogue.\nDiscipline: Videography & Film (script rehearsal).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT logs the actual spoken timing and overlapping dialogue during a table read for structural analysis.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for script rehearsal:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken script rehearsal notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for script rehearsal.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — screenwriting software market",
      "sam": "$200M — script breakdown and rehearsal tools",
      "som": "$8M — indie production pre-production suites"
    },
    {
      "id": "video-lens-logger-9",
      "theme": "video",
      "title": "Lens Logger",
      "pitch": "Lovable AI structures your verbal lens changes into camera reports so the DIT stays organized; ElevenLabs captures your set calls.",
      "subDiscipline": "camera department",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows the AC to verbally log focal lengths while physically changing lenses on a hot set.",
      "megaPrompt": "Build \"Lens Logger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures your verbal lens changes into camera reports so the DIT stays organized; ElevenLabs captures your set calls.\nDiscipline: Videography & Film (camera department).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows the AC to verbally log focal lengths while physically changing lenses on a hot set.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for camera department:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken camera department notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for camera department.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — video production management",
      "sam": "$300M — camera and grip logging software",
      "som": "$10M — AC and DIT utility apps"
    },
    {
      "id": "video-cut-review-catcher-10",
      "theme": "video",
      "title": "Cut Review Catcher",
      "pitch": "Lovable AI groups your scattered verbal notes into timestamped edit decisions so editors execute cleanly; ElevenLabs captures your live reactions.",
      "subDiscipline": "client feedback",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures a director's stream-of-consciousness feedback during a screening and maps it to the video timeline.",
      "megaPrompt": "Build \"Cut Review Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI groups your scattered verbal notes into timestamped edit decisions so editors execute cleanly; ElevenLabs captures your live reactions.\nDiscipline: Videography & Film (client feedback).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures a director's stream-of-consciousness feedback during a screening and maps it to the video timeline.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for client feedback:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken client feedback notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for client feedback.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — video editing software market",
      "sam": "$400M — video review and collaboration platforms",
      "som": "$15M — agency and post-house feedback tools"
    },
    {
      "id": "video-ad-script-drafter-11",
      "theme": "video",
      "title": "AD Script Drafter",
      "pitch": "Lovable AI drafts audio description scripts from your visual narration so blind audiences can watch; ElevenLabs captures your live descriptions.",
      "subDiscipline": "accessibility post-production",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows the describer to watch the film and naturally speak the visual cues for timecode formatting.",
      "megaPrompt": "Build \"AD Script Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts audio description scripts from your visual narration so blind audiences can watch; ElevenLabs captures your live descriptions.\nDiscipline: Videography & Film (accessibility post-production).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows the describer to watch the film and naturally speak the visual cues for timecode formatting.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for accessibility post-production:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken accessibility post-production notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for accessibility post-production.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200M — media accessibility services",
      "sam": "$80M — audio description and subtitling software",
      "som": "$5M — indie film compliance and accessibility tools"
    },
    {
      "id": "video-vibe-board-dictator-12",
      "theme": "video",
      "title": "Vibe Board Dictator",
      "pitch": "Lovable AI translates your abstract visual metaphors into mood boards so the label approves faster; ElevenLabs captures your brainstorming flow.",
      "subDiscipline": "music video pre-production",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the rapid associative thinking of a director brainstorming visual metaphors out loud.",
      "megaPrompt": "Build \"Vibe Board Dictator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates your abstract visual metaphors into mood boards so the label approves faster; ElevenLabs captures your brainstorming flow.\nDiscipline: Videography & Film (music video pre-production).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the rapid associative thinking of a director brainstorming visual metaphors out loud.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for music video pre-production:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken music video pre-production notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for music video pre-production.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — music video production market",
      "sam": "$200M — treatment and mood board software",
      "som": "$8M — freelance director pitch tools"
    },
    {
      "id": "video-stunt-safety-caller-13",
      "theme": "video",
      "title": "Stunt Safety Caller",
      "pitch": "Lovable AI extracts safety checkpoints from your verbal walkthroughs so the crew stays protected; ElevenLabs captures your rehearsal instructions.",
      "subDiscipline": "stunt coordination",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT logs the stunt coordinator's verbal safety briefings to automatically generate digital safety checklists.",
      "megaPrompt": "Build \"Stunt Safety Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts safety checkpoints from your verbal walkthroughs so the crew stays protected; ElevenLabs captures your rehearsal instructions.\nDiscipline: Videography & Film (stunt coordination).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT logs the stunt coordinator's verbal safety briefings to automatically generate digital safety checklists.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for stunt coordination:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken stunt coordination notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for stunt coordination.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — production safety and risk management",
      "sam": "$150M — stunt and special effects planning",
      "som": "$5M — indie action film safety protocols"
    },
    {
      "id": "video-plate-pixel-catcher-14",
      "theme": "video",
      "title": "Plate Pixel Catcher",
      "pitch": "Lovable AI converts your on-set verbal VFX notes into tracking markers so compositors know the plan; ElevenLabs captures your technical calls.",
      "subDiscipline": "VFX supervision",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows the VFX supervisor to dictate technical plate requirements while looking through the camera monitor.",
      "megaPrompt": "Build \"Plate Pixel Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI converts your on-set verbal VFX notes into tracking markers so compositors know the plan; ElevenLabs captures your technical calls.\nDiscipline: Videography & Film (VFX supervision).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows the VFX supervisor to dictate technical plate requirements while looking through the camera monitor.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for VFX supervision:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken VFX supervision notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for VFX supervision.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — VFX and animation market",
      "sam": "$600M — on-set VFX and virtual production tools",
      "som": "$20M — indie VFX supervisor utilities"
    },
    {
      "id": "video-archive-whisperer-15",
      "theme": "video",
      "title": "Archive Whisperer",
      "pitch": "Lovable AI links your verbal historical observations to database tags so you find footage faster; ElevenLabs captures your research monologues.",
      "subDiscipline": "archival research",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the researcher's spoken thought process to auto-tag and cross-reference archival video clips.",
      "megaPrompt": "Build \"Archive Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI links your verbal historical observations to database tags so you find footage faster; ElevenLabs captures your research monologues.\nDiscipline: Videography & Film (archival research).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the researcher's spoken thought process to auto-tag and cross-reference archival video clips.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for archival research:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken archival research notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for archival research.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — documentary and factual production",
      "sam": "$150M — archival research and media management",
      "som": "$5M — historical documentary researcher tools"
    },
    {
      "id": "video-exec-flow-coach-16",
      "theme": "video",
      "title": "Exec Flow Coach",
      "pitch": "Lovable AI simplifies your jargon-heavy script in real-time so the CEO sounds natural; ElevenLabs captures their hesitant first takes.",
      "subDiscipline": "corporate video production",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT monitors the executive's actual spoken cadence to dynamically rewrite the teleprompter text into conversational language.",
      "megaPrompt": "Build \"Exec Flow Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI simplifies your jargon-heavy script in real-time so the CEO sounds natural; ElevenLabs captures their hesitant first takes.\nDiscipline: Videography & Film (corporate video production).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT monitors the executive's actual spoken cadence to dynamically rewrite the teleprompter text into conversational language.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for corporate video production:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken corporate video production notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for corporate video production.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — corporate video and training market",
      "sam": "$500M — enterprise video production software",
      "som": "$15M — corporate A/V team teleprompter tools"
    },
    {
      "id": "video-crafty-count-caller-17",
      "theme": "video",
      "title": "Crafty Count Caller",
      "pitch": "Lovable AI predicts meal counts from your verbal headcounts so you reduce set food waste; ElevenLabs captures your walkie chatter.",
      "subDiscipline": "set catering logistics",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT transcribes the AD's radio calls about background actor numbers to update the caterer's live dashboard.",
      "megaPrompt": "Build \"Crafty Count Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI predicts meal counts from your verbal headcounts so you reduce set food waste; ElevenLabs captures your walkie chatter.\nDiscipline: Videography & Film (set catering logistics).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT transcribes the AD's radio calls about background actor numbers to update the caterer's live dashboard.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for set catering logistics:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken set catering logistics notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for set catering logistics.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$300M — film set catering and logistics",
      "sam": "$100M — production resource management software",
      "som": "$5M — indie film craft services planning"
    },
    {
      "id": "video-mix-stage-mumbler-18",
      "theme": "video",
      "title": "Mix Stage Mumbler",
      "pitch": "Lovable AI translates your subjective audio feedback into EQ parameters so the mix moves faster; ElevenLabs captures your dark-room mutters.",
      "subDiscipline": "audio mixing",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT enables the re-recording mixer to dictate frequency adjustments without breaking focus on the audio waveform.",
      "megaPrompt": "Build \"Mix Stage Mumbler\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates your subjective audio feedback into EQ parameters so the mix moves faster; ElevenLabs captures your dark-room mutters.\nDiscipline: Videography & Film (audio mixing).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT enables the re-recording mixer to dictate frequency adjustments without breaking focus on the audio waveform.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for audio mixing:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken audio mixing notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for audio mixing.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — audio post-production market",
      "sam": "$300M — dubbing and mixing stage software",
      "som": "$10M — freelance audio mixer workflow plugins"
    },
    {
      "id": "video-fest-strategy-pacer-19",
      "theme": "video",
      "title": "Fest Strategy Pacer",
      "pitch": "Lovable AI matches your verbal film themes to festival deadlines so you submit strategically; ElevenLabs captures your distribution brainstorms.",
      "subDiscipline": "film festival distribution",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT logs the producer's out-loud strategic thinking to automatically populate a festival submission calendar.",
      "megaPrompt": "Build \"Fest Strategy Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI matches your verbal film themes to festival deadlines so you submit strategically; ElevenLabs captures your distribution brainstorms.\nDiscipline: Videography & Film (film festival distribution).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT logs the producer's out-loud strategic thinking to automatically populate a festival submission calendar.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for film festival distribution:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken film festival distribution notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for film festival distribution.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200M — film distribution and festival market",
      "sam": "$80M — indie film marketing and submission tools",
      "som": "$5M — short film producer strategy apps"
    },
    {
      "id": "video-essay-thread-catcher-20",
      "theme": "video",
      "title": "Essay Thread Catcher",
      "pitch": "Lovable AI structures your rambling media critiques into video essays so your argument flows; ElevenLabs captures your analytical monologues.",
      "subDiscipline": "video essay scripting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the creator's spontaneous analytical thoughts while watching a film to build a structured script outline.",
      "megaPrompt": "Build \"Essay Thread Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures your rambling media critiques into video essays so your argument flows; ElevenLabs captures your analytical monologues.\nDiscipline: Videography & Film (video essay scripting).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the creator's spontaneous analytical thoughts while watching a film to build a structured script outline.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for video essay scripting:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken video essay scripting notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for video essay scripting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — creator economy software",
      "sam": "$600M — YouTube education and essayist tools",
      "som": "$20M — video essayist research and drafting apps"
    },
    {
      "id": "video-volume-vibe-caller-21",
      "theme": "video",
      "title": "Volume Vibe Caller",
      "pitch": "Lovable AI adjusts Unreal Engine lighting from your verbal cues so the LED wall matches; ElevenLabs captures your set commands.",
      "subDiscipline": "virtual production",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows the DP to call out lighting changes hands-free while operating the camera on a virtual stage.",
      "megaPrompt": "Build \"Volume Vibe Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adjusts Unreal Engine lighting from your verbal cues so the LED wall matches; ElevenLabs captures your set commands.\nDiscipline: Videography & Film (virtual production).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows the DP to call out lighting changes hands-free while operating the camera on a virtual stage.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for virtual production:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken virtual production notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for virtual production.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — VFX and virtual production market",
      "sam": "$800M — LED volume and Unreal Engine tools",
      "som": "$25M — virtual production DP utilities"
    },
    {
      "id": "video-scripty-eye-catcher-22",
      "theme": "video",
      "title": "Scripty Eye Catcher",
      "pitch": "Lovable AI flags continuity errors from your verbal scene notes so the editor avoids mistakes; ElevenLabs captures your live take logs.",
      "subDiscipline": "script supervision",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT allows the script supervisor to dictate prop placements while keeping their eyes on the monitor.",
      "megaPrompt": "Build \"Scripty Eye Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI flags continuity errors from your verbal scene notes so the editor avoids mistakes; ElevenLabs captures your live take logs.\nDiscipline: Videography & Film (script supervision).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT allows the script supervisor to dictate prop placements while keeping their eyes on the monitor.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for script supervision:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken script supervision notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for script supervision.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — video production management",
      "sam": "$200M — script supervision and continuity software",
      "som": "$8M — indie scripty digital notebook apps"
    },
    {
      "id": "video-drone-path-whisperer-23",
      "theme": "video",
      "title": "Drone Path Whisperer",
      "pitch": "Lovable AI converts your verbal flight descriptions into waypoint missions so you get the perfect shot; ElevenLabs captures your sky-bound narration.",
      "subDiscipline": "aerial cinematography",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT captures the drone pilot's spoken visual descriptions to generate automated and repeatable flight paths.",
      "megaPrompt": "Build \"Drone Path Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI converts your verbal flight descriptions into waypoint missions so you get the perfect shot; ElevenLabs captures your sky-bound narration.\nDiscipline: Videography & Film (aerial cinematography).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT captures the drone pilot's spoken visual descriptions to generate automated and repeatable flight paths.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for aerial cinematography:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken aerial cinematography notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for aerial cinematography.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — commercial drone and aerial video market",
      "sam": "$150M — drone flight planning and cinematography software",
      "som": "$5M — FPV and cinematic drone pilot tools"
    },
    {
      "id": "video-edit-bay-exhale-24",
      "theme": "video",
      "title": "Edit Bay Exhale",
      "pitch": "Lovable AI guides your post-production venting into actionable breaks so you avoid burnout; ElevenLabs captures your frustrated midnight rants.",
      "subDiscipline": "creator mental health",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime STT provides a private hands-free outlet for editors to vocalize stress for AI-driven wellness prompts.",
      "megaPrompt": "Build \"Edit Bay Exhale\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI guides your post-production venting into actionable breaks so you avoid burnout; ElevenLabs captures your frustrated midnight rants.\nDiscipline: Videography & Film (creator mental health).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime STT provides a private hands-free outlet for editors to vocalize stress for AI-driven wellness prompts.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for creator mental health:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken creator mental health notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for creator mental health.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — creator coaching and wellness",
      "sam": "$150M — freelancer mental health and productivity tools",
      "som": "$5M — video editor burnout prevention apps"
    },
    {
      "id": "video-foley-prompt-crafter-0",
      "theme": "video",
      "title": "Foley Prompt Crafter",
      "pitch": "Lovable AI writes foley prompts for silent scenes so editors hear their vision; ElevenLabs generates the SFX.",
      "subDiscipline": "foley drafting",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX allows editors to instantly prototype complex, layered environmental sounds without recording physical props.",
      "megaPrompt": "Build \"Foley Prompt Crafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes foley prompts for silent scenes so editors hear their vision; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (foley drafting).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX allows editors to instantly prototype complex, layered environmental sounds without recording physical props.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for foley drafting. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for foley drafting.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$300M — post-production sound design and foley tools",
      "som": "$45M — indie filmmaker and freelance editor audio plugins"
    },
    {
      "id": "video-rough-cut-scorer-1",
      "theme": "video",
      "title": "Rough Cut Scorer",
      "pitch": "Lovable AI writes emotional beat sheets for rough cuts so directors feel the story; ElevenLabs scores it.",
      "subDiscipline": "temp scoring",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music provides immediate, copyright-free temp tracks that match the precise emotional pacing of an unfinished edit.",
      "megaPrompt": "Build \"Rough Cut Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes emotional beat sheets for rough cuts so directors feel the story; ElevenLabs scores it.\nDiscipline: Videography & Film (temp scoring).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music provides immediate, copyright-free temp tracks that match the precise emotional pacing of an unfinished edit.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for temp scoring. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for temp scoring.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$250M — stock music and temp track licensing platforms",
      "som": "$30M — documentary and narrative short film scoring subscriptions"
    },
    {
      "id": "video-indie-room-toner-2",
      "theme": "video",
      "title": "Indie Room Toner",
      "pitch": "Lovable AI designs room tone profiles for indie sets so audio feels grounded; ElevenLabs generates the SFX.",
      "subDiscipline": "ambience layering",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation creates seamless, looping background room tones tailored to the specific visual environment of a scene.",
      "megaPrompt": "Build \"Indie Room Toner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs room tone profiles for indie sets so audio feels grounded; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (ambience layering).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation creates seamless, looping background room tones tailored to the specific visual environment of a scene.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for ambience layering. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ambience layering.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$150M — dialogue editing and noise reduction software",
      "som": "$20M — micro-budget film production audio toolkits"
    },
    {
      "id": "video-trailer-riser-builder-3",
      "theme": "video",
      "title": "Trailer Riser Builder",
      "pitch": "Lovable AI plans trailer pacing and hit points so editors build anticipation; ElevenLabs generates the SFX.",
      "subDiscipline": "trailer sound design",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation produces custom risers, hits, and whooshes perfectly timed to the structural beats of a trailer.",
      "megaPrompt": "Build \"Trailer Riser Builder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans trailer pacing and hit points so editors build anticipation; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (trailer sound design).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation produces custom risers, hits, and whooshes perfectly timed to the structural beats of a trailer.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for trailer sound design. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for trailer sound design.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$200M — trailer house and promotional video post-production",
      "som": "$35M — freelance trailer editor sound asset libraries"
    },
    {
      "id": "video-scifi-ui-sounder-4",
      "theme": "video",
      "title": "SciFi UI Sounder",
      "pitch": "Lovable AI conceptualizes futuristic interface interactions so VFX artists hear the tech; ElevenLabs generates the SFX.",
      "subDiscipline": "interface sound design",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX excels at creating synthetic, digital, and non-organic sounds required for futuristic screen graphics.",
      "megaPrompt": "Build \"SciFi UI Sounder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI conceptualizes futuristic interface interactions so VFX artists hear the tech; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (interface sound design).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX excels at creating synthetic, digital, and non-organic sounds required for futuristic screen graphics.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for interface sound design. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for interface sound design.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$180M — VFX and motion graphics audio post-production",
      "som": "$25M — sci-fi short film and game cinematic sound design"
    },
    {
      "id": "video-horror-stinger-mapper-5",
      "theme": "video",
      "title": "Horror Stinger Mapper",
      "pitch": "Lovable AI maps tension curves for horror shorts so filmmakers perfect the scare; ElevenLabs generates the SFX.",
      "subDiscipline": "tension scoring",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX creates abrupt, dissonant stingers and unsettling drones that precisely match the mapped jump-scare moments.",
      "megaPrompt": "Build \"Horror Stinger Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps tension curves for horror shorts so filmmakers perfect the scare; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (tension scoring).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX creates abrupt, dissonant stingers and unsettling drones that precisely match the mapped jump-scare moments.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for tension scoring. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tension scoring.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$120M — genre-specific film audio post-production",
      "som": "$15M — indie horror filmmaker sound design assets"
    },
    {
      "id": "video-wildlife-b-roll-voicer-6",
      "theme": "video",
      "title": "Wildlife B-Roll Voicer",
      "pitch": "Lovable AI identifies missing wildlife audio in B-roll so documentaries feel alive; ElevenLabs generates the SFX.",
      "subDiscipline": "documentary soundscape",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation synthesizes realistic animal calls and nature sounds to fill gaps in poorly mic'd documentary footage.",
      "megaPrompt": "Build \"Wildlife B-Roll Voicer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies missing wildlife audio in B-roll so documentaries feel alive; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (documentary soundscape).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation synthesizes realistic animal calls and nature sounds to fill gaps in poorly mic'd documentary footage.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for documentary soundscape. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for documentary soundscape.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$220M — documentary and nature film post-production",
      "som": "$30M — wildlife videographer and travel documentary audio tools"
    },
    {
      "id": "video-fight-scene-puncher-7",
      "theme": "video",
      "title": "Fight Scene Puncher",
      "pitch": "Lovable AI choreographs fight scene audio beats so action edits hit harder; ElevenLabs generates the SFX.",
      "subDiscipline": "action audio choreography",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX produces hyper-stylized impacts, cloth rustles, and weapon swings that enhance the visceral feel of action.",
      "megaPrompt": "Build \"Fight Scene Puncher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI choreographs fight scene audio beats so action edits hit harder; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (action audio choreography).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX produces hyper-stylized impacts, cloth rustles, and weapon swings that enhance the visceral feel of action.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for action audio choreography. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for action audio choreography.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$250M — action and martial arts film post-production",
      "som": "$40M — stunt coordinator and action editor audio libraries"
    },
    {
      "id": "video-vlog-swoosh-designer-8",
      "theme": "video",
      "title": "Vlog Swoosh Designer",
      "pitch": "Lovable AI brainstorms visual transition styles for travel vlogs so creators stand out; ElevenLabs generates the SFX.",
      "subDiscipline": "creator transition audio",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation creates unique, branded swooshes and risers that give content creators a signature audio identity.",
      "megaPrompt": "Build \"Vlog Swoosh Designer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI brainstorms visual transition styles for travel vlogs so creators stand out; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (creator transition audio).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation creates unique, branded swooshes and risers that give content creators a signature audio identity.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for creator transition audio. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for creator transition audio.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$400M — creator economy video editing and asset tools",
      "som": "$60M — travel vlogger and lifestyle creator audio packs"
    },
    {
      "id": "video-vintage-street-noiser-9",
      "theme": "video",
      "title": "Vintage Street Noiser",
      "pitch": "Lovable AI researches historical street noise for vintage scenes so period pieces feel authentic; ElevenLabs generates the SFX.",
      "subDiscipline": "period piece audio",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX can blend specific historical elements like horse hooves and early engines into a cohesive era soundscape.",
      "megaPrompt": "Build \"Vintage Street Noiser\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI researches historical street noise for vintage scenes so period pieces feel authentic; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (period piece audio).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX can blend specific historical elements like horse hooves and early engines into a cohesive era soundscape.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for period piece audio. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for period piece audio.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$150M — historical drama and period piece post-production",
      "som": "$20M — student and indie period filmmaker audio assets"
    },
    {
      "id": "video-dream-sequence-scorer-10",
      "theme": "video",
      "title": "Dream Sequence Scorer",
      "pitch": "Lovable AI writes surreal narrative logic for dream scenes so directors capture the vibe; ElevenLabs scores it.",
      "subDiscipline": "surreal narrative scoring",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music produces ethereal, non-linear, and atmospheric tracks that perfectly underscore surreal visual storytelling.",
      "megaPrompt": "Build \"Dream Sequence Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes surreal narrative logic for dream scenes so directors capture the vibe; ElevenLabs scores it.\nDiscipline: Videography & Film (surreal narrative scoring).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music produces ethereal, non-linear, and atmospheric tracks that perfectly underscore surreal visual storytelling.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for surreal narrative scoring. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for surreal narrative scoring.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$180M — experimental and art-house film scoring",
      "som": "$25M — indie director and music video composer subscriptions"
    },
    {
      "id": "video-car-chase-layerer-11",
      "theme": "video",
      "title": "Car Chase Layerer",
      "pitch": "Lovable AI profiles vehicle acoustics for action edits so chases feel visceral; ElevenLabs generates the SFX.",
      "subDiscipline": "vehicle acoustic profiling",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation allows for the layering of specific engine revs, tire screeches, and gear shifts for dynamic chases.",
      "megaPrompt": "Build \"Car Chase Layerer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI profiles vehicle acoustics for action edits so chases feel visceral; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (vehicle acoustic profiling).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation allows for the layering of specific engine revs, tire screeches, and gear shifts for dynamic chases.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for vehicle acoustic profiling. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for vehicle acoustic profiling.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$200M — automotive and action film audio post-production",
      "som": "$30M — car videographer and action short sound design"
    },
    {
      "id": "video-tiktok-arc-scorer-12",
      "theme": "video",
      "title": "TikTok Arc Scorer",
      "pitch": "Lovable AI outlines sixty-second narrative arcs for TikTok so creators hook viewers; ElevenLabs scores it.",
      "subDiscipline": "micro-short pacing",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music creates fast-paced, engaging, and royalty-free backing tracks optimized for short-form retention algorithms.",
      "megaPrompt": "Build \"TikTok Arc Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI outlines sixty-second narrative arcs for TikTok so creators hook viewers; ElevenLabs scores it.\nDiscipline: Videography & Film (micro-short pacing).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music creates fast-paced, engaging, and royalty-free backing tracks optimized for short-form retention algorithms.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for micro-short pacing. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for micro-short pacing.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$500M — short-form mobile video creation tools",
      "som": "$75M — TikTok and Reels creator audio generation apps"
    },
    {
      "id": "video-podcast-underscore-builder-13",
      "theme": "video",
      "title": "Podcast Underscore Builder",
      "pitch": "Lovable AI extracts emotional peaks from podcast transcripts so video adaptations feel cinematic; ElevenLabs scores it.",
      "subDiscipline": "audio-to-video adaptation",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music provides dynamic underscore that reacts to the conversational highs and lows of podcast video edits.",
      "megaPrompt": "Build \"Podcast Underscore Builder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts emotional peaks from podcast transcripts so video adaptations feel cinematic; ElevenLabs scores it.\nDiscipline: Videography & Film (audio-to-video adaptation).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music provides dynamic underscore that reacts to the conversational highs and lows of podcast video edits.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for audio-to-video adaptation. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for audio-to-video adaptation.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$250M — podcast video production and repurposing tools",
      "som": "$40M — podcast network and independent creator scoring tools"
    },
    {
      "id": "video-b-roll-mood-scorer-14",
      "theme": "video",
      "title": "B-Roll Mood Scorer",
      "pitch": "Lovable AI analyzes the visual color palette of B-roll so montages match moods; ElevenLabs scores it.",
      "subDiscipline": "visual palette matching",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music translates visual color grading and lighting cues into complementary atmospheric and ambient tracks.",
      "megaPrompt": "Build \"B-Roll Mood Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes the visual color palette of B-roll so montages match moods; ElevenLabs scores it.\nDiscipline: Videography & Film (visual palette matching).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music translates visual color grading and lighting cues into complementary atmospheric and ambient tracks.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for visual palette matching. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for visual palette matching.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$300M — stock footage and B-roll editing platforms",
      "som": "$45M — commercial and corporate videographer scoring tools"
    },
    {
      "id": "video-silent-film-reviver-15",
      "theme": "video",
      "title": "Silent Film Reviver",
      "pitch": "Lovable AI writes modern narrative intertitles for vintage clips so old films entertain anew; ElevenLabs scores it.",
      "subDiscipline": "vintage media restoration",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music produces authentic ragtime or orchestral accompaniment tailored to the frame rate and action of silent films.",
      "megaPrompt": "Build \"Silent Film Reviver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes modern narrative intertitles for vintage clips so old films entertain anew; ElevenLabs scores it.\nDiscipline: Videography & Film (vintage media restoration).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music produces authentic ragtime or orchestral accompaniment tailored to the frame rate and action of silent films.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for vintage media restoration. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for vintage media restoration.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$100M — archival footage restoration and remastering",
      "som": "$15M — film historian and archive curator audio tools"
    },
    {
      "id": "video-corporate-beat-pacer-16",
      "theme": "video",
      "title": "Corporate Beat Pacer",
      "pitch": "Lovable AI structures engaging beats for boring training videos so employees stay awake; ElevenLabs scores it.",
      "subDiscipline": "training video engagement",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music creates upbeat, non-distracting corporate tracks that maintain viewer attention during long instructional segments.",
      "megaPrompt": "Build \"Corporate Beat Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures engaging beats for boring training videos so employees stay awake; ElevenLabs scores it.\nDiscipline: Videography & Film (training video engagement).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music creates upbeat, non-distracting corporate tracks that maintain viewer attention during long instructional segments.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for training video engagement. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for training video engagement.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$350M — corporate video production and e-learning media",
      "som": "$50M — enterprise L&D video creator audio subscriptions"
    },
    {
      "id": "video-music-video-mapper-17",
      "theme": "video",
      "title": "Music Video Mapper",
      "pitch": "Lovable AI plans visual cuts to match lyrical themes so music videos flow perfectly; ElevenLabs scores it.",
      "subDiscipline": "lyrical visual syncing",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music provides instrumental stems and backing tracks that editors can use to build the visual rhythm.",
      "megaPrompt": "Build \"Music Video Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans visual cuts to match lyrical themes so music videos flow perfectly; ElevenLabs scores it.\nDiscipline: Videography & Film (lyrical visual syncing).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music provides instrumental stems and backing tracks that editors can use to build the visual rhythm.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for lyrical visual syncing. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for lyrical visual syncing.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$280M — music video and visualizer post-production",
      "som": "$40M — independent musician and director video editing tools"
    },
    {
      "id": "video-asmr-trigger-sounder-18",
      "theme": "video",
      "title": "ASMR Trigger Sounder",
      "pitch": "Lovable AI suggests tactile objects for ASMR triggers so creators find new sounds; ElevenLabs generates the SFX.",
      "subDiscipline": "tactile audio design",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation produces hyper-detailed, close-mic style textures and scratches essential for high-quality ASMR content.",
      "megaPrompt": "Build \"ASMR Trigger Sounder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI suggests tactile objects for ASMR triggers so creators find new sounds; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (tactile audio design).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation produces hyper-detailed, close-mic style textures and scratches essential for high-quality ASMR content.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for tactile audio design. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tactile audio design.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$150M — ASMR and sensory video production tools",
      "som": "$25M — ASMR creator audio enhancement and generation apps"
    },
    {
      "id": "video-drone-flight-scorer-19",
      "theme": "video",
      "title": "Drone Flight Scorer",
      "pitch": "Lovable AI maps the altitude and speed of FPV drone footage so flights feel epic; ElevenLabs scores it.",
      "subDiscipline": "FPV dynamic scoring",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music creates high-energy, tempo-shifting electronic tracks that mirror the kinetic movement of drone cinematography.",
      "megaPrompt": "Build \"Drone Flight Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps the altitude and speed of FPV drone footage so flights feel epic; ElevenLabs scores it.\nDiscipline: Videography & Film (FPV dynamic scoring).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music creates high-energy, tempo-shifting electronic tracks that mirror the kinetic movement of drone cinematography.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for FPV dynamic scoring. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for FPV dynamic scoring.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$200M — aerial cinematography and drone video editing",
      "som": "$35M — FPV drone pilot and action sports scorer tools"
    },
    {
      "id": "video-claymation-foley-planner-20",
      "theme": "video",
      "title": "Claymation Foley Planner",
      "pitch": "Lovable AI plans the quirky physical movements of claymation so animators hear their characters; ElevenLabs generates the SFX.",
      "subDiscipline": "stop-motion sound design",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation creates the specific, tactile, and slightly exaggerated squishes and steps needed for stop-motion animation.",
      "megaPrompt": "Build \"Claymation Foley Planner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI plans the quirky physical movements of claymation so animators hear their characters; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (stop-motion sound design).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation creates the specific, tactile, and slightly exaggerated squishes and steps needed for stop-motion animation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for stop-motion sound design. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for stop-motion sound design.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$120M — animation and stop-motion post-production audio",
      "som": "$20M — indie animator and student filmmaker foley tools"
    },
    {
      "id": "video-wedding-tear-scorer-21",
      "theme": "video",
      "title": "Wedding Tear Scorer",
      "pitch": "Lovable AI identifies the tear-jerker moments in raw wedding footage so videographers highlight emotion; ElevenLabs scores it.",
      "subDiscipline": "emotional highlight scoring",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music produces sweeping, cinematic string and piano arrangements timed perfectly to vows and emotional reactions.",
      "megaPrompt": "Build \"Wedding Tear Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies the tear-jerker moments in raw wedding footage so videographers highlight emotion; ElevenLabs scores it.\nDiscipline: Videography & Film (emotional highlight scoring).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music produces sweeping, cinematic string and piano arrangements timed perfectly to vows and emotional reactions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for emotional highlight scoring. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for emotional highlight scoring.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$250M — wedding and event videography post-production",
      "som": "$45M — professional wedding filmmaker scoring subscriptions"
    },
    {
      "id": "video-sports-hype-dropper-22",
      "theme": "video",
      "title": "Sports Hype Dropper",
      "pitch": "Lovable AI spots the most explosive athletic plays so highlight reels go viral; ElevenLabs generates the SFX.",
      "subDiscipline": "highlight reel audio",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation creates heavy bass drops, impacts, and crowd swells that amplify the intensity of sports highlights.",
      "megaPrompt": "Build \"Sports Hype Dropper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI spots the most explosive athletic plays so highlight reels go viral; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (highlight reel audio).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation creates heavy bass drops, impacts, and crowd swells that amplify the intensity of sports highlights.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for highlight reel audio. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for highlight reel audio.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$300M — sports media and highlight video production",
      "som": "$50M — freelance sports editor and team media audio tools"
    },
    {
      "id": "video-culinary-sizzle-timer-23",
      "theme": "video",
      "title": "Culinary Sizzle Timer",
      "pitch": "Lovable AI times the chopping and searing actions so food videos sound delicious; ElevenLabs generates the SFX.",
      "subDiscipline": "food video audio",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation enhances the crisp, mouth-watering sounds of cooking actions that are often lost in kitchen environments.",
      "megaPrompt": "Build \"Culinary Sizzle Timer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI times the chopping and searing actions so food videos sound delicious; ElevenLabs generates the SFX.\nDiscipline: Videography & Film (food video audio).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation enhances the crisp, mouth-watering sounds of cooking actions that are often lost in kitchen environments.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for food video audio. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for food video audio.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$180M — culinary and lifestyle video post-production",
      "som": "$30M — food vlogger and recipe creator audio enhancement"
    },
    {
      "id": "video-runway-strut-scorer-24",
      "theme": "video",
      "title": "Runway Strut Scorer",
      "pitch": "Lovable AI analyzes the strut tempo and garment textures so fashion films feel avant-garde; ElevenLabs scores it.",
      "subDiscipline": "fashion film audio",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative music produces rhythmic, high-fashion electronic beats that sync perfectly with the pacing of runway and lookbook edits.",
      "megaPrompt": "Build \"Runway Strut Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes the strut tempo and garment textures so fashion films feel avant-garde; ElevenLabs scores it.\nDiscipline: Videography & Film (fashion film audio).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative music produces rhythmic, high-fashion electronic beats that sync perfectly with the pacing of runway and lookbook edits.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for fashion film audio. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for fashion film audio.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$150M — fashion film and commercial video production",
      "som": "$25M — fashion videographer and brand media scoring tools"
    }
  ]
}