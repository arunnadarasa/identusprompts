{
  "theme": {
    "slug": "video",
    "name": "Videography & Film",
    "emoji": "🎥",
    "audience": "videographers, editors, content creators",
    "market_anchor": "the video editing software market (~$1.1B) and >50M creators"
  },
  "ideas": [
    {
      "id": "video-lens-whisperer-0",
      "theme": "video",
      "title": "Lens Whisperer",
      "pitch": "Lens Whisperer spins up a fresh public Sprite for each session so shot listing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "shot listing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits shot listing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Lens Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLens Whisperer spins up a fresh public Sprite for each session so shot listing gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (shot listing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits shot listing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `shot-listing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for shot listing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and production software",
      "sam": "$400M — professional on-set production and directing tools",
      "som": "$60M — indie directors and commercial production agencies"
    },
    {
      "id": "video-chroma-meditation-1",
      "theme": "video",
      "title": "Chroma Meditation",
      "pitch": "Chroma Meditation composes colorist mindset in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "colorist mindset",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits colorist mindset in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Chroma Meditation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nChroma Meditation composes colorist mindset in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (colorist mindset).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits colorist mindset in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `colorist-mindset-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for colorist mindset (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for colorist mindset.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global post-production and color grading market",
      "sam": "$250M — professional color grading software and plugins",
      "som": "$35M — freelance colorists and boutique post-houses"
    },
    {
      "id": "video-table-read-phantom-2",
      "theme": "video",
      "title": "Table Read Phantom",
      "pitch": "Table Read Phantom boots a long-running service inside a Sprite for script rehearsal — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "script rehearsal",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches script rehearsal in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Table Read Phantom\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTable Read Phantom boots a long-running service inside a Sprite for script rehearsal — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (script rehearsal).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches script rehearsal in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `script-rehearsal-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for script rehearsal.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits script rehearsal — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for script rehearsal.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global screenwriting and pre-production software",
      "sam": "$300M — professional script development and rehearsal tools",
      "som": "$45M — independent screenwriters and showrunner assistants"
    },
    {
      "id": "video-footstep-maestro-3",
      "theme": "video",
      "title": "Footstep Maestro",
      "pitch": "Footstep Maestro runs a one-shot shell command inside a Sprite for foley artistry and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "foley artistry",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits foley artistry in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Footstep Maestro\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFootstep Maestro runs a one-shot shell command inside a Sprite for foley artistry and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (foley artistry).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits foley artistry in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `foley-artistry-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a foley artistry-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for foley artistry.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global audio post-production and sound design market",
      "sam": "$200M — professional foley and sound effects creation tools",
      "som": "$30M — freelance foley artists and sound design studios"
    },
    {
      "id": "video-cut-pacer-4",
      "theme": "video",
      "title": "Cut Pacer",
      "pitch": "Cut Pacer spins up a fresh public Sprite for each session so editing rhythm gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "editing rhythm",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits editing rhythm in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Cut Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCut Pacer spins up a fresh public Sprite for each session so editing rhythm gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (editing rhythm).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits editing rhythm in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `editing-rhythm-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for editing rhythm.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$500M — professional NLE and timeline editing tools",
      "som": "$80M — YouTube creators and freelance video editors"
    },
    {
      "id": "video-lumen-caller-5",
      "theme": "video",
      "title": "Lumen Caller",
      "pitch": "Lumen Caller composes lighting design in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "lighting design",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits lighting design in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Lumen Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLumen Caller composes lighting design in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (lighting design).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits lighting design in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `lighting-design-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for lighting design (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for lighting design.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global cinematography and lighting equipment market",
      "sam": "$350M — professional on-set lighting design and planning tools",
      "som": "$50M — directors of photography and commercial gaffers"
    },
    {
      "id": "video-scripty-echo-6",
      "theme": "video",
      "title": "Scripty Echo",
      "pitch": "Scripty Echo boots a long-running service inside a Sprite for continuity tracking — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "continuity tracking",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches continuity tracking in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Scripty Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nScripty Echo boots a long-running service inside a Sprite for continuity tracking — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (continuity tracking).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches continuity tracking in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `continuity-tracking-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for continuity tracking.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits continuity tracking — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for continuity tracking.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production management software",
      "sam": "$150M — on-set continuity and script supervision tools",
      "som": "$25M — freelance script supervisors and indie producers"
    },
    {
      "id": "video-node-navigator-7",
      "theme": "video",
      "title": "Node Navigator",
      "pitch": "Node Navigator runs a one-shot shell command inside a Sprite for VFX compositing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "VFX compositing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits VFX compositing in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Node Navigator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nNode Navigator runs a one-shot shell command inside a Sprite for VFX compositing and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (VFX compositing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits VFX compositing in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `vfx-compositing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a VFX compositing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for VFX compositing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global VFX and motion graphics software market",
      "sam": "$450M — professional node-based compositing applications",
      "som": "$70M — freelance VFX artists and boutique FX studios"
    },
    {
      "id": "video-emotion-prompter-8",
      "theme": "video",
      "title": "Emotion Prompter",
      "pitch": "Emotion Prompter spins up a fresh public Sprite for each session so actor coaching gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "actor coaching",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits actor coaching in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Emotion Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEmotion Prompter spins up a fresh public Sprite for each session so actor coaching gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (actor coaching).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits actor coaching in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `actor-coaching-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for actor coaching.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production and directing tools",
      "sam": "$200M — professional actor coaching and rehearsal software",
      "som": "$30M — indie directors and acting coaches"
    },
    {
      "id": "video-archive-scout-9",
      "theme": "video",
      "title": "Archive Scout",
      "pitch": "Archive Scout composes stock footage curation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "stock footage curation",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits stock footage curation in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Archive Scout\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArchive Scout composes stock footage curation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (stock footage curation).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits stock footage curation in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `stock-footage-curation-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for stock footage curation (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for stock footage curation.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global stock media and asset management market",
      "sam": "$300M — professional video asset curation and licensing tools",
      "som": "$45M — documentary editors and newsroom producers"
    },
    {
      "id": "video-rotor-choreographer-10",
      "theme": "video",
      "title": "Rotor Choreographer",
      "pitch": "Rotor Choreographer boots a long-running service inside a Sprite for drone cinematography — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "drone cinematography",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches drone cinematography in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Rotor Choreographer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRotor Choreographer boots a long-running service inside a Sprite for drone cinematography — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (drone cinematography).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches drone cinematography in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `drone-cinematography-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for drone cinematography.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits drone cinematography — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for drone cinematography.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global aerial cinematography and drone software",
      "sam": "$250M — professional drone flight planning and camera tools",
      "som": "$40M — freelance drone operators and aerial production houses"
    },
    {
      "id": "video-logline-rehearser-11",
      "theme": "video",
      "title": "Logline Rehearser",
      "pitch": "Logline Rehearser runs a one-shot shell command inside a Sprite for pitch preparation and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "pitch preparation",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits pitch preparation in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Logline Rehearser\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLogline Rehearser runs a one-shot shell command inside a Sprite for pitch preparation and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (pitch preparation).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits pitch preparation in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `pitch-preparation-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a pitch preparation-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for pitch preparation.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film financing and pitch deck software",
      "sam": "$100M — professional pitch preparation and presentation tools",
      "som": "$15M — independent producers and film festival applicants"
    },
    {
      "id": "video-frequencies-ghost-12",
      "theme": "video",
      "title": "Frequencies Ghost",
      "pitch": "Frequencies Ghost spins up a fresh public Sprite for each session so audio mixing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "audio mixing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits audio mixing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Frequencies Ghost\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFrequencies Ghost spins up a fresh public Sprite for each session so audio mixing gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (audio mixing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits audio mixing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `audio-mixing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for audio mixing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global audio post-production and mixing market",
      "sam": "$350M — professional DAW and mixing plugin software",
      "som": "$55M — freelance re-recording mixers and podcast editors"
    },
    {
      "id": "video-sun-tracker-13",
      "theme": "video",
      "title": "Sun Tracker",
      "pitch": "Sun Tracker composes location scouting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "location scouting",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits location scouting in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Sun Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSun Tracker composes location scouting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (location scouting).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits location scouting in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `location-scouting-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for location scouting (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for location scouting.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global pre-production and location management software",
      "sam": "$150M — professional location scouting and sun-tracking apps",
      "som": "$25M — location managers and indie cinematographers"
    },
    {
      "id": "video-sensor-sweeper-14",
      "theme": "video",
      "title": "Sensor Sweeper",
      "pitch": "Sensor Sweeper boots a long-running service inside a Sprite for gear maintenance — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "gear maintenance",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches gear maintenance in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Sensor Sweeper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSensor Sweeper boots a long-running service inside a Sprite for gear maintenance — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (gear maintenance).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches gear maintenance in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `gear-maintenance-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for gear maintenance.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits gear maintenance — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for gear maintenance.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global camera rental and equipment maintenance market",
      "sam": "$200M — professional camera tech and repair management tools",
      "som": "$30M — camera rental houses and on-set DITs"
    },
    {
      "id": "video-clickbait-critic-15",
      "theme": "video",
      "title": "Clickbait Critic",
      "pitch": "Clickbait Critic runs a one-shot shell command inside a Sprite for thumbnail strategy and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "thumbnail strategy",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits thumbnail strategy in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Clickbait Critic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nClickbait Critic runs a one-shot shell command inside a Sprite for thumbnail strategy and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (thumbnail strategy).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits thumbnail strategy in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `thumbnail-strategy-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a thumbnail strategy-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for thumbnail strategy.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global creator economy and video marketing tools",
      "sam": "$400M — professional YouTube optimization and thumbnail software",
      "som": "$60M — full-time YouTubers and digital marketing agencies"
    },
    {
      "id": "video-fall-caller-16",
      "theme": "video",
      "title": "Fall Caller",
      "pitch": "Fall Caller spins up a fresh public Sprite for each session so stunt safety gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "stunt safety",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits stunt safety in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Fall Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFall Caller spins up a fresh public Sprite for each session so stunt safety gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (stunt safety).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits stunt safety in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `stunt-safety-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for stunt safety.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production and stunt coordination market",
      "sam": "$100M — professional stunt planning and safety management tools",
      "som": "$15M — stunt coordinators and action sequence directors"
    },
    {
      "id": "video-alt-text-weaver-17",
      "theme": "video",
      "title": "Alt-Text Weaver",
      "pitch": "Alt-Text Weaver composes video accessibility in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "video accessibility",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits video accessibility in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Alt-Text Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAlt-Text Weaver composes video accessibility in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (video accessibility).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits video accessibility in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `video-accessibility-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for video accessibility (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for video accessibility.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video localization and accessibility software",
      "sam": "$250M — professional audio description and captioning tools",
      "som": "$40M — streaming platforms and indie film distributors"
    },
    {
      "id": "video-set-menu-minder-18",
      "theme": "video",
      "title": "Set Menu Minder",
      "pitch": "Set Menu Minder boots a long-running service inside a Sprite for craft services planning — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "craft services planning",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches craft services planning in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Set Menu Minder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSet Menu Minder boots a long-running service inside a Sprite for craft services planning — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (craft services planning).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches craft services planning in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `craft-services-planning-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for craft services planning.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits craft services planning — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for craft services planning.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production logistics and catering market",
      "sam": "$150M — professional set management and craft services tools",
      "som": "$20M — unit production managers and craft service captains"
    },
    {
      "id": "video-focus-whisper-19",
      "theme": "video",
      "title": "Focus Whisper",
      "pitch": "Focus Whisper runs a one-shot shell command inside a Sprite for focus pulling and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "focus pulling",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits focus pulling in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Focus Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFocus Whisper runs a one-shot shell command inside a Sprite for focus pulling and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (focus pulling).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits focus pulling in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `focus-pulling-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a focus pulling-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for focus pulling.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global cinematography and camera department tools",
      "sam": "$200M — professional focus pulling and lens measurement apps",
      "som": "$35M — first assistant cameras and indie DP crews"
    },
    {
      "id": "video-kinetic-type-coach-20",
      "theme": "video",
      "title": "Kinetic Type Coach",
      "pitch": "Kinetic Type Coach spins up a fresh public Sprite for each session so motion graphics gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "motion graphics",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits motion graphics in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Kinetic Type Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nKinetic Type Coach spins up a fresh public Sprite for each session so motion graphics gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (motion graphics).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits motion graphics in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `motion-graphics-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for motion graphics.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global motion graphics and title design software",
      "sam": "$350M — professional 2D animation and typography tools",
      "som": "$50M — freelance motion designers and broadcast networks"
    },
    {
      "id": "video-radio-protocol-tutor-21",
      "theme": "video",
      "title": "Radio Protocol Tutor",
      "pitch": "Radio Protocol Tutor composes production assistant training in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "production assistant training",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits production assistant training in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Radio Protocol Tutor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRadio Protocol Tutor composes production assistant training in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (production assistant training).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits production assistant training in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `production-assistant-tra-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for production assistant training (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for production assistant training.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production training and management software",
      "sam": "$100M — professional crew onboarding and set protocol tools",
      "som": "$15M — production coordinators and film school students"
    },
    {
      "id": "video-xml-translator-22",
      "theme": "video",
      "title": "XML Translator",
      "pitch": "XML Translator boots a long-running service inside a Sprite for workflow handoffs — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "workflow handoffs",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches workflow handoffs in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"XML Translator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nXML Translator boots a long-running service inside a Sprite for workflow handoffs — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (workflow handoffs).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches workflow handoffs in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `workflow-handoffs-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for workflow handoffs.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits workflow handoffs — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for workflow handoffs.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global post-production pipeline and collaboration software",
      "sam": "$300M — professional NLE handoff and XML translation tools",
      "som": "$45M — post-production supervisors and assistant editors"
    },
    {
      "id": "video-retention-detective-23",
      "theme": "video",
      "title": "Retention Detective",
      "pitch": "Retention Detective runs a one-shot shell command inside a Sprite for audience analytics and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "audience analytics",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits audience analytics in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Retention Detective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRetention Detective runs a one-shot shell command inside a Sprite for audience analytics and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (audience analytics).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits audience analytics in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `audience-analytics-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a audience analytics-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for audience analytics.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global creator analytics and video optimization market",
      "sam": "$400M — professional audience retention and A/B testing tools",
      "som": "$60M — high-tier YouTubers and digital content strategists"
    },
    {
      "id": "video-wrap-speech-writer-24",
      "theme": "video",
      "title": "Wrap Speech Writer",
      "pitch": "Wrap Speech Writer spins up a fresh public Sprite for each session so production wrap gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "production wrap",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits production wrap in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Wrap Speech Writer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWrap Speech Writer spins up a fresh public Sprite for each session so production wrap gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (production wrap).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits production wrap in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `production-wrap-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for production wrap.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global film production management and wrap software",
      "sam": "$100M — professional production wrap and crew morale tools",
      "som": "$15M — line producers and indie film directors"
    },
    {
      "id": "video-scene-emotion-whisperer-0",
      "theme": "video",
      "title": "Scene Emotion Whisperer",
      "pitch": "Scene Emotion Whisperer composes actor direction in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "actor direction",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits actor direction in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Scene Emotion Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nScene Emotion Whisperer composes actor direction in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (actor direction).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits actor direction in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `actor-direction-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for actor direction (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for actor direction.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and production software",
      "sam": "$400M — indie film and commercial production tools",
      "som": "$50M — on-set digital assistant apps"
    },
    {
      "id": "video-palette-vision-coach-1",
      "theme": "video",
      "title": "Palette Vision Coach",
      "pitch": "Palette Vision Coach boots a long-running service inside a Sprite for color grading — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "color grading",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches color grading in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Palette Vision Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPalette Vision Coach boots a long-running service inside a Sprite for color grading — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (color grading).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches color grading in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `color-grading-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for color grading.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits color grading — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for color grading.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$300M — professional post-production tools",
      "som": "$20M — AI color grading assistants"
    },
    {
      "id": "video-footstep-foley-director-2",
      "theme": "video",
      "title": "Footstep Foley Director",
      "pitch": "Footstep Foley Director runs a one-shot shell command inside a Sprite for foley artistry and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "foley artistry",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits foley artistry in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Footstep Foley Director\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFootstep Foley Director runs a one-shot shell command inside a Sprite for foley artistry and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (foley artistry).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits foley artistry in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `foley-artistry-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a foley artistry-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for foley artistry.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and production software",
      "sam": "$250M — audio post-production software",
      "som": "$15M — foley and sound design aids"
    },
    {
      "id": "video-craft-service-chef-3",
      "theme": "video",
      "title": "Craft Service Chef",
      "pitch": "Craft Service Chef spins up a fresh public Sprite for each session so set catering gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "set catering",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits set catering in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Craft Service Chef\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCraft Service Chef spins up a fresh public Sprite for each session so set catering gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (set catering).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits set catering in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `set-catering-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for set catering.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production services market",
      "sam": "$200M — on-set logistics and catering",
      "som": "$10M — crew wellness and catering apps"
    },
    {
      "id": "video-dialogue-rhythm-surgeon-4",
      "theme": "video",
      "title": "Dialogue Rhythm Surgeon",
      "pitch": "Dialogue Rhythm Surgeon composes script polishing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "script polishing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits script polishing in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Dialogue Rhythm Surgeon\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDialogue Rhythm Surgeon composes script polishing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (script polishing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits script polishing in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `script-polishing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for script polishing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for script polishing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and pre-production software",
      "sam": "$350M — screenwriting and story development tools",
      "som": "$25M — AI script doctoring assistants"
    },
    {
      "id": "video-dolly-track-choreographer-5",
      "theme": "video",
      "title": "Dolly Track Choreographer",
      "pitch": "Dolly Track Choreographer boots a long-running service inside a Sprite for camera operation — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "camera operation",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches camera operation in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Dolly Track Choreographer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDolly Track Choreographer boots a long-running service inside a Sprite for camera operation — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (camera operation).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches camera operation in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `camera-operation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for camera operation.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits camera operation — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for camera operation.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software and tools",
      "sam": "$400M — cinematography and camera operation aids",
      "som": "$30M — on-set camera movement assistants"
    },
    {
      "id": "video-lumens-ratio-mentor-6",
      "theme": "video",
      "title": "Lumens Ratio Mentor",
      "pitch": "Lumens Ratio Mentor runs a one-shot shell command inside a Sprite for cinematography lighting and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "cinematography lighting",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits cinematography lighting in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Lumens Ratio Mentor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLumens Ratio Mentor runs a one-shot shell command inside a Sprite for cinematography lighting and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (cinematography lighting).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits cinematography lighting in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `cinematography-lighting-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a cinematography lighting-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for cinematography lighting.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software market",
      "sam": "$300M — lighting design and pre-visualization tools",
      "som": "$20M — AI gaffer and lighting assistants"
    },
    {
      "id": "video-cut-point-metronome-7",
      "theme": "video",
      "title": "Cut Point Metronome",
      "pitch": "Cut Point Metronome spins up a fresh public Sprite for each session so video editing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "video editing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits video editing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Cut Point Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCut Point Metronome spins up a fresh public Sprite for each session so video editing gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (video editing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits video editing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `video-editing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for video editing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$500M — professional NLE and editing plugins",
      "som": "$40M — AI pacing and editing assistants"
    },
    {
      "id": "video-showrunner-pitch-sparrer-8",
      "theme": "video",
      "title": "Showrunner Pitch Sparrer",
      "pitch": "Showrunner Pitch Sparrer composes project pitching in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "project pitching",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits project pitching in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Showrunner Pitch Sparrer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nShowrunner Pitch Sparrer composes project pitching in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (project pitching).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits project pitching in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `project-pitching-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for project pitching (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for project pitching.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global creator economy and production software",
      "sam": "$200M — pitch deck and presentation tools",
      "som": "$15M — AI pitch coaching for filmmakers"
    },
    {
      "id": "video-prop-placement-watcher-9",
      "theme": "video",
      "title": "Prop Placement Watcher",
      "pitch": "Prop Placement Watcher boots a long-running service inside a Sprite for script supervision — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "script supervision",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches script supervision in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Prop Placement Watcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProp Placement Watcher boots a long-running service inside a Sprite for script supervision — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (script supervision).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches script supervision in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `script-supervision-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for script supervision.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits script supervision — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for script supervision.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software market",
      "sam": "$150M — script supervision and continuity tools",
      "som": "$10M — AI continuity tracking assistants"
    },
    {
      "id": "video-roto-mask-guide-10",
      "theme": "video",
      "title": "Roto Mask Guide",
      "pitch": "Roto Mask Guide runs a one-shot shell command inside a Sprite for visual effects and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "visual effects",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits visual effects in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Roto Mask Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRoto Mask Guide runs a one-shot shell command inside a Sprite for visual effects and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (visual effects).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits visual effects in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `visual-effects-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a visual effects-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for visual effects.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and VFX software",
      "sam": "$400M — compositing and rotoscoping tools",
      "som": "$30M — AI VFX workflow assistants"
    },
    {
      "id": "video-render-break-therapist-11",
      "theme": "video",
      "title": "Render Break Therapist",
      "pitch": "Render Break Therapist spins up a fresh public Sprite for each session so creator wellness gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "creator wellness",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits creator wellness in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Render Break Therapist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRender Break Therapist spins up a fresh public Sprite for each session so creator wellness gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (creator wellness).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits creator wellness in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `creator-wellness-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for creator wellness.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$100M — creator wellness and productivity tools",
      "som": "$5M — AI mental health aids for editors"
    },
    {
      "id": "video-sun-angle-scout-12",
      "theme": "video",
      "title": "Sun Angle Scout",
      "pitch": "Sun Angle Scout composes location management in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "location management",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits location management in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Sun Angle Scout\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSun Angle Scout composes location management in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (location management).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits location management in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `location-management-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for location management (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for location management.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software market",
      "sam": "$150M — location scouting and pre-production apps",
      "som": "$10M — AI location management assistants"
    },
    {
      "id": "video-lip-sync-prompter-13",
      "theme": "video",
      "title": "Lip Sync Prompter",
      "pitch": "Lip Sync Prompter boots a long-running service inside a Sprite for dialogue replacement — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "dialogue replacement",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches dialogue replacement in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Lip Sync Prompter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLip Sync Prompter boots a long-running service inside a Sprite for dialogue replacement — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (dialogue replacement).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches dialogue replacement in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `dialogue-replacement-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for dialogue replacement.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits dialogue replacement — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for dialogue replacement.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and audio software",
      "sam": "$250M — ADR and dubbing studio tools",
      "som": "$20M — AI dialogue replacement assistants"
    },
    {
      "id": "video-lens-fungus-inspector-14",
      "theme": "video",
      "title": "Lens Fungus Inspector",
      "pitch": "Lens Fungus Inspector runs a one-shot shell command inside a Sprite for equipment upkeep and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "equipment upkeep",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits equipment upkeep in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Lens Fungus Inspector\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLens Fungus Inspector runs a one-shot shell command inside a Sprite for equipment upkeep and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (equipment upkeep).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits equipment upkeep in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `equipment-upkeep-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a equipment upkeep-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for equipment upkeep.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software and services",
      "sam": "$100M — gear rental and maintenance software",
      "som": "$5M — AI camera equipment upkeep aids"
    },
    {
      "id": "video-hook-retention-hacker-15",
      "theme": "video",
      "title": "Hook Retention Hacker",
      "pitch": "Hook Retention Hacker spins up a fresh public Sprite for each session so short-form editing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "short-form editing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits short-form editing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Hook Retention Hacker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHook Retention Hacker spins up a fresh public Sprite for each session so short-form editing gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (short-form editing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits short-form editing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `short-form-editing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for short-form editing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$600M — social media and short-form video tools",
      "som": "$50M — AI retention and hook assistants"
    },
    {
      "id": "video-shot-list-architect-16",
      "theme": "video",
      "title": "Shot List Architect",
      "pitch": "Shot List Architect composes pre-visualization in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "pre-visualization",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits pre-visualization in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Shot List Architect\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nShot List Architect composes pre-visualization in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (pre-visualization).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits pre-visualization in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `pre-visualization-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for pre-visualization (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for pre-visualization.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software market",
      "sam": "$300M — storyboarding and pre-visualization tools",
      "som": "$25M — AI shot listing assistants"
    },
    {
      "id": "video-eq-frequency-hunter-17",
      "theme": "video",
      "title": "EQ Frequency Hunter",
      "pitch": "EQ Frequency Hunter boots a long-running service inside a Sprite for sound mixing — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "sound mixing",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches sound mixing in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"EQ Frequency Hunter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEQ Frequency Hunter boots a long-running service inside a Sprite for sound mixing — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (sound mixing).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches sound mixing in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `sound-mixing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for sound mixing.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits sound mixing — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sound mixing.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and audio software",
      "sam": "$350M — professional audio mixing plugins",
      "som": "$25M — AI audio EQ assistants"
    },
    {
      "id": "video-cutaway-context-weaver-18",
      "theme": "video",
      "title": "Cutaway Context Weaver",
      "pitch": "Cutaway Context Weaver runs a one-shot shell command inside a Sprite for documentary editing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "documentary editing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits documentary editing in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Cutaway Context Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCutaway Context Weaver runs a one-shot shell command inside a Sprite for documentary editing and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (documentary editing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits documentary editing in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `documentary-editing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a documentary editing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for documentary editing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$200M — documentary and long-form editing tools",
      "som": "$15M — AI b-roll and metaphor assistants"
    },
    {
      "id": "video-stunt-rigging-caller-19",
      "theme": "video",
      "title": "Stunt Rigging Caller",
      "pitch": "Stunt Rigging Caller spins up a fresh public Sprite for each session so on-set safety gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "on-set safety",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits on-set safety in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Stunt Rigging Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStunt Rigging Caller spins up a fresh public Sprite for each session so on-set safety gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (on-set safety).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits on-set safety in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `on-set-safety-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for on-set safety.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production software market",
      "sam": "$100M — stunt coordination and safety software",
      "som": "$5M — AI on-set safety assistants"
    },
    {
      "id": "video-idiom-adaptation-coach-20",
      "theme": "video",
      "title": "Idiom Adaptation Coach",
      "pitch": "Idiom Adaptation Coach composes language localization in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "language localization",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits language localization in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Idiom Adaptation Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIdiom Adaptation Coach composes language localization in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (language localization).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits language localization in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `language-localization-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for language localization (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for language localization.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and localization software",
      "sam": "$400M — video dubbing and translation tools",
      "som": "$30M — AI localization and adaptation aids"
    },
    {
      "id": "video-click-contrast-critic-21",
      "theme": "video",
      "title": "Click Contrast Critic",
      "pitch": "Click Contrast Critic boots a long-running service inside a Sprite for packaging design — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "packaging design",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches packaging design in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Click Contrast Critic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nClick Contrast Critic boots a long-running service inside a Sprite for packaging design — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (packaging design).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches packaging design in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `packaging-design-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for packaging design.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits packaging design — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for packaging design.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global creator economy and video software",
      "sam": "$200M — thumbnail and metadata optimization tools",
      "som": "$15M — AI thumbnail critique assistants"
    },
    {
      "id": "video-footage-metadata-miner-22",
      "theme": "video",
      "title": "Footage Metadata Miner",
      "pitch": "Footage Metadata Miner runs a one-shot shell command inside a Sprite for archival production and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "archival production",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits archival production in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Footage Metadata Miner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFootage Metadata Miner runs a one-shot shell command inside a Sprite for archival production and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (archival production).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits archival production in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `archival-production-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a archival production-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for archival production.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and asset management software",
      "sam": "$150M — archival and MAM tools",
      "som": "$10M — AI archival research assistants"
    },
    {
      "id": "video-tempo-map-conductor-23",
      "theme": "video",
      "title": "Tempo Map Conductor",
      "pitch": "Tempo Map Conductor spins up a fresh public Sprite for each session so film scoring gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "film scoring",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits film scoring in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Tempo Map Conductor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTempo Map Conductor spins up a fresh public Sprite for each session so film scoring gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (film scoring).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits film scoring in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `film-scoring-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for film scoring.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing and audio software",
      "sam": "$300M — film scoring and DAW tools",
      "som": "$20M — AI tempo mapping assistants"
    },
    {
      "id": "video-focus-group-simulator-24",
      "theme": "video",
      "title": "Focus Group Simulator",
      "pitch": "Focus Group Simulator composes test screening in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "test screening",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits test screening in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Focus Group Simulator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFocus Group Simulator composes test screening in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (test screening).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits test screening in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `test-screening-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for test screening (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for test screening.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video production and analytics software",
      "sam": "$100M — test screening and audience feedback tools",
      "som": "$5M — AI focus group simulators"
    },
    {
      "id": "video-scout-whisper-0",
      "theme": "video",
      "title": "Scout Whisper",
      "pitch": "Scout Whisper boots a long-running service inside a Sprite for location scouting — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "location scouting",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches location scouting in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Scout Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nScout Whisper boots a long-running service inside a Sprite for location scouting — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (location scouting).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches location scouting in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `location-scouting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for location scouting.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits location scouting — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for location scouting.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global film production services",
      "sam": "$500M — location scouting and pre-production software",
      "som": "$15M — indie filmmaker location management tools"
    },
    {
      "id": "video-dailies-dictator-1",
      "theme": "video",
      "title": "Dailies Dictator",
      "pitch": "Dailies Dictator runs a one-shot shell command inside a Sprite for footage logging and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "footage logging",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits footage logging in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Dailies Dictator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDailies Dictator runs a one-shot shell command inside a Sprite for footage logging and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (footage logging).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits footage logging in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `footage-logging-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a footage logging-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for footage logging.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — video editing software market",
      "sam": "$300M — post-production asset management",
      "som": "$10M — DIT and dailies logging utilities"
    },
    {
      "id": "video-blocking-caller-2",
      "theme": "video",
      "title": "Blocking Caller",
      "pitch": "Blocking Caller spins up a fresh public Sprite for each session so scene blocking gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "scene blocking",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits scene blocking in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Blocking Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlocking Caller spins up a fresh public Sprite for each session so scene blocking gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (scene blocking).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits scene blocking in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `scene-blocking-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for scene blocking.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — production planning software",
      "sam": "$200M — digital storyboarding and blocking tools",
      "som": "$5M — indie director rehearsal aids"
    },
    {
      "id": "video-b-roll-brainstormer-3",
      "theme": "video",
      "title": "B-Roll Brainstormer",
      "pitch": "B-Roll Brainstormer composes documentary interviewing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "documentary interviewing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits documentary interviewing in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"B-Roll Brainstormer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nB-Roll Brainstormer composes documentary interviewing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (documentary interviewing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits documentary interviewing in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `documentary-interviewing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for documentary interviewing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for documentary interviewing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — documentary production market",
      "sam": "$400M — interview and field production gear and software",
      "som": "$12M — solo documentary creator tools"
    },
    {
      "id": "video-grade-whisperer-4",
      "theme": "video",
      "title": "Grade Whisperer",
      "pitch": "Grade Whisperer boots a long-running service inside a Sprite for color grading — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "color grading",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches color grading in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Grade Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGrade Whisperer boots a long-running service inside a Sprite for color grading — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (color grading).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches color grading in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `color-grading-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for color grading.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits color grading — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for color grading.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — video editing software market",
      "sam": "$250M — color grading and finishing software",
      "som": "$8M — freelance colorist workflow plugins"
    },
    {
      "id": "video-foley-dictation-5",
      "theme": "video",
      "title": "Foley Dictation",
      "pitch": "Foley Dictation runs a one-shot shell command inside a Sprite for sound design and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "sound design",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits sound design in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Foley Dictation\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFoley Dictation runs a one-shot shell command inside a Sprite for sound design and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (sound design).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits sound design in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `sound-design-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a sound design-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sound design.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — audio post-production market",
      "sam": "$300M — foley and sound effects libraries",
      "som": "$10M — indie sound designer workflow tools"
    },
    {
      "id": "video-pitch-pacer-6",
      "theme": "video",
      "title": "Pitch Pacer",
      "pitch": "Pitch Pacer spins up a fresh public Sprite for each session so project pitching gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "project pitching",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits project pitching in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Pitch Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPitch Pacer spins up a fresh public Sprite for each session so project pitching gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (project pitching).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits project pitching in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `project-pitching-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for project pitching.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — creator coaching and education",
      "sam": "$150M — filmmaker pitch and business tools",
      "som": "$5M — film student career prep software"
    },
    {
      "id": "video-vlog-flow-catcher-7",
      "theme": "video",
      "title": "Vlog Flow Catcher",
      "pitch": "Vlog Flow Catcher composes solo content creation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "solo content creation",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits solo content creation in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Vlog Flow Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVlog Flow Catcher composes solo content creation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (solo content creation).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits solo content creation in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `solo-content-creation-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for solo content creation (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for solo content creation.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — creator economy software",
      "sam": "$800M — vlogging and YouTube production tools",
      "som": "$20M — solo creator teleprompter apps"
    },
    {
      "id": "video-table-read-tracker-8",
      "theme": "video",
      "title": "Table Read Tracker",
      "pitch": "Table Read Tracker boots a long-running service inside a Sprite for script rehearsal — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "script rehearsal",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches script rehearsal in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Table Read Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTable Read Tracker boots a long-running service inside a Sprite for script rehearsal — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (script rehearsal).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches script rehearsal in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `script-rehearsal-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for script rehearsal.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits script rehearsal — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for script rehearsal.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — screenwriting software market",
      "sam": "$200M — script breakdown and rehearsal tools",
      "som": "$8M — indie production pre-production suites"
    },
    {
      "id": "video-lens-logger-9",
      "theme": "video",
      "title": "Lens Logger",
      "pitch": "Lens Logger runs a one-shot shell command inside a Sprite for camera department and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "camera department",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits camera department in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Lens Logger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLens Logger runs a one-shot shell command inside a Sprite for camera department and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (camera department).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits camera department in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `camera-department-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a camera department-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for camera department.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — video production management",
      "sam": "$300M — camera and grip logging software",
      "som": "$10M — AC and DIT utility apps"
    },
    {
      "id": "video-cut-review-catcher-10",
      "theme": "video",
      "title": "Cut Review Catcher",
      "pitch": "Cut Review Catcher spins up a fresh public Sprite for each session so client feedback gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "client feedback",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits client feedback in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Cut Review Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCut Review Catcher spins up a fresh public Sprite for each session so client feedback gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (client feedback).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits client feedback in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `client-feedback-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for client feedback.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — video editing software market",
      "sam": "$400M — video review and collaboration platforms",
      "som": "$15M — agency and post-house feedback tools"
    },
    {
      "id": "video-ad-script-drafter-11",
      "theme": "video",
      "title": "AD Script Drafter",
      "pitch": "AD Script Drafter composes accessibility post-production in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "accessibility post-production",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits accessibility post-production in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"AD Script Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAD Script Drafter composes accessibility post-production in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (accessibility post-production).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits accessibility post-production in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `accessibility-post-produ-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for accessibility post-production (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for accessibility post-production.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200M — media accessibility services",
      "sam": "$80M — audio description and subtitling software",
      "som": "$5M — indie film compliance and accessibility tools"
    },
    {
      "id": "video-vibe-board-dictator-12",
      "theme": "video",
      "title": "Vibe Board Dictator",
      "pitch": "Vibe Board Dictator boots a long-running service inside a Sprite for music video pre-production — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "music video pre-production",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches music video pre-production in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Vibe Board Dictator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVibe Board Dictator boots a long-running service inside a Sprite for music video pre-production — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (music video pre-production).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches music video pre-production in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `music-video-pre-producti-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for music video pre-production.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits music video pre-production — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for music video pre-production.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — music video production market",
      "sam": "$200M — treatment and mood board software",
      "som": "$8M — freelance director pitch tools"
    },
    {
      "id": "video-stunt-safety-caller-13",
      "theme": "video",
      "title": "Stunt Safety Caller",
      "pitch": "Stunt Safety Caller runs a one-shot shell command inside a Sprite for stunt coordination and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "stunt coordination",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits stunt coordination in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Stunt Safety Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStunt Safety Caller runs a one-shot shell command inside a Sprite for stunt coordination and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (stunt coordination).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits stunt coordination in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `stunt-coordination-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a stunt coordination-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for stunt coordination.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — production safety and risk management",
      "sam": "$150M — stunt and special effects planning",
      "som": "$5M — indie action film safety protocols"
    },
    {
      "id": "video-plate-pixel-catcher-14",
      "theme": "video",
      "title": "Plate Pixel Catcher",
      "pitch": "Plate Pixel Catcher spins up a fresh public Sprite for each session so VFX supervision gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "VFX supervision",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits VFX supervision in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Plate Pixel Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlate Pixel Catcher spins up a fresh public Sprite for each session so VFX supervision gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (VFX supervision).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits VFX supervision in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `vfx-supervision-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for VFX supervision.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — VFX and animation market",
      "sam": "$600M — on-set VFX and virtual production tools",
      "som": "$20M — indie VFX supervisor utilities"
    },
    {
      "id": "video-archive-whisperer-15",
      "theme": "video",
      "title": "Archive Whisperer",
      "pitch": "Archive Whisperer composes archival research in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "archival research",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits archival research in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Archive Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nArchive Whisperer composes archival research in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (archival research).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits archival research in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `archival-research-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for archival research (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for archival research.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — documentary and factual production",
      "sam": "$150M — archival research and media management",
      "som": "$5M — historical documentary researcher tools"
    },
    {
      "id": "video-exec-flow-coach-16",
      "theme": "video",
      "title": "Exec Flow Coach",
      "pitch": "Exec Flow Coach boots a long-running service inside a Sprite for corporate video production — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "corporate video production",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches corporate video production in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Exec Flow Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nExec Flow Coach boots a long-running service inside a Sprite for corporate video production — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (corporate video production).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches corporate video production in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `corporate-video-producti-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for corporate video production.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits corporate video production — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for corporate video production.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — corporate video and training market",
      "sam": "$500M — enterprise video production software",
      "som": "$15M — corporate A/V team teleprompter tools"
    },
    {
      "id": "video-crafty-count-caller-17",
      "theme": "video",
      "title": "Crafty Count Caller",
      "pitch": "Crafty Count Caller runs a one-shot shell command inside a Sprite for set catering logistics and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "set catering logistics",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits set catering logistics in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Crafty Count Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCrafty Count Caller runs a one-shot shell command inside a Sprite for set catering logistics and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (set catering logistics).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits set catering logistics in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `set-catering-logistics-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a set catering logistics-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for set catering logistics.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$300M — film set catering and logistics",
      "sam": "$100M — production resource management software",
      "som": "$5M — indie film craft services planning"
    },
    {
      "id": "video-mix-stage-mumbler-18",
      "theme": "video",
      "title": "Mix Stage Mumbler",
      "pitch": "Mix Stage Mumbler spins up a fresh public Sprite for each session so audio mixing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "audio mixing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits audio mixing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Mix Stage Mumbler\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMix Stage Mumbler spins up a fresh public Sprite for each session so audio mixing gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (audio mixing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits audio mixing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `audio-mixing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for audio mixing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — audio post-production market",
      "sam": "$300M — dubbing and mixing stage software",
      "som": "$10M — freelance audio mixer workflow plugins"
    },
    {
      "id": "video-fest-strategy-pacer-19",
      "theme": "video",
      "title": "Fest Strategy Pacer",
      "pitch": "Fest Strategy Pacer composes film festival distribution in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "film festival distribution",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits film festival distribution in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Fest Strategy Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFest Strategy Pacer composes film festival distribution in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (film festival distribution).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits film festival distribution in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `film-festival-distributi-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for film festival distribution (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for film festival distribution.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200M — film distribution and festival market",
      "sam": "$80M — indie film marketing and submission tools",
      "som": "$5M — short film producer strategy apps"
    },
    {
      "id": "video-essay-thread-catcher-20",
      "theme": "video",
      "title": "Essay Thread Catcher",
      "pitch": "Essay Thread Catcher boots a long-running service inside a Sprite for video essay scripting — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "video essay scripting",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches video essay scripting in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Essay Thread Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEssay Thread Catcher boots a long-running service inside a Sprite for video essay scripting — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (video essay scripting).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches video essay scripting in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `video-essay-scripting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for video essay scripting.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits video essay scripting — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for video essay scripting.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — creator economy software",
      "sam": "$600M — YouTube education and essayist tools",
      "som": "$20M — video essayist research and drafting apps"
    },
    {
      "id": "video-volume-vibe-caller-21",
      "theme": "video",
      "title": "Volume Vibe Caller",
      "pitch": "Volume Vibe Caller runs a one-shot shell command inside a Sprite for virtual production and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "virtual production",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits virtual production in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Volume Vibe Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVolume Vibe Caller runs a one-shot shell command inside a Sprite for virtual production and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (virtual production).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits virtual production in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `virtual-production-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a virtual production-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for virtual production.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — VFX and virtual production market",
      "sam": "$800M — LED volume and Unreal Engine tools",
      "som": "$25M — virtual production DP utilities"
    },
    {
      "id": "video-scripty-eye-catcher-22",
      "theme": "video",
      "title": "Scripty Eye Catcher",
      "pitch": "Scripty Eye Catcher spins up a fresh public Sprite for each session so script supervision gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "script supervision",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits script supervision in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Scripty Eye Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nScripty Eye Catcher spins up a fresh public Sprite for each session so script supervision gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (script supervision).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits script supervision in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `script-supervision-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for script supervision.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — video production management",
      "sam": "$200M — script supervision and continuity software",
      "som": "$8M — indie scripty digital notebook apps"
    },
    {
      "id": "video-drone-path-whisperer-23",
      "theme": "video",
      "title": "Drone Path Whisperer",
      "pitch": "Drone Path Whisperer composes aerial cinematography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "aerial cinematography",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits aerial cinematography in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Drone Path Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDrone Path Whisperer composes aerial cinematography in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (aerial cinematography).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits aerial cinematography in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `aerial-cinematography-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for aerial cinematography (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for aerial cinematography.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — commercial drone and aerial video market",
      "sam": "$150M — drone flight planning and cinematography software",
      "som": "$5M — FPV and cinematic drone pilot tools"
    },
    {
      "id": "video-edit-bay-exhale-24",
      "theme": "video",
      "title": "Edit Bay Exhale",
      "pitch": "Edit Bay Exhale boots a long-running service inside a Sprite for creator mental health — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "creator mental health",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches creator mental health in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Edit Bay Exhale\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEdit Bay Exhale boots a long-running service inside a Sprite for creator mental health — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (creator mental health).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches creator mental health in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `creator-mental-health-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for creator mental health.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits creator mental health — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for creator mental health.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — creator coaching and wellness",
      "sam": "$150M — freelancer mental health and productivity tools",
      "som": "$5M — video editor burnout prevention apps"
    },
    {
      "id": "video-foley-prompt-crafter-0",
      "theme": "video",
      "title": "Foley Prompt Crafter",
      "pitch": "Foley Prompt Crafter runs a one-shot shell command inside a Sprite for foley drafting and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "foley drafting",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits foley drafting in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Foley Prompt Crafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFoley Prompt Crafter runs a one-shot shell command inside a Sprite for foley drafting and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (foley drafting).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits foley drafting in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `foley-drafting-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a foley drafting-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for foley drafting.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$300M — post-production sound design and foley tools",
      "som": "$45M — indie filmmaker and freelance editor audio plugins"
    },
    {
      "id": "video-rough-cut-scorer-1",
      "theme": "video",
      "title": "Rough Cut Scorer",
      "pitch": "Rough Cut Scorer spins up a fresh public Sprite for each session so temp scoring gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "temp scoring",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits temp scoring in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Rough Cut Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRough Cut Scorer spins up a fresh public Sprite for each session so temp scoring gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (temp scoring).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits temp scoring in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `temp-scoring-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for temp scoring.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$250M — stock music and temp track licensing platforms",
      "som": "$30M — documentary and narrative short film scoring subscriptions"
    },
    {
      "id": "video-indie-room-toner-2",
      "theme": "video",
      "title": "Indie Room Toner",
      "pitch": "Indie Room Toner composes ambience layering in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "ambience layering",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits ambience layering in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Indie Room Toner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIndie Room Toner composes ambience layering in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (ambience layering).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits ambience layering in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `ambience-layering-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for ambience layering (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for ambience layering.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$150M — dialogue editing and noise reduction software",
      "som": "$20M — micro-budget film production audio toolkits"
    },
    {
      "id": "video-trailer-riser-builder-3",
      "theme": "video",
      "title": "Trailer Riser Builder",
      "pitch": "Trailer Riser Builder boots a long-running service inside a Sprite for trailer sound design — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "trailer sound design",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches trailer sound design in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Trailer Riser Builder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTrailer Riser Builder boots a long-running service inside a Sprite for trailer sound design — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (trailer sound design).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches trailer sound design in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `trailer-sound-design-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for trailer sound design.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits trailer sound design — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for trailer sound design.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$200M — trailer house and promotional video post-production",
      "som": "$35M — freelance trailer editor sound asset libraries"
    },
    {
      "id": "video-scifi-ui-sounder-4",
      "theme": "video",
      "title": "SciFi UI Sounder",
      "pitch": "SciFi UI Sounder runs a one-shot shell command inside a Sprite for interface sound design and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "interface sound design",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits interface sound design in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"SciFi UI Sounder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSciFi UI Sounder runs a one-shot shell command inside a Sprite for interface sound design and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (interface sound design).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits interface sound design in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `interface-sound-design-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a interface sound design-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for interface sound design.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$180M — VFX and motion graphics audio post-production",
      "som": "$25M — sci-fi short film and game cinematic sound design"
    },
    {
      "id": "video-horror-stinger-mapper-5",
      "theme": "video",
      "title": "Horror Stinger Mapper",
      "pitch": "Horror Stinger Mapper spins up a fresh public Sprite for each session so tension scoring gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "tension scoring",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits tension scoring in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Horror Stinger Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHorror Stinger Mapper spins up a fresh public Sprite for each session so tension scoring gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (tension scoring).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits tension scoring in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `tension-scoring-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for tension scoring.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$120M — genre-specific film audio post-production",
      "som": "$15M — indie horror filmmaker sound design assets"
    },
    {
      "id": "video-wildlife-b-roll-voicer-6",
      "theme": "video",
      "title": "Wildlife B-Roll Voicer",
      "pitch": "Wildlife B-Roll Voicer composes documentary soundscape in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "documentary soundscape",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits documentary soundscape in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Wildlife B-Roll Voicer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWildlife B-Roll Voicer composes documentary soundscape in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (documentary soundscape).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits documentary soundscape in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `documentary-soundscape-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for documentary soundscape (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for documentary soundscape.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$220M — documentary and nature film post-production",
      "som": "$30M — wildlife videographer and travel documentary audio tools"
    },
    {
      "id": "video-fight-scene-puncher-7",
      "theme": "video",
      "title": "Fight Scene Puncher",
      "pitch": "Fight Scene Puncher boots a long-running service inside a Sprite for action audio choreography — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "action audio choreography",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches action audio choreography in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Fight Scene Puncher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFight Scene Puncher boots a long-running service inside a Sprite for action audio choreography — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (action audio choreography).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches action audio choreography in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `action-audio-choreograph-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for action audio choreography.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits action audio choreography — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for action audio choreography.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$250M — action and martial arts film post-production",
      "som": "$40M — stunt coordinator and action editor audio libraries"
    },
    {
      "id": "video-vlog-swoosh-designer-8",
      "theme": "video",
      "title": "Vlog Swoosh Designer",
      "pitch": "Vlog Swoosh Designer runs a one-shot shell command inside a Sprite for creator transition audio and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "creator transition audio",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits creator transition audio in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Vlog Swoosh Designer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVlog Swoosh Designer runs a one-shot shell command inside a Sprite for creator transition audio and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (creator transition audio).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits creator transition audio in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `creator-transition-audio-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a creator transition audio-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for creator transition audio.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$400M — creator economy video editing and asset tools",
      "som": "$60M — travel vlogger and lifestyle creator audio packs"
    },
    {
      "id": "video-vintage-street-noiser-9",
      "theme": "video",
      "title": "Vintage Street Noiser",
      "pitch": "Vintage Street Noiser spins up a fresh public Sprite for each session so period piece audio gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "period piece audio",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits period piece audio in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Vintage Street Noiser\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nVintage Street Noiser spins up a fresh public Sprite for each session so period piece audio gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (period piece audio).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits period piece audio in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `period-piece-audio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for period piece audio.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$150M — historical drama and period piece post-production",
      "som": "$20M — student and indie period filmmaker audio assets"
    },
    {
      "id": "video-dream-sequence-scorer-10",
      "theme": "video",
      "title": "Dream Sequence Scorer",
      "pitch": "Dream Sequence Scorer composes surreal narrative scoring in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "surreal narrative scoring",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits surreal narrative scoring in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Dream Sequence Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDream Sequence Scorer composes surreal narrative scoring in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (surreal narrative scoring).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits surreal narrative scoring in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `surreal-narrative-scorin-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for surreal narrative scoring (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for surreal narrative scoring.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$180M — experimental and art-house film scoring",
      "som": "$25M — indie director and music video composer subscriptions"
    },
    {
      "id": "video-car-chase-layerer-11",
      "theme": "video",
      "title": "Car Chase Layerer",
      "pitch": "Car Chase Layerer boots a long-running service inside a Sprite for vehicle acoustic profiling — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "vehicle acoustic profiling",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches vehicle acoustic profiling in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Car Chase Layerer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCar Chase Layerer boots a long-running service inside a Sprite for vehicle acoustic profiling — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (vehicle acoustic profiling).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches vehicle acoustic profiling in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `vehicle-acoustic-profili-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for vehicle acoustic profiling.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits vehicle acoustic profiling — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for vehicle acoustic profiling.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$200M — automotive and action film audio post-production",
      "som": "$30M — car videographer and action short sound design"
    },
    {
      "id": "video-tiktok-arc-scorer-12",
      "theme": "video",
      "title": "TikTok Arc Scorer",
      "pitch": "TikTok Arc Scorer runs a one-shot shell command inside a Sprite for micro-short pacing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "micro-short pacing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits micro-short pacing in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"TikTok Arc Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTikTok Arc Scorer runs a one-shot shell command inside a Sprite for micro-short pacing and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (micro-short pacing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits micro-short pacing in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `micro-short-pacing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a micro-short pacing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for micro-short pacing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$500M — short-form mobile video creation tools",
      "som": "$75M — TikTok and Reels creator audio generation apps"
    },
    {
      "id": "video-podcast-underscore-builder-13",
      "theme": "video",
      "title": "Podcast Underscore Builder",
      "pitch": "Podcast Underscore Builder spins up a fresh public Sprite for each session so audio-to-video adaptation gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "audio-to-video adaptation",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits audio-to-video adaptation in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Podcast Underscore Builder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPodcast Underscore Builder spins up a fresh public Sprite for each session so audio-to-video adaptation gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (audio-to-video adaptation).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits audio-to-video adaptation in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `audio-to-video-adaptatio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for audio-to-video adaptation.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$250M — podcast video production and repurposing tools",
      "som": "$40M — podcast network and independent creator scoring tools"
    },
    {
      "id": "video-b-roll-mood-scorer-14",
      "theme": "video",
      "title": "B-Roll Mood Scorer",
      "pitch": "B-Roll Mood Scorer composes visual palette matching in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "visual palette matching",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits visual palette matching in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"B-Roll Mood Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nB-Roll Mood Scorer composes visual palette matching in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (visual palette matching).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits visual palette matching in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `visual-palette-matching-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for visual palette matching (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for visual palette matching.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$300M — stock footage and B-roll editing platforms",
      "som": "$45M — commercial and corporate videographer scoring tools"
    },
    {
      "id": "video-silent-film-reviver-15",
      "theme": "video",
      "title": "Silent Film Reviver",
      "pitch": "Silent Film Reviver boots a long-running service inside a Sprite for vintage media restoration — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "vintage media restoration",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches vintage media restoration in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Silent Film Reviver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSilent Film Reviver boots a long-running service inside a Sprite for vintage media restoration — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (vintage media restoration).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches vintage media restoration in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `vintage-media-restoratio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for vintage media restoration.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits vintage media restoration — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for vintage media restoration.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$100M — archival footage restoration and remastering",
      "som": "$15M — film historian and archive curator audio tools"
    },
    {
      "id": "video-corporate-beat-pacer-16",
      "theme": "video",
      "title": "Corporate Beat Pacer",
      "pitch": "Corporate Beat Pacer runs a one-shot shell command inside a Sprite for training video engagement and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "training video engagement",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits training video engagement in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Corporate Beat Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCorporate Beat Pacer runs a one-shot shell command inside a Sprite for training video engagement and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (training video engagement).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits training video engagement in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `training-video-engagemen-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a training video engagement-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for training video engagement.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$350M — corporate video production and e-learning media",
      "som": "$50M — enterprise L&D video creator audio subscriptions"
    },
    {
      "id": "video-music-video-mapper-17",
      "theme": "video",
      "title": "Music Video Mapper",
      "pitch": "Music Video Mapper spins up a fresh public Sprite for each session so lyrical visual syncing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "lyrical visual syncing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits lyrical visual syncing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Music Video Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMusic Video Mapper spins up a fresh public Sprite for each session so lyrical visual syncing gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (lyrical visual syncing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits lyrical visual syncing in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `lyrical-visual-syncing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for lyrical visual syncing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$280M — music video and visualizer post-production",
      "som": "$40M — independent musician and director video editing tools"
    },
    {
      "id": "video-asmr-trigger-sounder-18",
      "theme": "video",
      "title": "ASMR Trigger Sounder",
      "pitch": "ASMR Trigger Sounder composes tactile audio design in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "tactile audio design",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits tactile audio design in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"ASMR Trigger Sounder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nASMR Trigger Sounder composes tactile audio design in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (tactile audio design).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits tactile audio design in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `tactile-audio-design-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for tactile audio design (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for tactile audio design.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$150M — ASMR and sensory video production tools",
      "som": "$25M — ASMR creator audio enhancement and generation apps"
    },
    {
      "id": "video-drone-flight-scorer-19",
      "theme": "video",
      "title": "Drone Flight Scorer",
      "pitch": "Drone Flight Scorer boots a long-running service inside a Sprite for FPV dynamic scoring — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "FPV dynamic scoring",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches FPV dynamic scoring in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Drone Flight Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDrone Flight Scorer boots a long-running service inside a Sprite for FPV dynamic scoring — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (FPV dynamic scoring).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches FPV dynamic scoring in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `fpv-dynamic-scoring-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for FPV dynamic scoring.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits FPV dynamic scoring — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for FPV dynamic scoring.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$200M — aerial cinematography and drone video editing",
      "som": "$35M — FPV drone pilot and action sports scorer tools"
    },
    {
      "id": "video-claymation-foley-planner-20",
      "theme": "video",
      "title": "Claymation Foley Planner",
      "pitch": "Claymation Foley Planner runs a one-shot shell command inside a Sprite for stop-motion sound design and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "stop-motion sound design",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits stop-motion sound design in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Claymation Foley Planner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nClaymation Foley Planner runs a one-shot shell command inside a Sprite for stop-motion sound design and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (stop-motion sound design).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits stop-motion sound design in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `stop-motion-sound-design-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a stop-motion sound design-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for stop-motion sound design.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$120M — animation and stop-motion post-production audio",
      "som": "$20M — indie animator and student filmmaker foley tools"
    },
    {
      "id": "video-wedding-tear-scorer-21",
      "theme": "video",
      "title": "Wedding Tear Scorer",
      "pitch": "Wedding Tear Scorer spins up a fresh public Sprite for each session so emotional highlight scoring gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "emotional highlight scoring",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits emotional highlight scoring in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Wedding Tear Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWedding Tear Scorer spins up a fresh public Sprite for each session so emotional highlight scoring gets its own shareable sandbox URL in seconds.\nDiscipline: Videography & Film (emotional highlight scoring).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits emotional highlight scoring in Videography & Film because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `emotional-highlight-scor-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for emotional highlight scoring.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$250M — wedding and event videography post-production",
      "som": "$45M — professional wedding filmmaker scoring subscriptions"
    },
    {
      "id": "video-sports-hype-dropper-22",
      "theme": "video",
      "title": "Sports Hype Dropper",
      "pitch": "Sports Hype Dropper composes highlight reel audio in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "highlight reel audio",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits highlight reel audio in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Sports Hype Dropper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSports Hype Dropper composes highlight reel audio in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Videography & Film (highlight reel audio).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits highlight reel audio in Videography & Film because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `highlight-reel-audio-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for highlight reel audio (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for highlight reel audio.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$300M — sports media and highlight video production",
      "som": "$50M — freelance sports editor and team media audio tools"
    },
    {
      "id": "video-culinary-sizzle-timer-23",
      "theme": "video",
      "title": "Culinary Sizzle Timer",
      "pitch": "Culinary Sizzle Timer boots a long-running service inside a Sprite for food video audio — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "food video audio",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches food video audio in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Culinary Sizzle Timer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCulinary Sizzle Timer boots a long-running service inside a Sprite for food video audio — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Videography & Film (food video audio).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches food video audio in Videography & Film because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `food-video-audio-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for food video audio.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits food video audio — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for food video audio.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$180M — culinary and lifestyle video post-production",
      "som": "$30M — food vlogger and recipe creator audio enhancement"
    },
    {
      "id": "video-runway-strut-scorer-24",
      "theme": "video",
      "title": "Runway Strut Scorer",
      "pitch": "Runway Strut Scorer runs a one-shot shell command inside a Sprite for fashion film audio and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "fashion film audio",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits fashion film audio in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Runway Strut Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRunway Strut Scorer runs a one-shot shell command inside a Sprite for fashion film audio and streams the stdout back so the user watches the work happen live.\nDiscipline: Videography & Film (fashion film audio).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits fashion film audio in Videography & Film because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `fashion-film-audio-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a fashion film audio-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for fashion film audio.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — global video editing software market",
      "sam": "$150M — fashion film and commercial video production",
      "som": "$25M — fashion videographer and brand media scoring tools"
    }
  ]
}