{
  "theme": {
    "slug": "writing",
    "name": "Writing, Poetry & Narrative",
    "emoji": "✍️",
    "audience": "writers, poets, screenwriters, narrative designers",
    "market_anchor": "the writing tools market (~$1.5B) and >100M working writers"
  },
  "ideas": [
    {
      "id": "writing-scene-partner-whisper-0",
      "theme": "writing",
      "title": "Scene Partner Whisper",
      "pitch": "Scene Partner Whisper spins up a fresh public Sprite for each session so screenplay dialogue rehearsal gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "screenplay dialogue rehearsal",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits screenplay dialogue rehearsal in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Scene Partner Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nScene Partner Whisper spins up a fresh public Sprite for each session so screenplay dialogue rehearsal gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (screenplay dialogue rehearsal).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits screenplay dialogue rehearsal in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `screenplay-dialogue-rehe-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for screenplay dialogue rehearsal.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global screenwriting software and film prep",
      "sam": "$800M — indie and professional screenplay tools",
      "som": "$50M — dialogue-focused rehearsal apps"
    },
    {
      "id": "writing-slam-cadence-metronome-1",
      "theme": "writing",
      "title": "Slam Cadence Metronome",
      "pitch": "Slam Cadence Metronome composes written word rhythm in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "written word rhythm",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits written word rhythm in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Slam Cadence Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSlam Cadence Metronome composes written word rhythm in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (written word rhythm).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits written word rhythm in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `written-word-rhythm-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for written word rhythm (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for written word rhythm.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global poetry publishing and performance market",
      "sam": "$300M — written word and slam poetry tools",
      "som": "$20M — performance coaching for poets"
    },
    {
      "id": "writing-character-voice-forge-2",
      "theme": "writing",
      "title": "Character Voice Forge",
      "pitch": "Character Voice Forge boots a long-running service inside a Sprite for audiobook character prep — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "audiobook character prep",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches audiobook character prep in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Character Voice Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCharacter Voice Forge boots a long-running service inside a Sprite for audiobook character prep — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (audiobook character prep).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches audiobook character prep in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `audiobook-character-prep-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for audiobook character prep.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits audiobook character prep — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for audiobook character prep.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global audiobook production market",
      "sam": "$500M — narrator coaching and prep software",
      "som": "$40M — indie audiobook creator tools"
    },
    {
      "id": "writing-worldbuilding-lore-echo-3",
      "theme": "writing",
      "title": "Worldbuilding Lore Echo",
      "pitch": "Worldbuilding Lore Echo runs a one-shot shell command inside a Sprite for fantasy worldbuilding and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "fantasy worldbuilding",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits fantasy worldbuilding in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Worldbuilding Lore Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWorldbuilding Lore Echo runs a one-shot shell command inside a Sprite for fantasy worldbuilding and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (fantasy worldbuilding).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits fantasy worldbuilding in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `fantasy-worldbuilding-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a fantasy worldbuilding-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for fantasy worldbuilding.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global fiction publishing market",
      "sam": "$1.2B — novel writing and organization software",
      "som": "$80M — fantasy and sci-fi writing tools"
    },
    {
      "id": "writing-bedtime-story-pacer-4",
      "theme": "writing",
      "title": "Bedtime Story Pacer",
      "pitch": "Bedtime Story Pacer spins up a fresh public Sprite for each session so children's book pacing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "children's book pacing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits children's book pacing in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Bedtime Story Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBedtime Story Pacer spins up a fresh public Sprite for each session so children's book pacing gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (children's book pacing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits children's book pacing in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `children-s-book-pacing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for children's book pacing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global children's book publishing",
      "sam": "$400M — children's author writing tools",
      "som": "$30M — read-aloud picture book drafting apps"
    },
    {
      "id": "writing-podium-breath-coach-5",
      "theme": "writing",
      "title": "Podium Breath Coach",
      "pitch": "Podium Breath Coach composes speechwriting cadence in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "speechwriting cadence",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits speechwriting cadence in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Podium Breath Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPodium Breath Coach composes speechwriting cadence in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (speechwriting cadence).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits speechwriting cadence in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `speechwriting-cadence-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for speechwriting cadence (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for speechwriting cadence.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global public speaking and executive coaching",
      "sam": "$900M — speechwriting and presentation software",
      "som": "$60M — political and keynote speech prep tools"
    },
    {
      "id": "writing-tavern-npc-herald-6",
      "theme": "writing",
      "title": "Tavern NPC Herald",
      "pitch": "Tavern NPC Herald boots a long-running service inside a Sprite for tabletop RPG improv — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "tabletop RPG improv",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches tabletop RPG improv in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Tavern NPC Herald\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTavern NPC Herald boots a long-running service inside a Sprite for tabletop RPG improv — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (tabletop RPG improv).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches tabletop RPG improv in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `tabletop-rpg-improv-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for tabletop RPG improv.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits tabletop RPG improv — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for tabletop RPG improv.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global tabletop gaming market",
      "sam": "$600M — digital GM companion tools",
      "som": "$45M — virtual tabletop audio plugins"
    },
    {
      "id": "writing-tongue-bridge-weaver-7",
      "theme": "writing",
      "title": "Tongue Bridge Weaver",
      "pitch": "Tongue Bridge Weaver runs a one-shot shell command inside a Sprite for bilingual poetry translation and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "bilingual poetry translation",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits bilingual poetry translation in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Tongue Bridge Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTongue Bridge Weaver runs a one-shot shell command inside a Sprite for bilingual poetry translation and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (bilingual poetry translation).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits bilingual poetry translation in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `bilingual-poetry-transla-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a bilingual poetry translation-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for bilingual poetry translation.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global literary translation market",
      "sam": "$200M — poetry and bilingual writing tools",
      "som": "$15M — cross-cultural literary apps"
    },
    {
      "id": "writing-thesis-flow-whisperer-8",
      "theme": "writing",
      "title": "Thesis Flow Whisperer",
      "pitch": "Thesis Flow Whisperer spins up a fresh public Sprite for each session so essay rhetorical flow gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "essay rhetorical flow",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits essay rhetorical flow in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Thesis Flow Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nThesis Flow Whisperer spins up a fresh public Sprite for each session so essay rhetorical flow gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (essay rhetorical flow).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits essay rhetorical flow in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `essay-rhetorical-flow-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for essay rhetorical flow.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global academic and essay writing software",
      "sam": "$400M — long-form nonfiction drafting tools",
      "som": "$30M — collegiate essay coaching apps"
    },
    {
      "id": "writing-blocking-cue-caller-9",
      "theme": "writing",
      "title": "Blocking Cue Caller",
      "pitch": "Blocking Cue Caller composes playwriting stage directions in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "playwriting stage directions",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits playwriting stage directions in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Blocking Cue Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlocking Cue Caller composes playwriting stage directions in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (playwriting stage directions).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits playwriting stage directions in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `playwriting-stage-direct-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for playwriting stage directions (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for playwriting stage directions.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global theater production software",
      "sam": "$200M — playwriting and script formatting tools",
      "som": "$15M — stage direction rehearsal aids"
    },
    {
      "id": "writing-nostalgia-prompt-oracle-10",
      "theme": "writing",
      "title": "Nostalgia Prompt Oracle",
      "pitch": "Nostalgia Prompt Oracle boots a long-running service inside a Sprite for memoir memory triggering — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "memoir memory triggering",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches memoir memory triggering in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Nostalgia Prompt Oracle\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nNostalgia Prompt Oracle boots a long-running service inside a Sprite for memoir memory triggering — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (memoir memory triggering).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches memoir memory triggering in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `memoir-memory-triggering-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for memoir memory triggering.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits memoir memory triggering — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for memoir memory triggering.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global autobiography and memoir publishing",
      "sam": "$300M — personal journaling and memoir software",
      "som": "$25M — guided memory recall apps"
    },
    {
      "id": "writing-dread-tension-metronome-11",
      "theme": "writing",
      "title": "Dread Tension Metronome",
      "pitch": "Dread Tension Metronome runs a one-shot shell command inside a Sprite for horror suspense pacing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "horror suspense pacing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits horror suspense pacing in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Dread Tension Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDread Tension Metronome runs a one-shot shell command inside a Sprite for horror suspense pacing and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (horror suspense pacing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits horror suspense pacing in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `horror-suspense-pacing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a horror suspense pacing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for horror suspense pacing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — global horror fiction publishing",
      "sam": "$150M — genre-specific writing tools",
      "som": "$10M — thriller and horror pacing apps"
    },
    {
      "id": "writing-standup-beat-dropper-12",
      "theme": "writing",
      "title": "Standup Beat Dropper",
      "pitch": "Standup Beat Dropper spins up a fresh public Sprite for each session so comedy punchline timing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "comedy punchline timing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits comedy punchline timing in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Standup Beat Dropper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStandup Beat Dropper spins up a fresh public Sprite for each session so comedy punchline timing gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (comedy punchline timing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits comedy punchline timing in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `comedy-punchline-timing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for comedy punchline timing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — global comedy writing and performance",
      "sam": "$250M — standup material management tools",
      "som": "$20M — joke structure and timing apps"
    },
    {
      "id": "writing-melody-syllable-mapper-13",
      "theme": "writing",
      "title": "Melody Syllable Mapper",
      "pitch": "Melody Syllable Mapper composes lyricist syllable mapping in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "lyricist syllable mapping",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits lyricist syllable mapping in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Melody Syllable Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMelody Syllable Mapper composes lyricist syllable mapping in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (lyricist syllable mapping).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits lyricist syllable mapping in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `lyricist-syllable-mappin-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for lyricist syllable mapping (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for lyricist syllable mapping.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global songwriting and music publishing",
      "sam": "$800M — lyric writing and collaboration software",
      "som": "$50M — vocal melody and lyric drafting tools"
    },
    {
      "id": "writing-citation-chorus-reader-14",
      "theme": "writing",
      "title": "Citation Chorus Reader",
      "pitch": "Citation Chorus Reader boots a long-running service inside a Sprite for academic literature review — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "academic literature review",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches academic literature review in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Citation Chorus Reader\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCitation Chorus Reader boots a long-running service inside a Sprite for academic literature review — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (academic literature review).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches academic literature review in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `academic-literature-revi-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for academic literature review.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits academic literature review — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for academic literature review.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global academic research software",
      "sam": "$600M — literature review and citation tools",
      "som": "$40M — PhD student productivity apps"
    },
    {
      "id": "writing-wanderlust-ambient-narrator-15",
      "theme": "writing",
      "title": "Wanderlust Ambient Narrator",
      "pitch": "Wanderlust Ambient Narrator runs a one-shot shell command inside a Sprite for travel writing scene-setting and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "travel writing scene-setting",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits travel writing scene-setting in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Wanderlust Ambient Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nWanderlust Ambient Narrator runs a one-shot shell command inside a Sprite for travel writing scene-setting and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (travel writing scene-setting).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits travel writing scene-setting in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `travel-writing-scene-set-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a travel writing scene-setting-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for travel writing scene-setting.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global travel journalism and blogging",
      "sam": "$200M — travel writing and itinerary tools",
      "som": "$15M — immersive travel journaling apps"
    },
    {
      "id": "writing-client-persona-mimic-16",
      "theme": "writing",
      "title": "Client Persona Mimic",
      "pitch": "Client Persona Mimic spins up a fresh public Sprite for each session so ghostwriter tone-matching gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "ghostwriter tone-matching",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits ghostwriter tone-matching in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Client Persona Mimic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nClient Persona Mimic spins up a fresh public Sprite for each session so ghostwriter tone-matching gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (ghostwriter tone-matching).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits ghostwriter tone-matching in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `ghostwriter-tone-matchin-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for ghostwriter tone-matching.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global ghostwriting and freelance writing",
      "sam": "$400M — client management and drafting tools",
      "som": "$30M — executive ghostwriting software"
    },
    {
      "id": "writing-micro-fiction-broadcaster-17",
      "theme": "writing",
      "title": "Micro Fiction Broadcaster",
      "pitch": "Micro Fiction Broadcaster composes flash fiction podcasting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "flash fiction podcasting",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits flash fiction podcasting in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Micro Fiction Broadcaster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMicro Fiction Broadcaster composes flash fiction podcasting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (flash fiction podcasting).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits flash fiction podcasting in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `flash-fiction-podcasting-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for flash fiction podcasting (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for flash fiction podcasting.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global podcasting and short fiction",
      "sam": "$300M — audio drama and flash fiction tools",
      "som": "$20M — indie fiction podcasting apps"
    },
    {
      "id": "writing-combat-bark-crier-18",
      "theme": "writing",
      "title": "Combat Bark Crier",
      "pitch": "Combat Bark Crier boots a long-running service inside a Sprite for video game narrative design — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "video game narrative design",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches video game narrative design in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Combat Bark Crier\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCombat Bark Crier boots a long-running service inside a Sprite for video game narrative design — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (video game narrative design).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches video game narrative design in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `video-game-narrative-des-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for video game narrative design.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits video game narrative design — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for video game narrative design.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global game narrative and audio design",
      "sam": "$1B — game writing and dialogue tools",
      "som": "$70M — indie game VO prototyping apps"
    },
    {
      "id": "writing-intimacy-whisper-tester-19",
      "theme": "writing",
      "title": "Intimacy Whisper Tester",
      "pitch": "Intimacy Whisper Tester runs a one-shot shell command inside a Sprite for romance novel dialogue and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "romance novel dialogue",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits romance novel dialogue in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Intimacy Whisper Tester\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nIntimacy Whisper Tester runs a one-shot shell command inside a Sprite for romance novel dialogue and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (romance novel dialogue).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits romance novel dialogue in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `romance-novel-dialogue-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a romance novel dialogue-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for romance novel dialogue.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global romance fiction publishing",
      "sam": "$300M — romance writing and plotting tools",
      "som": "$25M — character chemistry drafting apps"
    },
    {
      "id": "writing-xenolinguistics-vowel-forge-20",
      "theme": "writing",
      "title": "Xenolinguistics Vowel Forge",
      "pitch": "Xenolinguistics Vowel Forge spins up a fresh public Sprite for each session so sci-fi language creation gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "sci-fi language creation",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits sci-fi language creation in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Xenolinguistics Vowel Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nXenolinguistics Vowel Forge spins up a fresh public Sprite for each session so sci-fi language creation gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (sci-fi language creation).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits sci-fi language creation in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `sci-fi-language-creation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sci-fi language creation.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — global sci-fi and fantasy publishing",
      "sam": "$150M — conlang and worldbuilding software",
      "som": "$10M — speculative fiction language tools"
    },
    {
      "id": "writing-interview-quote-echo-21",
      "theme": "writing",
      "title": "Interview Quote Echo",
      "pitch": "Interview Quote Echo composes journalism fact-checking in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "journalism fact-checking",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits journalism fact-checking in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Interview Quote Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nInterview Quote Echo composes journalism fact-checking in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (journalism fact-checking).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits journalism fact-checking in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `journalism-fact-checking-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for journalism fact-checking (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for journalism fact-checking.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global journalism and newsroom software",
      "sam": "$400M — interview transcription and drafting tools",
      "som": "$30M — investigative journalism audio aids"
    },
    {
      "id": "writing-billboard-hook-shouter-22",
      "theme": "writing",
      "title": "Billboard Hook Shouter",
      "pitch": "Billboard Hook Shouter boots a long-running service inside a Sprite for advertising copywriting — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "advertising copywriting",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches advertising copywriting in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Billboard Hook Shouter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBillboard Hook Shouter boots a long-running service inside a Sprite for advertising copywriting — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (advertising copywriting).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches advertising copywriting in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `advertising-copywriting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for advertising copywriting.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits advertising copywriting — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for advertising copywriting.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global copywriting and ad tech",
      "sam": "$600M — creative agency drafting tools",
      "som": "$45M — audio and broadcast ad copy apps"
    },
    {
      "id": "writing-epoch-dialect-tutor-23",
      "theme": "writing",
      "title": "Epoch Dialect Tutor",
      "pitch": "Epoch Dialect Tutor runs a one-shot shell command inside a Sprite for historical fiction dialects and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "historical fiction dialects",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits historical fiction dialects in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Epoch Dialect Tutor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEpoch Dialect Tutor runs a one-shot shell command inside a Sprite for historical fiction dialects and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (historical fiction dialects).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits historical fiction dialects in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `historical-fiction-diale-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a historical fiction dialects-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for historical fiction dialects.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global historical fiction publishing",
      "sam": "$200M — historical research and writing tools",
      "som": "$15M — period-accurate dialogue drafting apps"
    },
    {
      "id": "writing-blank-page-soother-24",
      "theme": "writing",
      "title": "Blank Page Soother",
      "pitch": "Blank Page Soother spins up a fresh public Sprite for each session so writer mindset coaching gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "writer mindset coaching",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits writer mindset coaching in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Blank Page Soother\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlank Page Soother spins up a fresh public Sprite for each session so writer mindset coaching gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (writer mindset coaching).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits writer mindset coaching in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `writer-mindset-coaching-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for writer mindset coaching.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global mental wellness and productivity",
      "sam": "$300M — creative coaching and journaling tools",
      "som": "$20M — writer's block and mindfulness apps"
    },
    {
      "id": "writing-scene-sparring-partner-0",
      "theme": "writing",
      "title": "Scene Sparring Partner",
      "pitch": "Scene Sparring Partner composes screenplay rehearsal in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "screenplay rehearsal",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits screenplay rehearsal in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Scene Sparring Partner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nScene Sparring Partner composes screenplay rehearsal in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (screenplay rehearsal).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits screenplay rehearsal in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `screenplay-rehearsal-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for screenplay rehearsal (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for screenplay rehearsal.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and screenwriting software market",
      "sam": "$400M — professional screenwriter and playwright tools segment",
      "som": "$25M — AI dialogue rehearsal and acting prep applications"
    },
    {
      "id": "writing-meter-pacing-guide-1",
      "theme": "writing",
      "title": "Meter Pacing Guide",
      "pitch": "Meter Pacing Guide boots a long-running service inside a Sprite for poetry performance — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "poetry performance",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches poetry performance in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Meter Pacing Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMeter Pacing Guide boots a long-running service inside a Sprite for poetry performance — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (poetry performance).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches poetry performance in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `poetry-performance-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for poetry performance.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits poetry performance — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for poetry performance.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and publishing tools market",
      "sam": "$150M — poetry and written word creation software",
      "som": "$10M — AI-assisted poetic meter and performance coaching"
    },
    {
      "id": "writing-lore-interrogation-room-2",
      "theme": "writing",
      "title": "Lore Interrogation Room",
      "pitch": "Lore Interrogation Room runs a one-shot shell command inside a Sprite for fantasy worldbuilding and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "fantasy worldbuilding",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits fantasy worldbuilding in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Lore Interrogation Room\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLore Interrogation Room runs a one-shot shell command inside a Sprite for fantasy worldbuilding and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (fantasy worldbuilding).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits fantasy worldbuilding in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `fantasy-worldbuilding-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a fantasy worldbuilding-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for fantasy worldbuilding.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$600M — speculative fiction and fantasy authoring tools",
      "som": "$30M — AI worldbuilding and lore consistency checkers"
    },
    {
      "id": "writing-plot-hole-detective-3",
      "theme": "writing",
      "title": "Plot Hole Detective",
      "pitch": "Plot Hole Detective spins up a fresh public Sprite for each session so narrative structure gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "narrative structure",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits narrative structure in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Plot Hole Detective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPlot Hole Detective spins up a fresh public Sprite for each session so narrative structure gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (narrative structure).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits narrative structure in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `narrative-structure-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for narrative structure.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$800M — professional novelist and fiction editing software",
      "som": "$40M — AI structural editing and plot analysis tools"
    },
    {
      "id": "writing-character-voice-audition-4",
      "theme": "writing",
      "title": "Character Voice Audition",
      "pitch": "Character Voice Audition composes character development in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "character development",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits character development in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Character Voice Audition\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCharacter Voice Audition composes character development in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (character development).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits character development in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `character-development-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for character development (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for character development.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and character design market",
      "sam": "$500M — fiction writing and roleplay character creation",
      "som": "$20M — AI character voice and persona simulation tools"
    },
    {
      "id": "writing-block-breaking-therapist-5",
      "theme": "writing",
      "title": "Block Breaking Therapist",
      "pitch": "Block Breaking Therapist boots a long-running service inside a Sprite for creative mindset — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "creative mindset",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches creative mindset in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Block Breaking Therapist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBlock Breaking Therapist boots a long-running service inside a Sprite for creative mindset — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (creative mindset).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches creative mindset in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `creative-mindset-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for creative mindset.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits creative mindset — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for creative mindset.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and wellness market",
      "sam": "$300M — creative coaching and writer wellness applications",
      "som": "$15M — AI mindset and block-breaking companions for authors"
    },
    {
      "id": "writing-bedtime-story-weaver-6",
      "theme": "writing",
      "title": "Bedtime Story Weaver",
      "pitch": "Bedtime Story Weaver runs a one-shot shell command inside a Sprite for children's literature and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "children's literature",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits children's literature in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Bedtime Story Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBedtime Story Weaver runs a one-shot shell command inside a Sprite for children's literature and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (children's literature).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits children's literature in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `children-s-literature-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a children's literature-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for children's literature.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.0B — global children's book and audio story market",
      "sam": "$400M — interactive bedtime story and parenting apps",
      "som": "$25M — AI-generated real-time interactive children's audio tales"
    },
    {
      "id": "writing-dialect-dialogue-coach-7",
      "theme": "writing",
      "title": "Dialect Dialogue Coach",
      "pitch": "Dialect Dialogue Coach spins up a fresh public Sprite for each session so linguistic authenticity gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "linguistic authenticity",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits linguistic authenticity in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Dialect Dialogue Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDialect Dialogue Coach spins up a fresh public Sprite for each session so linguistic authenticity gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (linguistic authenticity).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits linguistic authenticity in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `linguistic-authenticity-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for linguistic authenticity.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and linguistics software market",
      "sam": "$200M — dialect, translation, and localization writing tools",
      "som": "$12M — AI dialect coaching for fiction and screenwriting"
    },
    {
      "id": "writing-audio-journaling-muse-8",
      "theme": "writing",
      "title": "Audio Journaling Muse",
      "pitch": "Audio Journaling Muse composes reflective journaling in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "reflective journaling",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits reflective journaling in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Audio Journaling Muse\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAudio Journaling Muse composes reflective journaling in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (reflective journaling).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits reflective journaling in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `reflective-journaling-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for reflective journaling (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for reflective journaling.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global journaling and personal development market",
      "sam": "$350M — digital journaling and audio diary applications",
      "som": "$18M — AI conversational journaling for creative professionals"
    },
    {
      "id": "writing-pitch-practice-simulator-9",
      "theme": "writing",
      "title": "Pitch Practice Simulator",
      "pitch": "Pitch Practice Simulator boots a long-running service inside a Sprite for author marketing — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "author marketing",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches author marketing in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Pitch Practice Simulator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPitch Practice Simulator boots a long-running service inside a Sprite for author marketing — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (author marketing).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches author marketing in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `author-marketing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for author marketing.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits author marketing — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for author marketing.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global author marketing and publishing tools market",
      "sam": "$250M — book pitching and author presentation software",
      "som": "$10M — AI pitch rehearsal for traditional publishing"
    },
    {
      "id": "writing-sensory-description-weaver-10",
      "theme": "writing",
      "title": "Sensory Description Weaver",
      "pitch": "Sensory Description Weaver runs a one-shot shell command inside a Sprite for descriptive prose and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "descriptive prose",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits descriptive prose in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Sensory Description Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSensory Description Weaver runs a one-shot shell command inside a Sprite for descriptive prose and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (descriptive prose).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits descriptive prose in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `descriptive-prose-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a descriptive prose-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for descriptive prose.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$700M — creative writing and prose enhancement software",
      "som": "$30M — AI sensory and descriptive prose generators"
    },
    {
      "id": "writing-tension-tuning-metronome-11",
      "theme": "writing",
      "title": "Tension Tuning Metronome",
      "pitch": "Tension Tuning Metronome spins up a fresh public Sprite for each session so thriller pacing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "thriller pacing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits thriller pacing in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Tension Tuning Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTension Tuning Metronome spins up a fresh public Sprite for each session so thriller pacing gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (thriller pacing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits thriller pacing in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `thriller-pacing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for thriller pacing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$400M — thriller, mystery, and suspense writing tools",
      "som": "$15M — AI pacing and tension analysis for novelists"
    },
    {
      "id": "writing-historical-fact-interrogator-12",
      "theme": "writing",
      "title": "Historical Fact Interrogator",
      "pitch": "Historical Fact Interrogator composes historical fiction in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "historical fiction",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits historical fiction in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Historical Fact Interrogator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHistorical Fact Interrogator composes historical fiction in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (historical fiction).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits historical fiction in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `historical-fiction-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for historical fiction (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for historical fiction.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and research software market",
      "sam": "$300M — historical fiction research and authoring tools",
      "som": "$12M — AI historical accuracy and anachronism checkers"
    },
    {
      "id": "writing-subtext-reading-coach-13",
      "theme": "writing",
      "title": "Subtext Reading Coach",
      "pitch": "Subtext Reading Coach boots a long-running service inside a Sprite for dramatic subtext — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "dramatic subtext",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches dramatic subtext in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Subtext Reading Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSubtext Reading Coach boots a long-running service inside a Sprite for dramatic subtext — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (dramatic subtext).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches dramatic subtext in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `dramatic-subtext-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for dramatic subtext.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits dramatic subtext — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for dramatic subtext.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global playwriting and screenwriting market",
      "sam": "$250M — dramatic writing and dialogue refinement tools",
      "som": "$10M — AI subtext and dramatic nuance coaching"
    },
    {
      "id": "writing-flash-fiction-sprinter-14",
      "theme": "writing",
      "title": "Flash Fiction Sprinter",
      "pitch": "Flash Fiction Sprinter runs a one-shot shell command inside a Sprite for micro-fiction and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "micro-fiction",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits micro-fiction in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Flash Fiction Sprinter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFlash Fiction Sprinter runs a one-shot shell command inside a Sprite for micro-fiction and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (micro-fiction).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits micro-fiction in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `micro-fiction-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a micro-fiction-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for micro-fiction.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$200M — short story and flash fiction writing apps",
      "som": "$8M — AI sprint hosting and prompt generation"
    },
    {
      "id": "writing-accessibility-beta-reader-15",
      "theme": "writing",
      "title": "Accessibility Beta Reader",
      "pitch": "Accessibility Beta Reader spins up a fresh public Sprite for each session so inclusive design gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "inclusive design",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits inclusive design in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Accessibility Beta Reader\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAccessibility Beta Reader spins up a fresh public Sprite for each session so inclusive design gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (inclusive design).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits inclusive design in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `inclusive-design-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for inclusive design.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global publishing and accessibility software market",
      "sam": "$150M — inclusive publishing and accessible ebook tools",
      "som": "$5M — AI screen-reader simulation for authors"
    },
    {
      "id": "writing-translation-tone-checker-16",
      "theme": "writing",
      "title": "Translation Tone Checker",
      "pitch": "Translation Tone Checker composes literary translation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "literary translation",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits literary translation in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Translation Tone Checker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTranslation Tone Checker composes literary translation in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (literary translation).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits literary translation in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `literary-translation-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for literary translation (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for literary translation.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.0B — global translation and localization software market",
      "sam": "$400M — literary translation and poetry localization tools",
      "som": "$15M — AI tonal and emotional translation checking"
    },
    {
      "id": "writing-monologue-memory-aid-17",
      "theme": "writing",
      "title": "Monologue Memory Aid",
      "pitch": "Monologue Memory Aid boots a long-running service inside a Sprite for actor preparation — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "actor preparation",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches actor preparation in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Monologue Memory Aid\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMonologue Memory Aid boots a long-running service inside a Sprite for actor preparation — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (actor preparation).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches actor preparation in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `actor-preparation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for actor preparation.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits actor preparation — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for actor preparation.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global acting and theater preparation market",
      "sam": "$100M — script memorization and actor rehearsal apps",
      "som": "$5M — AI conversational scene partner for actors"
    },
    {
      "id": "writing-epistolary-letter-companion-18",
      "theme": "writing",
      "title": "Epistolary Letter Companion",
      "pitch": "Epistolary Letter Companion runs a one-shot shell command inside a Sprite for historical correspondence and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "historical correspondence",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits historical correspondence in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Epistolary Letter Companion\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEpistolary Letter Companion runs a one-shot shell command inside a Sprite for historical correspondence and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (historical correspondence).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits historical correspondence in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `historical-correspondenc-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a historical correspondence-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for historical correspondence.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global game narrative and writing tools market",
      "sam": "$300M — interactive fiction and epistolary game design",
      "som": "$12M — AI letter writing and correspondence simulation"
    },
    {
      "id": "writing-comedy-timing-workshop-19",
      "theme": "writing",
      "title": "Comedy Timing Workshop",
      "pitch": "Comedy Timing Workshop spins up a fresh public Sprite for each session so humorous writing gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "humorous writing",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits humorous writing in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Comedy Timing Workshop\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nComedy Timing Workshop spins up a fresh public Sprite for each session so humorous writing gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (humorous writing).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits humorous writing in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `humorous-writing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for humorous writing.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global comedy writing and performance market",
      "sam": "$200M — standup comedy and sitcom writing tools",
      "som": "$8M — AI comedic timing and punchline testing"
    },
    {
      "id": "writing-narrative-branch-tester-20",
      "theme": "writing",
      "title": "Narrative Branch Tester",
      "pitch": "Narrative Branch Tester composes interactive fiction in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "interactive fiction",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits interactive fiction in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Narrative Branch Tester\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nNarrative Branch Tester composes interactive fiction in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (interactive fiction).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits interactive fiction in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `interactive-fiction-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for interactive fiction (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for interactive fiction.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.0B — global game narrative and interactive fiction market",
      "sam": "$800M — RPG and branching dialogue writing tools",
      "som": "$40M — AI NPC dialogue tree simulation and testing"
    },
    {
      "id": "writing-lyric-rhyme-sparring-21",
      "theme": "writing",
      "title": "Lyric Rhyme Sparring",
      "pitch": "Lyric Rhyme Sparring boots a long-running service inside a Sprite for songwriting — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "songwriting",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches songwriting in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Lyric Rhyme Sparring\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLyric Rhyme Sparring boots a long-running service inside a Sprite for songwriting — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (songwriting).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches songwriting in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `songwriting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for songwriting.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits songwriting — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for songwriting.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.0B — global songwriting and music creation market",
      "sam": "$500M — lyric writing and vocal melody tools",
      "som": "$20M — AI lyrical rhyming and rhythmic sparring"
    },
    {
      "id": "writing-public-reading-rehearsal-22",
      "theme": "writing",
      "title": "Public Reading Rehearsal",
      "pitch": "Public Reading Rehearsal runs a one-shot shell command inside a Sprite for author performance and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "author performance",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits author performance in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Public Reading Rehearsal\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPublic Reading Rehearsal runs a one-shot shell command inside a Sprite for author performance and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (author performance).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits author performance in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `author-performance-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a author performance-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for author performance.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global author marketing and event prep market",
      "sam": "$150M — book tour preparation and public speaking tools",
      "som": "$6M — AI audience simulation for author readings"
    },
    {
      "id": "writing-metaphor-mining-assistant-23",
      "theme": "writing",
      "title": "Metaphor Mining Assistant",
      "pitch": "Metaphor Mining Assistant spins up a fresh public Sprite for each session so poetic imagery gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "poetic imagery",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits poetic imagery in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Metaphor Mining Assistant\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMetaphor Mining Assistant spins up a fresh public Sprite for each session so poetic imagery gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (poetic imagery).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits poetic imagery in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `poetic-imagery-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for poetic imagery.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and poetry tools market",
      "sam": "$250M — creative prose and poetic imagery software",
      "som": "$10M — AI figurative language and metaphor generation"
    },
    {
      "id": "writing-sci-fi-jargon-builder-24",
      "theme": "writing",
      "title": "Sci-Fi Jargon Builder",
      "pitch": "Sci-Fi Jargon Builder composes speculative terminology in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "speculative terminology",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits speculative terminology in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Sci-Fi Jargon Builder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSci-Fi Jargon Builder composes speculative terminology in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (speculative terminology).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits speculative terminology in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `speculative-terminology-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for speculative terminology (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for speculative terminology.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and worldbuilding software market",
      "sam": "$400M — science fiction and speculative writing tools",
      "som": "$15M — AI speculative jargon and terminology generation"
    },
    {
      "id": "writing-dialogue-pacing-doctor-0",
      "theme": "writing",
      "title": "Dialogue Pacing Doctor",
      "pitch": "Dialogue Pacing Doctor boots a long-running service inside a Sprite for screenplay dialogue flow — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "screenplay dialogue flow",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches screenplay dialogue flow in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Dialogue Pacing Doctor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDialogue Pacing Doctor boots a long-running service inside a Sprite for screenplay dialogue flow — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (screenplay dialogue flow).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches screenplay dialogue flow in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `screenplay-dialogue-flow-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for screenplay dialogue flow.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits screenplay dialogue flow — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for screenplay dialogue flow.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$400M — screenwriting and playwriting software",
      "som": "$15M — indie screenplay pacing tools"
    },
    {
      "id": "writing-dream-weaver-scribe-1",
      "theme": "writing",
      "title": "Dream Weaver Live Notes",
      "pitch": "Dream Weaver Live Notes runs a one-shot shell command inside a Sprite for surreal narrative drafting and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "surreal narrative drafting",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits surreal narrative drafting in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Dream Weaver Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDream Weaver Live Notes runs a one-shot shell command inside a Sprite for surreal narrative drafting and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (surreal narrative drafting).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits surreal narrative drafting in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `surreal-narrative-drafti-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a surreal narrative drafting-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for surreal narrative drafting.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$300M — fiction and novel drafting software",
      "som": "$10M — surrealism and dream journaling apps"
    },
    {
      "id": "writing-lore-walker-catcher-2",
      "theme": "writing",
      "title": "Lore Walker Catcher",
      "pitch": "Lore Walker Catcher spins up a fresh public Sprite for each session so mobile worldbuilding gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "mobile worldbuilding",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits mobile worldbuilding in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Lore Walker Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLore Walker Catcher spins up a fresh public Sprite for each session so mobile worldbuilding gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (mobile worldbuilding).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits mobile worldbuilding in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `mobile-worldbuilding-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for mobile worldbuilding.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — fantasy and sci-fi authoring tools",
      "som": "$20M — mobile worldbuilding companions"
    },
    {
      "id": "writing-slam-breath-coach-3",
      "theme": "writing",
      "title": "Slam Breath Coach",
      "pitch": "Slam Breath Coach composes written word rehearsal in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "written word rehearsal",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits written word rehearsal in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Slam Breath Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSlam Breath Coach composes written word rehearsal in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (written word rehearsal).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits written word rehearsal in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `written-word-rehearsal-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for written word rehearsal (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for written word rehearsal.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$150M — poetry and written word platforms",
      "som": "$5M — performance rehearsal aids"
    },
    {
      "id": "writing-protagonist-interrogation-room-4",
      "theme": "writing",
      "title": "Protagonist Interrogation Room",
      "pitch": "Protagonist Interrogation Room boots a long-running service inside a Sprite for character psychology profiling — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "character psychology profiling",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches character psychology profiling in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Protagonist Interrogation Room\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nProtagonist Interrogation Room boots a long-running service inside a Sprite for character psychology profiling — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (character psychology profiling).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches character psychology profiling in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `character-psychology-pro-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for character psychology profiling.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits character psychology profiling — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for character psychology profiling.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$600M — character development and plotting software",
      "som": "$25M — psychological profiling tools for authors"
    },
    {
      "id": "writing-commute-beat-mapper-5",
      "theme": "writing",
      "title": "Commute Beat Mapper",
      "pitch": "Commute Beat Mapper runs a one-shot shell command inside a Sprite for structural outlining and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "structural outlining",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits structural outlining in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Commute Beat Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCommute Beat Mapper runs a one-shot shell command inside a Sprite for structural outlining and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (structural outlining).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits structural outlining in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `structural-outlining-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a structural outlining-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for structural outlining.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$450M — story outlining and structure software",
      "som": "$18M — hands-free mobile outlining apps"
    },
    {
      "id": "writing-freestyle-rhyme-net-6",
      "theme": "writing",
      "title": "Freestyle Rhyme Net",
      "pitch": "Freestyle Rhyme Net spins up a fresh public Sprite for each session so lyricist improvisation gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "lyricist improvisation",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits lyricist improvisation in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Freestyle Rhyme Net\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFreestyle Rhyme Net spins up a fresh public Sprite for each session so lyricist improvisation gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (lyricist improvisation).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits lyricist improvisation in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `lyricist-improvisation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for lyricist improvisation.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$250M — songwriting and lyricist software",
      "som": "$12M — hip-hop freestyle training tools"
    },
    {
      "id": "writing-scene-tension-grapher-7",
      "theme": "writing",
      "title": "Scene Tension Grapher",
      "pitch": "Scene Tension Grapher composes dramatic arc analysis in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "dramatic arc analysis",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits dramatic arc analysis in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Scene Tension Grapher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nScene Tension Grapher composes dramatic arc analysis in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (dramatic arc analysis).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits dramatic arc analysis in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `dramatic-arc-analysis-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for dramatic arc analysis (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for dramatic arc analysis.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$350M — theater and playwriting software",
      "som": "$14M — dramatic pacing analytics"
    },
    {
      "id": "writing-live-verse-projector-8",
      "theme": "writing",
      "title": "Live Verse Projector",
      "pitch": "Live Verse Projector boots a long-running service inside a Sprite for accessible performance captioning — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "accessible performance captioning",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches accessible performance captioning in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Live Verse Projector\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLive Verse Projector boots a long-running service inside a Sprite for accessible performance captioning — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (accessible performance captioning).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches accessible performance captioning in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `accessible-performance-c-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for accessible performance captioning.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits accessible performance captioning — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for accessible performance captioning.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$100M — live event accessibility software",
      "som": "$8M — poetry slam captioning tools"
    },
    {
      "id": "writing-bedtime-story-spinner-9",
      "theme": "writing",
      "title": "Bedtime Story Spinner",
      "pitch": "Bedtime Story Spinner runs a one-shot shell command inside a Sprite for interactive parenting narrative and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "interactive parenting narrative",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits interactive parenting narrative in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Bedtime Story Spinner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nBedtime Story Spinner runs a one-shot shell command inside a Sprite for interactive parenting narrative and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (interactive parenting narrative).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits interactive parenting narrative in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `interactive-parenting-na-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a interactive parenting narrative-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for interactive parenting narrative.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$200M — family and parenting creative apps",
      "som": "$10M — interactive bedtime story generators"
    },
    {
      "id": "writing-pacing-thesis-forger-10",
      "theme": "writing",
      "title": "Pacing Thesis Forger",
      "pitch": "Pacing Thesis Forger spins up a fresh public Sprite for each session so academic drafting gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "academic drafting",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits academic drafting in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Pacing Thesis Forger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPacing Thesis Forger spins up a fresh public Sprite for each session so academic drafting gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (academic drafting).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits academic drafting in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `academic-drafting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for academic drafting.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$800M — academic and research writing software",
      "som": "$30M — verbal thesis drafting tools"
    },
    {
      "id": "writing-dialect-authenticity-checker-11",
      "theme": "writing",
      "title": "Dialect Authenticity Checker",
      "pitch": "Dialect Authenticity Checker composes linguistic worldbuilding in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "linguistic worldbuilding",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits linguistic worldbuilding in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Dialect Authenticity Checker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDialect Authenticity Checker composes linguistic worldbuilding in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (linguistic worldbuilding).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits linguistic worldbuilding in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `linguistic-worldbuilding-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for linguistic worldbuilding (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for linguistic worldbuilding.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — historical and regional fiction tools",
      "som": "$22M — dialect and linguistic accuracy checkers"
    },
    {
      "id": "writing-standup-pause-analyzer-12",
      "theme": "writing",
      "title": "Standup Pause Analyzer",
      "pitch": "Standup Pause Analyzer boots a long-running service inside a Sprite for comedy set rehearsal — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "comedy set rehearsal",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches comedy set rehearsal in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Standup Pause Analyzer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStandup Pause Analyzer boots a long-running service inside a Sprite for comedy set rehearsal — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (comedy set rehearsal).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches comedy set rehearsal in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `comedy-set-rehearsal-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for comedy set rehearsal.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits comedy set rehearsal — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for comedy set rehearsal.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$150M — comedy writing and rehearsal software",
      "som": "$7M — standup timing analytics"
    },
    {
      "id": "writing-memoir-reframing-scribe-13",
      "theme": "writing",
      "title": "Memoir Reframing Live Notes",
      "pitch": "Memoir Reframing Live Notes runs a one-shot shell command inside a Sprite for therapeutic life writing and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "therapeutic life writing",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits therapeutic life writing in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Memoir Reframing Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nMemoir Reframing Live Notes runs a one-shot shell command inside a Sprite for therapeutic life writing and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (therapeutic life writing).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits therapeutic life writing in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `therapeutic-life-writing-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a therapeutic life writing-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for therapeutic life writing.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$300M — journaling and memoir software",
      "som": "$15M — therapeutic narrative processing tools"
    },
    {
      "id": "writing-dungeon-master-dictator-14",
      "theme": "writing",
      "title": "Dungeon Master Dictator",
      "pitch": "Dungeon Master Dictator spins up a fresh public Sprite for each session so tabletop RPG prep gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "tabletop RPG prep",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits tabletop RPG prep in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Dungeon Master Dictator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDungeon Master Dictator spins up a fresh public Sprite for each session so tabletop RPG prep gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (tabletop RPG prep).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits tabletop RPG prep in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `tabletop-rpg-prep-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for tabletop RPG prep.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$400M — tabletop RPG and game master tools",
      "som": "$18M — hands-free campaign prep software"
    },
    {
      "id": "writing-fluent-fable-weaver-15",
      "theme": "writing",
      "title": "Fluent Fable Weaver",
      "pitch": "Fluent Fable Weaver composes language acquisition narrative in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "language acquisition narrative",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits language acquisition narrative in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Fluent Fable Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFluent Fable Weaver composes language acquisition narrative in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (language acquisition narrative).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits language acquisition narrative in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `language-acquisition-nar-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for language acquisition narrative (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for language acquisition narrative.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$600M — language learning and immersion software",
      "som": "$25M — narrative-based language acquisition tools"
    },
    {
      "id": "writing-eulogy-memory-loom-16",
      "theme": "writing",
      "title": "Eulogy Memory Loom",
      "pitch": "Eulogy Memory Loom boots a long-running service inside a Sprite for commemorative writing — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "commemorative writing",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches commemorative writing in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Eulogy Memory Loom\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nEulogy Memory Loom boots a long-running service inside a Sprite for commemorative writing — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (commemorative writing).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches commemorative writing in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `commemorative-writing-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for commemorative writing.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits commemorative writing — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for commemorative writing.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$100M — personal and commemorative writing software",
      "som": "$5M — grief processing and eulogy drafting aids"
    },
    {
      "id": "writing-query-pitch-polisher-17",
      "theme": "writing",
      "title": "Query Pitch Polisher",
      "pitch": "Query Pitch Polisher runs a one-shot shell command inside a Sprite for publishing industry prep and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "publishing industry prep",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits publishing industry prep in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Query Pitch Polisher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nQuery Pitch Polisher runs a one-shot shell command inside a Sprite for publishing industry prep and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (publishing industry prep).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits publishing industry prep in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `publishing-industry-prep-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a publishing industry prep-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for publishing industry prep.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$350M — publishing and query letter software",
      "som": "$12M — author pitch rehearsal tools"
    },
    {
      "id": "writing-sensory-metaphor-injector-18",
      "theme": "writing",
      "title": "Sensory Metaphor Injector",
      "pitch": "Sensory Metaphor Injector spins up a fresh public Sprite for each session so descriptive prose enhancement gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "descriptive prose enhancement",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits descriptive prose enhancement in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Sensory Metaphor Injector\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSensory Metaphor Injector spins up a fresh public Sprite for each session so descriptive prose enhancement gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (descriptive prose enhancement).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits descriptive prose enhancement in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `descriptive-prose-enhanc-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for descriptive prose enhancement.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — prose enhancement and editing software",
      "som": "$20M — sensory description generators"
    },
    {
      "id": "writing-courtroom-jargon-referee-19",
      "theme": "writing",
      "title": "Courtroom Jargon Referee",
      "pitch": "Courtroom Jargon Referee composes procedural accuracy checking in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "procedural accuracy checking",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits procedural accuracy checking in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Courtroom Jargon Referee\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCourtroom Jargon Referee composes procedural accuracy checking in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (procedural accuracy checking).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits procedural accuracy checking in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `procedural-accuracy-chec-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for procedural accuracy checking (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for procedural accuracy checking.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$400M — thriller and procedural fiction tools",
      "som": "$16M — legal jargon accuracy checkers"
    },
    {
      "id": "writing-nature-haiku-catcher-20",
      "theme": "writing",
      "title": "Nature Haiku Catcher",
      "pitch": "Nature Haiku Catcher boots a long-running service inside a Sprite for minimalist poetry drafting — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "minimalist poetry drafting",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches minimalist poetry drafting in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Nature Haiku Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nNature Haiku Catcher boots a long-running service inside a Sprite for minimalist poetry drafting — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (minimalist poetry drafting).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches minimalist poetry drafting in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `minimalist-poetry-drafti-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for minimalist poetry drafting.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits minimalist poetry drafting — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for minimalist poetry drafting.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$150M — poetry drafting and formatting software",
      "som": "$6M — minimalist and nature poetry aids"
    },
    {
      "id": "writing-trope-subversion-outliner-21",
      "theme": "writing",
      "title": "Trope Subversion Outliner",
      "pitch": "Trope Subversion Outliner runs a one-shot shell command inside a Sprite for fanfiction plotting and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "fanfiction plotting",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits fanfiction plotting in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Trope Subversion Outliner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTrope Subversion Outliner runs a one-shot shell command inside a Sprite for fanfiction plotting and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (fanfiction plotting).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits fanfiction plotting in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `fanfiction-plotting-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a fanfiction plotting-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for fanfiction plotting.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$250M — fanfiction and community writing platforms",
      "som": "$10M — trope analysis and plotting tools"
    },
    {
      "id": "writing-audio-drama-formatter-22",
      "theme": "writing",
      "title": "Audio Drama Formatter",
      "pitch": "Audio Drama Formatter spins up a fresh public Sprite for each session so podcast script formatting gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "podcast script formatting",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits podcast script formatting in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Audio Drama Formatter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nAudio Drama Formatter spins up a fresh public Sprite for each session so podcast script formatting gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (podcast script formatting).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits podcast script formatting in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `podcast-script-formattin-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for podcast script formatting.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$300M — podcasting and audio drama software",
      "som": "$14M — audio script formatting tools"
    },
    {
      "id": "writing-culinary-story-extractor-23",
      "theme": "writing",
      "title": "Culinary Story Extractor",
      "pitch": "Culinary Story Extractor composes food blogging narrative in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "food blogging narrative",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits food blogging narrative in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Culinary Story Extractor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCulinary Story Extractor composes food blogging narrative in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (food blogging narrative).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits food blogging narrative in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `food-blogging-narrative-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for food blogging narrative (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for food blogging narrative.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$200M — food blogging and recipe software",
      "som": "$9M — culinary narrative extraction tools"
    },
    {
      "id": "writing-technobabble-consistency-engine-24",
      "theme": "writing",
      "title": "Technobabble Consistency Engine",
      "pitch": "Technobabble Consistency Engine boots a long-running service inside a Sprite for sci-fi prose drafting — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "sci-fi prose drafting",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches sci-fi prose drafting in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Technobabble Consistency Engine\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTechnobabble Consistency Engine boots a long-running service inside a Sprite for sci-fi prose drafting — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (sci-fi prose drafting).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches sci-fi prose drafting in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `sci-fi-prose-drafting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for sci-fi prose drafting.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits sci-fi prose drafting — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sci-fi prose drafting.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — sci-fi and fantasy authoring tools",
      "som": "$22M — fictional terminology consistency checkers"
    },
    {
      "id": "writing-script-foley-forge-0",
      "theme": "writing",
      "title": "Script Foley Forge",
      "pitch": "Script Foley Forge runs a one-shot shell command inside a Sprite for screenplay foley and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "screenplay foley",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits screenplay foley in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Script Foley Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nScript Foley Forge runs a one-shot shell command inside a Sprite for screenplay foley and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (screenplay foley).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits screenplay foley in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `screenplay-foley-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a screenplay foley-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for screenplay foley.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global audio post-production software",
      "sam": "$500M — indie film and theater sound design tools",
      "som": "$20M — freelance Foley artist workflow apps"
    },
    {
      "id": "writing-stanza-vibe-scorer-1",
      "theme": "writing",
      "title": "Stanza Vibe Scorer",
      "pitch": "Stanza Vibe Scorer spins up a fresh public Sprite for each session so poetry performance gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "poetry performance",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits poetry performance in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Stanza Vibe Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStanza Vibe Scorer spins up a fresh public Sprite for each session so poetry performance gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (poetry performance).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits poetry performance in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `poetry-performance-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for poetry performance.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global live performance and event software",
      "sam": "$150M — written word and poetry event platforms",
      "som": "$5M — indie poet performance and touring tools"
    },
    {
      "id": "writing-dread-pacing-engine-2",
      "theme": "writing",
      "title": "Dread Pacing Engine",
      "pitch": "Dread Pacing Engine composes horror drafting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "horror drafting",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits horror drafting in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Dread Pacing Engine\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nDread Pacing Engine composes horror drafting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (horror drafting).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits horror drafting in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `horror-drafting-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for horror drafting (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for horror drafting.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$300M — genre fiction drafting and plotting tools",
      "som": "$15M — horror and thriller author productivity apps"
    },
    {
      "id": "writing-xenotech-audio-blueprint-3",
      "theme": "writing",
      "title": "Xenotech Audio Blueprint",
      "pitch": "Xenotech Audio Blueprint boots a long-running service inside a Sprite for sci-fi worldbuilding — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "sci-fi worldbuilding",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches sci-fi worldbuilding in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Xenotech Audio Blueprint\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nXenotech Audio Blueprint boots a long-running service inside a Sprite for sci-fi worldbuilding — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (sci-fi worldbuilding).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches sci-fi worldbuilding in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `sci-fi-worldbuilding-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for sci-fi worldbuilding.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits sci-fi worldbuilding — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sci-fi worldbuilding.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development and engine software",
      "sam": "$1.2B — narrative design and worldbuilding tools",
      "som": "$40M — indie sci-fi game audio asset pipelines"
    },
    {
      "id": "writing-tavern-tale-weaver-4",
      "theme": "writing",
      "title": "Tavern Tale Weaver",
      "pitch": "Tavern Tale Weaver runs a one-shot shell command inside a Sprite for tabletop RPG prep and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "tabletop RPG prep",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits tabletop RPG prep in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Tavern Tale Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTavern Tale Weaver runs a one-shot shell command inside a Sprite for tabletop RPG prep and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (tabletop RPG prep).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits tabletop RPG prep in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `tabletop-rpg-prep-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a tabletop RPG prep-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for tabletop RPG prep.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global tabletop gaming and accessories market",
      "sam": "$400M — digital GM tools and virtual tabletop software",
      "som": "$25M — TTRPG audio and ambiance companion apps"
    },
    {
      "id": "writing-climax-swell-composer-5",
      "theme": "writing",
      "title": "Climax Swell Composer",
      "pitch": "Climax Swell Composer spins up a fresh public Sprite for each session so romance audiobooks gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "romance audiobooks",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits romance audiobooks in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Climax Swell Composer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nClimax Swell Composer spins up a fresh public Sprite for each session so romance audiobooks gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (romance audiobooks).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits romance audiobooks in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `romance-audiobooks-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for romance audiobooks.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global audiobook production and distribution",
      "sam": "$800M — audiobook narrator software and coaching tools",
      "som": "$30M — romance genre audiobook production suites"
    },
    {
      "id": "writing-pursuit-momentum-tracker-6",
      "theme": "writing",
      "title": "Pursuit Momentum Tracker",
      "pitch": "Pursuit Momentum Tracker composes thriller writing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "thriller writing",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits thriller writing in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Pursuit Momentum Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPursuit Momentum Tracker composes thriller writing in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (thriller writing).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits thriller writing in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `thriller-writing-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for thriller writing (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for thriller writing.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$200M — distraction-free writing and focus apps",
      "som": "$10M — thriller and action novelist workflow tools"
    },
    {
      "id": "writing-syllable-nature-sonifier-7",
      "theme": "writing",
      "title": "Syllable Nature Sonifier",
      "pitch": "Syllable Nature Sonifier boots a long-running service inside a Sprite for haiku meditation — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "haiku meditation",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches haiku meditation in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Syllable Nature Sonifier\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSyllable Nature Sonifier boots a long-running service inside a Sprite for haiku meditation — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (haiku meditation).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches haiku meditation in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `haiku-meditation-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for haiku meditation.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits haiku meditation — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for haiku meditation.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global meditation and wellness app market",
      "sam": "$500M — mindfulness audio and soundscape libraries",
      "som": "$15M — poetry-integrated meditation and breathwork tools"
    },
    {
      "id": "writing-neon-dystopia-drone-8",
      "theme": "writing",
      "title": "Neon Dystopia Drone",
      "pitch": "Neon Dystopia Drone runs a one-shot shell command inside a Sprite for cyberpunk focus and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "cyberpunk focus",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits cyberpunk focus in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Neon Dystopia Drone\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nNeon Dystopia Drone runs a one-shot shell command inside a Sprite for cyberpunk focus and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (cyberpunk focus).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits cyberpunk focus in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `cyberpunk-focus-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a cyberpunk focus-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for cyberpunk focus.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global productivity and focus software",
      "sam": "$300M — ambient noise and focus music generators",
      "som": "$12M — speculative fiction author deep-work environments"
    },
    {
      "id": "writing-fable-magic-spark-9",
      "theme": "writing",
      "title": "Fable Magic Spark",
      "pitch": "Fable Magic Spark spins up a fresh public Sprite for each session so children's interactive books gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "children's interactive books",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits children's interactive books in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Fable Magic Spark\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nFable Magic Spark spins up a fresh public Sprite for each session so children's interactive books gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (children's interactive books).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits children's interactive books in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `children-s-interactive-b-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for children's interactive books.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global children's book and edutainment market",
      "sam": "$1.5B — interactive storytelling and read-along apps",
      "som": "$50M — parent-led interactive bedtime story tools"
    },
    {
      "id": "writing-skirmish-clash-drafter-10",
      "theme": "writing",
      "title": "Skirmish Clash Drafter",
      "pitch": "Skirmish Clash Drafter composes historical fiction in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "historical fiction",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits historical fiction in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Skirmish Clash Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSkirmish Clash Drafter composes historical fiction in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (historical fiction).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits historical fiction in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `historical-fiction-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for historical fiction (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for historical fiction.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$250M — historical research and reference tools for writers",
      "som": "$8M — military and historical fiction drafting aids"
    },
    {
      "id": "writing-twist-stinger-cue-11",
      "theme": "writing",
      "title": "Twist Stinger Cue",
      "pitch": "Twist Stinger Cue boots a long-running service inside a Sprite for mystery podcasting — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "mystery podcasting",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches mystery podcasting in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Twist Stinger Cue\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nTwist Stinger Cue boots a long-running service inside a Sprite for mystery podcasting — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (mystery podcasting).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches mystery podcasting in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `mystery-podcasting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for mystery podcasting.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits mystery podcasting — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for mystery podcasting.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global podcasting hosting and production software",
      "sam": "$600M — narrative and fiction podcast production tools",
      "som": "$25M — mystery and true-crime podcast audio cue libraries"
    },
    {
      "id": "writing-nostalgia-memoir-loop-12",
      "theme": "writing",
      "title": "Nostalgia Memoir Loop",
      "pitch": "Nostalgia Memoir Loop runs a one-shot shell command inside a Sprite for journalistic journaling and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "journalistic journaling",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits journalistic journaling in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Nostalgia Memoir Loop\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nNostalgia Memoir Loop runs a one-shot shell command inside a Sprite for journalistic journaling and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (journalistic journaling).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits journalistic journaling in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `journalistic-journaling-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a journalistic journaling-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for journalistic journaling.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global digital journaling and diary apps",
      "sam": "$400M — guided journaling and memoir writing platforms",
      "som": "$15M — audio-enhanced reflective journaling tools"
    },
    {
      "id": "writing-impact-beat-choreographer-13",
      "theme": "writing",
      "title": "Impact Beat Choreographer",
      "pitch": "Impact Beat Choreographer spins up a fresh public Sprite for each session so action prose gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "action prose",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits action prose in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Impact Beat Choreographer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nImpact Beat Choreographer spins up a fresh public Sprite for each session so action prose gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (action prose).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits action prose in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `action-prose-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for action prose.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$150M — plot outlining and scene blocking tools",
      "som": "$5M — action and martial arts fiction drafting aids"
    },
    {
      "id": "writing-manor-creak-mapper-14",
      "theme": "writing",
      "title": "Manor Creak Mapper",
      "pitch": "Manor Creak Mapper composes gothic spatial audio in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "gothic spatial audio",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits gothic spatial audio in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Manor Creak Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nManor Creak Mapper composes gothic spatial audio in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (gothic spatial audio).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits gothic spatial audio in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `gothic-spatial-audio-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for gothic spatial audio (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for gothic spatial audio.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global spatial audio and VR sound design",
      "sam": "$800M — interactive horror and escape room audio tools",
      "som": "$30M — gothic and haunted house spatial audio drafting"
    },
    {
      "id": "writing-hero-leitmotif-caster-15",
      "theme": "writing",
      "title": "Hero Leitmotif Caster",
      "pitch": "Hero Leitmotif Caster boots a long-running service inside a Sprite for epic fantasy — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "epic fantasy",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches epic fantasy in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Hero Leitmotif Caster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nHero Leitmotif Caster boots a long-running service inside a Sprite for epic fantasy — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (epic fantasy).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches epic fantasy in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `epic-fantasy-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for epic fantasy.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits epic fantasy — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for epic fantasy.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$350M — character development and worldbuilding tools",
      "som": "$12M — epic fantasy author inspiration and playlist apps"
    },
    {
      "id": "writing-punchline-timing-rig-16",
      "theme": "writing",
      "title": "Punchline Timing Rig",
      "pitch": "Punchline Timing Rig runs a one-shot shell command inside a Sprite for sitcom rehearsal and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "sitcom rehearsal",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits sitcom rehearsal in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Punchline Timing Rig\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nPunchline Timing Rig runs a one-shot shell command inside a Sprite for sitcom rehearsal and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (sitcom rehearsal).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits sitcom rehearsal in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `sitcom-rehearsal-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a sitcom rehearsal-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sitcom rehearsal.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — global screenwriting and production software",
      "sam": "$900M — script rehearsal and table read platforms",
      "som": "$40M — sitcom and sketch comedy timing aids"
    },
    {
      "id": "writing-surreal-morph-soundscape-17",
      "theme": "writing",
      "title": "Surreal Morph Soundscape",
      "pitch": "Surreal Morph Soundscape spins up a fresh public Sprite for each session so avant-garde poetry gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "avant-garde poetry",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits avant-garde poetry in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Surreal Morph Soundscape\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSurreal Morph Soundscape spins up a fresh public Sprite for each session so avant-garde poetry gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (avant-garde poetry).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits avant-garde poetry in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `avant-garde-poetry-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for avant-garde poetry.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global live performance and event software",
      "sam": "$100M — experimental theater and fringe festival tools",
      "som": "$4M — avant-garde poetry and sound art performance apps"
    },
    {
      "id": "writing-noir-rain-focus-18",
      "theme": "writing",
      "title": "Noir Rain Focus",
      "pitch": "Noir Rain Focus composes detective drafting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "detective drafting",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits detective drafting in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Noir Rain Focus\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nNoir Rain Focus composes detective drafting in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (detective drafting).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits detective drafting in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `detective-drafting-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for detective drafting (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for detective drafting.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global productivity and focus software",
      "sam": "$250M — genre-specific writing environment generators",
      "som": "$10M — noir and hardboiled detective fiction drafting tools"
    },
    {
      "id": "writing-starship-hum-isolator-19",
      "theme": "writing",
      "title": "Starship Hum Isolator",
      "pitch": "Starship Hum Isolator boots a long-running service inside a Sprite for sci-fi deep work — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "sci-fi deep work",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches sci-fi deep work in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Starship Hum Isolator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nStarship Hum Isolator boots a long-running service inside a Sprite for sci-fi deep work — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (sci-fi deep work).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches sci-fi deep work in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `sci-fi-deep-work-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for sci-fi deep work.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits sci-fi deep work — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for sci-fi deep work.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global productivity and focus software",
      "sam": "$300M — ambient noise and white noise generators",
      "som": "$15M — sci-fi author isolation and deep-work soundscapes"
    },
    {
      "id": "writing-slam-rhythm-backing-20",
      "theme": "writing",
      "title": "Slam Rhythm Backing",
      "pitch": "Slam Rhythm Backing runs a one-shot shell command inside a Sprite for written word and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "written word",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits written word in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Slam Rhythm Backing\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nSlam Rhythm Backing runs a one-shot shell command inside a Sprite for written word and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (written word).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits written word in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `written-word-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a written word-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for written word.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global live performance and event software",
      "sam": "$200M — poetry slam and open mic event platforms",
      "som": "$8M — written word artist rehearsal and backing track tools"
    },
    {
      "id": "writing-grief-cello-calibrator-21",
      "theme": "writing",
      "title": "Grief Cello Calibrator",
      "pitch": "Grief Cello Calibrator spins up a fresh public Sprite for each session so tragedy playwriting gets its own shareable sandbox URL in seconds.",
      "subDiscipline": "tragedy playwriting",
      "quantumHook": "Sprite Sandbox",
      "quantumHookId": "sprite-create",
      "quantumTag": "public micro-VM",
      "quantumRationale": "Sprite Sandbox fits tragedy playwriting in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.",
      "megaPrompt": "Build \"Grief Cello Calibrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nGrief Cello Calibrator spins up a fresh public Sprite for each session so tragedy playwriting gets its own shareable sandbox URL in seconds.\nDiscipline: Writing, Poetry & Narrative (tragedy playwriting).\nRecipe: Sprite Sandbox (public micro-VM) as the single Sprites primitive.\nWhy Sprites: Sprite Sandbox fits tragedy playwriting in Writing, Poetry & Narrative because every session wants its own disposable, shareable canvas — spin one up on demand, hand over the URL, tear it down when done.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a public Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const launch = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    // sprite names must be lowercase, hyphens, letters, digits.\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").replace(/^-+|-+$/g, \"\").slice(0, 48) || \"sprite\";\n    const name = `tragedy-playwriting-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create (idempotent: 409 on repeat is fine).\n    const r = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!r.ok && r.status !== 409) throw new Error(`Sprite create failed: ${r.status} ${await r.text()}`);\n\n    // The response body carries the public URL. Fallback to the conventional pattern if absent.\n    const j = (r.ok ? await r.json() : null) as { url?: string; public_url?: string } | null;\n    const url = j?.public_url ?? j?.url ?? `https://${name}.sprites.run`;\n    return { name, url };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport { launch } from \"@/lib/sprites.functions\";\n\nconst run = useServerFn(launch);\nconst [label, setLabel] = useState(\"\");\nconst [sprite, setSprite] = useState<{ name: string; url: string } | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onLaunch = async () => {\n  setBusy(true);\n  try { setSprite(await run({ data: { label } })); }\n  finally { setBusy(false); }\n};\n```\n\nShow the returned URL as a clickable share link; render an `<iframe>` preview if\nthe sprite renders a page. Copy-to-clipboard on click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for tragedy playwriting.\n2. The primary action (a launch button that spins up a disposable, shareable sandbox on demand) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global screenwriting and playwriting software",
      "sam": "$200M — dramatic structure and emotional arc tools",
      "som": "$7M — tragedy and drama playwright emotional calibration aids"
    },
    {
      "id": "writing-clockwork-grind-inventor-22",
      "theme": "writing",
      "title": "Clockwork Grind Inventor",
      "pitch": "Clockwork Grind Inventor composes steampunk audiobooks in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.",
      "subDiscipline": "steampunk audiobooks",
      "quantumHook": "Filesystem Drop",
      "quantumHookId": "sprite-fs",
      "quantumTag": "instant asset push",
      "quantumRationale": "Filesystem Drop suits steampunk audiobooks in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.",
      "megaPrompt": "Build \"Clockwork Grind Inventor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nClockwork Grind Inventor composes steampunk audiobooks in the browser, then writes the finished HTML and assets straight into a live Sprite so the user opens a real URL.\nDiscipline: Writing, Poetry & Narrative (steampunk audiobooks).\nRecipe: Filesystem Drop (instant asset push) as the single Sprites primitive.\nWhy Sprites: Filesystem Drop suits steampunk audiobooks in Writing, Poetry & Narrative because the output is a file the user needs to see live — push generated HTML or assets straight into a running sandbox and share a URL that renders it immediately.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — create a Sprite and drop an index.html:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const publish = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ html: z.string().min(10).max(200_000), slug: z.string().min(1).max(48) }).parse(d))\n  .handler(async ({ data }) => {\n    const name = `steampunk-audiobooks-${data.slug.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40)}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create the sprite (409 already-exists is fine).\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Write the composed HTML straight into /root/www — parents auto-created.\n    const w = await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: data.html,\n    });\n    if (!w.ok) throw new Error(`Sprite fs write failed: ${w.status}`);\n\n    // 3. Start a python http.server on port 8080 (wake-on-request).\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    return { name, url: `https://${name}.sprites.run` };\n  });\n```\n\nCLIENT: compose surface for steampunk audiobooks (textarea, form, or generator). On submit, POST the finished\nHTML to the server fn, then render the returned URL as a live `<iframe>` and a copyable share link.\n\nServe from `/root/www` — `/home/sprite` may not exist and the service fails to start with\n`cd: No such file or directory`. `http_port` is REQUIRED for wake-on-request; without it the\nsprite reports Running but every request 502s.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for steampunk audiobooks.\n2. The primary action (a compose surface that pushes freshly-authored HTML, JSON, or media into the live sandbox) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global audiobook production and distribution",
      "sam": "$700M — audiobook sound design and foley libraries",
      "som": "$25M — steampunk and fantasy audiobook SFX generation"
    },
    {
      "id": "writing-cafe-chatter-blocker-23",
      "theme": "writing",
      "title": "Cafe Chatter Blocker",
      "pitch": "Cafe Chatter Blocker boots a long-running service inside a Sprite for slice-of-life plays — the process wakes on the first visit and streams straight to the user's browser.",
      "subDiscipline": "slice-of-life plays",
      "quantumHook": "Long-running Service",
      "quantumHookId": "sprite-service",
      "quantumTag": "wake-on-request server",
      "quantumRationale": "A Long-running Service matches slice-of-life plays in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.",
      "megaPrompt": "Build \"Cafe Chatter Blocker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nCafe Chatter Blocker boots a long-running service inside a Sprite for slice-of-life plays — the process wakes on the first visit and streams straight to the user's browser.\nDiscipline: Writing, Poetry & Narrative (slice-of-life plays).\nRecipe: Long-running Service (wake-on-request server) as the single Sprites primitive.\nWhy Sprites: A Long-running Service matches slice-of-life plays in Writing, Poetry & Narrative because the experience needs a process that keeps running — an HTTP server, a streaming loop, a playback engine — waking on the first request and sleeping when idle.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — long-running service inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const boot = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ label: z.string().min(1).max(80) }).parse(d))\n  .handler(async ({ data }) => {\n    const slug = data.label.toLowerCase().replace(/[^a-z0-9]+/g, \"-\").slice(0, 40) || \"svc\";\n    const name = `slice-of-life-plays-${slug}-${Math.random().toString(36).slice(2, 6)}`;\n\n    // 1. Create sprite.\n    const c = await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n    if (!c.ok && c.status !== 409) throw new Error(`Sprite create failed: ${c.status}`);\n\n    // 2. Drop the payload the service will read.\n    const html = `<!doctype html><meta charset=\"utf-8\"><title>${data.label}</title>` +\n                 `<body style=\"font:16px system-ui;padding:2rem\"><h1>${data.label}</h1>` +\n                 `<p>Long-running Sprite service for slice-of-life plays.</p></body>`;\n    await fetch(`${API}/sprites/${name}/fs/write?path=/root/www/index.html&workingDir=/`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/octet-stream\" },\n      body: html,\n    });\n\n    // 3. PUT a named service. http_port is REQUIRED for wake-on-request.\n    await fetch(`${API}/sprites/${name}/services/webapp`, { method: \"DELETE\", headers: auth() });\n    await fetch(`${API}/sprites/${name}/services/webapp`, {\n      method: \"PUT\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ cmd: \"python3\", args: [\"-m\", \"http.server\", \"8080\"], dir: \"/root/www\", needs: [], http_port: 8080 }),\n    });\n\n    // 4. Start (NDJSON stream — parse for error/exit events if you need them).\n    await fetch(`${API}/sprites/${name}/services/webapp/start`, {\n      method: \"POST\",\n      headers: { ...auth(), Accept: \"application/x-ndjson\" },\n    });\n\n    // 5. Warm-poll the URL so the user gets a live URL, not a cold-boot 502.\n    const url = `https://${name}.sprites.run`;\n    for (let i = 0; i < 12; i++) {\n      try {\n        const p = await fetch(url, { signal: AbortSignal.timeout(4000) });\n        if (p.ok) return { name, url };\n      } catch {}\n      await new Promise((r) => setTimeout(r, 1000));\n    }\n    return { name, url };\n  });\n```\n\nCLIENT: \"Boot service\" button. On success render an `<iframe src={url}>` and a share link.\nReset button DELETEs and re-PUTs the service if the user wants a fresh boot.\n\nSwap the cmd/args for the service that fits slice-of-life plays — ffmpeg loop, node websocket relay,\nstatic site behind /root/www, etc. Keep `dir: \"/root/www\"` and `http_port: 8080`.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for slice-of-life plays.\n2. The primary action (a launcher that boots a running process — HTTP server, streaming loop, playback engine — and hands the user its public URL) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$250M — dialogue writing and scene blocking tools",
      "som": "$10M — contemporary and slice-of-life playwright drafting aids"
    },
    {
      "id": "writing-ritual-chant-translator-24",
      "theme": "writing",
      "title": "Ritual Chant Translator",
      "pitch": "Ritual Chant Translator runs a one-shot shell command inside a Sprite for mythic worldbuilding and streams the stdout back so the user watches the work happen live.",
      "subDiscipline": "mythic worldbuilding",
      "quantumHook": "One-shot Exec",
      "quantumHookId": "sprite-exec",
      "quantumTag": "run any shell command",
      "quantumRationale": "One-shot Exec fits mythic worldbuilding in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.",
      "megaPrompt": "Build \"Ritual Chant Translator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nRitual Chant Translator runs a one-shot shell command inside a Sprite for mythic worldbuilding and streams the stdout back so the user watches the work happen live.\nDiscipline: Writing, Poetry & Narrative (mythic worldbuilding).\nRecipe: One-shot Exec (run any shell command) as the single Sprites primitive.\nWhy Sprites: One-shot Exec fits mythic worldbuilding in Writing, Poetry & Narrative because the user really wants to run a command and see stdout — ffmpeg, imagemagick, a python one-liner — inside an isolated micro-VM they don't have to set up.\n\nSPRITES PRIMER (assume the reader has never used Sprites)\n- What they are: fly.io micro-sandboxes (tiny disposable Linux VMs) managed via\n  a REST API at `https://api.sprites.dev/v1`. Each sprite gets a public URL at\n  `https://{name}.sprites.run` when created with `url_settings.auth: \"public\"`.\n- Sign-up: create a free account at https://sprites.dev, then copy the 4-part\n  token (`org-slug/org-id/token-id/token-value`) from https://sprites.dev/account.\n- Full API docs: https://docs.sprites.dev/ (only needed if you go beyond the\n  snippet below).\n- Sprite name rules: lowercase letters, digits, and hyphens only. Max ~63 chars.\n- Auth: every request sends `Authorization: Bearer <SPRITES_TOKEN>`. Server-side\n  only — the token is a bearer secret, never ship it to the browser.\n- Four primitives exist (create / fs.write / services / exec). This build uses\n  exactly one, wired end-to-end in the snippet below.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/sprites.functions.ts` that proxies the Sprites call.\n- ONE client surface (a textarea + launch button, or console box, or compose form) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads to Lovable, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- fly.io Sprites (api.sprites.dev/v1) is the only backend. All calls live inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays on the server.\n- Client surface fits the primitive: a form/prompt that returns the sprite URL or stdout.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/sprites.functions.ts) — one-shot exec inside a Sprite:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\nconst API = \"https://api.sprites.dev/v1\";\nconst auth = () => ({ Authorization: `Bearer ${process.env.SPRITES_TOKEN!}` });\n\n/** Built during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const run = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ script: z.string().min(1).max(4000) }).parse(d))\n  .handler(async ({ data }) => {\n    // Reuse a single sprite per session so exec is fast; create-if-missing.\n    const name = `mythic-worldbuilding-console`;\n    await fetch(`${API}/sprites`, {\n      method: \"POST\",\n      headers: { ...auth(), \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ name, url_settings: { auth: \"public\" } }),\n    });\n\n    // POST /exec with repeated ?cmd= params — Authorization ONLY, no Accept header.\n    const qs = new URLSearchParams();\n    qs.append(\"cmd\", \"bash\");\n    qs.append(\"cmd\", \"-lc\");\n    qs.append(\"cmd\", data.script);\n\n    const res = await fetch(`${API}/sprites/${name}/exec?${qs}`, {\n      method: \"POST\",\n      headers: auth(),\n    });\n    if (!res.ok) throw new Error(`Sprite exec failed: ${res.status}`);\n\n    // Response stream ends with 0x03 <exitCode>.\n    const bytes = new Uint8Array(await res.arrayBuffer());\n    let exit: number | null = null;\n    let end = bytes.length;\n    if (bytes.length >= 2 && bytes[bytes.length - 2] === 3) {\n      exit = bytes[bytes.length - 1];\n      end = bytes.length - 2;\n    }\n    return { stdout: new TextDecoder().decode(bytes.slice(0, end)), exit };\n  });\n```\n\nCLIENT: script textarea prefilled with a mythic worldbuilding-appropriate one-liner + \"Run\". Render stdout\nin a monospace pre with the exit code chip.\n\nDo NOT add `Accept: application/octet-stream` on /exec — Sprites returns 406.\nDo NOT create the sprite via PUT — it returns 404. Only POST /sprites creates.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the sandbox does for mythic worldbuilding.\n2. The primary action (a prompt-driven console that runs a shell command in the sandbox and streams the output back to the user) is one tap away; the rest of the layout supports it.\n3. Sprites runs the primitive server-side, the URL or stdout lands on screen, the user\n   can share, retry, or copy.\n\nGOTCHAS (universal — apply to every Sprites call in this build)\n- Token MUST be the 4-part `org-slug/org-id/token-id/token-value` from\n  sprites.dev/account. A raw Fly.io org token returns 401 authentication failed.\n- Create is POST-only: `POST /sprites`. `PUT /sprites/{name}` returns 404.\n- Services are PUT-addressed at `/sprites/{name}/services/{service}`. `POST`ing\n  the services collection returns 405.\n- `http_port` is REQUIRED in the service PUT body. Omit it and the sprite shows\n  \"Running\" but every request 502s.\n- Serve files from `/root/www`. `/home/sprite` may not exist — services fail to\n  start with `cd: No such file or directory`.\n- `/exec`: send `Authorization` ONLY. Adding `Accept: application/octet-stream`\n  returns 406. The response body ends with `0x03 <exitCode>` — strip those two\n  bytes before decoding stdout.\n- Cold boot: after `services/{name}/start`, warm-poll the public URL (up to\n  ~12x @ 1s, 4s fetch timeout) before returning it, or the user's first click\n  hits a 502.\n- Never call `api.sprites.dev` from the browser. Every fetch lives inside a\n  `createServerFn` handler so `SPRITES_TOKEN` stays server-side.\n\nKEY — only ONE secret is required:\n1. `SPRITES_TOKEN`. Grab the 4-part token (org-slug/org-id/token-id/token-value)\n   from https://sprites.dev/account and paste it into Project Settings ->\n   Secrets. Read it only on the server via `process.env.SPRITES_TOKEN`. Never\n   prefix with `VITE_`, never expose to the client. This single token unlocks\n   create, filesystem, services, and exec on api.sprites.dev.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Sprites Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development and engine software",
      "sam": "$1B — worldbuilding and lore management tools",
      "som": "$35M — fantasy and mythic worldbuilding audio companions"
    }
  ]
}