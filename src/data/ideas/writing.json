{
  "theme": {
    "slug": "writing",
    "name": "Writing, Poetry & Narrative",
    "emoji": "✍️",
    "audience": "writers, poets, screenwriters, narrative designers",
    "market_anchor": "the writing tools market (~$1.5B) and >100M working writers"
  },
  "ideas": [
    {
      "id": "writing-scene-partner-whisper-0",
      "theme": "writing",
      "title": "Scene Partner Whisper",
      "pitch": "Lovable AI rewrites flat dialogue so screenwriters; ElevenLabs speaks it.",
      "subDiscipline": "screenplay dialogue rehearsal",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS allows writers to hear pacing and subtext without needing a live reader.",
      "megaPrompt": "Build \"Scene Partner Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites flat dialogue so screenwriters; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (screenplay dialogue rehearsal).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS allows writers to hear pacing and subtext without needing a live reader.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the screenplay dialogue rehearsal use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for screenplay dialogue rehearsal. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for screenplay dialogue rehearsal.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global screenwriting software and film prep",
      "sam": "$800M — indie and professional screenplay tools",
      "som": "$50M — dialogue-focused rehearsal apps"
    },
    {
      "id": "writing-slam-cadence-metronome-1",
      "theme": "writing",
      "title": "Slam Cadence Metronome",
      "pitch": "Lovable AI maps syllable stresses so poets; ElevenLabs speaks it.",
      "subDiscipline": "spoken word rhythm",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "Hearing the generated rhythm via TTS helps poets internalize performance timing.",
      "megaPrompt": "Build \"Slam Cadence Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps syllable stresses so poets; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (spoken word rhythm).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: Hearing the generated rhythm via TTS helps poets internalize performance timing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the spoken word rhythm use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for spoken word rhythm. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for spoken word rhythm.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global poetry publishing and performance market",
      "sam": "$300M — spoken word and slam poetry tools",
      "som": "$20M — performance coaching for poets"
    },
    {
      "id": "writing-character-voice-forge-2",
      "theme": "writing",
      "title": "Character Voice Forge",
      "pitch": "Lovable AI generates character backstories so narrators; ElevenLabs speaks it.",
      "subDiscipline": "audiobook character prep",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS provides an immediate auditory baseline for narrators to mimic or contrast.",
      "megaPrompt": "Build \"Character Voice Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates character backstories so narrators; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (audiobook character prep).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS provides an immediate auditory baseline for narrators to mimic or contrast.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the audiobook character prep use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for audiobook character prep. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for audiobook character prep.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global audiobook production market",
      "sam": "$500M — narrator coaching and prep software",
      "som": "$40M — indie audiobook creator tools"
    },
    {
      "id": "writing-worldbuilding-lore-echo-3",
      "theme": "writing",
      "title": "Worldbuilding Lore Echo",
      "pitch": "Lovable AI summarizes dense lore so novelists; ElevenLabs speaks it.",
      "subDiscipline": "fantasy worldbuilding",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS lets writers absorb their own complex worldbuilding while walking or resting.",
      "megaPrompt": "Build \"Worldbuilding Lore Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI summarizes dense lore so novelists; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (fantasy worldbuilding).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS lets writers absorb their own complex worldbuilding while walking or resting.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the fantasy worldbuilding use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for fantasy worldbuilding. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for fantasy worldbuilding.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global fiction publishing market",
      "sam": "$1.2B — novel writing and organization software",
      "som": "$80M — fantasy and sci-fi writing tools"
    },
    {
      "id": "writing-bedtime-story-pacer-4",
      "theme": "writing",
      "title": "Bedtime Story Pacer",
      "pitch": "Lovable AI adjusts sentence lengths so authors; ElevenLabs speaks it.",
      "subDiscipline": "children's book pacing",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS reveals if the generated text flows naturally for a parent reading aloud.",
      "megaPrompt": "Build \"Bedtime Story Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adjusts sentence lengths so authors; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (children's book pacing).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS reveals if the generated text flows naturally for a parent reading aloud.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the children's book pacing use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for children's book pacing. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for children's book pacing.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global children's book publishing",
      "sam": "$400M — children's author writing tools",
      "som": "$30M — read-aloud picture book drafting apps"
    },
    {
      "id": "writing-podium-breath-coach-5",
      "theme": "writing",
      "title": "Podium Breath Coach",
      "pitch": "Lovable AI inserts rhetorical pauses so speechwriters; ElevenLabs speaks it.",
      "subDiscipline": "speechwriting cadence",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS demonstrates the exact timing of generated pauses for live delivery.",
      "megaPrompt": "Build \"Podium Breath Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI inserts rhetorical pauses so speechwriters; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (speechwriting cadence).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS demonstrates the exact timing of generated pauses for live delivery.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the speechwriting cadence use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for speechwriting cadence. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for speechwriting cadence.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global public speaking and executive coaching",
      "sam": "$900M — speechwriting and presentation software",
      "som": "$60M — political and keynote speech prep tools"
    },
    {
      "id": "writing-tavern-npc-herald-6",
      "theme": "writing",
      "title": "Tavern NPC Herald",
      "pitch": "Lovable AI generates NPC secrets so gamemasters; ElevenLabs speaks it.",
      "subDiscipline": "tabletop RPG improv",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS gives GMs instant, distinct voices for generated NPCs during live sessions.",
      "megaPrompt": "Build \"Tavern NPC Herald\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates NPC secrets so gamemasters; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (tabletop RPG improv).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS gives GMs instant, distinct voices for generated NPCs during live sessions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the tabletop RPG improv use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for tabletop RPG improv. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tabletop RPG improv.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global tabletop gaming market",
      "sam": "$600M — digital GM companion tools",
      "som": "$45M — virtual tabletop audio plugins"
    },
    {
      "id": "writing-tongue-bridge-weaver-7",
      "theme": "writing",
      "title": "Tongue Bridge Weaver",
      "pitch": "Lovable AI translates poetic metaphors so linguists; ElevenLabs speaks it.",
      "subDiscipline": "bilingual poetry translation",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS allows poets to hear the phonetic beauty of the generated translation.",
      "megaPrompt": "Build \"Tongue Bridge Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates poetic metaphors so linguists; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (bilingual poetry translation).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS allows poets to hear the phonetic beauty of the generated translation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the bilingual poetry translation use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for bilingual poetry translation. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for bilingual poetry translation.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global literary translation market",
      "sam": "$200M — poetry and bilingual writing tools",
      "som": "$15M — cross-cultural literary apps"
    },
    {
      "id": "writing-thesis-flow-whisperer-8",
      "theme": "writing",
      "title": "Thesis Flow Whisperer",
      "pitch": "Lovable AI smooths clunky transitions so essayists; ElevenLabs speaks it.",
      "subDiscipline": "essay rhetorical flow",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS exposes awkward phrasing in generated transitions that the eye might miss.",
      "megaPrompt": "Build \"Thesis Flow Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI smooths clunky transitions so essayists; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (essay rhetorical flow).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS exposes awkward phrasing in generated transitions that the eye might miss.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the essay rhetorical flow use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for essay rhetorical flow. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for essay rhetorical flow.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global academic and essay writing software",
      "sam": "$400M — long-form nonfiction drafting tools",
      "som": "$30M — collegiate essay coaching apps"
    },
    {
      "id": "writing-blocking-cue-caller-9",
      "theme": "writing",
      "title": "Blocking Cue Caller",
      "pitch": "Lovable AI writes physical blocking so playwrights; ElevenLabs speaks it.",
      "subDiscipline": "playwriting stage directions",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS acts as a live stage manager reading generated cues during table reads.",
      "megaPrompt": "Build \"Blocking Cue Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes physical blocking so playwrights; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (playwriting stage directions).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS acts as a live stage manager reading generated cues during table reads.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the playwriting stage directions use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for playwriting stage directions. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for playwriting stage directions.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global theater production software",
      "sam": "$200M — playwriting and script formatting tools",
      "som": "$15M — stage direction rehearsal aids"
    },
    {
      "id": "writing-nostalgia-prompt-oracle-10",
      "theme": "writing",
      "title": "Nostalgia Prompt Oracle",
      "pitch": "Lovable AI asks sensory questions so memoirists; ElevenLabs speaks it.",
      "subDiscipline": "memoir memory triggering",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS delivers generated prompts like a gentle interviewer, keeping the writer reflective.",
      "megaPrompt": "Build \"Nostalgia Prompt Oracle\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI asks sensory questions so memoirists; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (memoir memory triggering).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS delivers generated prompts like a gentle interviewer, keeping the writer reflective.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the memoir memory triggering use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for memoir memory triggering. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for memoir memory triggering.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global autobiography and memoir publishing",
      "sam": "$300M — personal journaling and memoir software",
      "som": "$25M — guided memory recall apps"
    },
    {
      "id": "writing-dread-tension-metronome-11",
      "theme": "writing",
      "title": "Dread Tension Metronome",
      "pitch": "Lovable AI stretches descriptive dread so horror writers; ElevenLabs speaks it.",
      "subDiscipline": "horror suspense pacing",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS proves if the generated slow-burn text actually builds auditory anxiety.",
      "megaPrompt": "Build \"Dread Tension Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI stretches descriptive dread so horror writers; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (horror suspense pacing).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS proves if the generated slow-burn text actually builds auditory anxiety.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the horror suspense pacing use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for horror suspense pacing. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for horror suspense pacing.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — global horror fiction publishing",
      "sam": "$150M — genre-specific writing tools",
      "som": "$10M — thriller and horror pacing apps"
    },
    {
      "id": "writing-standup-beat-dropper-12",
      "theme": "writing",
      "title": "Standup Beat Dropper",
      "pitch": "Lovable AI tightens joke setups so comedians; ElevenLabs speaks it.",
      "subDiscipline": "comedy punchline timing",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS tests the generated comedic timing and pause placement before live performance.",
      "megaPrompt": "Build \"Standup Beat Dropper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI tightens joke setups so comedians; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (comedy punchline timing).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS tests the generated comedic timing and pause placement before live performance.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the comedy punchline timing use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for comedy punchline timing. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for comedy punchline timing.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — global comedy writing and performance",
      "sam": "$250M — standup material management tools",
      "som": "$20M — joke structure and timing apps"
    },
    {
      "id": "writing-melody-syllable-mapper-13",
      "theme": "writing",
      "title": "Melody Syllable Mapper",
      "pitch": "Lovable AI aligns lyrics to beats so songwriters; ElevenLabs speaks it.",
      "subDiscipline": "lyricist syllable mapping",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS vocalizes the generated rhythmic stress to ensure it fits the melody.",
      "megaPrompt": "Build \"Melody Syllable Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI aligns lyrics to beats so songwriters; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (lyricist syllable mapping).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS vocalizes the generated rhythmic stress to ensure it fits the melody.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the lyricist syllable mapping use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for lyricist syllable mapping. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for lyricist syllable mapping.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global songwriting and music publishing",
      "sam": "$800M — lyric writing and collaboration software",
      "som": "$50M — vocal melody and lyric drafting tools"
    },
    {
      "id": "writing-citation-chorus-reader-14",
      "theme": "writing",
      "title": "Citation Chorus Reader",
      "pitch": "Lovable AI synthesizes dense papers so researchers; ElevenLabs speaks it.",
      "subDiscipline": "academic literature review",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS turns generated literature summaries into a hands-free audio digest for commuting.",
      "megaPrompt": "Build \"Citation Chorus Reader\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI synthesizes dense papers so researchers; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (academic literature review).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS turns generated literature summaries into a hands-free audio digest for commuting.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the academic literature review use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for academic literature review. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for academic literature review.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global academic research software",
      "sam": "$600M — literature review and citation tools",
      "som": "$40M — PhD student productivity apps"
    },
    {
      "id": "writing-wanderlust-ambient-narrator-15",
      "theme": "writing",
      "title": "Wanderlust Ambient Narrator",
      "pitch": "Lovable AI describes foreign landscapes so travel writers; ElevenLabs speaks it.",
      "subDiscipline": "travel writing scene-setting",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS immerses the writer in the generated atmosphere to inspire further drafting.",
      "megaPrompt": "Build \"Wanderlust Ambient Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI describes foreign landscapes so travel writers; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (travel writing scene-setting).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS immerses the writer in the generated atmosphere to inspire further drafting.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the travel writing scene-setting use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for travel writing scene-setting. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for travel writing scene-setting.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global travel journalism and blogging",
      "sam": "$200M — travel writing and itinerary tools",
      "som": "$15M — immersive travel journaling apps"
    },
    {
      "id": "writing-client-persona-mimic-16",
      "theme": "writing",
      "title": "Client Persona Mimic",
      "pitch": "Lovable AI adopts client quirks so ghostwriters; ElevenLabs speaks it.",
      "subDiscipline": "ghostwriter tone-matching",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS verifies if the generated text truly sounds like the specific client's voice.",
      "megaPrompt": "Build \"Client Persona Mimic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adopts client quirks so ghostwriters; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (ghostwriter tone-matching).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS verifies if the generated text truly sounds like the specific client's voice.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the ghostwriter tone-matching use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for ghostwriter tone-matching. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ghostwriter tone-matching.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global ghostwriting and freelance writing",
      "sam": "$400M — client management and drafting tools",
      "som": "$30M — executive ghostwriting software"
    },
    {
      "id": "writing-micro-fiction-broadcaster-17",
      "theme": "writing",
      "title": "Micro Fiction Broadcaster",
      "pitch": "Lovable AI writes twist endings so podcasters; ElevenLabs speaks it.",
      "subDiscipline": "flash fiction podcasting",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS instantly produces a broadcast-ready audio draft of the generated micro-story.",
      "megaPrompt": "Build \"Micro Fiction Broadcaster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes twist endings so podcasters; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (flash fiction podcasting).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS instantly produces a broadcast-ready audio draft of the generated micro-story.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the flash fiction podcasting use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for flash fiction podcasting. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for flash fiction podcasting.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global podcasting and short fiction",
      "sam": "$300M — audio drama and flash fiction tools",
      "som": "$20M — indie fiction podcasting apps"
    },
    {
      "id": "writing-combat-bark-crier-18",
      "theme": "writing",
      "title": "Combat Bark Crier",
      "pitch": "Lovable AI generates combat barks so designers; ElevenLabs speaks it.",
      "subDiscipline": "video game narrative design",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS provides immediate placeholder audio for generated in-game character lines.",
      "megaPrompt": "Build \"Combat Bark Crier\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates combat barks so designers; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (video game narrative design).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS provides immediate placeholder audio for generated in-game character lines.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the video game narrative design use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for video game narrative design. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for video game narrative design.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global game narrative and audio design",
      "sam": "$1B — game writing and dialogue tools",
      "som": "$70M — indie game VO prototyping apps"
    },
    {
      "id": "writing-intimacy-whisper-tester-19",
      "theme": "writing",
      "title": "Intimacy Whisper Tester",
      "pitch": "Lovable AI softens romantic banter so authors; ElevenLabs speaks it.",
      "subDiscipline": "romance novel dialogue",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS ensures the generated intimate dialogue sounds natural and not overly melodramatic.",
      "megaPrompt": "Build \"Intimacy Whisper Tester\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI softens romantic banter so authors; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (romance novel dialogue).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS ensures the generated intimate dialogue sounds natural and not overly melodramatic.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the romance novel dialogue use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for romance novel dialogue. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for romance novel dialogue.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global romance fiction publishing",
      "sam": "$300M — romance writing and plotting tools",
      "som": "$25M — character chemistry drafting apps"
    },
    {
      "id": "writing-xenolinguistics-vowel-forge-20",
      "theme": "writing",
      "title": "Xenolinguistics Vowel Forge",
      "pitch": "Lovable AI invents alien phonetics so worldbuilders; ElevenLabs speaks it.",
      "subDiscipline": "sci-fi language creation",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS tests the pronounceability and auditory texture of generated fictional languages.",
      "megaPrompt": "Build \"Xenolinguistics Vowel Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents alien phonetics so worldbuilders; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (sci-fi language creation).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS tests the pronounceability and auditory texture of generated fictional languages.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the sci-fi language creation use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for sci-fi language creation. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sci-fi language creation.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — global sci-fi and fantasy publishing",
      "sam": "$150M — conlang and worldbuilding software",
      "som": "$10M — speculative fiction language tools"
    },
    {
      "id": "writing-interview-quote-echo-21",
      "theme": "writing",
      "title": "Interview Quote Echo",
      "pitch": "Lovable AI contextualizes raw quotes so journalists; ElevenLabs speaks it.",
      "subDiscipline": "journalism fact-checking",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS lets reporters listen to generated quote summaries while transcribing or editing.",
      "megaPrompt": "Build \"Interview Quote Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI contextualizes raw quotes so journalists; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (journalism fact-checking).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS lets reporters listen to generated quote summaries while transcribing or editing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the journalism fact-checking use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for journalism fact-checking. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for journalism fact-checking.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global journalism and newsroom software",
      "sam": "$400M — interview transcription and drafting tools",
      "som": "$30M — investigative journalism audio aids"
    },
    {
      "id": "writing-billboard-hook-shouter-22",
      "theme": "writing",
      "title": "Billboard Hook Shouter",
      "pitch": "Lovable AI punches up slogans so copywriters; ElevenLabs speaks it.",
      "subDiscipline": "advertising copywriting",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS reveals if the generated tagline has the necessary auditory punch for broadcast.",
      "megaPrompt": "Build \"Billboard Hook Shouter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI punches up slogans so copywriters; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (advertising copywriting).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS reveals if the generated tagline has the necessary auditory punch for broadcast.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the advertising copywriting use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for advertising copywriting. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for advertising copywriting.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global copywriting and ad tech",
      "sam": "$600M — creative agency drafting tools",
      "som": "$45M — audio and broadcast ad copy apps"
    },
    {
      "id": "writing-epoch-dialect-tutor-23",
      "theme": "writing",
      "title": "Epoch Dialect Tutor",
      "pitch": "Lovable AI adapts modern slang so historical novelists; ElevenLabs speaks it.",
      "subDiscipline": "historical fiction dialects",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS checks if the generated period-accurate dialogue sounds authentic rather than stilted.",
      "megaPrompt": "Build \"Epoch Dialect Tutor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adapts modern slang so historical novelists; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (historical fiction dialects).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS checks if the generated period-accurate dialogue sounds authentic rather than stilted.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the historical fiction dialects use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for historical fiction dialects. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for historical fiction dialects.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global historical fiction publishing",
      "sam": "$200M — historical research and writing tools",
      "som": "$15M — period-accurate dialogue drafting apps"
    },
    {
      "id": "writing-blank-page-soother-24",
      "theme": "writing",
      "title": "Blank Page Soother",
      "pitch": "Lovable AI guides breathing exercises so blocked writers; ElevenLabs speaks it.",
      "subDiscipline": "writer mindset coaching",
      "quantumHook": "ElevenLabs Text-to-Speech",
      "quantumHookId": "tts-narration",
      "quantumTag": "streaming voice",
      "quantumRationale": "TTS delivers the generated meditation in a calming voice to reduce creative anxiety.",
      "megaPrompt": "Build \"Blank Page Soother\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI guides breathing exercises so blocked writers; ElevenLabs speaks it.\nDiscipline: Writing, Poetry & Narrative (writer mindset coaching).\nRecipe: Lovable AI brain + ElevenLabs Text-to-Speech (streaming voice) as the voice surface.\nWhy this voice surface: TTS delivers the generated meditation in a calming voice to reduce creative anxiety.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/coach.functions.ts) — brain + voice in one call:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const speakAdvice = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    topic: z.string().min(1).max(500),\n    language: z.string().default(\"English\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI writes the answer for the writer mindset coaching use case.\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are an expert helper for writer mindset coaching. Answer in ${data.language}. ` +\n              `Keep it warm, specific, under 120 words. Markdown is fine but no headings.`,\n      prompt: data.topic,\n    });\n\n    // 2. VOICE — ElevenLabs reads it back in the chosen language.\n    const isEn = data.language === \"English\";\n    const r = await fetch(\n      \"https://api.elevenlabs.io/v1/text-to-speech/JBFqnCBsd6RMkjVDRZzb/stream\" +\n        \"?output_format=mp3_44100_128\",\n      {\n        method: \"POST\",\n        headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          text,\n          model_id: isEn ? \"eleven_turbo_v2_5\" : \"eleven_multilingual_v2\",\n        }),\n      },\n    );\n    if (!r.ok) throw new Error(`TTS failed: ${r.status}`);\n    const buf = await r.arrayBuffer();\n    return { text, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT (in the page component):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { speakAdvice } from \"@/lib/coach.functions\";\n\nconst ask = useServerFn(speakAdvice);\nconst onSubmit = async (topic: string, language: string) => {\n  const { text, audio } = await ask({ data: { topic, language } });\n  // render `text` on screen with react-markdown, then:\n  await new Audio(`data:audio/mpeg;base64,${audio}`).play();\n};\n```\n\nVoice: George (`JBFqnCBsd6RMkjVDRZzb`). Swap from https://elevenlabs.io/voice-library.\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for writer mindset coaching.\n2. The primary action (a 'play' button (or auto-play) that streams lifelike narration of any text the app generates) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global mental wellness and productivity",
      "sam": "$300M — creative coaching and journaling tools",
      "som": "$20M — writer's block and mindfulness apps"
    },
    {
      "id": "writing-scene-sparring-partner-0",
      "theme": "writing",
      "title": "Scene Sparring Partner",
      "pitch": "Lovable AI generates character responses so writers can rehearse; ElevenLabs speaks them during live dialogue practice.",
      "subDiscipline": "screenplay rehearsal",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Real-time conversational back-and-forth is essential for rehearsing screenplay dialogue naturally.",
      "megaPrompt": "Build \"Scene Sparring Partner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates character responses so writers can rehearse; ElevenLabs speaks them during live dialogue practice.\nDiscipline: Writing, Poetry & Narrative (screenplay rehearsal).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Real-time conversational back-and-forth is essential for rehearsing screenplay dialogue naturally.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the screenplay rehearsal use case (e.g.\n\"You are a senior screenplay rehearsal mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for screenplay rehearsal.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and screenwriting software market",
      "sam": "$400M — professional screenwriter and playwright tools segment",
      "som": "$25M — AI dialogue rehearsal and acting prep applications"
    },
    {
      "id": "writing-meter-pacing-guide-1",
      "theme": "writing",
      "title": "Meter Pacing Guide",
      "pitch": "Lovable AI analyzes poetic rhythm so poets can refine flow; ElevenLabs speaks the verses to demonstrate cadence.",
      "subDiscipline": "poetry performance",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational agent allows the poet to interrupt, ask for tempo changes, and hear immediate rhythmic adjustments.",
      "megaPrompt": "Build \"Meter Pacing Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes poetic rhythm so poets can refine flow; ElevenLabs speaks the verses to demonstrate cadence.\nDiscipline: Writing, Poetry & Narrative (poetry performance).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: A conversational agent allows the poet to interrupt, ask for tempo changes, and hear immediate rhythmic adjustments.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the poetry performance use case (e.g.\n\"You are a senior poetry performance mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for poetry performance.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and publishing tools market",
      "sam": "$150M — poetry and spoken word creation software",
      "som": "$10M — AI-assisted poetic meter and performance coaching"
    },
    {
      "id": "writing-lore-interrogation-room-2",
      "theme": "writing",
      "title": "Lore Interrogation Room",
      "pitch": "Lovable AI adopts a citizen persona so authors can test lore; ElevenLabs speaks the character's probing questions.",
      "subDiscipline": "fantasy worldbuilding",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Authors need an interactive, in-character voice to spontaneously defend and refine their fictional world's logic.",
      "megaPrompt": "Build \"Lore Interrogation Room\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adopts a citizen persona so authors can test lore; ElevenLabs speaks the character's probing questions.\nDiscipline: Writing, Poetry & Narrative (fantasy worldbuilding).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Authors need an interactive, in-character voice to spontaneously defend and refine their fictional world's logic.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the fantasy worldbuilding use case (e.g.\n\"You are a senior fantasy worldbuilding mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for fantasy worldbuilding.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$600M — speculative fiction and fantasy authoring tools",
      "som": "$30M — AI worldbuilding and lore consistency checkers"
    },
    {
      "id": "writing-plot-hole-detective-3",
      "theme": "writing",
      "title": "Plot Hole Detective",
      "pitch": "Lovable AI identifies story inconsistencies so novelists can fix gaps; ElevenLabs speaks the critiques like a sharp editor.",
      "subDiscipline": "narrative structure",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational voice agent turns solitary editing into a dynamic, Socratic dialogue about narrative logic.",
      "megaPrompt": "Build \"Plot Hole Detective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies story inconsistencies so novelists can fix gaps; ElevenLabs speaks the critiques like a sharp editor.\nDiscipline: Writing, Poetry & Narrative (narrative structure).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: A conversational voice agent turns solitary editing into a dynamic, Socratic dialogue about narrative logic.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the narrative structure use case (e.g.\n\"You are a senior narrative structure mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for narrative structure.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$800M — professional novelist and fiction editing software",
      "som": "$40M — AI structural editing and plot analysis tools"
    },
    {
      "id": "writing-character-voice-audition-4",
      "theme": "writing",
      "title": "Character Voice Audition",
      "pitch": "Lovable AI generates unique monologues so writers can hear personas; ElevenLabs speaks each audition with distinct emotional tones.",
      "subDiscipline": "character development",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Real-time voice conversation lets writers instantly tweak personality traits and hear the character talk back.",
      "megaPrompt": "Build \"Character Voice Audition\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates unique monologues so writers can hear personas; ElevenLabs speaks each audition with distinct emotional tones.\nDiscipline: Writing, Poetry & Narrative (character development).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Real-time voice conversation lets writers instantly tweak personality traits and hear the character talk back.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the character development use case (e.g.\n\"You are a senior character development mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for character development.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and character design market",
      "sam": "$500M — fiction writing and roleplay character creation",
      "som": "$20M — AI character voice and persona simulation tools"
    },
    {
      "id": "writing-block-breaking-therapist-5",
      "theme": "writing",
      "title": "Block Breaking Therapist",
      "pitch": "Lovable AI guides mental exercises so stuck writers find inspiration; ElevenLabs speaks soothing prompts to unlock creativity.",
      "subDiscipline": "creative mindset",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational agent provides empathetic, real-time vocal coaching to navigate the emotional friction of writer's block.",
      "megaPrompt": "Build \"Block Breaking Therapist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI guides mental exercises so stuck writers find inspiration; ElevenLabs speaks soothing prompts to unlock creativity.\nDiscipline: Writing, Poetry & Narrative (creative mindset).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: A conversational agent provides empathetic, real-time vocal coaching to navigate the emotional friction of writer's block.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the creative mindset use case (e.g.\n\"You are a senior creative mindset mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for creative mindset.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and wellness market",
      "sam": "$300M — creative coaching and writer wellness applications",
      "som": "$15M — AI mindset and block-breaking companions for authors"
    },
    {
      "id": "writing-bedtime-story-weaver-6",
      "theme": "writing",
      "title": "Bedtime Story Weaver",
      "pitch": "Lovable AI invents branching tales so parents can entertain kids; ElevenLabs speaks the interactive narrative as it unfolds.",
      "subDiscipline": "children's literature",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "The agent listens to children's spontaneous choices and speaks the resulting story branches seamlessly in real time.",
      "megaPrompt": "Build \"Bedtime Story Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents branching tales so parents can entertain kids; ElevenLabs speaks the interactive narrative as it unfolds.\nDiscipline: Writing, Poetry & Narrative (children's literature).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: The agent listens to children's spontaneous choices and speaks the resulting story branches seamlessly in real time.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the children's literature use case (e.g.\n\"You are a senior children's literature mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for children's literature.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.0B — global children's book and audio story market",
      "sam": "$400M — interactive bedtime story and parenting apps",
      "som": "$25M — AI-generated real-time interactive children's audio tales"
    },
    {
      "id": "writing-dialect-dialogue-coach-7",
      "theme": "writing",
      "title": "Dialect Dialogue Coach",
      "pitch": "Lovable AI translates standard text into regional slang so writers verify authenticity; ElevenLabs speaks the localized dialogue aloud.",
      "subDiscipline": "linguistic authenticity",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Writers can converse with the agent to test how regional idioms sound in spontaneous, natural speech.",
      "megaPrompt": "Build \"Dialect Dialogue Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates standard text into regional slang so writers verify authenticity; ElevenLabs speaks the localized dialogue aloud.\nDiscipline: Writing, Poetry & Narrative (linguistic authenticity).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Writers can converse with the agent to test how regional idioms sound in spontaneous, natural speech.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the linguistic authenticity use case (e.g.\n\"You are a senior linguistic authenticity mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for linguistic authenticity.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and linguistics software market",
      "sam": "$200M — dialect, translation, and localization writing tools",
      "som": "$12M — AI dialect coaching for fiction and screenwriting"
    },
    {
      "id": "writing-audio-journaling-muse-8",
      "theme": "writing",
      "title": "Audio Journaling Muse",
      "pitch": "Lovable AI asks deep daily questions so authors can reflect aloud; ElevenLabs speaks the prompts and listens intently.",
      "subDiscipline": "reflective journaling",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Conversational voice allows for hands-free, intimate daily reflection without the friction of typing or staring at screens.",
      "megaPrompt": "Build \"Audio Journaling Muse\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI asks deep daily questions so authors can reflect aloud; ElevenLabs speaks the prompts and listens intently.\nDiscipline: Writing, Poetry & Narrative (reflective journaling).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Conversational voice allows for hands-free, intimate daily reflection without the friction of typing or staring at screens.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the reflective journaling use case (e.g.\n\"You are a senior reflective journaling mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for reflective journaling.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global journaling and personal development market",
      "sam": "$350M — digital journaling and audio diary applications",
      "som": "$18M — AI conversational journaling for creative professionals"
    },
    {
      "id": "writing-pitch-practice-simulator-9",
      "theme": "writing",
      "title": "Pitch Practice Simulator",
      "pitch": "Lovable AI acts as a tough publisher so authors can rehearse pitches; ElevenLabs speaks skeptical industry questions.",
      "subDiscipline": "author marketing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Real-time conversational pushback prepares writers for the high-pressure, unpredictable nature of live verbal book pitches.",
      "megaPrompt": "Build \"Pitch Practice Simulator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI acts as a tough publisher so authors can rehearse pitches; ElevenLabs speaks skeptical industry questions.\nDiscipline: Writing, Poetry & Narrative (author marketing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Real-time conversational pushback prepares writers for the high-pressure, unpredictable nature of live verbal book pitches.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the author marketing use case (e.g.\n\"You are a senior author marketing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for author marketing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global author marketing and publishing tools market",
      "sam": "$250M — book pitching and author presentation software",
      "som": "$10M — AI pitch rehearsal for traditional publishing"
    },
    {
      "id": "writing-sensory-description-weaver-10",
      "theme": "writing",
      "title": "Sensory Description Weaver",
      "pitch": "Lovable AI brainstorms vivid environmental details so writers can enrich scenes; ElevenLabs speaks the sensory descriptions evocatively.",
      "subDiscipline": "descriptive prose",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Hearing descriptions spoken aloud helps writers gauge the atmospheric impact and rhythm of their sensory prose.",
      "megaPrompt": "Build \"Sensory Description Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI brainstorms vivid environmental details so writers can enrich scenes; ElevenLabs speaks the sensory descriptions evocatively.\nDiscipline: Writing, Poetry & Narrative (descriptive prose).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Hearing descriptions spoken aloud helps writers gauge the atmospheric impact and rhythm of their sensory prose.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the descriptive prose use case (e.g.\n\"You are a senior descriptive prose mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for descriptive prose.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$700M — creative writing and prose enhancement software",
      "som": "$30M — AI sensory and descriptive prose generators"
    },
    {
      "id": "writing-tension-tuning-metronome-11",
      "theme": "writing",
      "title": "Tension Tuning Metronome",
      "pitch": "Lovable AI rewrites scenes to accelerate suspense so thriller authors can test pacing; ElevenLabs speaks the escalating action.",
      "subDiscipline": "thriller pacing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A voice agent can dynamically adjust its speaking speed and urgency based on the scene's real-time tension.",
      "megaPrompt": "Build \"Tension Tuning Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites scenes to accelerate suspense so thriller authors can test pacing; ElevenLabs speaks the escalating action.\nDiscipline: Writing, Poetry & Narrative (thriller pacing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: A voice agent can dynamically adjust its speaking speed and urgency based on the scene's real-time tension.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the thriller pacing use case (e.g.\n\"You are a senior thriller pacing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for thriller pacing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$400M — thriller, mystery, and suspense writing tools",
      "som": "$15M — AI pacing and tension analysis for novelists"
    },
    {
      "id": "writing-historical-fact-interrogator-12",
      "theme": "writing",
      "title": "Historical Fact Interrogator",
      "pitch": "Lovable AI cross-references era-specific details so historical novelists avoid anachronisms; ElevenLabs speaks the corrections as a period expert.",
      "subDiscipline": "historical fiction",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Conversing with a historical expert persona makes dry fact-checking feel like an immersive, era-appropriate consultation.",
      "megaPrompt": "Build \"Historical Fact Interrogator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI cross-references era-specific details so historical novelists avoid anachronisms; ElevenLabs speaks the corrections as a period expert.\nDiscipline: Writing, Poetry & Narrative (historical fiction).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Conversing with a historical expert persona makes dry fact-checking feel like an immersive, era-appropriate consultation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the historical fiction use case (e.g.\n\"You are a senior historical fiction mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for historical fiction.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and research software market",
      "sam": "$300M — historical fiction research and authoring tools",
      "som": "$12M — AI historical accuracy and anachronism checkers"
    },
    {
      "id": "writing-subtext-reading-coach-13",
      "theme": "writing",
      "title": "Subtext Reading Coach",
      "pitch": "Lovable AI analyzes hidden meanings in dialogue so playwrights can deepen subtext; ElevenLabs speaks the lines with nuance.",
      "subDiscipline": "dramatic subtext",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "The agent can converse about underlying emotions and demonstrate different vocal inflections to highlight unspoken tensions.",
      "megaPrompt": "Build \"Subtext Reading Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes hidden meanings in dialogue so playwrights can deepen subtext; ElevenLabs speaks the lines with nuance.\nDiscipline: Writing, Poetry & Narrative (dramatic subtext).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: The agent can converse about underlying emotions and demonstrate different vocal inflections to highlight unspoken tensions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the dramatic subtext use case (e.g.\n\"You are a senior dramatic subtext mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dramatic subtext.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global playwriting and screenwriting market",
      "sam": "$250M — dramatic writing and dialogue refinement tools",
      "som": "$10M — AI subtext and dramatic nuance coaching"
    },
    {
      "id": "writing-flash-fiction-sprinter-14",
      "theme": "writing",
      "title": "Flash Fiction Sprinter",
      "pitch": "Lovable AI provides rapid story prompts so writers can practice brevity; ElevenLabs speaks the countdowns and constraints aloud.",
      "subDiscipline": "micro-fiction",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational agent acts as an energetic, real-time host to keep the writer engaged during fast-paced sprints.",
      "megaPrompt": "Build \"Flash Fiction Sprinter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI provides rapid story prompts so writers can practice brevity; ElevenLabs speaks the countdowns and constraints aloud.\nDiscipline: Writing, Poetry & Narrative (micro-fiction).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: A conversational agent acts as an energetic, real-time host to keep the writer engaged during fast-paced sprints.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the micro-fiction use case (e.g.\n\"You are a senior micro-fiction mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for micro-fiction.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$200M — short story and flash fiction writing apps",
      "som": "$8M — AI sprint hosting and prompt generation"
    },
    {
      "id": "writing-accessibility-beta-reader-15",
      "theme": "writing",
      "title": "Accessibility Beta Reader",
      "pitch": "Lovable AI evaluates screen-reader compatibility so writers can format for visually impaired readers; ElevenLabs speaks the formatting glitches.",
      "subDiscipline": "inclusive design",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Hearing the agent stumble over bad formatting in real-time conversation highlights accessibility flaws instantly.",
      "megaPrompt": "Build \"Accessibility Beta Reader\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI evaluates screen-reader compatibility so writers can format for visually impaired readers; ElevenLabs speaks the formatting glitches.\nDiscipline: Writing, Poetry & Narrative (inclusive design).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Hearing the agent stumble over bad formatting in real-time conversation highlights accessibility flaws instantly.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the inclusive design use case (e.g.\n\"You are a senior inclusive design mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for inclusive design.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global publishing and accessibility software market",
      "sam": "$150M — inclusive publishing and accessible ebook tools",
      "som": "$5M — AI screen-reader simulation for authors"
    },
    {
      "id": "writing-translation-tone-checker-16",
      "theme": "writing",
      "title": "Translation Tone Checker",
      "pitch": "Lovable AI compares translated nuances so bilingual poets can preserve emotional weight; ElevenLabs speaks both versions for comparison.",
      "subDiscipline": "literary translation",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Conversational back-and-forth allows translators to ask the agent to emphasize specific cultural idioms and hear tonal shifts.",
      "megaPrompt": "Build \"Translation Tone Checker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI compares translated nuances so bilingual poets can preserve emotional weight; ElevenLabs speaks both versions for comparison.\nDiscipline: Writing, Poetry & Narrative (literary translation).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Conversational back-and-forth allows translators to ask the agent to emphasize specific cultural idioms and hear tonal shifts.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the literary translation use case (e.g.\n\"You are a senior literary translation mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for literary translation.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.0B — global translation and localization software market",
      "sam": "$400M — literary translation and poetry localization tools",
      "som": "$15M — AI tonal and emotional translation checking"
    },
    {
      "id": "writing-monologue-memory-aid-17",
      "theme": "writing",
      "title": "Monologue Memory Aid",
      "pitch": "Lovable AI breaks down complex soliloquies so actors can memorize beats; ElevenLabs speaks the cues and listens for mistakes.",
      "subDiscipline": "actor preparation",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "The agent can dynamically pause, repeat, or correct the actor in real-time during hands-free memorization drills.",
      "megaPrompt": "Build \"Monologue Memory Aid\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI breaks down complex soliloquies so actors can memorize beats; ElevenLabs speaks the cues and listens for mistakes.\nDiscipline: Writing, Poetry & Narrative (actor preparation).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: The agent can dynamically pause, repeat, or correct the actor in real-time during hands-free memorization drills.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the actor preparation use case (e.g.\n\"You are a senior actor preparation mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for actor preparation.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global acting and theater preparation market",
      "sam": "$100M — script memorization and actor rehearsal apps",
      "som": "$5M — AI conversational scene partner for actors"
    },
    {
      "id": "writing-epistolary-letter-companion-18",
      "theme": "writing",
      "title": "Epistolary Letter Companion",
      "pitch": "Lovable AI drafts era-accurate letters so narrative designers can build epistolary games; ElevenLabs speaks the correspondence like a penpal.",
      "subDiscipline": "historical correspondence",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Conversing with the agent as the penpal helps designers test the intimacy and pacing of letter-based storytelling.",
      "megaPrompt": "Build \"Epistolary Letter Companion\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts era-accurate letters so narrative designers can build epistolary games; ElevenLabs speaks the correspondence like a penpal.\nDiscipline: Writing, Poetry & Narrative (historical correspondence).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Conversing with the agent as the penpal helps designers test the intimacy and pacing of letter-based storytelling.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the historical correspondence use case (e.g.\n\"You are a senior historical correspondence mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for historical correspondence.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global game narrative and writing tools market",
      "sam": "$300M — interactive fiction and epistolary game design",
      "som": "$12M — AI letter writing and correspondence simulation"
    },
    {
      "id": "writing-comedy-timing-workshop-19",
      "theme": "writing",
      "title": "Comedy Timing Workshop",
      "pitch": "Lovable AI rewrites punchlines for better rhythm so comedy writers can test jokes; ElevenLabs speaks the setups and pauses.",
      "subDiscipline": "humorous writing",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "A conversational voice agent can execute comedic timing, pauses, and deadpan delivery to validate joke structures aloud.",
      "megaPrompt": "Build \"Comedy Timing Workshop\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites punchlines for better rhythm so comedy writers can test jokes; ElevenLabs speaks the setups and pauses.\nDiscipline: Writing, Poetry & Narrative (humorous writing).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: A conversational voice agent can execute comedic timing, pauses, and deadpan delivery to validate joke structures aloud.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the humorous writing use case (e.g.\n\"You are a senior humorous writing mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for humorous writing.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global comedy writing and performance market",
      "sam": "$200M — standup comedy and sitcom writing tools",
      "som": "$8M — AI comedic timing and punchline testing"
    },
    {
      "id": "writing-narrative-branch-tester-20",
      "theme": "writing",
      "title": "Narrative Branch Tester",
      "pitch": "Lovable AI simulates player choices so game writers can test dialogue trees; ElevenLabs speaks the branching paths dynamically.",
      "subDiscipline": "interactive fiction",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Real-time voice interaction perfectly mimics a player speaking to an NPC, exposing clunky dialogue tree transitions.",
      "megaPrompt": "Build \"Narrative Branch Tester\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI simulates player choices so game writers can test dialogue trees; ElevenLabs speaks the branching paths dynamically.\nDiscipline: Writing, Poetry & Narrative (interactive fiction).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Real-time voice interaction perfectly mimics a player speaking to an NPC, exposing clunky dialogue tree transitions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the interactive fiction use case (e.g.\n\"You are a senior interactive fiction mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for interactive fiction.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.0B — global game narrative and interactive fiction market",
      "sam": "$800M — RPG and branching dialogue writing tools",
      "som": "$40M — AI NPC dialogue tree simulation and testing"
    },
    {
      "id": "writing-lyric-rhyme-sparring-21",
      "theme": "writing",
      "title": "Lyric Rhyme Sparring",
      "pitch": "Lovable AI generates complex rhyme schemes so lyricists can overcome writer's block; ElevenLabs speaks the rhythmic suggestions aloud.",
      "subDiscipline": "songwriting",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "The conversational agent allows songwriters to verbally jam, requesting alternate rhymes and hearing them fitted to a beat.",
      "megaPrompt": "Build \"Lyric Rhyme Sparring\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates complex rhyme schemes so lyricists can overcome writer's block; ElevenLabs speaks the rhythmic suggestions aloud.\nDiscipline: Writing, Poetry & Narrative (songwriting).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: The conversational agent allows songwriters to verbally jam, requesting alternate rhymes and hearing them fitted to a beat.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the songwriting use case (e.g.\n\"You are a senior songwriting mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for songwriting.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.0B — global songwriting and music creation market",
      "sam": "$500M — lyric writing and vocal melody tools",
      "som": "$20M — AI lyrical rhyming and rhythmic sparring"
    },
    {
      "id": "writing-public-reading-rehearsal-22",
      "theme": "writing",
      "title": "Public Reading Rehearsal",
      "pitch": "Lovable AI selects engaging excerpts so authors can prepare for book tours; ElevenLabs speaks the crowd reactions and prompts.",
      "subDiscipline": "author performance",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "The agent simulates a live audience, conversing between pieces to help the author practice stage banter and pacing.",
      "megaPrompt": "Build \"Public Reading Rehearsal\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI selects engaging excerpts so authors can prepare for book tours; ElevenLabs speaks the crowd reactions and prompts.\nDiscipline: Writing, Poetry & Narrative (author performance).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: The agent simulates a live audience, conversing between pieces to help the author practice stage banter and pacing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the author performance use case (e.g.\n\"You are a senior author performance mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for author performance.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global author marketing and event prep market",
      "sam": "$150M — book tour preparation and public speaking tools",
      "som": "$6M — AI audience simulation for author readings"
    },
    {
      "id": "writing-metaphor-mining-assistant-23",
      "theme": "writing",
      "title": "Metaphor Mining Assistant",
      "pitch": "Lovable AI brainstorms unique figurative language so poets can avoid cliches; ElevenLabs speaks the metaphors to test their resonance.",
      "subDiscipline": "poetic imagery",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Hearing metaphors spoken in a conversational flow helps poets instantly judge if the imagery sounds natural or forced.",
      "megaPrompt": "Build \"Metaphor Mining Assistant\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI brainstorms unique figurative language so poets can avoid cliches; ElevenLabs speaks the metaphors to test their resonance.\nDiscipline: Writing, Poetry & Narrative (poetic imagery).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Hearing metaphors spoken in a conversational flow helps poets instantly judge if the imagery sounds natural or forced.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the poetic imagery use case (e.g.\n\"You are a senior poetic imagery mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for poetic imagery.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and poetry tools market",
      "sam": "$250M — creative prose and poetic imagery software",
      "som": "$10M — AI figurative language and metaphor generation"
    },
    {
      "id": "writing-sci-fi-jargon-builder-24",
      "theme": "writing",
      "title": "Sci-Fi Jargon Builder",
      "pitch": "Lovable AI invents consistent futuristic slang so sci-fi authors can enrich worldbuilding; ElevenLabs speaks the technobabble conversationally.",
      "subDiscipline": "speculative terminology",
      "quantumHook": "Conversational Voice Agent",
      "quantumHookId": "voice-agent",
      "quantumTag": "live conversation",
      "quantumRationale": "Conversing with the agent using the new jargon tests whether the speculative terminology feels natural in casual speech.",
      "megaPrompt": "Build \"Sci-Fi Jargon Builder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents consistent futuristic slang so sci-fi authors can enrich worldbuilding; ElevenLabs speaks the technobabble conversationally.\nDiscipline: Writing, Poetry & Narrative (speculative terminology).\nRecipe: Lovable AI brain + Conversational Voice Agent (live conversation) as the voice surface.\nWhy this voice surface: Conversing with the agent using the new jargon tests whether the speculative terminology feels natural in casual speech.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/agent.functions.ts) — mint a single-use token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintAgentToken = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ agentId: z.string() }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\n      `https://api.elevenlabs.io/v1/convai/conversation/token?agent_id=${data.agentId}`,\n      { headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n    );\n    const { token } = await r.json();\n    return { token };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useConversation } from \"@elevenlabs/react\";\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { mintAgentToken } from \"@/lib/agent.functions\";\n\nconst c = useConversation({ onMessage: (m) => console.log(m) });\nconst getToken = useServerFn(mintAgentToken);\n\nconst start = async (language: string) => {\n  await navigator.mediaDevices.getUserMedia({ audio: true });\n  const { token } = await getToken({ data: { agentId: import.meta.env.VITE_AGENT_ID } });\n  await c.startSession({\n    conversationToken: token,\n    connectionType: \"webrtc\",\n    overrides: { agent: { language } }, // ElevenLabs handles translation in-flight\n  });\n};\n```\n\nAGENT SETUP: create the agent at https://elevenlabs.io/app/agents. Its system\nprompt is the BRAIN for this build — write it for the speculative terminology use case (e.g.\n\"You are a senior speculative terminology mentor. Ask one focused question at a time.\"). Enable\nlanguage overrides. Copy the agent id into `VITE_AGENT_ID` (public env).\n\nTRANSLATION — included by default:\nAdd a language `<Select>` to the UI (English, Español, Français, Deutsch,\nPortuguês, हिंदी, 日本語). Before sending the AI output to ElevenLabs,\nask the gateway to translate it into the chosen language in the same server\nfunction. Then pass the translated text to ElevenLabs with\n`model_id: \"eleven_multilingual_v2\"` so the voice speaks naturally in that\nlanguage. Default the select to English so the demo works without a click.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for speculative terminology.\n2. The primary action (a microphone button that opens a back-and-forth voice conversation with an agent tuned for the discipline) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and worldbuilding software market",
      "sam": "$400M — science fiction and speculative writing tools",
      "som": "$15M — AI speculative jargon and terminology generation"
    },
    {
      "id": "writing-dialogue-pacing-doctor-0",
      "theme": "writing",
      "title": "Dialogue Pacing Doctor",
      "pitch": "Lovable AI analyzes cadence so writers perfect dialogue rhythm; ElevenLabs captures realtime word timestamps.",
      "subDiscipline": "screenplay dialogue flow",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Word timestamps reveal exact pause lengths and rushed syllables during spoken rehearsals.",
      "megaPrompt": "Build \"Dialogue Pacing Doctor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes cadence so writers perfect dialogue rhythm; ElevenLabs captures realtime word timestamps.\nDiscipline: Writing, Poetry & Narrative (screenplay dialogue flow).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Word timestamps reveal exact pause lengths and rushed syllables during spoken rehearsals.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for screenplay dialogue flow:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken screenplay dialogue flow notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for screenplay dialogue flow.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$400M — screenwriting and playwriting software",
      "som": "$15M — indie screenplay pacing tools"
    },
    {
      "id": "writing-dream-weaver-scribe-1",
      "theme": "writing",
      "title": "Dream Weaver Scribe",
      "pitch": "Lovable AI structures groggy ramblings so authors capture fleeting dreams; ElevenLabs transcribes sleepy morning audio.",
      "subDiscipline": "surreal narrative drafting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime streaming catches fragmented, low-energy dream murmurs before the user fully wakes and forgets.",
      "megaPrompt": "Build \"Dream Weaver Scribe\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures groggy ramblings so authors capture fleeting dreams; ElevenLabs transcribes sleepy morning audio.\nDiscipline: Writing, Poetry & Narrative (surreal narrative drafting).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime streaming catches fragmented, low-energy dream murmurs before the user fully wakes and forgets.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for surreal narrative drafting:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken surreal narrative drafting notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for surreal narrative drafting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$300M — fiction and novel drafting software",
      "som": "$10M — surrealism and dream journaling apps"
    },
    {
      "id": "writing-lore-walker-catcher-2",
      "theme": "writing",
      "title": "Lore Walker Catcher",
      "pitch": "Lovable AI organizes scattered thoughts so fantasy authors build wikis; ElevenLabs captures hiking dictation seamlessly.",
      "subDiscipline": "mobile worldbuilding",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Continuous streaming handles long, uninterrupted outdoor walks without dropping connection or losing worldbuilding context.",
      "megaPrompt": "Build \"Lore Walker Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI organizes scattered thoughts so fantasy authors build wikis; ElevenLabs captures hiking dictation seamlessly.\nDiscipline: Writing, Poetry & Narrative (mobile worldbuilding).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Continuous streaming handles long, uninterrupted outdoor walks without dropping connection or losing worldbuilding context.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for mobile worldbuilding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken mobile worldbuilding notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for mobile worldbuilding.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — fantasy and sci-fi authoring tools",
      "som": "$20M — mobile worldbuilding companions"
    },
    {
      "id": "writing-slam-breath-coach-3",
      "theme": "writing",
      "title": "Slam Breath Coach",
      "pitch": "Lovable AI identifies rushed syllables so poets optimize stage breathing; ElevenLabs captures precise word timestamps.",
      "subDiscipline": "spoken word rehearsal",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Timestamps map exact millisecond gaps between words to pinpoint where performers need to breathe.",
      "megaPrompt": "Build \"Slam Breath Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies rushed syllables so poets optimize stage breathing; ElevenLabs captures precise word timestamps.\nDiscipline: Writing, Poetry & Narrative (spoken word rehearsal).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Timestamps map exact millisecond gaps between words to pinpoint where performers need to breathe.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for spoken word rehearsal:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken spoken word rehearsal notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for spoken word rehearsal.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$150M — poetry and spoken word platforms",
      "som": "$5M — performance rehearsal aids"
    },
    {
      "id": "writing-protagonist-interrogation-room-4",
      "theme": "writing",
      "title": "Protagonist Interrogation Room",
      "pitch": "Lovable AI extracts psychological traits so novelists deepen protagonists; ElevenLabs captures improvised character rants.",
      "subDiscipline": "character psychology profiling",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime transcription allows writers to stay in character and maintain emotional momentum without typing.",
      "megaPrompt": "Build \"Protagonist Interrogation Room\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts psychological traits so novelists deepen protagonists; ElevenLabs captures improvised character rants.\nDiscipline: Writing, Poetry & Narrative (character psychology profiling).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime transcription allows writers to stay in character and maintain emotional momentum without typing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for character psychology profiling:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken character psychology profiling notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for character psychology profiling.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$600M — character development and plotting software",
      "som": "$25M — psychological profiling tools for authors"
    },
    {
      "id": "writing-commute-beat-mapper-5",
      "theme": "writing",
      "title": "Commute Beat Mapper",
      "pitch": "Lovable AI maps spoken ideas so drivers outline plots safely; ElevenLabs captures dashboard brainstorming audio.",
      "subDiscipline": "structural outlining",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Hands-free streaming lets commuters dictate structural beats while keeping their eyes on the road.",
      "megaPrompt": "Build \"Commute Beat Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps spoken ideas so drivers outline plots safely; ElevenLabs captures dashboard brainstorming audio.\nDiscipline: Writing, Poetry & Narrative (structural outlining).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Hands-free streaming lets commuters dictate structural beats while keeping their eyes on the road.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for structural outlining:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken structural outlining notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for structural outlining.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$450M — story outlining and structure software",
      "som": "$18M — hands-free mobile outlining apps"
    },
    {
      "id": "writing-freestyle-rhyme-net-6",
      "theme": "writing",
      "title": "Freestyle Rhyme Net",
      "pitch": "Lovable AI highlights missed rhymes so rappers refine freestyles; ElevenLabs captures rapid-fire vocal improvisation.",
      "subDiscipline": "lyricist improvisation",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Low-latency partial transcripts let artists see their rhymes materialize instantly without breaking their flow.",
      "megaPrompt": "Build \"Freestyle Rhyme Net\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI highlights missed rhymes so rappers refine freestyles; ElevenLabs captures rapid-fire vocal improvisation.\nDiscipline: Writing, Poetry & Narrative (lyricist improvisation).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Low-latency partial transcripts let artists see their rhymes materialize instantly without breaking their flow.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for lyricist improvisation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken lyricist improvisation notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for lyricist improvisation.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$250M — songwriting and lyricist software",
      "som": "$12M — hip-hop freestyle training tools"
    },
    {
      "id": "writing-scene-tension-grapher-7",
      "theme": "writing",
      "title": "Scene Tension Grapher",
      "pitch": "Lovable AI graphs emotional tension so playwrights pace climaxes; ElevenLabs captures actor rehearsal pauses.",
      "subDiscipline": "dramatic arc analysis",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Word timestamps measure the exact duration of dramatic pauses to quantify scene tension.",
      "megaPrompt": "Build \"Scene Tension Grapher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI graphs emotional tension so playwrights pace climaxes; ElevenLabs captures actor rehearsal pauses.\nDiscipline: Writing, Poetry & Narrative (dramatic arc analysis).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Word timestamps measure the exact duration of dramatic pauses to quantify scene tension.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for dramatic arc analysis:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken dramatic arc analysis notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dramatic arc analysis.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$350M — theater and playwriting software",
      "som": "$14M — dramatic pacing analytics"
    },
    {
      "id": "writing-live-verse-projector-8",
      "theme": "writing",
      "title": "Live Verse Projector",
      "pitch": "Lovable AI formats kinetic typography so venues project live poetry; ElevenLabs captures stage performances instantly.",
      "subDiscipline": "accessible performance captioning",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime partial transcripts enable low-latency, word-by-word caption projection for deaf audience members.",
      "megaPrompt": "Build \"Live Verse Projector\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI formats kinetic typography so venues project live poetry; ElevenLabs captures stage performances instantly.\nDiscipline: Writing, Poetry & Narrative (accessible performance captioning).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime partial transcripts enable low-latency, word-by-word caption projection for deaf audience members.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for accessible performance captioning:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken accessible performance captioning notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for accessible performance captioning.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$100M — live event accessibility software",
      "som": "$8M — poetry slam captioning tools"
    },
    {
      "id": "writing-bedtime-story-spinner-9",
      "theme": "writing",
      "title": "Bedtime Story Spinner",
      "pitch": "Lovable AI weaves magical tales so parents entertain toddlers; ElevenLabs captures whispered story prompts.",
      "subDiscipline": "interactive parenting narrative",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Streaming transcription accurately catches quiet, whispered prompts without waking the child or requiring screens.",
      "megaPrompt": "Build \"Bedtime Story Spinner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI weaves magical tales so parents entertain toddlers; ElevenLabs captures whispered story prompts.\nDiscipline: Writing, Poetry & Narrative (interactive parenting narrative).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Streaming transcription accurately catches quiet, whispered prompts without waking the child or requiring screens.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for interactive parenting narrative:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken interactive parenting narrative notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for interactive parenting narrative.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$200M — family and parenting creative apps",
      "som": "$10M — interactive bedtime story generators"
    },
    {
      "id": "writing-pacing-thesis-forger-10",
      "theme": "writing",
      "title": "Pacing Thesis Forger",
      "pitch": "Lovable AI formalizes spoken arguments so researchers draft papers; ElevenLabs captures pacing office dictation.",
      "subDiscipline": "academic drafting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Continuous audio streaming accommodates the long, complex, multi-clause sentences typical of academic thought processes.",
      "megaPrompt": "Build \"Pacing Thesis Forger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI formalizes spoken arguments so researchers draft papers; ElevenLabs captures pacing office dictation.\nDiscipline: Writing, Poetry & Narrative (academic drafting).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Continuous audio streaming accommodates the long, complex, multi-clause sentences typical of academic thought processes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for academic drafting:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken academic drafting notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for academic drafting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$800M — academic and research writing software",
      "som": "$30M — verbal thesis drafting tools"
    },
    {
      "id": "writing-dialect-authenticity-checker-11",
      "theme": "writing",
      "title": "Dialect Authenticity Checker",
      "pitch": "Lovable AI flags regional inaccuracies so authors perfect dialects; ElevenLabs captures spoken character voices.",
      "subDiscipline": "linguistic worldbuilding",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime phonetic transcription helps writers immediately verify if their spoken accent sounds authentic.",
      "megaPrompt": "Build \"Dialect Authenticity Checker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI flags regional inaccuracies so authors perfect dialects; ElevenLabs captures spoken character voices.\nDiscipline: Writing, Poetry & Narrative (linguistic worldbuilding).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime phonetic transcription helps writers immediately verify if their spoken accent sounds authentic.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for linguistic worldbuilding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken linguistic worldbuilding notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for linguistic worldbuilding.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — historical and regional fiction tools",
      "som": "$22M — dialect and linguistic accuracy checkers"
    },
    {
      "id": "writing-standup-pause-analyzer-12",
      "theme": "writing",
      "title": "Standup Pause Analyzer",
      "pitch": "Lovable AI optimizes punchline timing so comedians maximize laughs; ElevenLabs captures rehearsal laugh gaps.",
      "subDiscipline": "comedy set rehearsal",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Timestamps calculate the exact silence duration before punchlines to refine comedic delivery.",
      "megaPrompt": "Build \"Standup Pause Analyzer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI optimizes punchline timing so comedians maximize laughs; ElevenLabs captures rehearsal laugh gaps.\nDiscipline: Writing, Poetry & Narrative (comedy set rehearsal).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Timestamps calculate the exact silence duration before punchlines to refine comedic delivery.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for comedy set rehearsal:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken comedy set rehearsal notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for comedy set rehearsal.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$150M — comedy writing and rehearsal software",
      "som": "$7M — standup timing analytics"
    },
    {
      "id": "writing-memoir-reframing-scribe-13",
      "theme": "writing",
      "title": "Memoir Reframing Scribe",
      "pitch": "Lovable AI reframes painful memories so survivors write memoirs; ElevenLabs captures emotional journaling sessions.",
      "subDiscipline": "therapeutic life writing",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Streaming audio provides a safe, uninterrupted space for vulnerable emotional processing without typing barriers.",
      "megaPrompt": "Build \"Memoir Reframing Scribe\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI reframes painful memories so survivors write memoirs; ElevenLabs captures emotional journaling sessions.\nDiscipline: Writing, Poetry & Narrative (therapeutic life writing).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Streaming audio provides a safe, uninterrupted space for vulnerable emotional processing without typing barriers.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for therapeutic life writing:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken therapeutic life writing notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for therapeutic life writing.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$300M — journaling and memoir software",
      "som": "$15M — therapeutic narrative processing tools"
    },
    {
      "id": "writing-dungeon-master-dictator-14",
      "theme": "writing",
      "title": "Dungeon Master Dictator",
      "pitch": "Lovable AI generates sensory descriptions so game masters prep faster; ElevenLabs captures miniature painting commentary.",
      "subDiscipline": "tabletop RPG prep",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Hands-free transcription lets GMs describe rooms and generate lore while their hands paint miniatures.",
      "megaPrompt": "Build \"Dungeon Master Dictator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates sensory descriptions so game masters prep faster; ElevenLabs captures miniature painting commentary.\nDiscipline: Writing, Poetry & Narrative (tabletop RPG prep).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Hands-free transcription lets GMs describe rooms and generate lore while their hands paint miniatures.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for tabletop RPG prep:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken tabletop RPG prep notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tabletop RPG prep.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$400M — tabletop RPG and game master tools",
      "som": "$18M — hands-free campaign prep software"
    },
    {
      "id": "writing-fluent-fable-weaver-15",
      "theme": "writing",
      "title": "Fluent Fable Weaver",
      "pitch": "Lovable AI corrects grammar so students learn via storytelling; ElevenLabs captures foreign language practice.",
      "subDiscipline": "language acquisition narrative",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime feedback on spoken syntax helps language learners adjust their narrative grammar on the fly.",
      "megaPrompt": "Build \"Fluent Fable Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI corrects grammar so students learn via storytelling; ElevenLabs captures foreign language practice.\nDiscipline: Writing, Poetry & Narrative (language acquisition narrative).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime feedback on spoken syntax helps language learners adjust their narrative grammar on the fly.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for language acquisition narrative:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken language acquisition narrative notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for language acquisition narrative.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$600M — language learning and immersion software",
      "som": "$25M — narrative-based language acquisition tools"
    },
    {
      "id": "writing-eulogy-memory-loom-16",
      "theme": "writing",
      "title": "Eulogy Memory Loom",
      "pitch": "Lovable AI structures grieving thoughts so families write eulogies; ElevenLabs captures tearful memory sharing.",
      "subDiscipline": "commemorative writing",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Voice-first capture removes the clinical friction of typing during highly emotional, tearful remembrance.",
      "megaPrompt": "Build \"Eulogy Memory Loom\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures grieving thoughts so families write eulogies; ElevenLabs captures tearful memory sharing.\nDiscipline: Writing, Poetry & Narrative (commemorative writing).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Voice-first capture removes the clinical friction of typing during highly emotional, tearful remembrance.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for commemorative writing:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken commemorative writing notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for commemorative writing.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$100M — personal and commemorative writing software",
      "som": "$5M — grief processing and eulogy drafting aids"
    },
    {
      "id": "writing-query-pitch-polisher-17",
      "theme": "writing",
      "title": "Query Pitch Polisher",
      "pitch": "Lovable AI critiques hook strength so authors nail queries; ElevenLabs captures empty-room pitch rehearsals.",
      "subDiscipline": "publishing industry prep",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime transcription allows writers to hear and read their verbal pitches simultaneously to spot awkward phrasing.",
      "megaPrompt": "Build \"Query Pitch Polisher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI critiques hook strength so authors nail queries; ElevenLabs captures empty-room pitch rehearsals.\nDiscipline: Writing, Poetry & Narrative (publishing industry prep).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime transcription allows writers to hear and read their verbal pitches simultaneously to spot awkward phrasing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for publishing industry prep:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken publishing industry prep notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for publishing industry prep.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$350M — publishing and query letter software",
      "som": "$12M — author pitch rehearsal tools"
    },
    {
      "id": "writing-sensory-metaphor-injector-18",
      "theme": "writing",
      "title": "Sensory Metaphor Injector",
      "pitch": "Lovable AI injects vivid metaphors so novelists enrich prose; ElevenLabs captures kitchen counter brainstorming.",
      "subDiscipline": "descriptive prose enhancement",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Streaming audio lets writers describe scenes using their hands for gestures while cooking or doing chores.",
      "megaPrompt": "Build \"Sensory Metaphor Injector\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI injects vivid metaphors so novelists enrich prose; ElevenLabs captures kitchen counter brainstorming.\nDiscipline: Writing, Poetry & Narrative (descriptive prose enhancement).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Streaming audio lets writers describe scenes using their hands for gestures while cooking or doing chores.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for descriptive prose enhancement:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken descriptive prose enhancement notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for descriptive prose enhancement.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — prose enhancement and editing software",
      "som": "$20M — sensory description generators"
    },
    {
      "id": "writing-courtroom-jargon-referee-19",
      "theme": "writing",
      "title": "Courtroom Jargon Referee",
      "pitch": "Lovable AI flags legal inaccuracies so thriller writers maintain realism; ElevenLabs captures dictated trial scenes.",
      "subDiscipline": "procedural accuracy checking",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime transcription instantly catches mispronounced or misused legal terminology during rapid-fire courtroom dialogue dictation.",
      "megaPrompt": "Build \"Courtroom Jargon Referee\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI flags legal inaccuracies so thriller writers maintain realism; ElevenLabs captures dictated trial scenes.\nDiscipline: Writing, Poetry & Narrative (procedural accuracy checking).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime transcription instantly catches mispronounced or misused legal terminology during rapid-fire courtroom dialogue dictation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for procedural accuracy checking:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken procedural accuracy checking notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for procedural accuracy checking.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$400M — thriller and procedural fiction tools",
      "som": "$16M — legal jargon accuracy checkers"
    },
    {
      "id": "writing-nature-haiku-catcher-20",
      "theme": "writing",
      "title": "Nature Haiku Catcher",
      "pitch": "Lovable AI formats syllables so poets capture nature haikus; ElevenLabs captures breath-paced outdoor whispers.",
      "subDiscipline": "minimalist poetry drafting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Word timestamps help the AI count syllables and measure the natural breathing pauses of the poet.",
      "megaPrompt": "Build \"Nature Haiku Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI formats syllables so poets capture nature haikus; ElevenLabs captures breath-paced outdoor whispers.\nDiscipline: Writing, Poetry & Narrative (minimalist poetry drafting).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Word timestamps help the AI count syllables and measure the natural breathing pauses of the poet.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for minimalist poetry drafting:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken minimalist poetry drafting notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for minimalist poetry drafting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$150M — poetry drafting and formatting software",
      "som": "$6M — minimalist and nature poetry aids"
    },
    {
      "id": "writing-trope-subversion-outliner-21",
      "theme": "writing",
      "title": "Trope Subversion Outliner",
      "pitch": "Lovable AI subverts cliches so fanfiction writers plot twists; ElevenLabs captures enthusiastic what-if rants.",
      "subDiscipline": "fanfiction plotting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Low-latency transcription keeps up with the rapid, excited speech patterns of passionate fandom brainstorming.",
      "megaPrompt": "Build \"Trope Subversion Outliner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI subverts cliches so fanfiction writers plot twists; ElevenLabs captures enthusiastic what-if rants.\nDiscipline: Writing, Poetry & Narrative (fanfiction plotting).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Low-latency transcription keeps up with the rapid, excited speech patterns of passionate fandom brainstorming.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for fanfiction plotting:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken fanfiction plotting notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for fanfiction plotting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$250M — fanfiction and community writing platforms",
      "som": "$10M — trope analysis and plotting tools"
    },
    {
      "id": "writing-audio-drama-formatter-22",
      "theme": "writing",
      "title": "Audio Drama Formatter",
      "pitch": "Lovable AI structures dialogue so podcasters format scripts; ElevenLabs captures multi-actor table reads.",
      "subDiscipline": "podcast script formatting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime streaming differentiates the continuous flow of a table read into properly formatted audio drama cues.",
      "megaPrompt": "Build \"Audio Drama Formatter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures dialogue so podcasters format scripts; ElevenLabs captures multi-actor table reads.\nDiscipline: Writing, Poetry & Narrative (podcast script formatting).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime streaming differentiates the continuous flow of a table read into properly formatted audio drama cues.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for podcast script formatting:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken podcast script formatting notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for podcast script formatting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$300M — podcasting and audio drama software",
      "som": "$14M — audio script formatting tools"
    },
    {
      "id": "writing-culinary-story-extractor-23",
      "theme": "writing",
      "title": "Culinary Story Extractor",
      "pitch": "Lovable AI extracts engaging narratives so chefs write blogs; ElevenLabs captures messy-hands cooking commentary.",
      "subDiscipline": "food blogging narrative",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Voice capture is essential when the user's hands are covered in food prep ingredients.",
      "megaPrompt": "Build \"Culinary Story Extractor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts engaging narratives so chefs write blogs; ElevenLabs captures messy-hands cooking commentary.\nDiscipline: Writing, Poetry & Narrative (food blogging narrative).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Voice capture is essential when the user's hands are covered in food prep ingredients.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for food blogging narrative:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken food blogging narrative notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for food blogging narrative.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$200M — food blogging and recipe software",
      "som": "$9M — culinary narrative extraction tools"
    },
    {
      "id": "writing-technobabble-consistency-engine-24",
      "theme": "writing",
      "title": "Technobabble Consistency Engine",
      "pitch": "Lovable AI standardizes fictional terms so sci-fi authors maintain lore; ElevenLabs captures spaceship repair dictation.",
      "subDiscipline": "sci-fi prose drafting",
      "quantumHook": "Realtime Speech-to-Text",
      "quantumHookId": "realtime-stt",
      "quantumTag": "live transcription",
      "quantumRationale": "Realtime transcription allows the AI to instantly flag when a spoken made-up word deviates from established lore.",
      "megaPrompt": "Build \"Technobabble Consistency Engine\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI standardizes fictional terms so sci-fi authors maintain lore; ElevenLabs captures spaceship repair dictation.\nDiscipline: Writing, Poetry & Narrative (sci-fi prose drafting).\nRecipe: Lovable AI brain + Realtime Speech-to-Text (live transcription) as the voice surface.\nWhy this voice surface: Realtime transcription allows the AI to instantly flag when a spoken made-up word deviates from established lore.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/scribe.functions.ts) — mint a scribe token:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const mintScribeToken = createServerFn({ method: \"POST\" }).handler(async () => {\n  const r = await fetch(\n    \"https://api.elevenlabs.io/v1/single-use-token/realtime_scribe\",\n    { method: \"POST\", headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY! } },\n  );\n  const { token } = await r.json();\n  return { token };\n});\n```\n\nOPTIONAL BRAIN PASS (src/lib/refine.functions.ts) — Lovable AI shapes the\nraw transcript into something useful for sci-fi prose drafting:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const refine = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ transcript: z.string().min(1) }).parse(d))\n  .handler(async ({ data }) => {\n    const { text } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `Turn the user's spoken sci-fi prose drafting notes into a clean, actionable result.`,\n      prompt: data.transcript,\n    });\n    return { text };\n  });\n```\n\nCLIENT (`bun add @elevenlabs/react` first):\n```tsx\nimport { useScribe } from \"@elevenlabs/react\";\nconst scribe = useScribe({ modelId: \"scribe_v2_realtime\", commitStrategy: \"vad\" });\n// connect with the minted token, render scribe.partialTranscript live, then\n// call refine() with the joined committed transcripts when the user stops.\n```\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sci-fi prose drafting.\n2. The primary action (a live caption strip that types itself as the user speaks, with finalized lines saved as the user pauses) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — sci-fi and fantasy authoring tools",
      "som": "$22M — fictional terminology consistency checkers"
    },
    {
      "id": "writing-script-foley-forge-0",
      "theme": "writing",
      "title": "Script Foley Forge",
      "pitch": "Lovable AI extracts scene actions so Foley artists rehearse; ElevenLabs generates SFX for it.",
      "subDiscipline": "screenplay foley",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation provides realistic, customizable sounds that pre-recorded libraries cannot match for specific script actions.",
      "megaPrompt": "Build \"Script Foley Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts scene actions so Foley artists rehearse; ElevenLabs generates SFX for it.\nDiscipline: Writing, Poetry & Narrative (screenplay foley).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation provides realistic, customizable sounds that pre-recorded libraries cannot match for specific script actions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for screenplay foley. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for screenplay foley.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global audio post-production software",
      "sam": "$500M — indie film and theater sound design tools",
      "som": "$20M — freelance Foley artist workflow apps"
    },
    {
      "id": "writing-stanza-vibe-scorer-1",
      "theme": "writing",
      "title": "Stanza Vibe Scorer",
      "pitch": "Lovable AI extracts poetic mood so performers feel the vibe; ElevenLabs scores a backing track.",
      "subDiscipline": "poetry performance",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation creates unique, copyright-free atmospheric backing tracks tailored precisely to the emotional cadence of the poem.",
      "megaPrompt": "Build \"Stanza Vibe Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts poetic mood so performers feel the vibe; ElevenLabs scores a backing track.\nDiscipline: Writing, Poetry & Narrative (poetry performance).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation creates unique, copyright-free atmospheric backing tracks tailored precisely to the emotional cadence of the poem.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for poetry performance. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for poetry performance.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global live performance and event software",
      "sam": "$150M — spoken word and poetry event platforms",
      "som": "$5M — indie poet performance and touring tools"
    },
    {
      "id": "writing-dread-pacing-engine-2",
      "theme": "writing",
      "title": "Dread Pacing Engine",
      "pitch": "Lovable AI paces horror chapters so authors test anxiety; ElevenLabs generates creeping SFX for it.",
      "subDiscipline": "horror drafting",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Short SFX generation allows writers to test specific, unsettling audio cues that match the exact tension beats of their prose.",
      "megaPrompt": "Build \"Dread Pacing Engine\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI paces horror chapters so authors test anxiety; ElevenLabs generates creeping SFX for it.\nDiscipline: Writing, Poetry & Narrative (horror drafting).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Short SFX generation allows writers to test specific, unsettling audio cues that match the exact tension beats of their prose.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for horror drafting. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for horror drafting.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$300M — genre fiction drafting and plotting tools",
      "som": "$15M — horror and thriller author productivity apps"
    },
    {
      "id": "writing-xenotech-audio-blueprint-3",
      "theme": "writing",
      "title": "Xenotech Audio Blueprint",
      "pitch": "Lovable AI describes alien tech so narrative designers build worlds; ElevenLabs generates SFX for it.",
      "subDiscipline": "sci-fi worldbuilding",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX allows narrative designers to instantly prototype futuristic, non-existent mechanical sounds directly from text descriptions.",
      "megaPrompt": "Build \"Xenotech Audio Blueprint\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI describes alien tech so narrative designers build worlds; ElevenLabs generates SFX for it.\nDiscipline: Writing, Poetry & Narrative (sci-fi worldbuilding).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX allows narrative designers to instantly prototype futuristic, non-existent mechanical sounds directly from text descriptions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for sci-fi worldbuilding. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sci-fi worldbuilding.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development and engine software",
      "sam": "$1.2B — narrative design and worldbuilding tools",
      "som": "$40M — indie sci-fi game audio asset pipelines"
    },
    {
      "id": "writing-tavern-tale-weaver-4",
      "theme": "writing",
      "title": "Tavern Tale Weaver",
      "pitch": "Lovable AI outlines tabletop sessions so game masters prep; ElevenLabs scores a tavern track.",
      "subDiscipline": "tabletop RPG prep",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation provides seamless, mood-appropriate background scores that adapt to the specific setting outlined by the GM.",
      "megaPrompt": "Build \"Tavern Tale Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI outlines tabletop sessions so game masters prep; ElevenLabs scores a tavern track.\nDiscipline: Writing, Poetry & Narrative (tabletop RPG prep).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation provides seamless, mood-appropriate background scores that adapt to the specific setting outlined by the GM.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for tabletop RPG prep. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tabletop RPG prep.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global tabletop gaming and accessories market",
      "sam": "$400M — digital GM tools and virtual tabletop software",
      "som": "$25M — TTRPG audio and ambiance companion apps"
    },
    {
      "id": "writing-climax-swell-composer-5",
      "theme": "writing",
      "title": "Climax Swell Composer",
      "pitch": "Lovable AI identifies emotional climaxes so audiobook narrators pace; ElevenLabs scores a sweeping orchestral track.",
      "subDiscipline": "romance audiobooks",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Studio track generation gives narrators custom emotional swells to practice against, ensuring their vocal delivery matches the musical crescendo.",
      "megaPrompt": "Build \"Climax Swell Composer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies emotional climaxes so audiobook narrators pace; ElevenLabs scores a sweeping orchestral track.\nDiscipline: Writing, Poetry & Narrative (romance audiobooks).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Studio track generation gives narrators custom emotional swells to practice against, ensuring their vocal delivery matches the musical crescendo.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for romance audiobooks. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for romance audiobooks.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global audiobook production and distribution",
      "sam": "$800M — audiobook narrator software and coaching tools",
      "som": "$30M — romance genre audiobook production suites"
    },
    {
      "id": "writing-pursuit-momentum-tracker-6",
      "theme": "writing",
      "title": "Pursuit Momentum Tracker",
      "pitch": "Lovable AI maps chase sequences so writers maintain momentum; ElevenLabs generates high-BPM SFX for it.",
      "subDiscipline": "thriller writing",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "High-tempo SFX generation acts as a metronome for action writers, keeping their typing speed and prose rhythm aligned with the chase.",
      "megaPrompt": "Build \"Pursuit Momentum Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps chase sequences so writers maintain momentum; ElevenLabs generates high-BPM SFX for it.\nDiscipline: Writing, Poetry & Narrative (thriller writing).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: High-tempo SFX generation acts as a metronome for action writers, keeping their typing speed and prose rhythm aligned with the chase.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for thriller writing. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for thriller writing.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$200M — distraction-free writing and focus apps",
      "som": "$10M — thriller and action novelist workflow tools"
    },
    {
      "id": "writing-syllable-nature-sonifier-7",
      "theme": "writing",
      "title": "Syllable Nature Sonifier",
      "pitch": "Lovable AI interprets seasonal imagery so meditation apps relax users; ElevenLabs generates nature SFX for it.",
      "subDiscipline": "haiku meditation",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX creates hyper-specific, tranquil nature soundscapes that perfectly mirror the subtle seasonal shifts described in the haiku.",
      "megaPrompt": "Build \"Syllable Nature Sonifier\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI interprets seasonal imagery so meditation apps relax users; ElevenLabs generates nature SFX for it.\nDiscipline: Writing, Poetry & Narrative (haiku meditation).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX creates hyper-specific, tranquil nature soundscapes that perfectly mirror the subtle seasonal shifts described in the haiku.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for haiku meditation. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for haiku meditation.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global meditation and wellness app market",
      "sam": "$500M — mindfulness audio and soundscape libraries",
      "som": "$15M — poetry-integrated meditation and breathwork tools"
    },
    {
      "id": "writing-neon-dystopia-drone-8",
      "theme": "writing",
      "title": "Neon Dystopia Drone",
      "pitch": "Lovable AI builds dystopian bibles so authors focus deeply; ElevenLabs scores a dark synth track.",
      "subDiscipline": "cyberpunk focus",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation produces long-form, immersive synthwave drones that block out distractions and set a gritty cyberpunk tone for deep work.",
      "megaPrompt": "Build \"Neon Dystopia Drone\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI builds dystopian bibles so authors focus deeply; ElevenLabs scores a dark synth track.\nDiscipline: Writing, Poetry & Narrative (cyberpunk focus).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation produces long-form, immersive synthwave drones that block out distractions and set a gritty cyberpunk tone for deep work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for cyberpunk focus. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for cyberpunk focus.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global productivity and focus software",
      "sam": "$300M — ambient noise and focus music generators",
      "som": "$12M — speculative fiction author deep-work environments"
    },
    {
      "id": "writing-fable-magic-spark-9",
      "theme": "writing",
      "title": "Fable Magic Spark",
      "pitch": "Lovable AI writes fairy tale scenes so parents entertain kids; ElevenLabs generates whimsical SFX for it.",
      "subDiscipline": "children's interactive books",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation provides instant, magical audio cues that parents can trigger to make bedtime storytelling highly interactive and engaging.",
      "megaPrompt": "Build \"Fable Magic Spark\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes fairy tale scenes so parents entertain kids; ElevenLabs generates whimsical SFX for it.\nDiscipline: Writing, Poetry & Narrative (children's interactive books).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation provides instant, magical audio cues that parents can trigger to make bedtime storytelling highly interactive and engaging.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for children's interactive books. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for children's interactive books.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global children's book and edutainment market",
      "sam": "$1.5B — interactive storytelling and read-along apps",
      "som": "$50M — parent-led interactive bedtime story tools"
    },
    {
      "id": "writing-skirmish-clash-drafter-10",
      "theme": "writing",
      "title": "Skirmish Clash Drafter",
      "pitch": "Lovable AI researches war skirmishes so novelists draft immersively; ElevenLabs generates battlefield SFX for it.",
      "subDiscipline": "historical fiction",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX allows historical novelists to hear authentic, era-specific battle acoustics, grounding their combat descriptions in sensory reality.",
      "megaPrompt": "Build \"Skirmish Clash Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI researches war skirmishes so novelists draft immersively; ElevenLabs generates battlefield SFX for it.\nDiscipline: Writing, Poetry & Narrative (historical fiction).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX allows historical novelists to hear authentic, era-specific battle acoustics, grounding their combat descriptions in sensory reality.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for historical fiction. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for historical fiction.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$250M — historical research and reference tools for writers",
      "som": "$8M — military and historical fiction drafting aids"
    },
    {
      "id": "writing-twist-stinger-cue-11",
      "theme": "writing",
      "title": "Twist Stinger Cue",
      "pitch": "Lovable AI highlights plot twists so podcast narrators emphasize; ElevenLabs generates short musical stingers.",
      "subDiscipline": "mystery podcasting",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Short music generation creates precise, dramatic audio stingers that narrators can drop exactly when the script reveals a clue.",
      "megaPrompt": "Build \"Twist Stinger Cue\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI highlights plot twists so podcast narrators emphasize; ElevenLabs generates short musical stingers.\nDiscipline: Writing, Poetry & Narrative (mystery podcasting).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Short music generation creates precise, dramatic audio stingers that narrators can drop exactly when the script reveals a clue.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for mystery podcasting. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for mystery podcasting.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global podcasting hosting and production software",
      "sam": "$600M — narrative and fiction podcast production tools",
      "som": "$25M — mystery and true-crime podcast audio cue libraries"
    },
    {
      "id": "writing-nostalgia-memoir-loop-12",
      "theme": "writing",
      "title": "Nostalgia Memoir Loop",
      "pitch": "Lovable AI summarizes childhood memories so journalists reflect deeply; ElevenLabs scores a lo-fi nostalgic track.",
      "subDiscipline": "journalistic journaling",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation crafts personalized, evocative lo-fi loops that help memoirists unlock deep emotional memories during the journaling process.",
      "megaPrompt": "Build \"Nostalgia Memoir Loop\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI summarizes childhood memories so journalists reflect deeply; ElevenLabs scores a lo-fi nostalgic track.\nDiscipline: Writing, Poetry & Narrative (journalistic journaling).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation crafts personalized, evocative lo-fi loops that help memoirists unlock deep emotional memories during the journaling process.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for journalistic journaling. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for journalistic journaling.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global digital journaling and diary apps",
      "sam": "$400M — guided journaling and memoir writing platforms",
      "som": "$15M — audio-enhanced reflective journaling tools"
    },
    {
      "id": "writing-impact-beat-choreographer-13",
      "theme": "writing",
      "title": "Impact Beat Choreographer",
      "pitch": "Lovable AI choreographs fight scenes so writers time prose; ElevenLabs generates rhythmic impact SFX.",
      "subDiscipline": "action prose",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation provides rhythmic, percussive audio beats that help writers pace their sentence structure to match the physical strikes.",
      "megaPrompt": "Build \"Impact Beat Choreographer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI choreographs fight scenes so writers time prose; ElevenLabs generates rhythmic impact SFX.\nDiscipline: Writing, Poetry & Narrative (action prose).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation provides rhythmic, percussive audio beats that help writers pace their sentence structure to match the physical strikes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for action prose. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for action prose.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$150M — plot outlining and scene blocking tools",
      "som": "$5M — action and martial arts fiction drafting aids"
    },
    {
      "id": "writing-manor-creak-mapper-14",
      "theme": "writing",
      "title": "Manor Creak Mapper",
      "pitch": "Lovable AI designs haunted layouts so spatial audio writers map; ElevenLabs generates specific room SFX.",
      "subDiscipline": "gothic spatial audio",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX creates distinct, localized acoustic profiles for different rooms, aiding writers in designing immersive 3D audio experiences.",
      "megaPrompt": "Build \"Manor Creak Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs haunted layouts so spatial audio writers map; ElevenLabs generates specific room SFX.\nDiscipline: Writing, Poetry & Narrative (gothic spatial audio).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX creates distinct, localized acoustic profiles for different rooms, aiding writers in designing immersive 3D audio experiences.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for gothic spatial audio. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for gothic spatial audio.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global spatial audio and VR sound design",
      "sam": "$800M — interactive horror and escape room audio tools",
      "som": "$30M — gothic and haunted house spatial audio drafting"
    },
    {
      "id": "writing-hero-leitmotif-caster-15",
      "theme": "writing",
      "title": "Hero Leitmotif Caster",
      "pitch": "Lovable AI profiles protagonist journeys so authors stay inspired; ElevenLabs scores a heroic leitmotif track.",
      "subDiscipline": "epic fantasy",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation composes a unique, recurring character theme that authors can listen to while writing that specific character's POV.",
      "megaPrompt": "Build \"Hero Leitmotif Caster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI profiles protagonist journeys so authors stay inspired; ElevenLabs scores a heroic leitmotif track.\nDiscipline: Writing, Poetry & Narrative (epic fantasy).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation composes a unique, recurring character theme that authors can listen to while writing that specific character's POV.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for epic fantasy. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for epic fantasy.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$350M — character development and worldbuilding tools",
      "som": "$12M — epic fantasy author inspiration and playlist apps"
    },
    {
      "id": "writing-punchline-timing-rig-16",
      "theme": "writing",
      "title": "Punchline Timing Rig",
      "pitch": "Lovable AI punches up sitcom scripts so actors rehearse timing; ElevenLabs generates comedic SFX for it.",
      "subDiscipline": "sitcom rehearsal",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation provides custom rimshots, slide whistles, and audience reactions to help comedic actors nail their physical timing.",
      "megaPrompt": "Build \"Punchline Timing Rig\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI punches up sitcom scripts so actors rehearse timing; ElevenLabs generates comedic SFX for it.\nDiscipline: Writing, Poetry & Narrative (sitcom rehearsal).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation provides custom rimshots, slide whistles, and audience reactions to help comedic actors nail their physical timing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for sitcom rehearsal. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sitcom rehearsal.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — global screenwriting and production software",
      "sam": "$900M — script rehearsal and table read platforms",
      "som": "$40M — sitcom and sketch comedy timing aids"
    },
    {
      "id": "writing-surreal-morph-soundscape-17",
      "theme": "writing",
      "title": "Surreal Morph Soundscape",
      "pitch": "Lovable AI writes stream-of-consciousness pieces so avant-garde performers vibe; ElevenLabs generates morphing SFX for it.",
      "subDiscipline": "avant-garde poetry",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX creates bizarre, shifting audio textures that mirror the disjointed, surreal nature of experimental spoken word.",
      "megaPrompt": "Build \"Surreal Morph Soundscape\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes stream-of-consciousness pieces so avant-garde performers vibe; ElevenLabs generates morphing SFX for it.\nDiscipline: Writing, Poetry & Narrative (avant-garde poetry).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX creates bizarre, shifting audio textures that mirror the disjointed, surreal nature of experimental spoken word.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for avant-garde poetry. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for avant-garde poetry.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global live performance and event software",
      "sam": "$100M — experimental theater and fringe festival tools",
      "som": "$4M — avant-garde poetry and sound art performance apps"
    },
    {
      "id": "writing-noir-rain-focus-18",
      "theme": "writing",
      "title": "Noir Rain Focus",
      "pitch": "Lovable AI sets detective moods so mystery writers focus; ElevenLabs scores a moody jazz track.",
      "subDiscipline": "detective drafting",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation blends specific ambient elements like rain and saxophone to create a highly focused, genre-specific writing environment.",
      "megaPrompt": "Build \"Noir Rain Focus\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sets detective moods so mystery writers focus; ElevenLabs scores a moody jazz track.\nDiscipline: Writing, Poetry & Narrative (detective drafting).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation blends specific ambient elements like rain and saxophone to create a highly focused, genre-specific writing environment.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for detective drafting. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for detective drafting.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global productivity and focus software",
      "sam": "$250M — genre-specific writing environment generators",
      "som": "$10M — noir and hardboiled detective fiction drafting tools"
    },
    {
      "id": "writing-starship-hum-isolator-19",
      "theme": "writing",
      "title": "Starship Hum Isolator",
      "pitch": "Lovable AI describes starship bridges so sci-fi writers isolate; ElevenLabs generates continuous engine SFX.",
      "subDiscipline": "sci-fi deep work",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation produces seamless, low-frequency mechanical drones that mask background noise and simulate the isolation of deep space.",
      "megaPrompt": "Build \"Starship Hum Isolator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI describes starship bridges so sci-fi writers isolate; ElevenLabs generates continuous engine SFX.\nDiscipline: Writing, Poetry & Narrative (sci-fi deep work).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation produces seamless, low-frequency mechanical drones that mask background noise and simulate the isolation of deep space.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for sci-fi deep work. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sci-fi deep work.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global productivity and focus software",
      "sam": "$300M — ambient noise and white noise generators",
      "som": "$15M — sci-fi author isolation and deep-work soundscapes"
    },
    {
      "id": "writing-slam-rhythm-backing-20",
      "theme": "writing",
      "title": "Slam Rhythm Backing",
      "pitch": "Lovable AI analyzes slam meter so poets perform confidently; ElevenLabs scores a backing hip-hop track.",
      "subDiscipline": "spoken word",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation creates custom instrumental beats that perfectly match the BPM and rhythmic flow of the poet's specific cadence.",
      "megaPrompt": "Build \"Slam Rhythm Backing\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes slam meter so poets perform confidently; ElevenLabs scores a backing hip-hop track.\nDiscipline: Writing, Poetry & Narrative (spoken word).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation creates custom instrumental beats that perfectly match the BPM and rhythmic flow of the poet's specific cadence.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for spoken word. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for spoken word.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global live performance and event software",
      "sam": "$200M — poetry slam and open mic event platforms",
      "som": "$8M — spoken word artist rehearsal and backing track tools"
    },
    {
      "id": "writing-grief-cello-calibrator-21",
      "theme": "writing",
      "title": "Grief Cello Calibrator",
      "pitch": "Lovable AI outlines grief arcs so dramatists calibrate emotion; ElevenLabs generates a mournful cello track.",
      "subDiscipline": "tragedy playwriting",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation provides a deeply emotional, singular instrumental track that helps playwrights stay in the correct headspace for tragic scenes.",
      "megaPrompt": "Build \"Grief Cello Calibrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI outlines grief arcs so dramatists calibrate emotion; ElevenLabs generates a mournful cello track.\nDiscipline: Writing, Poetry & Narrative (tragedy playwriting).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation provides a deeply emotional, singular instrumental track that helps playwrights stay in the correct headspace for tragic scenes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for tragedy playwriting. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tragedy playwriting.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global screenwriting and playwriting software",
      "sam": "$200M — dramatic structure and emotional arc tools",
      "som": "$7M — tragedy and drama playwright emotional calibration aids"
    },
    {
      "id": "writing-clockwork-grind-inventor-22",
      "theme": "writing",
      "title": "Clockwork Grind Inventor",
      "pitch": "Lovable AI invents clockwork gadgets so audiobook producers design; ElevenLabs generates mechanical SFX for it.",
      "subDiscipline": "steampunk audiobooks",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "SFX generation allows producers to instantly prototype complex, multi-layered mechanical sounds for fictional steampunk inventions.",
      "megaPrompt": "Build \"Clockwork Grind Inventor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents clockwork gadgets so audiobook producers design; ElevenLabs generates mechanical SFX for it.\nDiscipline: Writing, Poetry & Narrative (steampunk audiobooks).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: SFX generation allows producers to instantly prototype complex, multi-layered mechanical sounds for fictional steampunk inventions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for steampunk audiobooks. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for steampunk audiobooks.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global audiobook production and distribution",
      "sam": "$700M — audiobook sound design and foley libraries",
      "som": "$25M — steampunk and fantasy audiobook SFX generation"
    },
    {
      "id": "writing-cafe-chatter-blocker-23",
      "theme": "writing",
      "title": "Cafe Chatter Blocker",
      "pitch": "Lovable AI blocks coffee shop scenes so playwrights draft realistically; ElevenLabs generates ambient cafe SFX.",
      "subDiscipline": "slice-of-life plays",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Generative SFX creates dynamic, non-repeating background chatter that helps playwrights write naturalistic dialogue without being distracted by real-world noise.",
      "megaPrompt": "Build \"Cafe Chatter Blocker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI blocks coffee shop scenes so playwrights draft realistically; ElevenLabs generates ambient cafe SFX.\nDiscipline: Writing, Poetry & Narrative (slice-of-life plays).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Generative SFX creates dynamic, non-repeating background chatter that helps playwrights write naturalistic dialogue without being distracted by real-world noise.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for slice-of-life plays. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for slice-of-life plays.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$250M — dialogue writing and scene blocking tools",
      "som": "$10M — contemporary and slice-of-life playwright drafting aids"
    },
    {
      "id": "writing-ritual-chant-translator-24",
      "theme": "writing",
      "title": "Ritual Chant Translator",
      "pitch": "Lovable AI translates epic invocations so worldbuilders perform rituals; ElevenLabs scores a tribal chant track.",
      "subDiscipline": "mythic worldbuilding",
      "quantumHook": "Generative Music & SFX",
      "quantumHookId": "music-sfx",
      "quantumTag": "audio generation",
      "quantumRationale": "Music generation produces deep, atmospheric vocal and percussive tracks that bring fictional mythologies and rituals to life for the creator.",
      "megaPrompt": "Build \"Ritual Chant Translator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates epic invocations so worldbuilders perform rituals; ElevenLabs scores a tribal chant track.\nDiscipline: Writing, Poetry & Narrative (mythic worldbuilding).\nRecipe: Lovable AI brain + Generative Music & SFX (audio generation) as the voice surface.\nWhy this voice surface: Music generation produces deep, atmospheric vocal and percussive tracks that bring fictional mythologies and rituals to life for the creator.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- TWO TanStack server functions max: one for the Lovable AI call (the brain),\n  one for the ElevenLabs call (the voice). Fold them into one if the voice\n  primitive does not need server-side text generation.\n- ONE client surface (a button, a mic, or a prompt box) wired to those server fns.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `ai` + `@ai-sdk/openai-compatible` + `zod`,\n  plus (only if the voice kernel needs it) `@elevenlabs/react`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- Lovable AI Gateway (the brain) + ElevenLabs (the voice). All calls live\n  inside `createServerFn` handlers so both keys stay on the server.\n- Client surface fits the kernel: a prompt box for TTS / music / SFX, a mic\n  for agents, a live caption strip for scribe. Render markdown from the brain\n  with `react-markdown` if you show the text on screen.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nBRAIN — Lovable AI Gateway (free for participants, no key prompt needed):\n```ts\n// src/lib/ai-gateway.server.ts\nimport { createOpenAICompatible } from \"@ai-sdk/openai-compatible\";\nexport function gateway() {\n  return createOpenAICompatible({\n    name: \"lovable\",\n    baseURL: \"https://ai.gateway.lovable.dev/v1\",\n    headers: {\n      \"Lovable-API-Key\": process.env.LOVABLE_API_KEY!,\n      \"X-Lovable-AIG-SDK\": \"vercel-ai-sdk\",\n    },\n  });\n}\n```\nDefault model: `google/gemini-3-flash-preview`. Use `generateText` (or\n`streamText` for long output) from `ai`. Keep prompts and model calls inside\nthe server function — never call the gateway from client code.\n\nSERVER FUNCTION (src/lib/score.functions.ts) — brain writes the prompt, voice generates the audio:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { generateText } from \"ai\";\nimport { z } from \"zod\";\nimport { gateway } from \"./ai-gateway.server\";\n\n/** Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const score = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({\n    intent: z.string().min(1).max(500),\n    kind: z.enum([\"sfx\", \"music\"]).default(\"music\"),\n  }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. BRAIN — Lovable AI turns a vague creative intent into a strong\n    //    ElevenLabs prompt (genre, mood, instruments, BPM, key, structure).\n    const { text: musicPrompt } = await generateText({\n      model: gateway()(\"google/gemini-3-flash-preview\"),\n      system: `You are a music director for mythic worldbuilding. Given the user's intent, ` +\n              `write a single-paragraph ElevenLabs prompt with genre, mood, ` +\n              `instrumentation, BPM, key, and structure. No preamble.`,\n      prompt: data.intent,\n    });\n\n    // 2. VOICE — ElevenLabs renders the audio.\n    const endpoint = data.kind === \"music\"\n      ? \"https://api.elevenlabs.io/v1/music\"\n      : \"https://api.elevenlabs.io/v1/sound-generation\";\n    const body = data.kind === \"music\"\n      ? { prompt: musicPrompt, duration_seconds: 30 }\n      : { text: musicPrompt, duration_seconds: 5, prompt_influence: 0.3 };\n    const r = await fetch(endpoint, {\n      method: \"POST\",\n      headers: { \"xi-api-key\": process.env.ELEVENLABS_API_KEY!, \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(body),\n    });\n    const buf = await r.arrayBuffer();\n    return { musicPrompt, audio: Buffer.from(buf).toString(\"base64\") };\n  });\n```\n\nCLIENT: input the creative intent, await `score()`, show the AI-written\n`musicPrompt` so the user understands the choice, then play the audio data URI.\n\nTRANSLATION — skip:\nThis kernel is non-linguistic (audio in or audio out), so do not add a\nlanguage selector or a translation pass. Keep the brain-to-voice path direct.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for mythic worldbuilding.\n2. The primary action (a prompt box that conjures a sound effect, sting, or short track on demand and pipes it into the workspace) is one tap away; the rest of the layout supports it.\n3. Lovable AI does the thinking, ElevenLabs handles the voice surface, and the\n   result (audio + any text) stays on screen so the user can retry or share.\n\nKEYS — both already provided to participants for free:\n1. `LOVABLE_API_KEY` (the AI brain). Auto-injected in every Lovable project.\n   Read it only on the server via `process.env.LOVABLE_API_KEY`. Never prefix\n   with `VITE_` and never expose to the client.\n2. `ELEVENLABS_API_KEY` (the voice). Best path: ask Lovable to connect the\n   ElevenLabs standard connector — it syncs the key automatically and rotates\n   from the dashboard. Fallback: paste a key from\n   https://elevenlabs.io/app/settings/api-keys into Project Settings -> Secrets.\n   Read it only on the server, header `xi-api-key`, never prefix with `VITE_`.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development and engine software",
      "sam": "$1B — worldbuilding and lore management tools",
      "som": "$35M — fantasy and mythic worldbuilding audio companions"
    }
  ]
}