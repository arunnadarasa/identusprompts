{
  "theme": {
    "slug": "writing",
    "name": "Writing, Poetry & Narrative",
    "emoji": "✍️",
    "audience": "writers, poets, screenwriters, narrative designers",
    "market_anchor": "the writing tools market (~$1.5B) and >100M working writers"
  },
  "ideas": [
    {
      "id": "writing-scene-partner-whisper-0",
      "theme": "writing",
      "title": "Scene Partner Whisper",
      "pitch": "Lovable AI rewrites flat dialogue so screenwriters; AIsa writes it.",
      "subDiscipline": "screenplay dialogue rehearsal",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for screenplay dialogue rehearsal in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Scene Partner Whisper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites flat dialogue so screenwriters; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (screenplay dialogue rehearsal).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for screenplay dialogue rehearsal in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for screenplay dialogue rehearsal. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for screenplay dialogue rehearsal.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global screenwriting software and film prep",
      "sam": "$800M — indie and professional screenplay tools",
      "som": "$50M — dialogue-focused rehearsal apps"
    },
    {
      "id": "writing-slam-cadence-metronome-1",
      "theme": "writing",
      "title": "Slam Cadence Metronome",
      "pitch": "Lovable AI maps syllable stresses so poets; AIsa writes it.",
      "subDiscipline": "written word rhythm",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for spoken word rhythm in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Slam Cadence Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps syllable stresses so poets; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (written word rhythm).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for spoken word rhythm in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for written word rhythm. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for written word rhythm.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global poetry publishing and performance market",
      "sam": "$300M — written word and slam poetry tools",
      "som": "$20M — performance coaching for poets"
    },
    {
      "id": "writing-character-voice-forge-2",
      "theme": "writing",
      "title": "Character Voice Forge",
      "pitch": "Lovable AI generates character backstories so narrators; AIsa writes it.",
      "subDiscipline": "audiobook character prep",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for audiobook character prep in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Character Voice Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates character backstories so narrators; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (audiobook character prep).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for audiobook character prep in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for audiobook character prep. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for audiobook character prep.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global audiobook production market",
      "sam": "$500M — narrator coaching and prep software",
      "som": "$40M — indie audiobook creator tools"
    },
    {
      "id": "writing-worldbuilding-lore-echo-3",
      "theme": "writing",
      "title": "Worldbuilding Lore Echo",
      "pitch": "Lovable AI summarizes dense lore so novelists; AIsa writes it.",
      "subDiscipline": "fantasy worldbuilding",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for fantasy worldbuilding in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Worldbuilding Lore Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI summarizes dense lore so novelists; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (fantasy worldbuilding).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for fantasy worldbuilding in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for fantasy worldbuilding. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for fantasy worldbuilding.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global fiction publishing market",
      "sam": "$1.2B — novel writing and organization software",
      "som": "$80M — fantasy and sci-fi writing tools"
    },
    {
      "id": "writing-bedtime-story-pacer-4",
      "theme": "writing",
      "title": "Bedtime Story Pacer",
      "pitch": "Lovable AI adjusts sentence lengths so authors; AIsa writes it.",
      "subDiscipline": "children's book pacing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for children's book pacing in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Bedtime Story Pacer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adjusts sentence lengths so authors; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (children's book pacing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for children's book pacing in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for children's book pacing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for children's book pacing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global children's book publishing",
      "sam": "$400M — children's author writing tools",
      "som": "$30M — read-aloud picture book drafting apps"
    },
    {
      "id": "writing-podium-breath-coach-5",
      "theme": "writing",
      "title": "Podium Breath Coach",
      "pitch": "Lovable AI inserts rhetorical pauses so speechwriters; AIsa writes it.",
      "subDiscipline": "speechwriting cadence",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for speechwriting cadence in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Podium Breath Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI inserts rhetorical pauses so speechwriters; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (speechwriting cadence).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for speechwriting cadence in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for speechwriting cadence. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for speechwriting cadence.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global public speaking and executive coaching",
      "sam": "$900M — speechwriting and presentation software",
      "som": "$60M — political and keynote speech prep tools"
    },
    {
      "id": "writing-tavern-npc-herald-6",
      "theme": "writing",
      "title": "Tavern NPC Herald",
      "pitch": "Lovable AI generates NPC secrets so gamemasters; AIsa writes it.",
      "subDiscipline": "tabletop RPG improv",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for tabletop RPG improv in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Tavern NPC Herald\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates NPC secrets so gamemasters; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (tabletop RPG improv).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for tabletop RPG improv in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for tabletop RPG improv. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tabletop RPG improv.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global tabletop gaming market",
      "sam": "$600M — digital GM companion tools",
      "som": "$45M — virtual tabletop audio plugins"
    },
    {
      "id": "writing-tongue-bridge-weaver-7",
      "theme": "writing",
      "title": "Tongue Bridge Weaver",
      "pitch": "Lovable AI translates poetic metaphors so linguists; AIsa writes it.",
      "subDiscipline": "bilingual poetry translation",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for bilingual poetry translation in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Tongue Bridge Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates poetic metaphors so linguists; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (bilingual poetry translation).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for bilingual poetry translation in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for bilingual poetry translation. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for bilingual poetry translation.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global literary translation market",
      "sam": "$200M — poetry and bilingual writing tools",
      "som": "$15M — cross-cultural literary apps"
    },
    {
      "id": "writing-thesis-flow-whisperer-8",
      "theme": "writing",
      "title": "Thesis Flow Whisperer",
      "pitch": "Lovable AI smooths clunky transitions so essayists; AIsa writes it.",
      "subDiscipline": "essay rhetorical flow",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for essay rhetorical flow in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Thesis Flow Whisperer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI smooths clunky transitions so essayists; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (essay rhetorical flow).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for essay rhetorical flow in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for essay rhetorical flow. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for essay rhetorical flow.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global academic and essay writing software",
      "sam": "$400M — long-form nonfiction drafting tools",
      "som": "$30M — collegiate essay coaching apps"
    },
    {
      "id": "writing-blocking-cue-caller-9",
      "theme": "writing",
      "title": "Blocking Cue Caller",
      "pitch": "Lovable AI writes physical blocking so playwrights; AIsa writes it.",
      "subDiscipline": "playwriting stage directions",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for playwriting stage directions in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Blocking Cue Caller\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes physical blocking so playwrights; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (playwriting stage directions).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for playwriting stage directions in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for playwriting stage directions. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for playwriting stage directions.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global theater production software",
      "sam": "$200M — playwriting and script formatting tools",
      "som": "$15M — stage direction rehearsal aids"
    },
    {
      "id": "writing-nostalgia-prompt-oracle-10",
      "theme": "writing",
      "title": "Nostalgia Prompt Oracle",
      "pitch": "Lovable AI asks sensory questions so memoirists; AIsa writes it.",
      "subDiscipline": "memoir memory triggering",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for memoir memory triggering in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Nostalgia Prompt Oracle\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI asks sensory questions so memoirists; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (memoir memory triggering).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for memoir memory triggering in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for memoir memory triggering. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for memoir memory triggering.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global autobiography and memoir publishing",
      "sam": "$300M — personal journaling and memoir software",
      "som": "$25M — guided memory recall apps"
    },
    {
      "id": "writing-dread-tension-metronome-11",
      "theme": "writing",
      "title": "Dread Tension Metronome",
      "pitch": "Lovable AI stretches descriptive dread so horror writers; AIsa writes it.",
      "subDiscipline": "horror suspense pacing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for horror suspense pacing in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Dread Tension Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI stretches descriptive dread so horror writers; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (horror suspense pacing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for horror suspense pacing in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for horror suspense pacing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for horror suspense pacing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — global horror fiction publishing",
      "sam": "$150M — genre-specific writing tools",
      "som": "$10M — thriller and horror pacing apps"
    },
    {
      "id": "writing-standup-beat-dropper-12",
      "theme": "writing",
      "title": "Standup Beat Dropper",
      "pitch": "Lovable AI tightens joke setups so comedians; AIsa writes it.",
      "subDiscipline": "comedy punchline timing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for comedy punchline timing in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Standup Beat Dropper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI tightens joke setups so comedians; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (comedy punchline timing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for comedy punchline timing in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for comedy punchline timing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for comedy punchline timing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — global comedy writing and performance",
      "sam": "$250M — standup material management tools",
      "som": "$20M — joke structure and timing apps"
    },
    {
      "id": "writing-melody-syllable-mapper-13",
      "theme": "writing",
      "title": "Melody Syllable Mapper",
      "pitch": "Lovable AI aligns lyrics to beats so songwriters; AIsa writes it.",
      "subDiscipline": "lyricist syllable mapping",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for lyricist syllable mapping in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Melody Syllable Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI aligns lyrics to beats so songwriters; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (lyricist syllable mapping).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for lyricist syllable mapping in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for lyricist syllable mapping. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for lyricist syllable mapping.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global songwriting and music publishing",
      "sam": "$800M — lyric writing and collaboration software",
      "som": "$50M — vocal melody and lyric drafting tools"
    },
    {
      "id": "writing-citation-chorus-reader-14",
      "theme": "writing",
      "title": "Citation Chorus Reader",
      "pitch": "Lovable AI synthesizes dense papers so researchers; AIsa writes it.",
      "subDiscipline": "academic literature review",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for academic literature review in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Citation Chorus Reader\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI synthesizes dense papers so researchers; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (academic literature review).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for academic literature review in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for academic literature review. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for academic literature review.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global academic research software",
      "sam": "$600M — literature review and citation tools",
      "som": "$40M — PhD student productivity apps"
    },
    {
      "id": "writing-wanderlust-ambient-narrator-15",
      "theme": "writing",
      "title": "Wanderlust Ambient Narrator",
      "pitch": "Lovable AI describes foreign landscapes so travel writers; AIsa writes it.",
      "subDiscipline": "travel writing scene-setting",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for travel writing scene-setting in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Wanderlust Ambient Narrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI describes foreign landscapes so travel writers; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (travel writing scene-setting).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for travel writing scene-setting in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for travel writing scene-setting. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for travel writing scene-setting.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global travel journalism and blogging",
      "sam": "$200M — travel writing and itinerary tools",
      "som": "$15M — immersive travel journaling apps"
    },
    {
      "id": "writing-client-persona-mimic-16",
      "theme": "writing",
      "title": "Client Persona Mimic",
      "pitch": "Lovable AI adopts client quirks so ghostwriters; AIsa writes it.",
      "subDiscipline": "ghostwriter tone-matching",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for ghostwriter tone-matching in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Client Persona Mimic\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adopts client quirks so ghostwriters; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (ghostwriter tone-matching).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for ghostwriter tone-matching in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for ghostwriter tone-matching. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for ghostwriter tone-matching.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global ghostwriting and freelance writing",
      "sam": "$400M — client management and drafting tools",
      "som": "$30M — executive ghostwriting software"
    },
    {
      "id": "writing-micro-fiction-broadcaster-17",
      "theme": "writing",
      "title": "Micro Fiction Broadcaster",
      "pitch": "Lovable AI writes twist endings so podcasters; AIsa writes it.",
      "subDiscipline": "flash fiction podcasting",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for flash fiction podcasting in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Micro Fiction Broadcaster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes twist endings so podcasters; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (flash fiction podcasting).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for flash fiction podcasting in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for flash fiction podcasting. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for flash fiction podcasting.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global podcasting and short fiction",
      "sam": "$300M — audio drama and flash fiction tools",
      "som": "$20M — indie fiction podcasting apps"
    },
    {
      "id": "writing-combat-bark-crier-18",
      "theme": "writing",
      "title": "Combat Bark Crier",
      "pitch": "Lovable AI generates combat barks so designers; AIsa writes it.",
      "subDiscipline": "video game narrative design",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for video game narrative design in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Combat Bark Crier\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates combat barks so designers; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (video game narrative design).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for video game narrative design in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for video game narrative design. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for video game narrative design.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global game narrative and audio design",
      "sam": "$1B — game writing and dialogue tools",
      "som": "$70M — indie game VO prototyping apps"
    },
    {
      "id": "writing-intimacy-whisper-tester-19",
      "theme": "writing",
      "title": "Intimacy Whisper Tester",
      "pitch": "Lovable AI softens romantic banter so authors; AIsa writes it.",
      "subDiscipline": "romance novel dialogue",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for romance novel dialogue in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Intimacy Whisper Tester\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI softens romantic banter so authors; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (romance novel dialogue).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for romance novel dialogue in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for romance novel dialogue. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for romance novel dialogue.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global romance fiction publishing",
      "sam": "$300M — romance writing and plotting tools",
      "som": "$25M — character chemistry drafting apps"
    },
    {
      "id": "writing-xenolinguistics-vowel-forge-20",
      "theme": "writing",
      "title": "Xenolinguistics Vowel Forge",
      "pitch": "Lovable AI invents alien phonetics so worldbuilders; AIsa writes it.",
      "subDiscipline": "sci-fi language creation",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for sci-fi language creation in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Xenolinguistics Vowel Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents alien phonetics so worldbuilders; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (sci-fi language creation).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for sci-fi language creation in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for sci-fi language creation. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sci-fi language creation.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — global sci-fi and fantasy publishing",
      "sam": "$150M — conlang and worldbuilding software",
      "som": "$10M — speculative fiction language tools"
    },
    {
      "id": "writing-interview-quote-echo-21",
      "theme": "writing",
      "title": "Interview Quote Echo",
      "pitch": "Lovable AI contextualizes raw quotes so journalists; AIsa writes it.",
      "subDiscipline": "journalism fact-checking",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for journalism fact-checking in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Interview Quote Echo\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI contextualizes raw quotes so journalists; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (journalism fact-checking).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for journalism fact-checking in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for journalism fact-checking. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for journalism fact-checking.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global journalism and newsroom software",
      "sam": "$400M — interview transcription and drafting tools",
      "som": "$30M — investigative journalism audio aids"
    },
    {
      "id": "writing-billboard-hook-shouter-22",
      "theme": "writing",
      "title": "Billboard Hook Shouter",
      "pitch": "Lovable AI punches up slogans so copywriters; AIsa writes it.",
      "subDiscipline": "advertising copywriting",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for advertising copywriting in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Billboard Hook Shouter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI punches up slogans so copywriters; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (advertising copywriting).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for advertising copywriting in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for advertising copywriting. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for advertising copywriting.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global copywriting and ad tech",
      "sam": "$600M — creative agency drafting tools",
      "som": "$45M — audio and broadcast ad copy apps"
    },
    {
      "id": "writing-epoch-dialect-tutor-23",
      "theme": "writing",
      "title": "Epoch Dialect Tutor",
      "pitch": "Lovable AI adapts modern slang so historical novelists; AIsa writes it.",
      "subDiscipline": "historical fiction dialects",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for historical fiction dialects in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Epoch Dialect Tutor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adapts modern slang so historical novelists; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (historical fiction dialects).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for historical fiction dialects in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for historical fiction dialects. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for historical fiction dialects.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global historical fiction publishing",
      "sam": "$200M — historical research and writing tools",
      "som": "$15M — period-accurate dialogue drafting apps"
    },
    {
      "id": "writing-blank-page-soother-24",
      "theme": "writing",
      "title": "Blank Page Soother",
      "pitch": "Lovable AI guides breathing exercises so blocked writers; AIsa writes it.",
      "subDiscipline": "writer mindset coaching",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for writer mindset coaching in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Blank Page Soother\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI guides breathing exercises so blocked writers; AIsa writes it.\nDiscipline: Writing, Poetry & Narrative (writer mindset coaching).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for writer mindset coaching in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for writer mindset coaching. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for writer mindset coaching.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global mental wellness and productivity",
      "sam": "$300M — creative coaching and journaling tools",
      "som": "$20M — writer's block and mindfulness apps"
    },
    {
      "id": "writing-scene-sparring-partner-0",
      "theme": "writing",
      "title": "Scene Sparring Partner",
      "pitch": "Lovable AI generates character responses so writers can rehearse; AIsa writes them during live dialogue practice.",
      "subDiscipline": "screenplay rehearsal",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Real-time conversational back-and-forth is essential for rehearsing screenplay dialogue naturally.",
      "megaPrompt": "Build \"Scene Sparring Partner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates character responses so writers can rehearse; AIsa writes them during live dialogue practice.\nDiscipline: Writing, Poetry & Narrative (screenplay rehearsal).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Real-time conversational back-and-forth is essential for rehearsing screenplay dialogue naturally.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for screenplay rehearsal. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for screenplay rehearsal.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and screenwriting software market",
      "sam": "$400M — professional screenwriter and playwright tools segment",
      "som": "$25M — AI dialogue rehearsal and acting prep applications"
    },
    {
      "id": "writing-meter-pacing-guide-1",
      "theme": "writing",
      "title": "Meter Pacing Guide",
      "pitch": "Lovable AI analyzes poetic rhythm so poets can refine flow; AIsa writes the verses to demonstrate cadence.",
      "subDiscipline": "poetry performance",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "A conversational agent allows the poet to interrupt, ask for tempo changes, and hear immediate rhythmic adjustments.",
      "megaPrompt": "Build \"Meter Pacing Guide\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes poetic rhythm so poets can refine flow; AIsa writes the verses to demonstrate cadence.\nDiscipline: Writing, Poetry & Narrative (poetry performance).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: A conversational agent allows the poet to interrupt, ask for tempo changes, and hear immediate rhythmic adjustments.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for poetry performance. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for poetry performance.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and publishing tools market",
      "sam": "$150M — poetry and written word creation software",
      "som": "$10M — AI-assisted poetic meter and performance coaching"
    },
    {
      "id": "writing-lore-interrogation-room-2",
      "theme": "writing",
      "title": "Lore Interrogation Room",
      "pitch": "Lovable AI adopts a citizen persona so authors can test lore; AIsa writes the character's probing questions.",
      "subDiscipline": "fantasy worldbuilding",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for fantasy worldbuilding in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Lore Interrogation Room\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI adopts a citizen persona so authors can test lore; AIsa writes the character's probing questions.\nDiscipline: Writing, Poetry & Narrative (fantasy worldbuilding).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for fantasy worldbuilding in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for fantasy worldbuilding. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for fantasy worldbuilding.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$600M — speculative fiction and fantasy authoring tools",
      "som": "$30M — AI worldbuilding and lore consistency checkers"
    },
    {
      "id": "writing-plot-hole-detective-3",
      "theme": "writing",
      "title": "Plot Hole Detective",
      "pitch": "Lovable AI identifies story inconsistencies so novelists can fix gaps; AIsa writes the critiques like a sharp editor.",
      "subDiscipline": "narrative structure",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for narrative structure in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Plot Hole Detective\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies story inconsistencies so novelists can fix gaps; AIsa writes the critiques like a sharp editor.\nDiscipline: Writing, Poetry & Narrative (narrative structure).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for narrative structure in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for narrative structure. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for narrative structure.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$800M — professional novelist and fiction editing software",
      "som": "$40M — AI structural editing and plot analysis tools"
    },
    {
      "id": "writing-character-voice-audition-4",
      "theme": "writing",
      "title": "Character Voice Audition",
      "pitch": "Lovable AI generates unique monologues so writers can hear personas; AIsa writes each audition with distinct emotional tones.",
      "subDiscipline": "character development",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for character development in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Character Voice Audition\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates unique monologues so writers can hear personas; AIsa writes each audition with distinct emotional tones.\nDiscipline: Writing, Poetry & Narrative (character development).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for character development in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for character development. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for character development.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and character design market",
      "sam": "$500M — fiction writing and roleplay character creation",
      "som": "$20M — AI character voice and persona simulation tools"
    },
    {
      "id": "writing-block-breaking-therapist-5",
      "theme": "writing",
      "title": "Block Breaking Therapist",
      "pitch": "Lovable AI guides mental exercises so stuck writers find inspiration; AIsa writes soothing prompts to unlock creativity.",
      "subDiscipline": "creative mindset",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "A conversational agent provides empathetic, real-time vocal coaching to navigate the emotional friction of writer's block.",
      "megaPrompt": "Build \"Block Breaking Therapist\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI guides mental exercises so stuck writers find inspiration; AIsa writes soothing prompts to unlock creativity.\nDiscipline: Writing, Poetry & Narrative (creative mindset).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: A conversational agent provides empathetic, real-time vocal coaching to navigate the emotional friction of writer's block.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for creative mindset. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for creative mindset.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and wellness market",
      "sam": "$300M — creative coaching and writer wellness applications",
      "som": "$15M — AI mindset and block-breaking companions for authors"
    },
    {
      "id": "writing-bedtime-story-weaver-6",
      "theme": "writing",
      "title": "Bedtime Story Weaver",
      "pitch": "Lovable AI invents branching tales so parents can entertain kids; AIsa writes the interactive narrative as it unfolds.",
      "subDiscipline": "children's literature",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "The agent listens to children's spontaneous choices and speaks the resulting story branches seamlessly in real time.",
      "megaPrompt": "Build \"Bedtime Story Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents branching tales so parents can entertain kids; AIsa writes the interactive narrative as it unfolds.\nDiscipline: Writing, Poetry & Narrative (children's literature).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: The agent listens to children's spontaneous choices and speaks the resulting story branches seamlessly in real time.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for children's literature. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for children's literature.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.0B — global children's book and audio story market",
      "sam": "$400M — interactive bedtime story and parenting apps",
      "som": "$25M — AI-generated real-time interactive children's audio tales"
    },
    {
      "id": "writing-dialect-dialogue-coach-7",
      "theme": "writing",
      "title": "Dialect Dialogue Coach",
      "pitch": "Lovable AI translates standard text into regional slang so writers verify authenticity; AIsa writes the localized dialogue aloud.",
      "subDiscipline": "linguistic authenticity",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Writers can converse with the agent to test how regional idioms sound in spontaneous, natural speech.",
      "megaPrompt": "Build \"Dialect Dialogue Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates standard text into regional slang so writers verify authenticity; AIsa writes the localized dialogue aloud.\nDiscipline: Writing, Poetry & Narrative (linguistic authenticity).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Writers can converse with the agent to test how regional idioms sound in spontaneous, natural speech.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for linguistic authenticity. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for linguistic authenticity.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and linguistics software market",
      "sam": "$200M — dialect, translation, and localization writing tools",
      "som": "$12M — AI dialect coaching for fiction and screenwriting"
    },
    {
      "id": "writing-audio-journaling-muse-8",
      "theme": "writing",
      "title": "Audio Journaling Muse",
      "pitch": "Lovable AI asks deep daily questions so authors can reflect aloud; AIsa writes the prompts and reads intently.",
      "subDiscipline": "reflective journaling",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for reflective journaling in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Audio Journaling Muse\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI asks deep daily questions so authors can reflect aloud; AIsa writes the prompts and reads intently.\nDiscipline: Writing, Poetry & Narrative (reflective journaling).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for reflective journaling in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for reflective journaling. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for reflective journaling.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global journaling and personal development market",
      "sam": "$350M — digital journaling and audio diary applications",
      "som": "$18M — AI conversational journaling for creative professionals"
    },
    {
      "id": "writing-pitch-practice-simulator-9",
      "theme": "writing",
      "title": "Pitch Practice Simulator",
      "pitch": "Lovable AI acts as a tough publisher so authors can rehearse pitches; AIsa writes skeptical industry questions.",
      "subDiscipline": "author marketing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Real-time conversational pushback prepares writers for the high-pressure, unpredictable nature of live verbal book pitches.",
      "megaPrompt": "Build \"Pitch Practice Simulator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI acts as a tough publisher so authors can rehearse pitches; AIsa writes skeptical industry questions.\nDiscipline: Writing, Poetry & Narrative (author marketing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Real-time conversational pushback prepares writers for the high-pressure, unpredictable nature of live verbal book pitches.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for author marketing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for author marketing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global author marketing and publishing tools market",
      "sam": "$250M — book pitching and author presentation software",
      "som": "$10M — AI pitch rehearsal for traditional publishing"
    },
    {
      "id": "writing-sensory-description-weaver-10",
      "theme": "writing",
      "title": "Sensory Description Weaver",
      "pitch": "Lovable AI brainstorms vivid environmental details so writers can enrich scenes; AIsa writes the sensory descriptions evocatively.",
      "subDiscipline": "descriptive prose",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Hearing descriptions spoken aloud helps writers gauge the atmospheric impact and rhythm of their sensory prose.",
      "megaPrompt": "Build \"Sensory Description Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI brainstorms vivid environmental details so writers can enrich scenes; AIsa writes the sensory descriptions evocatively.\nDiscipline: Writing, Poetry & Narrative (descriptive prose).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Hearing descriptions spoken aloud helps writers gauge the atmospheric impact and rhythm of their sensory prose.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for descriptive prose. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for descriptive prose.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$700M — creative writing and prose enhancement software",
      "som": "$30M — AI sensory and descriptive prose generators"
    },
    {
      "id": "writing-tension-tuning-metronome-11",
      "theme": "writing",
      "title": "Tension Tuning Metronome",
      "pitch": "Lovable AI rewrites scenes to accelerate suspense so thriller authors can test pacing; AIsa writes the escalating action.",
      "subDiscipline": "thriller pacing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for thriller pacing in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Tension Tuning Metronome\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites scenes to accelerate suspense so thriller authors can test pacing; AIsa writes the escalating action.\nDiscipline: Writing, Poetry & Narrative (thriller pacing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for thriller pacing in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for thriller pacing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for thriller pacing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$400M — thriller, mystery, and suspense writing tools",
      "som": "$15M — AI pacing and tension analysis for novelists"
    },
    {
      "id": "writing-historical-fact-interrogator-12",
      "theme": "writing",
      "title": "Historical Fact Interrogator",
      "pitch": "Lovable AI cross-references era-specific details so historical novelists avoid anachronisms; AIsa writes the corrections as a period expert.",
      "subDiscipline": "historical fiction",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Conversing with a historical expert persona makes dry fact-checking feel like an immersive, era-appropriate consultation.",
      "megaPrompt": "Build \"Historical Fact Interrogator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI cross-references era-specific details so historical novelists avoid anachronisms; AIsa writes the corrections as a period expert.\nDiscipline: Writing, Poetry & Narrative (historical fiction).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Conversing with a historical expert persona makes dry fact-checking feel like an immersive, era-appropriate consultation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for historical fiction. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for historical fiction.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and research software market",
      "sam": "$300M — historical fiction research and authoring tools",
      "som": "$12M — AI historical accuracy and anachronism checkers"
    },
    {
      "id": "writing-subtext-reading-coach-13",
      "theme": "writing",
      "title": "Subtext Reading Coach",
      "pitch": "Lovable AI analyzes hidden meanings in dialogue so playwrights can deepen subtext; AIsa writes the lines with nuance.",
      "subDiscipline": "dramatic subtext",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "The agent can converse about underlying emotions and demonstrate different vocal inflections to highlight unspoken tensions.",
      "megaPrompt": "Build \"Subtext Reading Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes hidden meanings in dialogue so playwrights can deepen subtext; AIsa writes the lines with nuance.\nDiscipline: Writing, Poetry & Narrative (dramatic subtext).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: The agent can converse about underlying emotions and demonstrate different vocal inflections to highlight unspoken tensions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for dramatic subtext. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dramatic subtext.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global playwriting and screenwriting market",
      "sam": "$250M — dramatic writing and dialogue refinement tools",
      "som": "$10M — AI subtext and dramatic nuance coaching"
    },
    {
      "id": "writing-flash-fiction-sprinter-14",
      "theme": "writing",
      "title": "Flash Fiction Sprinter",
      "pitch": "Lovable AI provides rapid story prompts so writers can practice brevity; AIsa writes the countdowns and constraints aloud.",
      "subDiscipline": "micro-fiction",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "A conversational agent acts as an energetic, real-time host to keep the writer engaged during fast-paced sprints.",
      "megaPrompt": "Build \"Flash Fiction Sprinter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI provides rapid story prompts so writers can practice brevity; AIsa writes the countdowns and constraints aloud.\nDiscipline: Writing, Poetry & Narrative (micro-fiction).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: A conversational agent acts as an energetic, real-time host to keep the writer engaged during fast-paced sprints.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for micro-fiction. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for micro-fiction.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools and software market",
      "sam": "$200M — short story and flash fiction writing apps",
      "som": "$8M — AI sprint hosting and prompt generation"
    },
    {
      "id": "writing-accessibility-beta-reader-15",
      "theme": "writing",
      "title": "Accessibility Beta Reader",
      "pitch": "Lovable AI evaluates screen-reader compatibility so writers can format for visually impaired readers; AIsa writes the formatting glitches.",
      "subDiscipline": "inclusive design",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Hearing the agent stumble over bad formatting in real-time conversation highlights accessibility flaws instantly.",
      "megaPrompt": "Build \"Accessibility Beta Reader\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI evaluates screen-reader compatibility so writers can format for visually impaired readers; AIsa writes the formatting glitches.\nDiscipline: Writing, Poetry & Narrative (inclusive design).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Hearing the agent stumble over bad formatting in real-time conversation highlights accessibility flaws instantly.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for inclusive design. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for inclusive design.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global publishing and accessibility software market",
      "sam": "$150M — inclusive publishing and accessible ebook tools",
      "som": "$5M — AI screen-reader simulation for authors"
    },
    {
      "id": "writing-translation-tone-checker-16",
      "theme": "writing",
      "title": "Translation Tone Checker",
      "pitch": "Lovable AI compares translated nuances so bilingual poets can preserve emotional weight; AIsa writes both versions for comparison.",
      "subDiscipline": "literary translation",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Conversational back-and-forth allows translators to ask the agent to emphasize specific cultural idioms and hear tonal shifts.",
      "megaPrompt": "Build \"Translation Tone Checker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI compares translated nuances so bilingual poets can preserve emotional weight; AIsa writes both versions for comparison.\nDiscipline: Writing, Poetry & Narrative (literary translation).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Conversational back-and-forth allows translators to ask the agent to emphasize specific cultural idioms and hear tonal shifts.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for literary translation. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for literary translation.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.0B — global translation and localization software market",
      "sam": "$400M — literary translation and poetry localization tools",
      "som": "$15M — AI tonal and emotional translation checking"
    },
    {
      "id": "writing-monologue-memory-aid-17",
      "theme": "writing",
      "title": "Monologue Memory Aid",
      "pitch": "Lovable AI breaks down complex soliloquies so actors can memorize beats; AIsa writes the cues and reads for mistakes.",
      "subDiscipline": "actor preparation",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "The agent can dynamically pause, repeat, or correct the actor in real-time during hands-free memorization drills.",
      "megaPrompt": "Build \"Monologue Memory Aid\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI breaks down complex soliloquies so actors can memorize beats; AIsa writes the cues and reads for mistakes.\nDiscipline: Writing, Poetry & Narrative (actor preparation).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: The agent can dynamically pause, repeat, or correct the actor in real-time during hands-free memorization drills.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for actor preparation. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for actor preparation.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global acting and theater preparation market",
      "sam": "$100M — script memorization and actor rehearsal apps",
      "som": "$5M — AI conversational scene partner for actors"
    },
    {
      "id": "writing-epistolary-letter-companion-18",
      "theme": "writing",
      "title": "Epistolary Letter Companion",
      "pitch": "Lovable AI drafts era-accurate letters so narrative designers can build epistolary games; AIsa writes the correspondence like a penpal.",
      "subDiscipline": "historical correspondence",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Conversing with the agent as the penpal helps designers test the intimacy and pacing of letter-based storytelling.",
      "megaPrompt": "Build \"Epistolary Letter Companion\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI drafts era-accurate letters so narrative designers can build epistolary games; AIsa writes the correspondence like a penpal.\nDiscipline: Writing, Poetry & Narrative (historical correspondence).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Conversing with the agent as the penpal helps designers test the intimacy and pacing of letter-based storytelling.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for historical correspondence. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for historical correspondence.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global game narrative and writing tools market",
      "sam": "$300M — interactive fiction and epistolary game design",
      "som": "$12M — AI letter writing and correspondence simulation"
    },
    {
      "id": "writing-comedy-timing-workshop-19",
      "theme": "writing",
      "title": "Comedy Timing Workshop",
      "pitch": "Lovable AI rewrites punchlines for better rhythm so comedy writers can test jokes; AIsa writes the setups and pauses.",
      "subDiscipline": "humorous writing",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for humorous writing in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Comedy Timing Workshop\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI rewrites punchlines for better rhythm so comedy writers can test jokes; AIsa writes the setups and pauses.\nDiscipline: Writing, Poetry & Narrative (humorous writing).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for humorous writing in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for humorous writing. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for humorous writing.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global comedy writing and performance market",
      "sam": "$200M — standup comedy and sitcom writing tools",
      "som": "$8M — AI comedic timing and punchline testing"
    },
    {
      "id": "writing-narrative-branch-tester-20",
      "theme": "writing",
      "title": "Narrative Branch Tester",
      "pitch": "Lovable AI simulates player choices so game writers can test dialogue trees; AIsa writes the branching paths dynamically.",
      "subDiscipline": "interactive fiction",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "AIsa's LLM router is the brain for interactive fiction in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.",
      "megaPrompt": "Build \"Narrative Branch Tester\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI simulates player choices so game writers can test dialogue trees; AIsa writes the branching paths dynamically.\nDiscipline: Writing, Poetry & Narrative (interactive fiction).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: AIsa's LLM router is the brain for interactive fiction in Writing, Poetry & Narrative — one endpoint, any frontier model, so the answer is grounded, fast, and on brand.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for interactive fiction. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for interactive fiction.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.0B — global game narrative and interactive fiction market",
      "sam": "$800M — RPG and branching dialogue writing tools",
      "som": "$40M — AI NPC dialogue tree simulation and testing"
    },
    {
      "id": "writing-lyric-rhyme-sparring-21",
      "theme": "writing",
      "title": "Lyric Rhyme Sparring",
      "pitch": "Lovable AI generates complex rhyme schemes so lyricists can overcome writer's block; AIsa writes the rhythmic suggestions aloud.",
      "subDiscipline": "songwriting",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "The conversational agent allows songwriters to verbally jam, requesting alternate rhymes and hearing them fitted to a beat.",
      "megaPrompt": "Build \"Lyric Rhyme Sparring\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates complex rhyme schemes so lyricists can overcome writer's block; AIsa writes the rhythmic suggestions aloud.\nDiscipline: Writing, Poetry & Narrative (songwriting).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: The conversational agent allows songwriters to verbally jam, requesting alternate rhymes and hearing them fitted to a beat.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for songwriting. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for songwriting.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.0B — global songwriting and music creation market",
      "sam": "$500M — lyric writing and vocal melody tools",
      "som": "$20M — AI lyrical rhyming and rhythmic sparring"
    },
    {
      "id": "writing-public-reading-rehearsal-22",
      "theme": "writing",
      "title": "Public Reading Rehearsal",
      "pitch": "Lovable AI selects engaging excerpts so authors can prepare for book tours; AIsa writes the crowd reactions and prompts.",
      "subDiscipline": "author performance",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "The agent simulates a live audience, conversing between pieces to help the author practice stage banter and pacing.",
      "megaPrompt": "Build \"Public Reading Rehearsal\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI selects engaging excerpts so authors can prepare for book tours; AIsa writes the crowd reactions and prompts.\nDiscipline: Writing, Poetry & Narrative (author performance).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: The agent simulates a live audience, conversing between pieces to help the author practice stage banter and pacing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for author performance. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for author performance.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global author marketing and event prep market",
      "sam": "$150M — book tour preparation and public speaking tools",
      "som": "$6M — AI audience simulation for author readings"
    },
    {
      "id": "writing-metaphor-mining-assistant-23",
      "theme": "writing",
      "title": "Metaphor Mining Assistant",
      "pitch": "Lovable AI brainstorms unique figurative language so poets can avoid cliches; AIsa writes the metaphors to test their resonance.",
      "subDiscipline": "poetic imagery",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Hearing metaphors spoken in a conversational flow helps poets instantly judge if the imagery sounds natural or forced.",
      "megaPrompt": "Build \"Metaphor Mining Assistant\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI brainstorms unique figurative language so poets can avoid cliches; AIsa writes the metaphors to test their resonance.\nDiscipline: Writing, Poetry & Narrative (poetic imagery).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Hearing metaphors spoken in a conversational flow helps poets instantly judge if the imagery sounds natural or forced.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for poetic imagery. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for poetic imagery.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and poetry tools market",
      "sam": "$250M — creative prose and poetic imagery software",
      "som": "$10M — AI figurative language and metaphor generation"
    },
    {
      "id": "writing-sci-fi-jargon-builder-24",
      "theme": "writing",
      "title": "Sci-Fi Jargon Builder",
      "pitch": "Lovable AI invents consistent futuristic slang so sci-fi authors can enrich worldbuilding; AIsa writes the technobabble conversationally.",
      "subDiscipline": "speculative terminology",
      "quantumHook": "AIsa Chat (LLM Router)",
      "quantumHookId": "aisa-chat",
      "quantumTag": "frontier reasoning",
      "quantumRationale": "Conversing with the agent using the new jargon tests whether the speculative terminology feels natural in casual speech.",
      "megaPrompt": "Build \"Sci-Fi Jargon Builder\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents consistent futuristic slang so sci-fi authors can enrich worldbuilding; AIsa writes the technobabble conversationally.\nDiscipline: Writing, Poetry & Narrative (speculative terminology).\nRecipe: AIsa Chat (LLM Router) (frontier reasoning) as the single creative surface.\nWhy this kernel: Conversing with the agent using the new jargon tests whether the speculative terminology feels natural in casual speech.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa chat completions:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const ask = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ topic: z.string().min(1).max(2000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are an expert helper for speculative terminology. ` +\n              `Reply in friendly markdown, under 180 words, with concrete next steps.` },\n          { role: \"user\", content: data.topic },\n        ],\n        temperature: 0.7,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa chat failed: ${r.status}`);\n    const j = await r.json();\n    return { reply: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT (in `src/routes/index.tsx`):\n```tsx\nimport { useServerFn } from \"@tanstack/react-start\";\nimport { useState } from \"react\";\nimport ReactMarkdown from \"react-markdown\";\nimport { ask } from \"@/lib/aisa.functions\";\n\nconst run = useServerFn(ask);\nconst [topic, setTopic] = useState(\"\");\nconst [reply, setReply] = useState<string | null>(null);\nconst [busy, setBusy] = useState(false);\n\nconst onAsk = async () => {\n  setBusy(true); setReply(null);\n  try { const { reply } = await run({ data: { topic } }); setReply(reply); }\n  finally { setBusy(false); }\n};\n```\n\nUse BARE model ids (e.g. `gpt-4o-mini`, `claude-3-5-sonnet`, `gemini-2.5-flash`,\n`qwen2.5-72b`) — NO `openai/`, `anthropic/`, or `google/` prefix. Browse the live\nlist at https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for speculative terminology.\n2. The primary action (a prompt box or chat surface that returns markdown the user can read, copy, or act on) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing and worldbuilding software market",
      "sam": "$400M — science fiction and speculative writing tools",
      "som": "$15M — AI speculative jargon and terminology generation"
    },
    {
      "id": "writing-dialogue-pacing-doctor-0",
      "theme": "writing",
      "title": "Dialogue Pacing Doctor",
      "pitch": "Lovable AI analyzes cadence so writers perfect dialogue rhythm; AIsa captures realtime word timestamps.",
      "subDiscipline": "screenplay dialogue flow",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Word timestamps reveal exact pause lengths and rushed syllables during spoken rehearsals.",
      "megaPrompt": "Build \"Dialogue Pacing Doctor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes cadence so writers perfect dialogue rhythm; AIsa captures realtime word timestamps.\nDiscipline: Writing, Poetry & Narrative (screenplay dialogue flow).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Word timestamps reveal exact pause lengths and rushed syllables during spoken rehearsals.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for screenplay dialogue flow.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for screenplay dialogue flow. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for screenplay dialogue flow.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$400M — screenwriting and playwriting software",
      "som": "$15M — indie screenplay pacing tools"
    },
    {
      "id": "writing-dream-weaver-scribe-1",
      "theme": "writing",
      "title": "Dream Weaver Live Notes",
      "pitch": "Lovable AI structures groggy ramblings so authors capture fleeting dreams; AIsa transcribes sleepy morning audio.",
      "subDiscipline": "surreal narrative drafting",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime streaming catches fragmented, low-energy dream murmurs before the user fully wakes and forgets.",
      "megaPrompt": "Build \"Dream Weaver Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures groggy ramblings so authors capture fleeting dreams; AIsa transcribes sleepy morning audio.\nDiscipline: Writing, Poetry & Narrative (surreal narrative drafting).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime streaming catches fragmented, low-energy dream murmurs before the user fully wakes and forgets.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for surreal narrative drafting.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for surreal narrative drafting. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for surreal narrative drafting.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$300M — fiction and novel drafting software",
      "som": "$10M — surrealism and dream journaling apps"
    },
    {
      "id": "writing-lore-walker-catcher-2",
      "theme": "writing",
      "title": "Lore Walker Catcher",
      "pitch": "Lovable AI organizes scattered thoughts so fantasy authors build wikis; AIsa captures hiking dictation seamlessly.",
      "subDiscipline": "mobile worldbuilding",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Continuous streaming handles long, uninterrupted outdoor walks without dropping connection or losing worldbuilding context.",
      "megaPrompt": "Build \"Lore Walker Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI organizes scattered thoughts so fantasy authors build wikis; AIsa captures hiking dictation seamlessly.\nDiscipline: Writing, Poetry & Narrative (mobile worldbuilding).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Continuous streaming handles long, uninterrupted outdoor walks without dropping connection or losing worldbuilding context.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for mobile worldbuilding.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for mobile worldbuilding. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for mobile worldbuilding.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — fantasy and sci-fi authoring tools",
      "som": "$20M — mobile worldbuilding companions"
    },
    {
      "id": "writing-slam-breath-coach-3",
      "theme": "writing",
      "title": "Slam Breath Coach",
      "pitch": "Lovable AI identifies rushed syllables so poets optimize stage breathing; AIsa captures precise word timestamps.",
      "subDiscipline": "written word rehearsal",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Timestamps map exact millisecond gaps between words to pinpoint where performers need to breathe.",
      "megaPrompt": "Build \"Slam Breath Coach\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies rushed syllables so poets optimize stage breathing; AIsa captures precise word timestamps.\nDiscipline: Writing, Poetry & Narrative (written word rehearsal).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Timestamps map exact millisecond gaps between words to pinpoint where performers need to breathe.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for written word rehearsal.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for written word rehearsal. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for written word rehearsal.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$150M — poetry and written word platforms",
      "som": "$5M — performance rehearsal aids"
    },
    {
      "id": "writing-protagonist-interrogation-room-4",
      "theme": "writing",
      "title": "Protagonist Interrogation Room",
      "pitch": "Lovable AI extracts psychological traits so novelists deepen protagonists; AIsa captures improvised character rants.",
      "subDiscipline": "character psychology profiling",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime transcription allows writers to stay in character and maintain emotional momentum without typing.",
      "megaPrompt": "Build \"Protagonist Interrogation Room\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts psychological traits so novelists deepen protagonists; AIsa captures improvised character rants.\nDiscipline: Writing, Poetry & Narrative (character psychology profiling).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime transcription allows writers to stay in character and maintain emotional momentum without typing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for character psychology profiling.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for character psychology profiling. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for character psychology profiling.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$600M — character development and plotting software",
      "som": "$25M — psychological profiling tools for authors"
    },
    {
      "id": "writing-commute-beat-mapper-5",
      "theme": "writing",
      "title": "Commute Beat Mapper",
      "pitch": "Lovable AI maps written ideas so drivers outline plots safely; AIsa captures dashboard brainstorming audio.",
      "subDiscipline": "structural outlining",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Hands-free streaming lets commuters dictate structural beats while keeping their eyes on the road.",
      "megaPrompt": "Build \"Commute Beat Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps written ideas so drivers outline plots safely; AIsa captures dashboard brainstorming audio.\nDiscipline: Writing, Poetry & Narrative (structural outlining).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Hands-free streaming lets commuters dictate structural beats while keeping their eyes on the road.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for structural outlining.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for structural outlining. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for structural outlining.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$450M — story outlining and structure software",
      "som": "$18M — hands-free mobile outlining apps"
    },
    {
      "id": "writing-freestyle-rhyme-net-6",
      "theme": "writing",
      "title": "Freestyle Rhyme Net",
      "pitch": "Lovable AI highlights missed rhymes so rappers refine freestyles; AIsa captures rapid-fire vocal improvisation.",
      "subDiscipline": "lyricist improvisation",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Low-latency partial transcripts let artists see their rhymes materialize instantly without breaking their flow.",
      "megaPrompt": "Build \"Freestyle Rhyme Net\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI highlights missed rhymes so rappers refine freestyles; AIsa captures rapid-fire vocal improvisation.\nDiscipline: Writing, Poetry & Narrative (lyricist improvisation).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Low-latency partial transcripts let artists see their rhymes materialize instantly without breaking their flow.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for lyricist improvisation.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for lyricist improvisation. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for lyricist improvisation.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$250M — songwriting and lyricist software",
      "som": "$12M — hip-hop freestyle training tools"
    },
    {
      "id": "writing-scene-tension-grapher-7",
      "theme": "writing",
      "title": "Scene Tension Grapher",
      "pitch": "Lovable AI graphs emotional tension so playwrights pace climaxes; AIsa captures actor rehearsal pauses.",
      "subDiscipline": "dramatic arc analysis",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Word timestamps measure the exact duration of dramatic pauses to quantify scene tension.",
      "megaPrompt": "Build \"Scene Tension Grapher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI graphs emotional tension so playwrights pace climaxes; AIsa captures actor rehearsal pauses.\nDiscipline: Writing, Poetry & Narrative (dramatic arc analysis).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Word timestamps measure the exact duration of dramatic pauses to quantify scene tension.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for dramatic arc analysis.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for dramatic arc analysis. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for dramatic arc analysis.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$350M — theater and playwriting software",
      "som": "$14M — dramatic pacing analytics"
    },
    {
      "id": "writing-live-verse-projector-8",
      "theme": "writing",
      "title": "Live Verse Projector",
      "pitch": "Lovable AI formats kinetic typography so venues project live poetry; AIsa captures stage performances instantly.",
      "subDiscipline": "accessible performance captioning",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime partial transcripts enable low-latency, word-by-word caption projection for deaf audience members.",
      "megaPrompt": "Build \"Live Verse Projector\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI formats kinetic typography so venues project live poetry; AIsa captures stage performances instantly.\nDiscipline: Writing, Poetry & Narrative (accessible performance captioning).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime partial transcripts enable low-latency, word-by-word caption projection for deaf audience members.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for accessible performance captioning.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for accessible performance captioning. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for accessible performance captioning.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$100M — live event accessibility software",
      "som": "$8M — poetry slam captioning tools"
    },
    {
      "id": "writing-bedtime-story-spinner-9",
      "theme": "writing",
      "title": "Bedtime Story Spinner",
      "pitch": "Lovable AI weaves magical tales so parents entertain toddlers; AIsa captures whispered story prompts.",
      "subDiscipline": "interactive parenting narrative",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Streaming transcription accurately catches quiet, whispered prompts without waking the child or requiring screens.",
      "megaPrompt": "Build \"Bedtime Story Spinner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI weaves magical tales so parents entertain toddlers; AIsa captures whispered story prompts.\nDiscipline: Writing, Poetry & Narrative (interactive parenting narrative).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Streaming transcription accurately catches quiet, whispered prompts without waking the child or requiring screens.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for interactive parenting narrative.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for interactive parenting narrative. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for interactive parenting narrative.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$200M — family and parenting creative apps",
      "som": "$10M — interactive bedtime story generators"
    },
    {
      "id": "writing-pacing-thesis-forger-10",
      "theme": "writing",
      "title": "Pacing Thesis Forger",
      "pitch": "Lovable AI formalizes written arguments so researchers draft papers; AIsa captures pacing office dictation.",
      "subDiscipline": "academic drafting",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Continuous audio streaming accommodates the long, complex, multi-clause sentences typical of academic thought processes.",
      "megaPrompt": "Build \"Pacing Thesis Forger\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI formalizes written arguments so researchers draft papers; AIsa captures pacing office dictation.\nDiscipline: Writing, Poetry & Narrative (academic drafting).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Continuous audio streaming accommodates the long, complex, multi-clause sentences typical of academic thought processes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for academic drafting.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for academic drafting. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for academic drafting.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$800M — academic and research writing software",
      "som": "$30M — verbal thesis drafting tools"
    },
    {
      "id": "writing-dialect-authenticity-checker-11",
      "theme": "writing",
      "title": "Dialect Authenticity Checker",
      "pitch": "Lovable AI flags regional inaccuracies so authors perfect dialects; AIsa captures written character voices.",
      "subDiscipline": "linguistic worldbuilding",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime phonetic transcription helps writers immediately verify if their spoken accent sounds authentic.",
      "megaPrompt": "Build \"Dialect Authenticity Checker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI flags regional inaccuracies so authors perfect dialects; AIsa captures written character voices.\nDiscipline: Writing, Poetry & Narrative (linguistic worldbuilding).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime phonetic transcription helps writers immediately verify if their spoken accent sounds authentic.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for linguistic worldbuilding.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for linguistic worldbuilding. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for linguistic worldbuilding.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — historical and regional fiction tools",
      "som": "$22M — dialect and linguistic accuracy checkers"
    },
    {
      "id": "writing-standup-pause-analyzer-12",
      "theme": "writing",
      "title": "Standup Pause Analyzer",
      "pitch": "Lovable AI optimizes punchline timing so comedians maximize laughs; AIsa captures rehearsal laugh gaps.",
      "subDiscipline": "comedy set rehearsal",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Timestamps calculate the exact silence duration before punchlines to refine comedic delivery.",
      "megaPrompt": "Build \"Standup Pause Analyzer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI optimizes punchline timing so comedians maximize laughs; AIsa captures rehearsal laugh gaps.\nDiscipline: Writing, Poetry & Narrative (comedy set rehearsal).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Timestamps calculate the exact silence duration before punchlines to refine comedic delivery.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for comedy set rehearsal.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for comedy set rehearsal. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for comedy set rehearsal.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$150M — comedy writing and rehearsal software",
      "som": "$7M — standup timing analytics"
    },
    {
      "id": "writing-memoir-reframing-scribe-13",
      "theme": "writing",
      "title": "Memoir Reframing Live Notes",
      "pitch": "Lovable AI reframes painful memories so survivors write memoirs; AIsa captures emotional journaling sessions.",
      "subDiscipline": "therapeutic life writing",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Streaming audio provides a safe, uninterrupted space for vulnerable emotional processing without typing barriers.",
      "megaPrompt": "Build \"Memoir Reframing Live Notes\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI reframes painful memories so survivors write memoirs; AIsa captures emotional journaling sessions.\nDiscipline: Writing, Poetry & Narrative (therapeutic life writing).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Streaming audio provides a safe, uninterrupted space for vulnerable emotional processing without typing barriers.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for therapeutic life writing.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for therapeutic life writing. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for therapeutic life writing.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$300M — journaling and memoir software",
      "som": "$15M — therapeutic narrative processing tools"
    },
    {
      "id": "writing-dungeon-master-dictator-14",
      "theme": "writing",
      "title": "Dungeon Master Dictator",
      "pitch": "Lovable AI generates sensory descriptions so game masters prep faster; AIsa captures miniature painting commentary.",
      "subDiscipline": "tabletop RPG prep",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "AIsa Skills (web / scholar / trends / market data) ground tabletop RPG prep in Writing, Poetry & Narrative with real-world signal so the LLM's output is sourced, not invented.",
      "megaPrompt": "Build \"Dungeon Master Dictator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI generates sensory descriptions so game masters prep faster; AIsa captures miniature painting commentary.\nDiscipline: Writing, Poetry & Narrative (tabletop RPG prep).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: AIsa Skills (web / scholar / trends / market data) ground tabletop RPG prep in Writing, Poetry & Narrative with real-world signal so the LLM's output is sourced, not invented.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for tabletop RPG prep.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for tabletop RPG prep. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tabletop RPG prep.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$400M — tabletop RPG and game master tools",
      "som": "$18M — hands-free campaign prep software"
    },
    {
      "id": "writing-fluent-fable-weaver-15",
      "theme": "writing",
      "title": "Fluent Fable Weaver",
      "pitch": "Lovable AI corrects grammar so students learn via storytelling; AIsa captures foreign language practice.",
      "subDiscipline": "language acquisition narrative",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime feedback on spoken syntax helps language learners adjust their narrative grammar on the fly.",
      "megaPrompt": "Build \"Fluent Fable Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI corrects grammar so students learn via storytelling; AIsa captures foreign language practice.\nDiscipline: Writing, Poetry & Narrative (language acquisition narrative).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime feedback on spoken syntax helps language learners adjust their narrative grammar on the fly.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for language acquisition narrative.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for language acquisition narrative. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for language acquisition narrative.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$600M — language learning and immersion software",
      "som": "$25M — narrative-based language acquisition tools"
    },
    {
      "id": "writing-eulogy-memory-loom-16",
      "theme": "writing",
      "title": "Eulogy Memory Loom",
      "pitch": "Lovable AI structures grieving thoughts so families write eulogies; AIsa captures tearful memory sharing.",
      "subDiscipline": "commemorative writing",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "AIsa Skills (web / scholar / trends / market data) ground commemorative writing in Writing, Poetry & Narrative with real-world signal so the LLM's output is sourced, not invented.",
      "megaPrompt": "Build \"Eulogy Memory Loom\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures grieving thoughts so families write eulogies; AIsa captures tearful memory sharing.\nDiscipline: Writing, Poetry & Narrative (commemorative writing).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: AIsa Skills (web / scholar / trends / market data) ground commemorative writing in Writing, Poetry & Narrative with real-world signal so the LLM's output is sourced, not invented.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for commemorative writing.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for commemorative writing. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for commemorative writing.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$100M — personal and commemorative writing software",
      "som": "$5M — grief processing and eulogy drafting aids"
    },
    {
      "id": "writing-query-pitch-polisher-17",
      "theme": "writing",
      "title": "Query Pitch Polisher",
      "pitch": "Lovable AI critiques hook strength so authors nail queries; AIsa captures empty-room pitch rehearsals.",
      "subDiscipline": "publishing industry prep",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime transcription allows writers to hear and read their verbal pitches simultaneously to spot awkward phrasing.",
      "megaPrompt": "Build \"Query Pitch Polisher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI critiques hook strength so authors nail queries; AIsa captures empty-room pitch rehearsals.\nDiscipline: Writing, Poetry & Narrative (publishing industry prep).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime transcription allows writers to hear and read their verbal pitches simultaneously to spot awkward phrasing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for publishing industry prep.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for publishing industry prep. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for publishing industry prep.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$350M — publishing and query letter software",
      "som": "$12M — author pitch rehearsal tools"
    },
    {
      "id": "writing-sensory-metaphor-injector-18",
      "theme": "writing",
      "title": "Sensory Metaphor Injector",
      "pitch": "Lovable AI injects vivid metaphors so novelists enrich prose; AIsa captures kitchen counter brainstorming.",
      "subDiscipline": "descriptive prose enhancement",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "AIsa Skills (web / scholar / trends / market data) ground descriptive prose enhancement in Writing, Poetry & Narrative with real-world signal so the LLM's output is sourced, not invented.",
      "megaPrompt": "Build \"Sensory Metaphor Injector\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI injects vivid metaphors so novelists enrich prose; AIsa captures kitchen counter brainstorming.\nDiscipline: Writing, Poetry & Narrative (descriptive prose enhancement).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: AIsa Skills (web / scholar / trends / market data) ground descriptive prose enhancement in Writing, Poetry & Narrative with real-world signal so the LLM's output is sourced, not invented.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for descriptive prose enhancement.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for descriptive prose enhancement. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for descriptive prose enhancement.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — prose enhancement and editing software",
      "som": "$20M — sensory description generators"
    },
    {
      "id": "writing-courtroom-jargon-referee-19",
      "theme": "writing",
      "title": "Courtroom Jargon Referee",
      "pitch": "Lovable AI flags legal inaccuracies so thriller writers maintain realism; AIsa captures dictated trial scenes.",
      "subDiscipline": "procedural accuracy checking",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime transcription instantly catches mispronounced or misused legal terminology during rapid-fire courtroom dialogue dictation.",
      "megaPrompt": "Build \"Courtroom Jargon Referee\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI flags legal inaccuracies so thriller writers maintain realism; AIsa captures dictated trial scenes.\nDiscipline: Writing, Poetry & Narrative (procedural accuracy checking).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime transcription instantly catches mispronounced or misused legal terminology during rapid-fire courtroom dialogue dictation.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for procedural accuracy checking.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for procedural accuracy checking. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for procedural accuracy checking.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$400M — thriller and procedural fiction tools",
      "som": "$16M — legal jargon accuracy checkers"
    },
    {
      "id": "writing-nature-haiku-catcher-20",
      "theme": "writing",
      "title": "Nature Haiku Catcher",
      "pitch": "Lovable AI formats syllables so poets capture nature haikus; AIsa captures breath-paced outdoor whispers.",
      "subDiscipline": "minimalist poetry drafting",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Word timestamps help the AI count syllables and measure the natural breathing pauses of the poet.",
      "megaPrompt": "Build \"Nature Haiku Catcher\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI formats syllables so poets capture nature haikus; AIsa captures breath-paced outdoor whispers.\nDiscipline: Writing, Poetry & Narrative (minimalist poetry drafting).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Word timestamps help the AI count syllables and measure the natural breathing pauses of the poet.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for minimalist poetry drafting.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for minimalist poetry drafting. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for minimalist poetry drafting.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$150M — poetry drafting and formatting software",
      "som": "$6M — minimalist and nature poetry aids"
    },
    {
      "id": "writing-trope-subversion-outliner-21",
      "theme": "writing",
      "title": "Trope Subversion Outliner",
      "pitch": "Lovable AI subverts cliches so fanfiction writers plot twists; AIsa captures enthusiastic what-if rants.",
      "subDiscipline": "fanfiction plotting",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Low-latency transcription keeps up with the rapid, excited speech patterns of passionate fandom brainstorming.",
      "megaPrompt": "Build \"Trope Subversion Outliner\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI subverts cliches so fanfiction writers plot twists; AIsa captures enthusiastic what-if rants.\nDiscipline: Writing, Poetry & Narrative (fanfiction plotting).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Low-latency transcription keeps up with the rapid, excited speech patterns of passionate fandom brainstorming.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for fanfiction plotting.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for fanfiction plotting. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for fanfiction plotting.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$250M — fanfiction and community writing platforms",
      "som": "$10M — trope analysis and plotting tools"
    },
    {
      "id": "writing-audio-drama-formatter-22",
      "theme": "writing",
      "title": "Audio Drama Formatter",
      "pitch": "Lovable AI structures dialogue so podcasters format scripts; AIsa captures multi-actor table reads.",
      "subDiscipline": "podcast script formatting",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime streaming differentiates the continuous flow of a table read into properly formatted audio drama cues.",
      "megaPrompt": "Build \"Audio Drama Formatter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI structures dialogue so podcasters format scripts; AIsa captures multi-actor table reads.\nDiscipline: Writing, Poetry & Narrative (podcast script formatting).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime streaming differentiates the continuous flow of a table read into properly formatted audio drama cues.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for podcast script formatting.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for podcast script formatting. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for podcast script formatting.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$300M — podcasting and audio drama software",
      "som": "$14M — audio script formatting tools"
    },
    {
      "id": "writing-culinary-story-extractor-23",
      "theme": "writing",
      "title": "Culinary Story Extractor",
      "pitch": "Lovable AI extracts engaging narratives so chefs write blogs; AIsa captures messy-hands cooking commentary.",
      "subDiscipline": "food blogging narrative",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "AIsa Skills (web / scholar / trends / market data) ground food blogging narrative in Writing, Poetry & Narrative with real-world signal so the LLM's output is sourced, not invented.",
      "megaPrompt": "Build \"Culinary Story Extractor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts engaging narratives so chefs write blogs; AIsa captures messy-hands cooking commentary.\nDiscipline: Writing, Poetry & Narrative (food blogging narrative).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: AIsa Skills (web / scholar / trends / market data) ground food blogging narrative in Writing, Poetry & Narrative with real-world signal so the LLM's output is sourced, not invented.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for food blogging narrative.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for food blogging narrative. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for food blogging narrative.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$200M — food blogging and recipe software",
      "som": "$9M — culinary narrative extraction tools"
    },
    {
      "id": "writing-technobabble-consistency-engine-24",
      "theme": "writing",
      "title": "Technobabble Consistency Engine",
      "pitch": "Lovable AI standardizes fictional terms so sci-fi authors maintain lore; AIsa captures spaceship repair dictation.",
      "subDiscipline": "sci-fi prose drafting",
      "quantumHook": "AIsa Skills & Live Data",
      "quantumHookId": "aisa-skills",
      "quantumTag": "real-world signal",
      "quantumRationale": "Realtime transcription allows the AI to instantly flag when a spoken made-up word deviates from established lore.",
      "megaPrompt": "Build \"Technobabble Consistency Engine\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI standardizes fictional terms so sci-fi authors maintain lore; AIsa captures spaceship repair dictation.\nDiscipline: Writing, Poetry & Narrative (sci-fi prose drafting).\nRecipe: AIsa Skills & Live Data (real-world signal) as the single creative surface.\nWhy this kernel: Realtime transcription allows the AI to instantly flag when a spoken made-up word deviates from established lore.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa Skill + LLM grounding:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const research = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ query: z.string().min(1).max(400) }).parse(d))\n  .handler(async ({ data }) => {\n    // 1. SKILL — pull live web/search signal for the topic.\n    const sk = await fetch(\"https://api.aisa.one/v1/skills/aisa-tavily-search\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({ query: data.query, max_results: 6 }),\n    });\n    if (sk.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (!sk.ok) throw new Error(`AIsa skill failed: ${sk.status}`);\n    const skill = await sk.json();\n\n    // 2. BRAIN — frontier LLM synthesises a grounded answer for sci-fi prose drafting.\n    const chat = await fetch(\"https://api.aisa.one/v1/chat/completions\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"gpt-4o-mini\",\n        messages: [\n          { role: \"system\", content: `You are a research aide for sci-fi prose drafting. ` +\n              `Synthesise the supplied sources into a sourced, markdown answer ` +\n              `with inline links [Title](url). Be concrete, never invent facts.` },\n          { role: \"user\", content: `Question: ${data.query}\\n\\nSources JSON:\\n${JSON.stringify(skill).slice(0, 6000)}` },\n        ],\n      }),\n    });\n    if (!chat.ok) throw new Error(`AIsa chat failed: ${chat.status}`);\n    const j = await chat.json();\n    return { answer: j.choices[0].message.content as string };\n  });\n```\n\nCLIENT: search-style input + \"Research\" button. Render the markdown answer\n(react-markdown). Pick the right skill from https://aisa.one/skills — Tavily\nsearch, YouTube SERP, scholar, market data, perplexity research, etc.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sci-fi prose drafting.\n2. The primary action (a search/topic box that returns citation-grade results, trend signals, or research the user can drop into the work) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global writing tools market",
      "sam": "$500M — sci-fi and fantasy authoring tools",
      "som": "$22M — fictional terminology consistency checkers"
    },
    {
      "id": "writing-script-foley-forge-0",
      "theme": "writing",
      "title": "Script Foley Forge",
      "pitch": "Lovable AI extracts scene actions so Foley artists rehearse; AIsa generates SFX for it.",
      "subDiscipline": "screenplay foley",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation provides realistic, customizable sounds that pre-recorded libraries cannot match for specific script actions.",
      "megaPrompt": "Build \"Script Foley Forge\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts scene actions so Foley artists rehearse; AIsa generates SFX for it.\nDiscipline: Writing, Poetry & Narrative (screenplay foley).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation provides realistic, customizable sounds that pre-recorded libraries cannot match for specific script actions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for screenplay foley: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for screenplay foley.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global audio post-production software",
      "sam": "$500M — indie film and theater sound design tools",
      "som": "$20M — freelance Foley artist workflow apps"
    },
    {
      "id": "writing-stanza-vibe-scorer-1",
      "theme": "writing",
      "title": "Stanza Vibe Scorer",
      "pitch": "Lovable AI extracts poetic mood so performers feel the vibe; AIsa scores a backing track.",
      "subDiscipline": "poetry performance",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation creates unique, copyright-free atmospheric backing tracks tailored precisely to the emotional cadence of the poem.",
      "megaPrompt": "Build \"Stanza Vibe Scorer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI extracts poetic mood so performers feel the vibe; AIsa scores a backing track.\nDiscipline: Writing, Poetry & Narrative (poetry performance).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation creates unique, copyright-free atmospheric backing tracks tailored precisely to the emotional cadence of the poem.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for poetry performance: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for poetry performance.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global live performance and event software",
      "sam": "$150M — written word and poetry event platforms",
      "som": "$5M — indie poet performance and touring tools"
    },
    {
      "id": "writing-dread-pacing-engine-2",
      "theme": "writing",
      "title": "Dread Pacing Engine",
      "pitch": "Lovable AI paces horror chapters so authors test anxiety; AIsa generates creeping SFX for it.",
      "subDiscipline": "horror drafting",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Short SFX generation allows writers to test specific, unsettling audio cues that match the exact tension beats of their prose.",
      "megaPrompt": "Build \"Dread Pacing Engine\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI paces horror chapters so authors test anxiety; AIsa generates creeping SFX for it.\nDiscipline: Writing, Poetry & Narrative (horror drafting).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Short SFX generation allows writers to test specific, unsettling audio cues that match the exact tension beats of their prose.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for horror drafting: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for horror drafting.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$300M — genre fiction drafting and plotting tools",
      "som": "$15M — horror and thriller author productivity apps"
    },
    {
      "id": "writing-xenotech-audio-blueprint-3",
      "theme": "writing",
      "title": "Xenotech Audio Blueprint",
      "pitch": "Lovable AI describes alien tech so narrative designers build worlds; AIsa generates SFX for it.",
      "subDiscipline": "sci-fi worldbuilding",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Generative SFX allows narrative designers to instantly prototype futuristic, non-existent mechanical sounds directly from text descriptions.",
      "megaPrompt": "Build \"Xenotech Audio Blueprint\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI describes alien tech so narrative designers build worlds; AIsa generates SFX for it.\nDiscipline: Writing, Poetry & Narrative (sci-fi worldbuilding).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Generative SFX allows narrative designers to instantly prototype futuristic, non-existent mechanical sounds directly from text descriptions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for sci-fi worldbuilding: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sci-fi worldbuilding.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development and engine software",
      "sam": "$1.2B — narrative design and worldbuilding tools",
      "som": "$40M — indie sci-fi game audio asset pipelines"
    },
    {
      "id": "writing-tavern-tale-weaver-4",
      "theme": "writing",
      "title": "Tavern Tale Weaver",
      "pitch": "Lovable AI outlines tabletop sessions so game masters prep; AIsa scores a tavern track.",
      "subDiscipline": "tabletop RPG prep",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation provides seamless, mood-appropriate background scores that adapt to the specific setting outlined by the GM.",
      "megaPrompt": "Build \"Tavern Tale Weaver\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI outlines tabletop sessions so game masters prep; AIsa scores a tavern track.\nDiscipline: Writing, Poetry & Narrative (tabletop RPG prep).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation provides seamless, mood-appropriate background scores that adapt to the specific setting outlined by the GM.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for tabletop RPG prep: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tabletop RPG prep.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global tabletop gaming and accessories market",
      "sam": "$400M — digital GM tools and virtual tabletop software",
      "som": "$25M — TTRPG audio and ambiance companion apps"
    },
    {
      "id": "writing-climax-swell-composer-5",
      "theme": "writing",
      "title": "Climax Swell Composer",
      "pitch": "Lovable AI identifies emotional climaxes so audiobook narrators pace; AIsa scores a sweeping orchestral track.",
      "subDiscipline": "romance audiobooks",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Studio track generation gives narrators custom emotional swells to practice against, ensuring their vocal delivery matches the musical crescendo.",
      "megaPrompt": "Build \"Climax Swell Composer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI identifies emotional climaxes so audiobook narrators pace; AIsa scores a sweeping orchestral track.\nDiscipline: Writing, Poetry & Narrative (romance audiobooks).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Studio track generation gives narrators custom emotional swells to practice against, ensuring their vocal delivery matches the musical crescendo.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for romance audiobooks: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for romance audiobooks.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global audiobook production and distribution",
      "sam": "$800M — audiobook narrator software and coaching tools",
      "som": "$30M — romance genre audiobook production suites"
    },
    {
      "id": "writing-pursuit-momentum-tracker-6",
      "theme": "writing",
      "title": "Pursuit Momentum Tracker",
      "pitch": "Lovable AI maps chase sequences so writers maintain momentum; AIsa generates high-BPM SFX for it.",
      "subDiscipline": "thriller writing",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "High-tempo SFX generation acts as a metronome for action writers, keeping their typing speed and prose rhythm aligned with the chase.",
      "megaPrompt": "Build \"Pursuit Momentum Tracker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI maps chase sequences so writers maintain momentum; AIsa generates high-BPM SFX for it.\nDiscipline: Writing, Poetry & Narrative (thriller writing).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: High-tempo SFX generation acts as a metronome for action writers, keeping their typing speed and prose rhythm aligned with the chase.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for thriller writing: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for thriller writing.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$200M — distraction-free writing and focus apps",
      "som": "$10M — thriller and action novelist workflow tools"
    },
    {
      "id": "writing-syllable-nature-sonifier-7",
      "theme": "writing",
      "title": "Syllable Nature Sonifier",
      "pitch": "Lovable AI interprets seasonal imagery so meditation apps relax users; AIsa generates nature SFX for it.",
      "subDiscipline": "haiku meditation",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "AIsa Image Generation matches haiku meditation in Writing, Poetry & Narrative because the work is visual — the user types a brief and a finished frame appears in seconds.",
      "megaPrompt": "Build \"Syllable Nature Sonifier\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI interprets seasonal imagery so meditation apps relax users; AIsa generates nature SFX for it.\nDiscipline: Writing, Poetry & Narrative (haiku meditation).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: AIsa Image Generation matches haiku meditation in Writing, Poetry & Narrative because the work is visual — the user types a brief and a finished frame appears in seconds.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for haiku meditation: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for haiku meditation.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global meditation and wellness app market",
      "sam": "$500M — mindfulness audio and soundscape libraries",
      "som": "$15M — poetry-integrated meditation and breathwork tools"
    },
    {
      "id": "writing-neon-dystopia-drone-8",
      "theme": "writing",
      "title": "Neon Dystopia Drone",
      "pitch": "Lovable AI builds dystopian bibles so authors focus deeply; AIsa scores a dark synth track.",
      "subDiscipline": "cyberpunk focus",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation produces long-form, immersive synthwave drones that block out distractions and set a gritty cyberpunk tone for deep work.",
      "megaPrompt": "Build \"Neon Dystopia Drone\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI builds dystopian bibles so authors focus deeply; AIsa scores a dark synth track.\nDiscipline: Writing, Poetry & Narrative (cyberpunk focus).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation produces long-form, immersive synthwave drones that block out distractions and set a gritty cyberpunk tone for deep work.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for cyberpunk focus: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for cyberpunk focus.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global productivity and focus software",
      "sam": "$300M — ambient noise and focus music generators",
      "som": "$12M — speculative fiction author deep-work environments"
    },
    {
      "id": "writing-fable-magic-spark-9",
      "theme": "writing",
      "title": "Fable Magic Spark",
      "pitch": "Lovable AI writes fairy tale scenes so parents entertain kids; AIsa generates whimsical SFX for it.",
      "subDiscipline": "children's interactive books",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation provides instant, magical audio cues that parents can trigger to make bedtime storytelling highly interactive and engaging.",
      "megaPrompt": "Build \"Fable Magic Spark\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes fairy tale scenes so parents entertain kids; AIsa generates whimsical SFX for it.\nDiscipline: Writing, Poetry & Narrative (children's interactive books).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation provides instant, magical audio cues that parents can trigger to make bedtime storytelling highly interactive and engaging.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for children's interactive books: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for children's interactive books.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global children's book and edutainment market",
      "sam": "$1.5B — interactive storytelling and read-along apps",
      "som": "$50M — parent-led interactive bedtime story tools"
    },
    {
      "id": "writing-skirmish-clash-drafter-10",
      "theme": "writing",
      "title": "Skirmish Clash Drafter",
      "pitch": "Lovable AI researches war skirmishes so novelists draft immersively; AIsa generates battlefield SFX for it.",
      "subDiscipline": "historical fiction",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Generative SFX allows historical novelists to hear authentic, era-specific battle acoustics, grounding their combat descriptions in sensory reality.",
      "megaPrompt": "Build \"Skirmish Clash Drafter\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI researches war skirmishes so novelists draft immersively; AIsa generates battlefield SFX for it.\nDiscipline: Writing, Poetry & Narrative (historical fiction).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Generative SFX allows historical novelists to hear authentic, era-specific battle acoustics, grounding their combat descriptions in sensory reality.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for historical fiction: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for historical fiction.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$250M — historical research and reference tools for writers",
      "som": "$8M — military and historical fiction drafting aids"
    },
    {
      "id": "writing-twist-stinger-cue-11",
      "theme": "writing",
      "title": "Twist Stinger Cue",
      "pitch": "Lovable AI highlights plot twists so podcast narrators emphasize; AIsa generates short musical stingers.",
      "subDiscipline": "mystery podcasting",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Short music generation creates precise, dramatic audio stingers that narrators can drop exactly when the script reveals a clue.",
      "megaPrompt": "Build \"Twist Stinger Cue\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI highlights plot twists so podcast narrators emphasize; AIsa generates short musical stingers.\nDiscipline: Writing, Poetry & Narrative (mystery podcasting).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Short music generation creates precise, dramatic audio stingers that narrators can drop exactly when the script reveals a clue.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for mystery podcasting: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for mystery podcasting.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global podcasting hosting and production software",
      "sam": "$600M — narrative and fiction podcast production tools",
      "som": "$25M — mystery and true-crime podcast audio cue libraries"
    },
    {
      "id": "writing-nostalgia-memoir-loop-12",
      "theme": "writing",
      "title": "Nostalgia Memoir Loop",
      "pitch": "Lovable AI summarizes childhood memories so journalists reflect deeply; AIsa scores a lo-fi nostalgic track.",
      "subDiscipline": "journalistic journaling",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation crafts personalized, evocative lo-fi loops that help memoirists unlock deep emotional memories during the journaling process.",
      "megaPrompt": "Build \"Nostalgia Memoir Loop\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI summarizes childhood memories so journalists reflect deeply; AIsa scores a lo-fi nostalgic track.\nDiscipline: Writing, Poetry & Narrative (journalistic journaling).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation crafts personalized, evocative lo-fi loops that help memoirists unlock deep emotional memories during the journaling process.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for journalistic journaling: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for journalistic journaling.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global digital journaling and diary apps",
      "sam": "$400M — guided journaling and memoir writing platforms",
      "som": "$15M — audio-enhanced reflective journaling tools"
    },
    {
      "id": "writing-impact-beat-choreographer-13",
      "theme": "writing",
      "title": "Impact Beat Choreographer",
      "pitch": "Lovable AI choreographs fight scenes so writers time prose; AIsa generates rhythmic impact SFX.",
      "subDiscipline": "action prose",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation provides rhythmic, percussive audio beats that help writers pace their sentence structure to match the physical strikes.",
      "megaPrompt": "Build \"Impact Beat Choreographer\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI choreographs fight scenes so writers time prose; AIsa generates rhythmic impact SFX.\nDiscipline: Writing, Poetry & Narrative (action prose).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation provides rhythmic, percussive audio beats that help writers pace their sentence structure to match the physical strikes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for action prose: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for action prose.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$150M — plot outlining and scene blocking tools",
      "som": "$5M — action and martial arts fiction drafting aids"
    },
    {
      "id": "writing-manor-creak-mapper-14",
      "theme": "writing",
      "title": "Manor Creak Mapper",
      "pitch": "Lovable AI designs haunted layouts so spatial audio writers map; AIsa generates specific room SFX.",
      "subDiscipline": "gothic spatial audio",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Generative SFX creates distinct, localized acoustic profiles for different rooms, aiding writers in designing immersive 3D audio experiences.",
      "megaPrompt": "Build \"Manor Creak Mapper\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI designs haunted layouts so spatial audio writers map; AIsa generates specific room SFX.\nDiscipline: Writing, Poetry & Narrative (gothic spatial audio).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Generative SFX creates distinct, localized acoustic profiles for different rooms, aiding writers in designing immersive 3D audio experiences.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for gothic spatial audio: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for gothic spatial audio.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global spatial audio and VR sound design",
      "sam": "$800M — interactive horror and escape room audio tools",
      "som": "$30M — gothic and haunted house spatial audio drafting"
    },
    {
      "id": "writing-hero-leitmotif-caster-15",
      "theme": "writing",
      "title": "Hero Leitmotif Caster",
      "pitch": "Lovable AI profiles protagonist journeys so authors stay inspired; AIsa scores a heroic leitmotif track.",
      "subDiscipline": "epic fantasy",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation composes a unique, recurring character theme that authors can listen to while writing that specific character's POV.",
      "megaPrompt": "Build \"Hero Leitmotif Caster\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI profiles protagonist journeys so authors stay inspired; AIsa scores a heroic leitmotif track.\nDiscipline: Writing, Poetry & Narrative (epic fantasy).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation composes a unique, recurring character theme that authors can listen to while writing that specific character's POV.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for epic fantasy: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for epic fantasy.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$350M — character development and worldbuilding tools",
      "som": "$12M — epic fantasy author inspiration and playlist apps"
    },
    {
      "id": "writing-punchline-timing-rig-16",
      "theme": "writing",
      "title": "Punchline Timing Rig",
      "pitch": "Lovable AI punches up sitcom scripts so actors rehearse timing; AIsa generates comedic SFX for it.",
      "subDiscipline": "sitcom rehearsal",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation provides custom rimshots, slide whistles, and audience reactions to help comedic actors nail their physical timing.",
      "megaPrompt": "Build \"Punchline Timing Rig\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI punches up sitcom scripts so actors rehearse timing; AIsa generates comedic SFX for it.\nDiscipline: Writing, Poetry & Narrative (sitcom rehearsal).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation provides custom rimshots, slide whistles, and audience reactions to help comedic actors nail their physical timing.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for sitcom rehearsal: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sitcom rehearsal.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — global screenwriting and production software",
      "sam": "$900M — script rehearsal and table read platforms",
      "som": "$40M — sitcom and sketch comedy timing aids"
    },
    {
      "id": "writing-surreal-morph-soundscape-17",
      "theme": "writing",
      "title": "Surreal Morph Soundscape",
      "pitch": "Lovable AI writes stream-of-consciousness pieces so avant-garde performers vibe; AIsa generates morphing SFX for it.",
      "subDiscipline": "avant-garde poetry",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Generative SFX creates bizarre, shifting audio textures that mirror the disjointed, surreal nature of experimental spoken word.",
      "megaPrompt": "Build \"Surreal Morph Soundscape\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI writes stream-of-consciousness pieces so avant-garde performers vibe; AIsa generates morphing SFX for it.\nDiscipline: Writing, Poetry & Narrative (avant-garde poetry).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Generative SFX creates bizarre, shifting audio textures that mirror the disjointed, surreal nature of experimental spoken word.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for avant-garde poetry: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for avant-garde poetry.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global live performance and event software",
      "sam": "$100M — experimental theater and fringe festival tools",
      "som": "$4M — avant-garde poetry and sound art performance apps"
    },
    {
      "id": "writing-noir-rain-focus-18",
      "theme": "writing",
      "title": "Noir Rain Focus",
      "pitch": "Lovable AI sets detective moods so mystery writers focus; AIsa scores a moody jazz track.",
      "subDiscipline": "detective drafting",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation blends specific ambient elements like rain and saxophone to create a highly focused, genre-specific writing environment.",
      "megaPrompt": "Build \"Noir Rain Focus\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI sets detective moods so mystery writers focus; AIsa scores a moody jazz track.\nDiscipline: Writing, Poetry & Narrative (detective drafting).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation blends specific ambient elements like rain and saxophone to create a highly focused, genre-specific writing environment.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for detective drafting: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for detective drafting.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global productivity and focus software",
      "sam": "$250M — genre-specific writing environment generators",
      "som": "$10M — noir and hardboiled detective fiction drafting tools"
    },
    {
      "id": "writing-starship-hum-isolator-19",
      "theme": "writing",
      "title": "Starship Hum Isolator",
      "pitch": "Lovable AI describes starship bridges so sci-fi writers isolate; AIsa generates continuous engine SFX.",
      "subDiscipline": "sci-fi deep work",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation produces seamless, low-frequency mechanical drones that mask background noise and simulate the isolation of deep space.",
      "megaPrompt": "Build \"Starship Hum Isolator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI describes starship bridges so sci-fi writers isolate; AIsa generates continuous engine SFX.\nDiscipline: Writing, Poetry & Narrative (sci-fi deep work).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation produces seamless, low-frequency mechanical drones that mask background noise and simulate the isolation of deep space.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for sci-fi deep work: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for sci-fi deep work.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global productivity and focus software",
      "sam": "$300M — ambient noise and white noise generators",
      "som": "$15M — sci-fi author isolation and deep-work soundscapes"
    },
    {
      "id": "writing-slam-rhythm-backing-20",
      "theme": "writing",
      "title": "Slam Rhythm Backing",
      "pitch": "Lovable AI analyzes slam meter so poets perform confidently; AIsa scores a backing hip-hop track.",
      "subDiscipline": "written word",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation creates custom instrumental beats that perfectly match the BPM and rhythmic flow of the poet's specific cadence.",
      "megaPrompt": "Build \"Slam Rhythm Backing\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI analyzes slam meter so poets perform confidently; AIsa scores a backing hip-hop track.\nDiscipline: Writing, Poetry & Narrative (written word).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation creates custom instrumental beats that perfectly match the BPM and rhythmic flow of the poet's specific cadence.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for written word: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for written word.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global live performance and event software",
      "sam": "$200M — poetry slam and open mic event platforms",
      "som": "$8M — written word artist rehearsal and backing track tools"
    },
    {
      "id": "writing-grief-cello-calibrator-21",
      "theme": "writing",
      "title": "Grief Cello Calibrator",
      "pitch": "Lovable AI outlines grief arcs so dramatists calibrate emotion; AIsa generates a mournful cello track.",
      "subDiscipline": "tragedy playwriting",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation provides a deeply emotional, singular instrumental track that helps playwrights stay in the correct headspace for tragic scenes.",
      "megaPrompt": "Build \"Grief Cello Calibrator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI outlines grief arcs so dramatists calibrate emotion; AIsa generates a mournful cello track.\nDiscipline: Writing, Poetry & Narrative (tragedy playwriting).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation provides a deeply emotional, singular instrumental track that helps playwrights stay in the correct headspace for tragic scenes.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for tragedy playwriting: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for tragedy playwriting.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global screenwriting and playwriting software",
      "sam": "$200M — dramatic structure and emotional arc tools",
      "som": "$7M — tragedy and drama playwright emotional calibration aids"
    },
    {
      "id": "writing-clockwork-grind-inventor-22",
      "theme": "writing",
      "title": "Clockwork Grind Inventor",
      "pitch": "Lovable AI invents clockwork gadgets so audiobook producers design; AIsa generates mechanical SFX for it.",
      "subDiscipline": "steampunk audiobooks",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "SFX generation allows producers to instantly prototype complex, multi-layered mechanical sounds for fictional steampunk inventions.",
      "megaPrompt": "Build \"Clockwork Grind Inventor\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI invents clockwork gadgets so audiobook producers design; AIsa generates mechanical SFX for it.\nDiscipline: Writing, Poetry & Narrative (steampunk audiobooks).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: SFX generation allows producers to instantly prototype complex, multi-layered mechanical sounds for fictional steampunk inventions.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for steampunk audiobooks: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for steampunk audiobooks.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global audiobook production and distribution",
      "sam": "$700M — audiobook sound design and foley libraries",
      "som": "$25M — steampunk and fantasy audiobook SFX generation"
    },
    {
      "id": "writing-cafe-chatter-blocker-23",
      "theme": "writing",
      "title": "Cafe Chatter Blocker",
      "pitch": "Lovable AI blocks coffee shop scenes so playwrights draft realistically; AIsa generates ambient cafe SFX.",
      "subDiscipline": "slice-of-life plays",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Generative SFX creates dynamic, non-repeating background chatter that helps playwrights write naturalistic dialogue without being distracted by real-world noise.",
      "megaPrompt": "Build \"Cafe Chatter Blocker\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI blocks coffee shop scenes so playwrights draft realistically; AIsa generates ambient cafe SFX.\nDiscipline: Writing, Poetry & Narrative (slice-of-life plays).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Generative SFX creates dynamic, non-repeating background chatter that helps playwrights write naturalistic dialogue without being distracted by real-world noise.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for slice-of-life plays: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for slice-of-life plays.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global creative writing software",
      "sam": "$250M — dialogue writing and scene blocking tools",
      "som": "$10M — contemporary and slice-of-life playwright drafting aids"
    },
    {
      "id": "writing-ritual-chant-translator-24",
      "theme": "writing",
      "title": "Ritual Chant Translator",
      "pitch": "Lovable AI translates epic invocations so worldbuilders perform rituals; AIsa scores a tribal chant track.",
      "subDiscipline": "mythic worldbuilding",
      "quantumHook": "AIsa Image Generation",
      "quantumHookId": "aisa-image",
      "quantumTag": "visual generation",
      "quantumRationale": "Music generation produces deep, atmospheric vocal and percussive tracks that bring fictional mythologies and rituals to life for the creator.",
      "megaPrompt": "Build \"Ritual Chant Translator\" as a ONE-SHOT Lovable build. The participant has only\n5 credits — this single message must produce a working demo with no follow-ups.\nSingle-page TanStack Start app. Cut scope ruthlessly.\n\nCONCEPT\nLovable AI translates epic invocations so worldbuilders perform rituals; AIsa scores a tribal chant track.\nDiscipline: Writing, Poetry & Narrative (mythic worldbuilding).\nRecipe: AIsa Image Generation (visual generation) as the single creative surface.\nWhy this kernel: Music generation produces deep, atmospheric vocal and percussive tracks that bring fictional mythologies and rituals to life for the creator.\n\nLOVABLE BUDGET (HARD CAP: ONE-SHOT, ~5 CREDITS TOTAL):\nThe participant has FIVE Lovable credits for the whole build. This prompt MUST\nship a working demo on the FIRST message with zero follow-ups. Engineer for that.\n- ONE TanStack Start app, ONE route (`src/routes/index.tsx`). No extra pages, no auth, no nav.\n- ONE TanStack server function in `src/lib/aisa.functions.ts` that proxies the AIsa call.\n- ONE client surface (a textarea + button, or chat box, or prompt-to-canvas) wired to it.\n- NO database, NO Lovable Cloud, NO auth, NO file uploads, NO extra integrations.\n- NO tests, NO docs pages, NO settings screens, NO theming toggles.\n- Libraries: template defaults + `zod`. Nothing else.\n- Keep the diff small enough to land in one build pass. If a feature is not on\n  screen in the user flow below, do not build it. Cut scope before adding scope.\n\nSTACK\n- TanStack Start app, the index route only.\n- AIsa is the only AI dependency. All API calls live inside a `createServerFn`\n  handler so `AISA_API_KEY` stays on the server.\n- Client surface fits the kernel: a prompt box that returns the result.\n- Tailwind + shadcn. Editorial look: gold accent on a dark or warm-cream\n  background, generous type, one strong headline, one primary action.\n- Footer renders: \"Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\".\n\nSERVER FUNCTION (src/lib/aisa.functions.ts) — AIsa image generation:\n```ts\nimport { createServerFn } from \"@tanstack/react-start\";\nimport { z } from \"zod\";\n\n/** Built during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14 */\nexport const render = createServerFn({ method: \"POST\" })\n  .inputValidator((d) => z.object({ brief: z.string().min(1).max(1000) }).parse(d))\n  .handler(async ({ data }) => {\n    const r = await fetch(\"https://api.aisa.one/v1/images/generations\", {\n      method: \"POST\",\n      headers: {\n        \"Authorization\": `Bearer ${process.env.AISA_API_KEY!}`,\n        \"Content-Type\": \"application/json\",\n      },\n      body: JSON.stringify({\n        model: \"seedream-3.0\",\n        prompt: `Concept art for mythic worldbuilding: ${data.brief}. Editorial, high detail.`,\n        size: \"1024x1024\",\n        n: 1,\n      }),\n    });\n    if (r.status === 402) throw new Error(\"AIsa balance exhausted — top up at console.aisa.one.\");\n    if (r.status === 429) throw new Error(\"AIsa rate limited — try again in a moment.\");\n    if (!r.ok) throw new Error(`AIsa image failed: ${r.status}`);\n    const j = await r.json();\n    return { url: j.data[0].url as string };\n  });\n```\n\nCLIENT: textarea + \"Render\" button. On success, show `<img src={url} />` and a\ndownload link. Keep one canvas on screen, replace it on the next render.\n\nUse BARE model ids — `gpt-image-1` (text fidelity) or `seedream-3.0` (default,\nfast + artistic). NO vendor prefix. Browse https://aisa.one/models.\n\nUSER FLOW (the entire app — nothing else exists)\n1. Land on the page; the headline previews what the demo does for mythic worldbuilding.\n2. The primary action (a prompt-driven canvas that renders covers, moodboards, posters or scene art on demand) is one tap away; the rest of the layout supports it.\n3. AIsa runs the kernel server-side, the result lands on screen, the user can retry or copy.\n\nKEY — only ONE secret is required:\n1. `AISA_API_KEY`. Sign up at https://console.aisa.one, copy the key into\n   Project Settings -> Secrets. Read it only on the server via\n   `process.env.AISA_API_KEY`. Never prefix with `VITE_`, never expose\n   to the client. A single key unlocks chat, image, video and skills.\n\nCREDIT (must appear in UI footer AND as JSDoc on the server function):\nBuilt during the AIsa Creative Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global game development and engine software",
      "sam": "$1B — worldbuilding and lore management tools",
      "som": "$35M — fantasy and mythic worldbuilding audio companions"
    }
  ]
}